---
phase: 12-history-detail-chart-home-dashboard
plan: 07
type: execute
wave: 3
depends_on: ["12-02"]
files_modified:
  - app/app/(app)/history/[sessionId].tsx
autonomous: true
requirements: [SKIN-06]
user_setup: []

must_haves:
  decisions: [D-13, D-15, D-17, D-20, D-21, D-22, D-24]
  truths:
    - "Session detail uses a custom in-content Forge header (circular back + circular ellipsis), native Stack header hidden (D-17)"
    - "Exercise breakdown is the hybrid card: name + right max-weight stat WITH the expanded per-set list (w × r + RPE) kept (D-15)"
    - "The ellipsis hosts the existing delete-session flow; overlays stay inline (no Modal portal) with keyboard-lift preserved (D-22)"
    - "No PB trophy anywhere (D-13); every weight/volume converts to the chosen unit (D-20)"
  artifacts:
    - path: "app/app/(app)/history/[sessionId].tsx"
      provides: "FSessionDetail re-skin + custom header (logic preserved)"
      contains: "headerShown: false"
  key_links:
    - from: "session detail header ellipsis"
      to: "inline overflow → delete-confirm flow"
      via: "preserved mutate-not-mutateAsync inline overlay"
      pattern: "accessibilityLabel"
---

<objective>
Re-skin the session-detail screen to FSessionDetail: a custom in-content Forge header (circular back + ellipsis hosting delete, D-17/FLAG-1), a 3-stat grid, an accent notes block, and the hybrid exercise-breakdown cards (D-15 — Forge frame + kept expanded per-set list). Preserve ALL offline-critical overlay/delete/notes logic verbatim (D-22). Omit the PB trophy (D-13). Convert every figure to the chosen unit (D-20) and i18n every string (D-21).

Purpose: SKIN-06 (session detail). The logic is offline-critical; this is a chrome-only re-skin over preserved behavior.
Output: a re-skinned session-detail screen.
</objective>

<execution_context>
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-history-detail-chart-home-dashboard/12-UI-SPEC.md
@.planning/phases/12-history-detail-chart-home-dashboard/12-PATTERNS.md
@.planning/phases/12-history-detail-chart-home-dashboard/12-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-skin session detail — custom header + 3-stat grid + notes + hybrid exercise cards (preserve all overlay/delete/notes logic)</name>
  <files>app/app/(app)/history/[sessionId].tsx</files>
  <read_first>
    - app/app/(app)/history/[sessionId].tsx (whole file) — preserve VERBATIM (offline-critical, D-22): keyboard-height lift (136-152) + paddingBottom = keyboardHeight + 16 (624); useFocusEffect overlay reset (158-167); mutate-not-mutateAsync for delete (285-298) + edit-notes (180-190); inline-overlay overflow menu (436-496) / delete-confirm (502-601) / edit-notes (609-689) with tap-on-scrim dismiss + setTimeout(...50) before the stacked confirm (475); post-delete router.replace to /(tabs)/history with toast:"deleted" (294-298); loading gate on !session not isPending (258) + initialData seeding
    - Phase 11 workout screen custom header (D-09 precedent) — the headerShown:false + circular back/ellipsis pattern
    - 12-UI-SPEC.md "Layout Contract → Session detail" (FSessionDetail 649-764) + Copywriting Contract session-detail rows + "Icon-only-control accessibility (FLAG-1)" (back/moreOptions labels) + Color (delete-confirm = forge-danger)
    - design source: app/design v2/Sources/design/forge-screens.jsx FSessionDetail 649-764 (OMIT PB trophy 739-746)
    - app/lib/units.ts (formatWeight/formatVolume) + app/lib/prefs.ts (getPref fm:units)
    - 12-PATTERNS.md "app/app/(app)/history/[sessionId].tsx" section (deltas)
  </read_first>
  <action>
    D-17 custom header: set Stack.Screen options headerShown:false; render an in-content Forge header — 40px circular back button (surface+border, Icon chevronLeft, accessibilityLabel t('back'), accessibilityRole "button", 44px hit-slop) + 40px circular ellipsis button (surface+border, Icon ellipsis, accessibilityLabel t('moreOptions'), same a11y, hosts the EXISTING overflow→delete flow) + eyebrow (plan snapshot, uppercase) + display date title. This replaces the current headerRight ellipsis (310-320). 3-stat grid card: Set / kg·volym / min cells (1fr·1fr·1fr, dividers), tabular-nums, converted via the units helpers. Notes block: accentSoft card + accent border-25% + accent pencil Icon (F12 note verbatim, never translated). "Övningar" header via t(). D-15 hybrid ExerciseCard (701-776): Forge card frame with exercise name + right-aligned max-weight stat AND the kept expanded per-set list ({w} × {r} + RPE per set, 757-772) re-skinned greys→forge-*. Delete-confirm button colored forge-danger (#D70015 → #FF453A) with forge-accentText label — replaces the current #EF4444/#DC2626 (585). Convert every per-set weight + max-weight + volume via formatWeight/formatVolume (D-20) — currently raw kg (400, 547, 750, 764). All hardcoded Swedish → t(). OMIT the PB trophy (D-13). Box styling in className (NativeWind 4). DO NOT touch the overlay/delete/notes LOGIC — re-skin chrome only.
  </action>
  <verify>
    <automated>cd app && grep -q "headerShown: false" "app/(app)/history/[sessionId].tsx" && grep -q "accessibilityLabel" "app/(app)/history/[sessionId].tsx" && grep -q "forge-danger" "app/(app)/history/[sessionId].tsx" && grep -q "mutate(" "app/(app)/history/[sessionId].tsx" && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Stack header hidden (headerShown:false) + in-content circular back + ellipsis with accessibilityLabel via t('back')/t('moreOptions') + accessibilityRole="button"
    - Exercise cards keep the expanded per-set list (w × r + RPE) beneath the name + max stat (D-15 hybrid)
    - Delete-confirm button uses forge-danger; no PB trophy rendered (D-13)
    - Overlay/delete/notes logic byte-preserved: `mutate(` present (not mutateAsync), useFocusEffect reset + keyboard-lift + inline overlays intact; NO Modal portal
    - Every weight/volume figure routes through the units helpers (no raw `kg` literal)
    - `npm run typecheck` + `npm run lint` exit 0
  </acceptance_criteria>
  <done>Session detail matches FSessionDetail with the custom header, hybrid exercise cards, forge-danger delete-confirm, and full unit/i18n conversion; all offline-critical logic preserved.</done>
</task>

<task type="auto">
  <name>Task 2: F13 regression gate</name>
  <files>app/app/(app)/history/[sessionId].tsx</files>
  <read_first>
    - app/scripts/verify-f13-brutal-test.ts
    - 12-CONTEXT.md D-22/D-24 + CLAUDE.md SKIN-08
  </read_first>
  <action>
    Confirm the re-skin preserved mutate-not-mutateAsync and touched no mutation defaults / persister scope / exercise_sets logging (D-22/D-24). Run the brutal test.
  </action>
  <verify>
    <automated>cd app && npm run test:f13-brutal</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:f13-brutal` exits 0
  </acceptance_criteria>
  <done>F13 brutal test green after the session-detail re-skin.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user note text (F12) → render | Stored as written, never translated; rendered verbatim |
| delete-session mutation → DB | RLS-scoped delete on own-row workout_sessions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-15 | Tampering | inline delete-confirm overlay | mitigate | Preserve the offline-safe mutate(...) + inline-overlay logic verbatim (D-22); confirmation required before the irreversible delete |
| T-12-16 | Tampering/DoS | F13 hot path | mitigate | Chrome-only re-skin; Task 2 test:f13-brutal gate; mutate-not-mutateAsync preserved (D-24) |
| T-12-SC | Tampering | npm installs | mitigate | No packages installed |
</threat_model>

<verification>
- `npm run typecheck` + `npm run lint` exit 0
- `npm run test:f13-brutal` exits 0
- custom header + hybrid cards + forge-danger confirm present; mutate(...) preserved; no Modal portal
- manual device UAT (overflow→delete flow, edit-notes keyboard-lift, unit toggle) per VALIDATION
</verification>

<success_criteria>
SKIN-06 (session detail) done; D-17 custom header, D-15 hybrid breakdown, D-22 preserved overlay logic, D-13 omission, D-20 unit conversion, D-21 i18n all in place; F13 green.
</success_criteria>

<output>
Create `.planning/phases/12-history-detail-chart-home-dashboard/12-07-SUMMARY.md` when done
</output>
