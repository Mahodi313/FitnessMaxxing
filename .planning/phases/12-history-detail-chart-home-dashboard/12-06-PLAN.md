---
phase: 12-history-detail-chart-home-dashboard
plan: 06
type: execute
wave: 3
depends_on: ["12-02", "12-03", "12-04"]
files_modified:
  - app/app/(app)/(tabs)/history.tsx
autonomous: true
requirements: [SKIN-06, DASH-03, DASH-04, DASH-05]
user_setup: []

must_haves:
  decisions: [D-05, D-08, D-09, D-13, D-16, D-18, D-20, D-21, D-24]
  truths:
    - "History shows a lifetime eyebrow (total sessions + total hours, D-09)"
    - "History shows a weekly-volume card: big volume + delta vs prior week + animated sparkline (D-05 satisfies DASH-03/DASH-04)"
    - "Session rows are Forge cards with a DD/MON date badge + plan name + 'X set · Y kg · Z min' meta (D-16)"
    - "No PR trophy anywhere on the row (D-13)"
    - "All strings are t()-keyed and every figure converts to the chosen unit (D-20/D-21)"
  artifacts:
    - path: "app/app/(app)/(tabs)/history.tsx"
      provides: "FHistory re-skin: eyebrow + volume card + Forge rows"
      contains: "useDashboardSummaryQuery"
  key_links:
    - from: "app/app/(app)/(tabs)/history.tsx"
      to: "useDashboardSummaryQuery + Sparkline"
      via: "volume card reads volume_this/prior_week_kg + weekly_volume_series"
      pattern: "weekly_volume_series\\|Sparkline"
---

<objective>
Re-skin the History list to FHistory: a lifetime-stats eyebrow (D-09), a weekly-volume overview card carrying the volume + delta + animated sparkline (D-05, which is how DASH-03/DASH-04 are satisfied — on History, NOT Home), and Forge session rows with a date badge + meta (D-16). Preserve all v1 list plumbing verbatim. Omit the PR trophy (D-13).

Purpose: SKIN-06 (history) + the DASH-03/DASH-04 mock-literal split live here. The verifier checks History for the delta + sparkline.
Output: a re-skinned History tab.
</objective>

<execution_context>
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-history-detail-chart-home-dashboard/12-UI-SPEC.md
@.planning/phases/12-history-detail-chart-home-dashboard/12-PATTERNS.md
@.planning/phases/12-history-detail-chart-home-dashboard/12-04-SUMMARY.md
@.planning/phases/12-history-detail-chart-home-dashboard/12-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-skin History — eyebrow + volume card + Forge rows (preserve list plumbing)</name>
  <files>app/app/(app)/(tabs)/history.tsx</files>
  <read_first>
    - app/app/(app)/(tabs)/history.tsx (whole file) — preserve VERBATIM: infinite-list memo data?.pages.flat() (139-142), onEndReached guard hasNextPage && !isFetchingNextPage (164-171), RefreshControl (180-186), post-delete toast read-and-clear (108-134) + unmount cleanup ref, plan-name fallback (228), formatNumber idiom (77)
    - app/app/(app)/(tabs)/index.tsx (84-111) — the TOKENS hex split idiom to reuse for Skia/Icon strokes
    - app/components/ui/Sparkline.tsx (post-12-03, animated) + ForgeCard + Icon (chevronRight, arrowUp for delta) + ForgeChip
    - app/lib/queries/dashboard.ts (useDashboardSummaryQuery) + app/lib/units.ts (formatVolume/formatWeight) + app/lib/prefs.ts (getPref fm:units)
    - 12-UI-SPEC.md "Layout Contract → History list" (FHistory 550-642) + Copywriting Contract History rows + Color table (delta chip = forge-success, sparkline = accent stroke)
    - design source: app/design v2/Sources/design/forge-screens.jsx FHistory lines 550-642 (OMIT row trophy 627-635)
    - 12-PATTERNS.md "app/app/(app)/(tabs)/history.tsx" section (deltas)
  </read_first>
  <action>
    Replace greys (bg-white dark:bg-gray-900 / bg-gray-100 dark:bg-gray-800 / text-blue-600) with forge-* tokens per the Color table. ADD above the list, both fed by useDashboardSummaryQuery(): (1) lifetime eyebrow (D-09) "{N} pass · {H} timmar" via t() with lifetime_sessions + lifetime_hours; (2) a volume-overview ForgeCard (D-05): "Veckans volym" eyebrow + big numeral formatVolume(volume_this_week_kg, unit) + a forge-success delta chip with Icon arrowUp showing the client-computed "+{N}%" = (this-prior)/prior (guard prior===0) + an animated Sparkline fed weekly_volume_series mapped through toDisplayVolume (accent stroke hex from TOKENS). Volume-card empty copy (t()) when no finished sessions. Re-skin the screen title "Historik" via t() (display 36px). Row (D-16): ForgeCard radius 16, gap 14 — 44px surface2 date-badge (DD over MON, date-fns) + plan name (fallback t('noPlanFallback')) + meta "X set · Y kg · Z min" (set_count, formatVolume or formatWeight for the kg figure per the meta semantics, duration min) + trailing Icon chevronRight. OMIT any trophy (D-13). Convert every weight/volume figure via the units helpers (D-20) — currently raw ${kg} kg (261, 239). All hardcoded Swedish → t(). Box styling in className (NativeWind 4). Preserve all list plumbing + toast logic verbatim.
  </action>
  <verify>
    <automated>cd app && grep -q "useDashboardSummaryQuery" "app/(app)/(tabs)/history.tsx" && grep -q "Sparkline" "app/(app)/(tabs)/history.tsx" && grep -qv "text-blue-600" "app/(app)/(tabs)/history.tsx"; grep -q "weekly_volume_series" "app/(app)/(tabs)/history.tsx" && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - history.tsx renders the lifetime eyebrow + volume card (delta chip + animated Sparkline) + Forge rows above/within the preserved infinite list
    - The delta chip uses forge-success (not accent, not a separate red) and the sparkline uses the accent stroke
    - No `trophy` rendered on rows (D-13); grep finds no trophy markup
    - Every kg/volume figure routes through formatVolume/formatWeight (no raw `kg` string literal in the rendered output)
    - Infinite-list memo, onEndReached guard, RefreshControl, and post-delete toast logic are byte-preserved
    - `npm run typecheck` + `npm run lint` exit 0
  </acceptance_criteria>
  <done>History matches FHistory with the lifetime eyebrow, volume card (DASH-03/DASH-04 satisfied here), and Forge rows; no trophy; list plumbing intact.</done>
</task>

<task type="auto">
  <name>Task 2: F13 regression gate</name>
  <files>app/app/(app)/(tabs)/history.tsx</files>
  <read_first>
    - app/scripts/verify-f13-brutal-test.ts — confirm the read-side re-skin does not touch the hot path
    - 12-CONTEXT.md D-24 + CLAUDE.md SKIN-08
  </read_first>
  <action>
    Confirm the History re-skin touched no mutation defaults / query keys / persister scope / exercise_sets logging (D-24). Run the brutal test as the gate.
  </action>
  <verify>
    <automated>cd app && npm run test:f13-brutal</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:f13-brutal` exits 0
  </acceptance_criteria>
  <done>F13 brutal test green after the History re-skin.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard RPC data → History render | Zod-parsed in the hook; display-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-13 | Information Disclosure | History volume/lifetime aggregates | mitigate | RLS-scoped security-invoker RPC (T-12-01); no client cross-user aggregation |
| T-12-14 | Tampering/DoS | F13 hot path | mitigate | Read-only re-skin; Task 2 test:f13-brutal gate; no mutation/persister-scope touched (D-24) |
| T-12-SC | Tampering | npm installs | mitigate | No packages installed |
</threat_model>

<verification>
- `npm run typecheck` + `npm run lint` exit 0
- `npm run test:f13-brutal` exits 0
- History renders eyebrow + volume card (delta + sparkline) + Forge rows; no trophy
- manual device UAT (sparkline draw-in, unit toggle correctness) per VALIDATION
</verification>

<success_criteria>
SKIN-06 (history) done; DASH-03 (volume + delta) + DASH-04 (sparkline) satisfied on the History screen per the D-05 split; D-09 lifetime eyebrow + D-16 rows in place; D-13 omission honored; F13 green.
</success_criteria>

<output>
Create `.planning/phases/12-history-detail-chart-home-dashboard/12-06-SUMMARY.md` when done
</output>
