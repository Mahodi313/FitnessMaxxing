---
phase: 12-history-detail-chart-home-dashboard
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/ui/ProgressRing.tsx
  - app/components/ui/Sparkline.tsx
autonomous: true
requirements: [MOTN-02, DASH-04, DASH-01]
user_setup: []

must_haves:
  decisions: [D-18, D-19]
  truths:
    - "ProgressRing sweep fills 0→value on mount via a Reanimated shared value (MOTN-02)"
    - "Sparkline draws in left→right on mount (D-18)"
    - "Both snap to the final value when OS reduce-motion is enabled (D-18)"
    - "Ring overfills past 100% with a glow/second-lap when value>1; center label shows the real count (D-19)"
  artifacts:
    - path: "app/components/ui/ProgressRing.tsx"
      provides: "animated fill + overflow treatment (static-only restriction lifted)"
      contains: "useSharedValue"
    - path: "app/components/ui/Sparkline.tsx"
      provides: "animated draw-in (static-only restriction lifted)"
      contains: "useDerivedValue"
  key_links:
    - from: "ProgressRing"
      to: "Skia Path arc"
      via: "useDerivedValue rebuilds path from animated progress"
      pattern: "useDerivedValue"
    - from: "Sparkline"
      to: "Skia Group clip"
      via: "animated-width clip rect"
      pattern: "clip"
---

<objective>
Add mount animations to the two Phase-8 Skia primitives that are currently static-by-design: `ProgressRing` (fill 0→value + overflow glow, MOTN-02 + D-19) and `Sparkline` (draw-in left→right, D-18). Both honor OS reduce-motion (snap to final).

Purpose: Independent of the data layer — these primitives take props. Sequenced in Wave 1 so the Wave-3 Home hero (ring) and History volume card (sparkline) consume an already-animated component. The files carry explicit "animate in Phase 12" headers that this plan lifts.
Output: 2 animated Skia primitives with reduce-motion support.
</objective>

<execution_context>
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-history-detail-chart-home-dashboard/12-RESEARCH.md
@.planning/phases/12-history-detail-chart-home-dashboard/12-PATTERNS.md
@.planning/phases/12-history-detail-chart-home-dashboard/12-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Animate ProgressRing fill + overflow glow (MOTN-02, D-19)</name>
  <files>app/components/ui/ProgressRing.tsx</files>
  <read_first>
    - app/components/ui/ProgressRing.tsx (whole file) — the static arc math (lines 68-77: `v` clamp + `fg.addArc({...}, -90, 360 * v)`), the useColorScheme() track resolution (63-66), and the static-only header restriction (9-12) that this plan lifts
    - app/app/(app)/exercise/[exerciseId]/chart.tsx (lines 66, 224-234) — the Reanimated useDerivedValue→Skia worklet idiom (read a SharedValue, Skia re-renders reactively)
    - app/app/(app)/(tabs)/index.tsx (line ~62) — the existing withSpring import + §07 spring usage
    - 12-RESEARCH.md §Mandate 3 "ProgressRing animated fill" + "Reduce-motion CONFIRMED API" + "Overflow / goal-beaten (D-19)"
    - 12-PATTERNS.md "app/components/ui/ProgressRing.tsx" section (deltas, incl. don't-clamp on overflow + real-count label)
  </read_first>
  <action>
    Lift the static-only header comment. Import useSharedValue, useDerivedValue, withSpring, useReducedMotion from react-native-reanimated. Drive a `progress` shared value 0→value: in a useEffect, `progress.value = reduced ? value : withSpring(value, { damping: 18, stiffness: 220 })` (§07 spring). Rebuild the foreground arc inside useDerivedValue (Skia consumes the derived path on the UI thread): `p.addArc({...}, -90, 360 * progress.value)` and feed it to the animated `<Path path={fgPath} />`. Keep the existing value math + useColorScheme() track-resolution intact (animation is additive; prop-driven SharedValue form). For D-19 overflow: when value>1, do NOT clamp — draw a second-lap arc for `(progress.value - 1) * 360` in the brand gradient over the first, plus a Skia glow (a `<Blur>` child or a wider low-opacity stroke underneath). The center count label ALWAYS shows the real count passed in (e.g. "5 / 4"), never the capped fraction. reduced=useReducedMotion() is a synchronous boolean → snap to final.
  </action>
  <verify>
    <automated>cd app && grep -q "useSharedValue" components/ui/ProgressRing.tsx && grep -q "useReducedMotion" components/ui/ProgressRing.tsx && grep -q "damping: 18" components/ui/ProgressRing.tsx && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - ProgressRing imports useSharedValue + useDerivedValue + useReducedMotion + withSpring
    - The fill is driven by withSpring({ damping: 18, stiffness: 220 }) and snaps to final when reduced
    - value>1 is NOT clamped to 1 (overflow second-lap + glow path present); center label renders the real count
    - The static-only header restriction comment is removed/updated
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>ProgressRing animates its fill on mount, honors reduce-motion, and overfills with a glow when the goal is beaten while showing the true count.</done>
</task>

<task type="auto">
  <name>Task 2: Animate Sparkline draw-in (D-18)</name>
  <files>app/components/ui/Sparkline.tsx</files>
  <read_first>
    - app/components/ui/Sparkline.tsx (whole file) — the static line/area paths (lines 74-83), the last-point dot (106-113), and the static-only header restriction (11-13) this plan lifts
    - app/app/(app)/exercise/[exerciseId]/chart.tsx — the same Reanimated→Skia clip/derived-value idiom referenced for the chart line draw
    - 12-RESEARCH.md §Mandate 3 "Sparkline draw-in" + "Reduce-motion CONFIRMED API"
    - 12-PATTERNS.md "app/components/ui/Sparkline.tsx" section (deltas)
  </read_first>
  <action>
    Lift the static-only header. Wrap the existing line+area paths in a `<Group clip={clipRect}>` whose width animates 0→full: a `drawProgress` useSharedValue driven `withSpring(1, { damping: 18, stiffness: 220 })` on mount (snap to 1 when useReducedMotion()), and `clipRect = useDerivedValue(() => Skia.XYWHRect(x, y, fullWidth * drawProgress.value, fullHeight))`. The last-point dot (106-113) fades/scales in at the end of the draw (withDelay or a second shared value). Keep the path-building math intact; animation is additive.
  </action>
  <verify>
    <automated>cd app && grep -q "useSharedValue" components/ui/Sparkline.tsx && grep -q "useReducedMotion" components/ui/Sparkline.tsx && grep -q "clip" components/ui/Sparkline.tsx && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Sparkline wraps its paths in a Skia Group with an animated-width clip rect
    - The draw is driven by withSpring and snaps to full when reduce-motion is on
    - The last-point dot animates in at the end of the draw
    - The static-only header restriction comment is removed/updated
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Sparkline draws in left→right on mount and snaps to final under reduce-motion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure presentational Skia animation driven by props; no read/write gated by motion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07 | Denial of Service | mount animation on hot surfaces | accept | Animations are additive polish, never gate a read or write (F13 untouched, D-24); reduce-motion snaps to final. No data-path impact |
| T-12-SC | Tampering | npm installs | mitigate | No packages installed (Reanimated/Skia already pinned) |
</threat_model>

<verification>
- `npm run typecheck` exits 0 after both component changes
- ProgressRing: useSharedValue + useReducedMotion + damping:18 present; value not clamped on overflow
- Sparkline: useSharedValue + useReducedMotion + clip present
- (manual device UAT for the visual fill/draw + reduce-motion snap happens at Wave-3 closeout per VALIDATION manual-only table)
</verification>

<success_criteria>
Both Skia primitives animate on mount with §07 spring, honor OS reduce-motion, and the ring celebrates a beaten goal with an overflow glow showing the real count. MOTN-02 (ring fill) + D-18/D-19 component-side complete; Wave-3 screens consume them.
</success_criteria>

<output>
Create `.planning/phases/12-history-detail-chart-home-dashboard/12-03-SUMMARY.md` when done
</output>
