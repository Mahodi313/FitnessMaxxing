---
phase: 12-history-detail-chart-home-dashboard
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - app/app/(app)/history/[sessionId].tsx
  - app/locales/en.json
  - app/locales/sv.json
autonomous: true
gap_closure: true
requirements: [SKIN-06, MOTN-03]
must_haves:
  truths:
    - "From a session-detail screen, tapping an exercise breakdown card navigates to that exercise's progress chart at /exercise/[exerciseId]/chart"
    - "The exercise chart screen (and its MOTN-03 draw-on-mount) is reachable from the UI again"
  artifacts:
    - path: "app/app/(app)/history/[sessionId].tsx"
      provides: "ExerciseCard is a tappable affordance that router.push-es /exercise/${exerciseId}/chart, matching the Forge FSessionDetail design (D-15 hybrid card, D-17 header preserved)"
      contains: "exercise/${exerciseId}/chart"
  key_links:
    - from: "app/app/(app)/history/[sessionId].tsx ExerciseCard"
      to: "/exercise/[exerciseId]/chart route"
      via: "router.push on card press"
      pattern: "router\\.push.*exercise/\\$\\{exerciseId\\}/chart"
  decisions:
    - "D-15: re-introduce the tappable affordance on the EXISTING hybrid ExerciseCard (frame + max-weight stat + per-set list) — do not revert the Phase-12 re-skin, just make the Forge card pressable"
    - "D-17: the FSessionDetail custom Forge header stays intact — the chart entry lives on the breakdown card, not a reverted native header button"
    - "D-21: the chart-link a11y label is a new sv+en locale key routed through t() (Phase 8 D-10 flat-key convention)"
---

<objective>
Close FIT-110 (major, REGRESSION): the exercise chart route `/exercise/[exerciseId]/chart` has NO navigation entry point anywhere in the app, so the fully-built (and re-skinned in 12-08) chart screen + its MOTN-03 draw-on-mount animation are orphaned.

Root cause (confirmed via git + code): Phase 6 (commit 57d792e, `[sessionId].tsx:322`) made each session-detail ExerciseCard a Pressable cross-link via `router.push(`/exercise/${exerciseId}/chart`)` and passed `exerciseId` + `onShowChart` props. The Phase 12-07 re-skin (commit d7711f3) dropped the `exerciseId`/`onShowChart` props and the Pressable, leaving a self-contradictory comment at L757-759 ("the chart route is reached from the chart screen per the Phase-12 IA; the breakdown card is informational only"). No replacement was wired anywhere. The 12-CONTEXT.md / 12-UI-SPEC reference confirms NO intentional relocation of the chart link to another surface — the IA always reached the chart from session-detail (the "Senaste 10 passen" list inside the chart routes the OTHER direction: chart → source session). So the correct fix is to restore the session-detail → chart entry point on the re-skinned hybrid card.

Purpose: Re-introduce the chart entry point on the Forge `FSessionDetail` ExerciseCard (D-15 hybrid) WITHOUT reverting the re-skin — make the existing card pressable and route to the chart. This unblocks UAT test 3 (chart line draw-on-mount), the chart portion of test 4 (reduce-motion), and test 5 (chart visual fidelity).

Output: `ExerciseCard` accepts an `exerciseId` prop + an `onPress`/route, becomes a Pressable Forge card, and pushes `/exercise/${exerciseId}/chart`. One new sv+en locale key for the card's a11y/affordance label.
</objective>

<execution_context>
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-history-detail-chart-home-dashboard/12-CONTEXT.md
@.planning/phases/12-history-detail-chart-home-dashboard/12-UAT.md

<interfaces>
<!-- Current ExerciseCard call site — exerciseId is ALREADY in scope (Map key). app/app/(app)/history/[sessionId].tsx L478-487. -->
The breakdown maps `setsByExercise` (a `Map<exerciseId, SetRow[]>`); the map key IS the exerciseId:
```tsx
{Array.from(setsByExercise.entries()).map(([exerciseId, sets]) => (
  <ExerciseCard
    key={exerciseId}
    exerciseName={exerciseNameById.get(exerciseId) ?? t("errorGeneric")}
    sets={sets}
    units={units}
  />
))}
```

<!-- Current ExerciseCard signature — a plain View (L762-834). Make it Pressable + accept exerciseId. -->
```tsx
function ExerciseCard({ exerciseName, sets, units }:
  { exerciseName: string; sets: SetRow[]; units: UnitPref }) {
  // ...returns a <View className="rounded-forge-md border bg-forge-surface-light ... px-4 py-3.5"> with
  //   - header row: exercise name (left) + max-weight stat (right)
  //   - per-set list (D-15)
}
```

<!-- Phase 6 original wiring to restore (semantics, NOT the old grey styling). From git 57d792e. -->
Phase 6 passed `exerciseId={exerciseId}` and `onShowChart={() => router.push(`/exercise/${exerciseId}/chart` as Href)}`, and the card itself was a Pressable.

<!-- Router: already imported. app/app/(app)/history/[sessionId].tsx L60-62 imports useRouter; `const router = useRouter()` at L80. Href is NOT currently imported (Phase 6 imported it; the re-skin removed it). -->

<!-- Icon primitive available for an optional chevron/chart affordance — used elsewhere in the re-skinned screens. -->
app/components/ui — `Icon` primitive (chevronRight is used on the Forge history rows in history.tsx).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make ExerciseCard a tappable chart cross-link</name>
  <files>app/app/(app)/history/[sessionId].tsx</files>
  <action>
Restore the session-detail → chart navigation entry point on the re-skinned hybrid ExerciseCard, preserving the Phase-12 Forge design (D-15 card frame + max-weight stat + per-set list; D-17 header untouched).

1. Extend the `ExerciseCard` prop type (L762-770) to accept `exerciseId: string` and `onPress: () => void`. Keep `exerciseName`, `sets`, `units`.

2. Change the card's root from `<View ...>` (L779) to a `Pressable` with the SAME className (the Forge surface card classes stay byte-identical — re-skin preserved; the NativeWind box-decoration learning means bg/border/radius/padding MUST remain in className, not a style() callback). Wire `onPress={onPress}`. Add `accessibilityRole="button"` and `accessibilityLabel={t("viewExerciseChart", { exercise: exerciseName })}` (new key — Task 2). Add a subtle pressed affordance via the Pressable `style={({ pressed }) => ({ opacity: pressed ? 0.85 : 1 })}` callback ONLY (opacity is the documented exception that may live in style(); all box decoration stays in className).

3. Add a small trailing chart/chevron affordance so the card visibly signals it is tappable (the regression's root harm is an invisible affordance). Place an `Icon` (chevronRight, matching the Forge history-row convention) at the right edge of the header row — sized ~16-18px, `text-forge-text3` token color, vertically centered with the max-weight stat. Do NOT crowd the max-weight stat; the chevron sits to its right with a small gap. (If composing the chevron into the existing right-aligned stat column is cleaner, that is the executor's call — the requirement is a visible "this opens the chart" cue consistent with FSessionDetail/forge-screens.jsx affordances.)

4. Remove the stale L757-759 comment block ("No chart cross-link in this re-skin ... informational only") since it is now false; replace it with a one-line comment noting the FIT-110 restore (e.g. "FIT-110: tappable → /exercise/[exerciseId]/chart restored (Phase-6 cross-link dropped in the 12-07 re-skin); Forge card frame preserved (D-15)").

5. At the call site (L478-487), pass `exerciseId={exerciseId}` and `onPress={() => router.push(`/exercise/${exerciseId}/chart`)}`. `router` is already in scope (L80). Use the typed-route string directly; if `experiments.typedRoutes` trips the typecheck on the template literal (cross-route reference), add a localized `as Href` cast and import `Href` from `expo-router` (Phase 4 Plan 02 precedent in this repo) — prefer NO cast if tsc is clean without it.

Preserve ALL other session-detail logic verbatim: the D-17 custom header, the overflow/delete/edit-notes inline overlays, useFocusEffect resets, keyboard-lift, the D-15 per-set list, D-20 unit conversion. This change touches only the ExerciseCard component + its one call site.
  </action>
  <verify>
    <automated>cd app && grep -q "exercise/\${exerciseId}/chart" "app/(app)/history/[sessionId].tsx" && npx tsc --noEmit && npx expo lint</automated>
  </verify>
  <done>ExerciseCard is a Pressable that routes to `/exercise/${exerciseId}/chart` on tap; a visible affordance (chevron/chart icon) is present; the Forge card frame (className box-decoration) is unchanged; the stale "informational only" comment is gone; tsc + lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add the chart-link a11y locale key (sv + en parity)</name>
  <files>app/locales/en.json, app/locales/sv.json</files>
  <action>
Add ONE new flat key `viewExerciseChart` to BOTH `app/locales/en.json` and `app/locales/sv.json` (Phase 8 D-10 flat 1:1 key convention; current count 213 each — both go to 214 at parity). The value is an accessibility/affordance label with an `{{exercise}}` interpolation:
  - en: "View progress chart for {{exercise}}"
  - sv: "Visa progressionsgraf för {{exercise}}"

Match the existing JSON formatting (indentation, trailing-comma style, alphabetical-or-append position — follow whatever the file already does; the repo appends new keys per 12-02 SUMMARY "new keys over mutating overloaded existing keys"). Keep the two files in lockstep — identical key set, only the value differs.

Do NOT relabel or mutate any existing key (the streak days→weeks relabel and other Phase-12 keys are locked — 12-02). This is purely additive.
  </action>
  <verify>
    <automated>cd app && node -e "const en=require('./locales/en.json'),sv=require('./locales/sv.json'); const ek=Object.keys(en),sk=Object.keys(sv); if(!('viewExerciseChart' in en)||!('viewExerciseChart' in sv)) throw new Error('key missing'); if(ek.length!==sk.length) throw new Error('parity: '+ek.length+' vs '+sk.length); const d=ek.filter(k=>!sk.includes(k)); if(d.length) throw new Error('sv missing: '+d); console.log('parity ok @ '+ek.length);"</automated>
  </verify>
  <done>`viewExerciseChart` exists in both locales with the `{{exercise}}` interpolation; sv/en key counts are equal (214 each); no existing key mutated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-app navigation | `router.push("/exercise/${exerciseId}/chart")` — `exerciseId` is a Map key sourced from the user's own already-loaded `exercise_sets` (RLS-scoped read). No untrusted external input crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-10-01 | Tampering | route param `exerciseId` | accept | `exerciseId` originates from the caller's own RLS-scoped session sets (not user free-text); the chart screen's own `get_exercise_summary`/`get_exercise_chart` RPCs are `security invoker` + RLS-scoped (cross-user test:rls PASS, 12-VERIFICATION T-12-02) — an out-of-scope id returns empty, never another user's data. |
| T-12-10-02 | Information disclosure | chart screen reached for an exercise | accept | The chart RPCs scope to the caller's JWT; navigating cannot surface another user's progression. Existing, unchanged control. |
| T-12-10-SC | Tampering | npm/pip/cargo installs | accept | No package installs — re-uses existing `expo-router`, `Icon` primitive, and locale infra. |
</threat_model>

<verification>
- `grep "exercise/${exerciseId}/chart" [sessionId].tsx` matches (entry point restored).
- ExerciseCard root is a `Pressable` with the unchanged Forge className box-decoration (no naked re-skin — NativeWind box-decoration-via-className learning honored).
- Locale parity script passes at 214/214; `viewExerciseChart` present in both.
- `npx tsc --noEmit` + `npx expo lint` clean.
- Device re-test (phase UAT re-run): open a session detail → tap an exercise card → chart screen opens and the line draws left→right on mount (MOTN-03).
</verification>

<success_criteria>
- Tapping an ExerciseCard on session-detail navigates to `/exercise/[exerciseId]/chart`.
- The card shows a visible tappable affordance; the Forge FSessionDetail design (D-15 card, D-17 header) is preserved — no revert of the re-skin.
- New `viewExerciseChart` key at sv/en parity.
- tsc + lint clean.
</success_criteria>

<output>
Create `.planning/phases/12-history-detail-chart-home-dashboard/12-10-SUMMARY.md` when done.
</output>
