---
phase: 12-history-detail-chart-home-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/supabase/migrations/0011_phase12_dashboard_rpcs.sql
  - app/types/database.ts
  - app/scripts/verify-deploy.ts
  - app/scripts/test-rls.ts
  - app/scripts/test-dashboard-aggregates.ts
autonomous: true
requirements: [DASH-05, SKIN-06, DASH-01, DASH-02, DASH-03, DASH-04]
user_setup: []

must_haves:
  decisions: [D-06, D-07, D-08, D-09, D-23, D-24]
  truths:
    - "Two read-only RPCs (get_dashboard_summary, get_exercise_summary) are deployed and security invoker"
    - "Dashboard aggregates RLS-scope per user (one user's sessions never inflate another's totals)"
    - "Streak counts consecutive calendar weeks meeting weekly_goal, not days (D-07)"
    - "Week buckets use Monday-Sunday local-time boundaries (D-06)"
    - "F13 hot path stays green after migration (D-24)"
  artifacts:
    - path: "app/supabase/migrations/0011_phase12_dashboard_rpcs.sql"
      provides: "get_dashboard_summary + get_exercise_summary RPCs (security invoker, stable, search_path='')"
      contains: "create or replace function public.get_dashboard_summary"
    - path: "app/scripts/test-dashboard-aggregates.ts"
      provides: "Wave-0 streak + week-boundary fixtures (DASH-02, D-06)"
      min_lines: 40
    - path: "app/types/database.ts"
      provides: "regenerated types with the two new RPC return shapes"
  key_links:
    - from: "app/scripts/verify-deploy.ts"
      to: "pg_proc get_dashboard_summary/get_exercise_summary"
      via: "phase12Functions pg_proc INVOKER + search_path check"
      pattern: "phase12Functions"
    - from: "app/scripts/test-rls.ts"
      to: "supabase.rpc cross-user assertion"
      via: "clientA calls RPC, asserts B's data not surfaced"
      pattern: "get_dashboard_summary"
---

<objective>
Ship migration `0011_phase12_dashboard_rpcs.sql` with the two new read-only aggregate RPCs that the dashboard hero, History volume card, and chart hero/stats consume — then push, regenerate types, and wire all three verification gates (verify-deploy, cross-user test:rls, Wave-0 streak/week fixtures).

Purpose: This is the data foundation. Wave 2 query hooks and Wave 3 screen re-skins cannot show real aggregates until these RPCs exist live. tsc passes off the migration file alone (false-positive), so the live push + cross-user RLS test are the real gates.
Output: 2 deployed RPCs, regenerated database.ts, extended verify-deploy + test-rls, new fixture script.
</objective>

<execution_context>
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-history-detail-chart-home-dashboard/12-RESEARCH.md
@.planning/phases/12-history-detail-chart-home-dashboard/12-PATTERNS.md
@.planning/phases/12-history-detail-chart-home-dashboard/12-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 0011 with get_dashboard_summary + get_exercise_summary</name>
  <files>app/supabase/migrations/0011_phase12_dashboard_rpcs.sql</files>
  <read_first>
    - app/supabase/migrations/0006_phase6_chart_rpcs.sql — the exact RPC template to copy byte-for-byte (file-header comment block, language sql / security invoker / stable / set search_path = '', fully-qualified public.*, set_type = 'working', s.finished_at is not null, per-function revoke/grant trailer)
    - app/supabase/migrations/0007_profiles_weekly_goal.sql — confirm profiles.weekly_goal column (int 1..7 default 3)
    - app/supabase/migrations/0001_initial_schema.sql — confirm exercise_sets.rpe nullability + weight_kg/reps/set_type/completed_at column names, and workout_sessions.started_at/finished_at
    - 12-RESEARCH.md §Mandate 1 (combined dashboard RPC SQL sketch + streak gaps-and-islands sketch + local-time week nuance) and §Mandate 2 (get_exercise_summary 6-col shape + avg-RPE filter)
    - 12-PATTERNS.md "0011_phase12_dashboard_rpcs.sql" section (the 5 deltas + non-negotiable clauses)
  </read_first>
  <action>
    Create the migration with TWO functions following 0006 exactly (D-23). Function 1: public.get_dashboard_summary(p_tz text default 'Europe/Stockholm') returns table (sessions_this_week int, weekly_goal int, streak_weeks int, volume_this_week_kg numeric, volume_prior_week_kg numeric, weekly_volume_series jsonb, lifetime_sessions int, lifetime_hours numeric). Use date_trunc('week', s.started_at at time zone p_tz) for Mon-Sun local bucketing (D-06; date_trunc('week') is Monday-based). volume = coalesce(sum(es.weight_kg * es.reps),0) filtered set_type='working'. weekly_volume_series = jsonb_agg of {week, volume_kg} over a generate_series(11,0,-1) calendar spine joined to per-week aggregates (last 12 weeks, D-08). lifetime_hours = sum(extract(epoch from (finished_at - started_at))/3600.0). streak_weeks via the islands form from RESEARCH §Mandate 1: qualifying weeks (count(*) >= weekly_goal) → 60-week calendar spine → row_number islands → count the island touching rn<=2 (the <=2 boundary keeps a streak alive across an in-progress current week; lock it with the Task 3 fixture). Function 2: public.get_exercise_summary(p_exercise_id uuid, p_metric text, p_since timestamptz) returns table (current_best numeric, range_first_value numeric, top_set_weight_kg numeric, top_set_reps int, vol_per_session_kg numeric, avg_rpe numeric). current_best = max(weight_kg) when p_metric='weight' else latest-session volume; avg_rpe = avg(es.rpe) filter (where es.set_type='working') (Postgres AVG ignores NULLs, returns NULL only when all NULL → UI shows '–'). vol_per_session averaged over finished sessions that include this exercise in range. Both: language sql, security invoker, stable, set search_path = '', fully-qualified public.workout_sessions/exercise_sets/profiles, set_type='working', s.finished_at is not null, per-function revoke all from public + grant execute to authenticated. Open with the 0006-style file-header comment block enumerating both functions + a "referenced by verify-deploy.ts" note.
  </action>
  <verify>
    <automated>grep -c "security invoker" app/supabase/migrations/0011_phase12_dashboard_rpcs.sql | grep -qx 2 && grep -q "set search_path = ''" app/supabase/migrations/0011_phase12_dashboard_rpcs.sql && grep -q "at time zone p_tz" app/supabase/migrations/0011_phase12_dashboard_rpcs.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - 0011 contains exactly 2 `create or replace function public.` definitions: get_dashboard_summary and get_exercise_summary
    - Both functions declare `security invoker` + `stable` + `set search_path = ''`
    - Dashboard week bucketing uses `date_trunc('week', ... at time zone p_tz)` (D-06 local Mon-Sun)
    - Every volume aggregate filters `set_type = 'working'` and `s.finished_at is not null`
    - avg_rpe uses `avg(es.rpe) filter (where es.set_type = 'working')` (D-14 NULL handling)
    - Each function has a `revoke all ... from public; grant execute ... to authenticated;` trailer
  </acceptance_criteria>
  <done>Migration file written with both RPCs conforming to the 0006 pattern + the local-time week + islands-streak + jsonb sparkline deltas.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Push migration 0011, regenerate types, verify deployed state</name>
  <files>app/types/database.ts</files>
  <read_first>
    - CLAUDE.md "Database conventions" — migration-as-truth, gen:types co-commit, verify-deploy.ts is the Docker-free drift check
    - 12-RESEARCH.md §Mandate 6 (verify gates table) and §Environment Availability (push command + non-TTY workaround)
  </read_first>
  <action>
    From app/ run `supabase db push` to deploy 0011 to the live project (non-TTY: set SUPABASE_ACCESS_TOKEN env var; the push reads empty stdin as Y per the Phase 9 precedent). If push needs an interactive prompt that cannot be suppressed, STOP and flag for manual run. Then run `npm run gen:types` to regenerate app/types/database.ts (hand-editing forbidden — the new RPC return types appear automatically). database.ts MUST be co-committed in the SAME commit as the migration. Finally run `npx tsx --env-file=.env.local scripts/verify-deploy.ts` to confirm both functions exist in pg_proc as INVOKER with search_path set — but note Task 3 adds the phase12 block to that script, so run verify-deploy AFTER Task 3 lands (or run it twice: once to confirm push succeeded structurally, once after the block is added). This task is MANDATORY: tsc passes off the migration file without the push (false-positive); the live functions are required for the cross-user test:rls and all Wave 2 hooks.
  </action>
  <verify>
    <automated>cd app && npx tsx --env-file=.env.local -e "import postgres from 'postgres'; const sql=postgres(process.env.DATABASE_URL||process.env.SUPABASE_DB_URL); const r=await sql\`select proname from pg_proc where proname in ('get_dashboard_summary','get_exercise_summary')\`; console.log(r.length===2?'OK':'MISSING:'+r.length); await sql.end();" 2>/dev/null || echo "run verify-deploy.ts after Task 3"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` exits 0 (0011 applied live)
    - `npm run gen:types` regenerates app/types/database.ts (git diff shows the new RPC types)
    - pg_proc contains both get_dashboard_summary and get_exercise_summary
  </acceptance_criteria>
  <done>0011 deployed live; database.ts regenerated and ready to co-commit with the migration; deployed RPCs confirmed in pg_proc.</done>
</task>

<task type="auto">
  <name>Task 3: Wire verify-deploy, cross-user test:rls, and Wave-0 streak/week fixtures</name>
  <files>app/scripts/verify-deploy.ts, app/scripts/test-rls.ts, app/scripts/test-dashboard-aggregates.ts</files>
  <read_first>
    - app/scripts/verify-deploy.ts (lines 81-119, the phase6Functions block) — copy the exact pg_proc INVOKER + search_path loop shape
    - app/scripts/test-rls.ts (lines 938-1009, the Phase 6 cross-user RPC blocks) — copy the "A calls RPC on B's data → expect empty/no-leak" shape
    - 12-RESEARCH.md §Mandate 6 (verify-deploy.ts extension block + test-rls.ts extension block) and §Mandate 1 streak fixture spec (3 consecutive goal-weeks → 3; gap resets; current week not-yet-met but last 4 met → 4)
    - 12-VALIDATION.md Wave 0 Requirements + Phase Requirements→Test Map
  </read_first>
  <action>
    (a) verify-deploy.ts: add `const phase12Functions = ["get_dashboard_summary", "get_exercise_summary"];` with the identical pg_proc loop (assert rows.length===1, prosecdef===false for INVOKER, proconfig includes "search_path="), a phase12Failures counter, and process.exit(1) on failure — mirror lines 113-119. (b) test-rls.ts: add ONE cross-user assertion per new RPC (D-23). get_dashboard_summary: A calls it, assert returned aggregates reflect ONLY A's seeded sessions (B's finished sessions must NOT inflate A's lifetime_sessions/volume/sessions_this_week) — mirror the get_session_summaries non-leak block. get_exercise_summary: A calls it with B's exId → expect empty/zeroed (RLS-filtered) — mirror lines 987-1009. (c) NEW test-dashboard-aggregates.ts (Node tsx, extends the test-rls.ts header/pass-fail/try-finally-cleanup convention): seed a user with fixtures and assert streak_weeks for: 3 consecutive goal-weeks → 3; a gap-week → streak resets; current week not-yet-met but prior 4 weeks met → 4 (locks the <=2 boundary, A2). Also seed a Sunday-23:30-local session and assert it buckets into the correct ISO week (D-06 / Pitfall 1). Add an npm script `test:dashboard` to package.json if a runner alias is convenient (optional). Use service-role only within these Node scripts (audit gate: service_role stays out of bundled paths).
  </action>
  <verify>
    <automated>cd app && npx tsx --env-file=.env.local scripts/verify-deploy.ts && npm run test:rls && npx tsx --env-file=.env.local scripts/test-dashboard-aggregates.ts</automated>
  </verify>
  <acceptance_criteria>
    - verify-deploy.ts lists get_dashboard_summary AND get_exercise_summary in a phase12Functions block; running it exits 0 with both confirmed INVOKER + search_path set
    - `npm run test:rls` exits 0 with a new cross-user assertion per new RPC (no cross-user leak)
    - test-dashboard-aggregates.ts exits 0 — fixtures pass: (a) 3 consecutive goal-weeks → streak 3; (b) a gap week → streak resets; (c) in-progress current week already meeting goal → counts; in-progress not-yet-met → does NOT break prior streak; (d) a Sunday 23:30 local session buckets into the correct Mon–Sun week (UTC-vs-local boundary)
    - `git grep "service_role\|SERVICE_ROLE"` still matches only the allowed script/doc paths
  </acceptance_criteria>
  <done>All three verification gates green: deploy-check confirms the RPCs, cross-user RLS confirms no leak, streak/week fixtures confirm the aggregate logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RN client → Supabase RPC | Caller JWT crosses into the DB; RLS scopes all reads |
| client tz string → RPC `p_tz` | Untrusted display-only text parameter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information Disclosure | get_dashboard_summary | mitigate | `security invoker` + 0001 RLS scope every table read to caller's JWT; cross-user `test:rls` assertion proves B's sessions never inflate A's aggregates |
| T-12-02 | Information Disclosure | get_exercise_summary | mitigate | `security invoker` + 0001 RLS; cross-user `test:rls` assertion: A calling on B's exId returns empty/zeroed |
| T-12-03 | Tampering | `p_tz` parameter | mitigate | `p_tz` used ONLY as `AT TIME ZONE p_tz` (Postgres validates the zone string; never concatenated into dynamic SQL) — no injection vector. Treated as untrusted display-only input |
| T-12-04 | Elevation of Privilege | INVOKER function search_path | mitigate | `set search_path = ''` + fully-qualified `public.*` (D-23, copied from 0006) blocks search_path hijack |
| T-12-05 | Tampering/DoS | F13 shared write surface | mitigate | RPCs are read-only; no mutation defaults / query keys / persister scope / exercise_sets logging touched (D-24); `test:f13-brutal` gate runs in Wave 3 closeout |
| T-12-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed this phase (RESEARCH Package Legitimacy Audit: zero installs) — N/A but registered per contract |
</threat_model>

<verification>
- `grep -c "security invoker" 0011_*.sql` == 2; `set search_path = ''` present; `at time zone p_tz` present
- `npx tsx --env-file=.env.local scripts/verify-deploy.ts` exits 0 (phase12Functions confirmed)
- `npm run test:rls` exits 0 (cross-user no-leak per new RPC)
- test-dashboard-aggregates.ts exits 0 (streak + week-boundary fixtures)
- database.ts regenerated (git diff shows new RPC types), co-committed with the migration
</verification>

<success_criteria>
Both RPCs deployed live as security-invoker + search_path-locked functions; types regenerated; all three verification gates (deploy-check, cross-user RLS, streak/week fixtures) green. DASH-05 data foundation complete; the streak (D-07), week boundary (D-06), and sparkline series (D-08) semantics are proven by fixtures.
</success_criteria>

<output>
Create `.planning/phases/12-history-detail-chart-home-dashboard/12-01-SUMMARY.md` when done
</output>
