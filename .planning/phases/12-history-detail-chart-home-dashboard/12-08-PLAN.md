---
phase: 12-history-detail-chart-home-dashboard
plan: 08
type: execute
wave: 3
depends_on: ["12-02", "12-04"]
files_modified:
  - app/app/(app)/exercise/[exerciseId]/chart.tsx
autonomous: true
requirements: [SKIN-06, MOTN-03]
user_setup: []

must_haves:
  decisions: [D-10, D-11, D-12, D-13, D-14, D-17, D-18, D-20, D-21, D-24]
  truths:
    - "Chart uses a custom in-content Forge header (back + ellipsis), native Stack header hidden (D-17)"
    - "Metric toggle (Max vikt / Total volym) is preserved as a Forge segmented control (D-10)"
    - "Range selector is the 3-state 30d/90d/All, default 90d (D-11)"
    - "Hero shows the active-metric current best + range delta — real data, not e1RM (D-12)"
    - "A 3-stat row (top set / vol-per-session / avg RPE, '–' when no RPE) + the Senaste-10 tappable list are present (D-14)"
    - "The chart line draws left→right on mount, snapping under reduce-motion (MOTN-03)"
    - "matchFont FIT-67 fix is preserved; every figure converts to the chosen unit (D-20)"
  artifacts:
    - path: "app/app/(app)/exercise/[exerciseId]/chart.tsx"
      provides: "FChart re-skin: custom header + 3-state range + hero + 3-stat row + draw-on-mount"
      contains: "useExerciseSummaryQuery"
  key_links:
    - from: "app/app/(app)/exercise/[exerciseId]/chart.tsx"
      to: "useExerciseSummaryQuery + rangeToSince"
      via: "hero + 3-stat row + range-parameterized line"
      pattern: "useExerciseSummaryQuery"
---

<objective>
Re-skin the exercise chart to FChart: a custom Forge header (D-17), the preserved metric toggle (D-10) + the new 3-state range selector (D-11), a real current-best + range-delta hero (D-12, NOT e1RM), a 3-stat row + the preserved Senaste-10 list (D-14), and the chart line drawing on mount (MOTN-03). Preserve matchFont/tooltip/routing/memo-contract verbatim. Omit e1RM framing (D-13). Convert every figure (D-20), i18n every string (D-21).

Purpose: SKIN-06 (chart) + MOTN-03. The chart has regression-critical internals (FIT-67 matchFont, the memo contract, the tooltip worklet mirror, the Senaste-10 routing) that must survive the re-skin.
Output: a re-skinned exercise-chart screen.
</objective>

<execution_context>
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-history-detail-chart-home-dashboard/12-UI-SPEC.md
@.planning/phases/12-history-detail-chart-home-dashboard/12-PATTERNS.md
@.planning/phases/12-history-detail-chart-home-dashboard/12-04-SUMMARY.md
@.planning/phases/12-history-detail-chart-home-dashboard/12-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-skin chart — custom header + 3-state range + hero + 3-stat row + metric toggle (preserve internals)</name>
  <files>app/app/(app)/exercise/[exerciseId]/chart.tsx</files>
  <read_first>
    - app/app/(app)/exercise/[exerciseId]/chart.tsx (whole file) — preserve VERBATIM (regression-critical): matchFont FIT-67 fix (195, NEVER useFont(null)); memo contract chartData dep array EXACTLY [chartQuery.data] (146-153); tooltip worklet→SharedValue mirror (210-234); Senaste-10 tap-to-source-session routing router.push(/history/${row.session_id}) (376-379) + useExerciseTopSetsQuery; SegmentedControl reuse for the metric toggle (258-263)
    - app/lib/queries/exercise-chart.ts (post-12-04) — useExerciseSummaryQuery + ChartRange + rangeToSince
    - 12-UI-SPEC.md "Layout Contract → Exercise chart" (FChart 769-861) + Copywriting Contract chart rows + Color (selected segment = surface3 NEUTRAL not accent; line = accent stroke) + "Icon-only-control accessibility (FLAG-1)" (same back/moreOptions labels as detail)
    - design source: app/design v2/Sources/design/forge-screens.jsx FChart 769-861 (hero current-best NOT e1RM; OMIT e1RM framing 793-816)
    - app/lib/units.ts (formatWeight/formatVolume) + app/lib/prefs.ts (getPref fm:units)
    - 12-PATTERNS.md "app/app/(app)/exercise/[exerciseId]/chart.tsx" section (deltas)
  </read_first>
  <action>
    D-11 range: replace the 5-state WINDOW_OPTIONS (82-88) with the 3-state ChartRange 30d / 90d (default) / All mapped via the new rangeToSince; default state "90d" not "3M" (119). D-17 custom header: set headerShown:false (currently true, 248) + in-content Forge header (40px circular back + ellipsis, same accessibilityLabel t('back')/t('moreOptions') + role + 44px hit-slop as session detail). D-10 metric toggle: keep the SegmentedControl (Max vikt / Total volym, re-skinned Forge; selected segment surface3 neutral). D-12 hero: 52px display numeral = active-metric current_best + a forge-success range-delta chip (current_best - range_first_value for weight, % for volume) from useExerciseSummaryQuery(exerciseId, metric, range) — real data, NOT "Estimerat 1RM" (Phase 13). D-14 3-stat row (FChartStat: Tungaste set "{w} × {r}" / Volym per pass / Snitt RPE) from the summary RPC; render '–' when avg_rpe is null. MOTN-03 chart draw-on-mount: a Reanimated-driven Skia clip rect animating 0→full width (withSpring §07), re-fire when chartQuery.data identity changes (honor the memo contract — list chartData in the effect dep, not metric/range), snap on useReducedMotion(). D-20: route the Senaste-10 ${row.weight_kg} kg × ${row.reps} (389), tooltip strings (167-182), and y-axis formatYAxisLabel (236-237) through formatWeight/formatVolume — v1 chart predates the units helper. Re-skin bg-gray-100 dark:bg-gray-800 chart card + greys → forge-* (radius 20 surface card); re-derive Skia color props (accent/gridColor/tooltipBg, 108-115) from forge-* hexes. All hardcoded Swedish ("Senaste 10 passen", "Max vikt", "Inga pass i detta intervall") → t(). OMIT e1RM framing (D-13). PRESERVE matchFont, the memo contract, the tooltip mirror, and the Senaste-10 routing verbatim. Box styling in className (NativeWind 4).
  </action>
  <verify>
    <automated>cd app && grep -q "useExerciseSummaryQuery" "app/(app)/exercise/[exerciseId]/chart.tsx" && grep -q "headerShown: false" "app/(app)/exercise/[exerciseId]/chart.tsx" && grep -q "matchFont" "app/(app)/exercise/[exerciseId]/chart.tsx" && grep -q "90d" "app/(app)/exercise/[exerciseId]/chart.tsx" && grep -q "router.push" "app/(app)/exercise/[exerciseId]/chart.tsx" && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Range is the 3-state 30d/90d/All with default "90d"; the 5-state WINDOW_OPTIONS is gone
    - Custom header (headerShown:false + circular back/ellipsis with a11y labels); metric toggle preserved as Forge SegmentedControl (selected = surface3 neutral)
    - Hero shows current_best + range-delta from useExerciseSummaryQuery (NOT e1RM); 3-stat row renders with '–' for null avg_rpe
    - Chart line draw-on-mount present (Reanimated clip), re-fires on chartQuery.data identity, snaps under reduce-motion
    - matchFont preserved (no useFont(null)); memo contract dep array unchanged; Senaste-10 router.push routing preserved
    - Every figure routes through the units helpers (no raw `kg` literal in Senaste-10/tooltip/axis)
    - `npm run typecheck` + `npm run lint` exit 0
  </acceptance_criteria>
  <done>Chart matches FChart with the custom header, 3-state range, real current-best hero, 3-stat row, preserved Senaste-10, and draw-on-mount; FIT-67 + memo + routing intact; e1RM omitted.</done>
</task>

<task type="auto">
  <name>Task 2: F13 regression gate</name>
  <files>app/app/(app)/exercise/[exerciseId]/chart.tsx</files>
  <read_first>
    - app/scripts/verify-f13-brutal-test.ts
    - 12-CONTEXT.md D-24 + CLAUDE.md SKIN-08
  </read_first>
  <action>
    Confirm the chart re-skin touched no mutation defaults / existing query keys / persister scope / exercise_sets logging (D-24 — note it ADDS new range/summary keys but does not edit the 5-state factories). Run the brutal test.
  </action>
  <verify>
    <automated>cd app && npm run test:f13-brutal</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:f13-brutal` exits 0
  </acceptance_criteria>
  <done>F13 brutal test green after the chart re-skin.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| summary/chart RPC data → render | Zod-parsed in the hooks; display-only |
| exercise name (user content) → title | Rendered verbatim, never translated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-17 | Information Disclosure | chart hero/stats aggregates | mitigate | RLS-scoped security-invoker get_exercise_summary (T-12-02); cross-user test:rls proves no leak |
| T-12-18 | Tampering/DoS | F13 hot path | mitigate | Read-only re-skin; Task 2 test:f13-brutal gate; only NEW query keys added, existing factories untouched (D-24) |
| T-12-SC | Tampering | npm installs | mitigate | No packages installed |
</threat_model>

<verification>
- `npm run typecheck` + `npm run lint` exit 0
- `npm run test:f13-brutal` exits 0
- 3-state range default 90d; custom header; real current-best hero; matchFont + routing preserved; draw-on-mount present
- manual device UAT (line draw-on-mount, reduce-motion snap, unit toggle on axis + Senaste-10, axis labels visible = FIT-67) per VALIDATION
</verification>

<success_criteria>
SKIN-06 (chart) + MOTN-03 done; D-10 metric toggle, D-11 3-state range, D-12 real current-best hero, D-14 stats+Senaste-10, D-17 custom header, D-18/D-20/D-21 motion/units/i18n, D-13 e1RM omission all in place; FIT-67 not regressed; F13 green.
</success_criteria>

<output>
Create `.planning/phases/12-history-detail-chart-home-dashboard/12-08-SUMMARY.md` when done
</output>
