---
phase: 12-history-detail-chart-home-dashboard
plan: 05
type: execute
wave: 3
depends_on: ["12-02", "12-03", "12-04"]
files_modified:
  - app/app/(app)/(tabs)/index.tsx
autonomous: true
requirements: [DASH-01, DASH-02, DASH-05, MOTN-02, SKIN-06]
user_setup: []

must_haves:
  decisions: [D-01, D-02, D-03, D-04, D-07, D-18, D-19, D-20, D-21, D-24]
  truths:
    - "Home Planer tab shows an activity-ring hero (sessions-this-week / weekly_goal) above the unchanged plan list (D-01)"
    - "The hero shows a streak chip (weeks, not days) and a this-week-volume chip (D-01/D-07)"
    - "During an active session the ring-hero is replaced by the ActiveSessionBanner (D-02)"
    - "A brand-new user (0 sessions) sees a zeroed hero + 'Logga ditt första pass' nudge (D-04)"
    - "Skeleton shows only on a truly-empty cache; cached value shows instantly offline (D-03)"
  artifacts:
    - path: "app/app/(app)/(tabs)/index.tsx"
      provides: "ring hero region + active-session swap above the Phase-10 plan list"
      contains: "useDashboardSummaryQuery"
  key_links:
    - from: "app/app/(app)/(tabs)/index.tsx"
      to: "useDashboardSummaryQuery"
      via: "hero reads sessions_this_week/weekly_goal/streak_weeks/volume_this_week_kg"
      pattern: "useDashboardSummaryQuery"
    - from: "Home hero"
      to: "ProgressRing (animated) + ForgeChip"
      via: "ring fill + streak/volume chips"
      pattern: "ProgressRing"
---

<objective>
Add the Forge activity-ring hero to the Home (Planer) tab above the existing Phase-10 plan list: an animated ProgressRing (sessions-this-week / weekly_goal) + a weeks-streak chip + a this-week-volume chip, with the active-session swap (D-02), the zeroed-hero empty state (D-04), and offline-first skeleton gating (D-03). The plan list itself is unchanged.

Purpose: DASH-01/DASH-02 + MOTN-02 land on the user's first-glance surface. Consumes the Wave-1 animated ring, Wave-2 dashboard hook, and the units/i18n foundations.
Output: a re-skinned Home tab hero region.
</objective>

<execution_context>
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-history-detail-chart-home-dashboard/12-UI-SPEC.md
@.planning/phases/12-history-detail-chart-home-dashboard/12-PATTERNS.md
@.planning/phases/12-history-detail-chart-home-dashboard/12-04-SUMMARY.md
@.planning/phases/12-history-detail-chart-home-dashboard/12-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the ring hero + chips, wire the dashboard hook + empty/skeleton states</name>
  <files>app/app/(app)/(tabs)/index.tsx</files>
  <read_first>
    - app/app/(app)/(tabs)/index.tsx (whole file) — the Phase-10 Forge composition: TOKENS.light/.dark hex map (84-111, Skia primitives + Icon strokes consume hex since NativeWind classes don't apply inside a Skia canvas), useColorScheme/useTranslation, the existing useActiveSessionQuery import (~68), tabular-nums idiom, and the plan list (must stay unchanged)
    - app/components/active-session-banner.tsx — the already-Forge banner rendered in the hero slot when active (D-02)
    - app/components/ui/ProgressRing.tsx (post-12-03, animated) + ForgeChip + Icon
    - app/lib/queries/dashboard.ts (useDashboardSummaryQuery) + app/lib/prefs.ts (getPref fm:units for the unit) + app/lib/units.ts (formatVolume)
    - 12-UI-SPEC.md "Layout Contract → Home hero" (FHome 125-158: radius 24, padding 20, 104px ring stroke 11, eyebrow + big N/goal numeral + chip row) + Copywriting Contract Home rows + Color "accent reserved for" item 1
    - design source: app/design v2/Sources/design/forge-screens.jsx FHome lines 125-158 + FHomeActive 1766-1769
    - 12-PATTERNS.md "app/app/(app)/(tabs)/index.tsx" section (deltas)
  </read_first>
  <action>
    ADD a hero region ABOVE the existing plan list (leave the list code untouched). Render a surface ForgeCard (radius 24, padding 20, 16px side margin): left = ProgressRing 104px / stroke 11 / accent + brand-gradient fill (resolve forge-accent/gradFrom/gradTo to hex via the TOKENS map; D-01), center label "N / goal" using tabular-nums and the REAL count (D-19 overflow shows true count); right = "Pass denna vecka" eyebrow via t() + big "N / goal" numeral + a chip row: a streak ForgeChip with Icon name flame using t('weeks')/t('week') (singular at N=1; D-07 — NOT t('days')) + a this-week-volume ForgeChip via formatVolume(volume_this_week_kg, unit) (D-20, unit from getPref fm:units). Wire useDashboardSummaryQuery(): skeleton gate isPending && data === undefined → Forge skeleton (D-03); otherwise render with data — an all-zero row is the new-user zeroed hero (D-04) with an accent "Logga ditt första pass" ForgeButton (t() key). D-02: gate the hero with the existing useActiveSessionQuery — activeSession ? ActiveSessionBanner : RingHero (the hero + volume framing hides while a session is live). Box styling in className, not inline style() (NativeWind 4 naked-render rule). DO NOT touch any mutation/query-key/persister-scope (D-24).
  </action>
  <verify>
    <automated>cd app && grep -q "useDashboardSummaryQuery" "app/(app)/(tabs)/index.tsx" && grep -q "ProgressRing" "app/(app)/(tabs)/index.tsx" && grep -q "weeks" "app/(app)/(tabs)/index.tsx" && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - index.tsx imports + calls useDashboardSummaryQuery and renders ProgressRing in a hero above the plan list
    - The streak chip composes t('weeks')/t('week') (NOT t('days'))
    - Volume chip routes through formatVolume with the fm:units pref (no raw kg literal)
    - Active-session swap: ActiveSessionBanner renders in the hero slot when a session is live
    - Zeroed-hero new-user branch renders the accent nudge CTA; skeleton only on data===undefined
    - `npm run typecheck` and `npm run lint` exit 0
  </acceptance_criteria>
  <done>Home hero renders the animated ring + weeks-streak + volume chips, swaps to the banner when active, zeroes cleanly for new users, and hydrates offline. Plan list unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: F13 regression gate</name>
  <files>app/app/(app)/(tabs)/index.tsx</files>
  <read_first>
    - app/scripts/verify-f13-brutal-test.ts — what the brutal test asserts (the hot path is untouched by this read-side hero)
    - 12-CONTEXT.md D-24 + CLAUDE.md SKIN-08 standing constraint
  </read_first>
  <action>
    Confirm the hero addition touched no mutation defaults, query keys, persister scope-bindings, or exercise_sets logging behavior (D-24). Run the brutal test as the gate. If it fails, the hero introduced a hot-path regression — revert the offending change (the hero is read-only and must not affect F13).
  </action>
  <verify>
    <automated>cd app && npm run test:f13-brutal</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:f13-brutal` exits 0 (≤3s budget + never-lose-a-set intact)
  </acceptance_criteria>
  <done>F13 brutal test green after the Home hero re-skin.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard RPC data → hero render | Already Zod-parsed in the hook; display-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-11 | Information Disclosure | Home hero aggregates | mitigate | Data comes from the RLS-scoped security-invoker RPC (T-12-01); no client-side cross-user aggregation |
| T-12-12 | Tampering/DoS | F13 hot path | mitigate | Read-only hero; Task 2 runs test:f13-brutal as the gate; no mutation/queue/persister-scope touched (D-24) |
| T-12-SC | Tampering | npm installs | mitigate | No packages installed |
</threat_model>

<verification>
- `npm run typecheck` + `npm run lint` exit 0
- `npm run test:f13-brutal` exits 0
- Home hero renders ring + weeks-streak + volume chips; active swap + zeroed empty state present
- manual device UAT (ring fill animation, overflow glow, reduce-motion snap, zeroed new-user state) per VALIDATION manual-only table
</verification>

<success_criteria>
DASH-01 (ring), DASH-02 (streak), DASH-05 (empty state), MOTN-02 (animated fill) visible on Home; active-session swap (D-02), offline-first (D-03), unit conversion (D-20), i18n incl. weeks relabel (D-07/D-21) all in place; F13 green.
</success_criteria>

<output>
Create `.planning/phases/12-history-detail-chart-home-dashboard/12-05-SUMMARY.md` when done
</output>
