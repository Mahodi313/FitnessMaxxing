---
phase: 12-history-detail-chart-home-dashboard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/units.ts
  - app/locales/sv.json
  - app/locales/en.json
autonomous: true
requirements: [SKIN-06, DASH-03]
user_setup: []

must_haves:
  decisions: [D-07, D-20, D-21]
  truths:
    - "A toDisplayVolume/formatVolume helper converts tonnage to the chosen unit WITHOUT 0.5-lb rounding (D-20)"
    - "Every new screen string has sv+en flat keys routed through t() (D-21)"
    - "Streak chip uses weeks/week keys, not days/day (D-07 relabel)"
  artifacts:
    - path: "app/lib/units.ts"
      provides: "toDisplayVolume + formatVolume (additive, pure)"
      contains: "export function toDisplayVolume"
    - path: "app/locales/sv.json"
      provides: "all phase-12 sv keys incl. weeks/week"
      contains: "weeks"
    - path: "app/locales/en.json"
      provides: "all phase-12 en keys at parity"
      contains: "weeks"
  key_links:
    - from: "app/lib/units.ts"
      to: "KG_PER_LB"
      via: "imperial division without roundHalf"
      pattern: "toDisplayVolume"
---

<objective>
Land the two shared client dependencies the Wave-3 screen re-skins consume: the volume-conversion helpers in `units.ts` (D-20) and the full sv+en i18n key set for all three screens + the Home hero (D-21), including the dagar→veckor streak relabel (D-07).

Purpose: Sequenced before the screen re-skins so every screen can import `toDisplayVolume`/`formatVolume` and `t('...')` keys that already exist. No screen should invent a key or hand-roll a conversion.
Output: 2 new units helpers + the complete phase-12 locale key set at sv/en parity.
</objective>

<execution_context>
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-history-detail-chart-home-dashboard/12-UI-SPEC.md
@.planning/phases/12-history-detail-chart-home-dashboard/12-PATTERNS.md

<interfaces>
From app/lib/units.ts (existing, do not change):
- const KG_PER_LB = 0.45359237;
- const roundHalf = (n: number) => Math.round(n * 2) / 2;  // weights ONLY
- export type UnitPref = "metric" | "imperial";
- export function toDisplayWeight(kg: number, unit: UnitPref): number;  // imperial: roundHalf(kg/KG_PER_LB)
- export function formatWeight(kg: number, unit: UnitPref): string;     // "100 kg" / "220.5 lb"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add toDisplayVolume + formatVolume to units.ts</name>
  <files>app/lib/units.ts</files>
  <behavior>
    - toDisplayVolume(28720, "metric") === 28720 (passthrough)
    - toDisplayVolume(28720, "imperial") === 28720 / 0.45359237 (NO 0.5 rounding)
    - toDisplayVolume(NaN, "metric") === 0 (finite guard)
    - formatVolume(28720, "metric") returns a locale-formatted "kg"-suffixed string (sv-SE non-breaking-space grouping)
    - formatVolume(28720, "imperial") returns a "lb"-suffixed converted string
  </behavior>
  <read_first>
    - app/lib/units.ts (whole file) — copy the existing toDisplayWeight/formatWeight shape, KG_PER_LB, the Number.isFinite guard
    - 12-RESEARCH.md §Mandate 5 "Volume-conversion nuance" (divide WITHOUT half-rounding; lbs-volume numbers get large — accepted) + the formatNumber/toLocaleString idiom note
    - 12-PATTERNS.md "app/lib/units.ts" section (deltas)
  </read_first>
  <action>
    Add toDisplayVolume(kg, unit): imperial returns kg / KG_PER_LB (NO roundHalf — 0.5-lb plate granularity is meaningless on a tonnage sum, RESEARCH Pitfall 5), metric passthrough, Number.isFinite guard returning 0. Add formatVolume(kg, unit): convert via toDisplayVolume then locale-format with toLocaleString("sv-SE") for the Swedish non-breaking-space grouping (match the existing formatNumber idiom in history.tsx:77 / chart.tsx:90), suffix " kg" or " lb". Keep the module pure/importable from .tsx tests like the existing helpers. Do NOT touch toDisplayWeight/formatWeight/roundHalf.
  </action>
  <verify>
    <automated>cd app && npx tsx -e "import {toDisplayVolume,formatVolume} from './lib/units.ts'; const a=toDisplayVolume(28720,'imperial'); console.log(Math.abs(a-28720/0.45359237)<1e-6 && toDisplayVolume(28720,'metric')===28720 && toDisplayVolume(NaN,'metric')===0 ? 'OK':'FAIL', formatVolume(28720,'metric'));"</automated>
  </verify>
  <acceptance_criteria>
    - units.ts exports `toDisplayVolume` and `formatVolume`
    - toDisplayVolume imperial divides by KG_PER_LB with NO 0.5 rounding (assert against 28720/0.45359237)
    - Non-finite input returns 0
    - tsc clean: `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Two pure volume helpers added; weights helpers untouched; tsc green.</done>
</task>

<task type="auto">
  <name>Task 2: Add all phase-12 i18n keys (sv + en parity) incl. weeks/week relabel</name>
  <files>app/locales/sv.json, app/locales/en.json</files>
  <read_first>
    - app/locales/sv.json + app/locales/en.json (whole files) — confirm the existing flat-key convention + existing in-surface keys (weekVolume, weekSessions, streak, days, day, history, sets) so you do NOT duplicate
    - 12-UI-SPEC.md "Copywriting Contract" table (lines 127-160) — the authoritative sv→en string list for every element
    - 12-UI-SPEC.md "Icon-only-control accessibility (FLAG-1)" — back / moreOptions a11y label keys
    - 12-RESEARCH.md §Mandate 6 "Streak relabel (D-07)" — add weeks/week, keep days/day in place
    - 12-PATTERNS.md "app/locales/{sv,en}.json" section
  </read_first>
  <action>
    Add flat 1:1 keys (Phase 8 D-10 convention) for every string the four surfaces render, sourced from the UI-SPEC Copywriting Contract: weeks ("veckor"/"weeks") + week ("vecka"/"week") for the streak chip (D-07 — do NOT use existing days/day for the streak chip; leave those keys in place), back ("Tillbaka"/"Back"), moreOptions ("Fler alternativ"/"More options"), the Home hero eyebrow/empty-nudge ("Logga ditt första pass"/"Log your first workout"), History lifetime eyebrow + title + volume-card eyebrow/empty + row meta + empty-state heading/body/CTA, session-detail stats-grid labels + exercises header + max label + delete-confirm copy ("Ta bort pass?"/"Delete workout?", "Det här går inte att ångra."/"This can't be undone.", "Ta bort"/"Delete", "Avbryt"/"Cancel"), chart metric/range labels (Max vikt/Total volym, 30 dagar/90 dagar/Allt), chart stats row (Tungaste set/Volym per pass/Snitt RPE), Senaste 10 passen, loading ("Laddar…"/"Loading…"), and the no-cache error ("Kunde inte ladda. Dra för att uppdatera."/"Couldn't load. Pull to refresh."). Use {{N}}/{{kg}} interpolation where the contract shows counts. Keep sv.json and en.json at exact key parity. Do NOT translate user content (plan/exercise names, notes).
  </action>
  <verify>
    <automated>cd app && npm run check-locale-parity 2>/dev/null || npx tsx -e "const sv=require('./locales/sv.json'),en=require('./locales/en.json'); const a=Object.keys(sv).sort().join(),b=Object.keys(en).sort().join(); console.log(a===b && 'weeks' in sv && 'back' in sv && 'moreOptions' in sv ? 'OK':'PARITY/KEY FAIL');"</automated>
  </verify>
  <acceptance_criteria>
    - sv.json and en.json have identical key sets (parity check passes)
    - Keys `weeks`, `week`, `back`, `moreOptions` exist in both files
    - The existing `days`/`day` keys remain present (not deleted)
    - Every Copywriting-Contract element has a corresponding key
  </acceptance_criteria>
  <done>Full phase-12 key set added at sv/en parity, streak relabel keys present, existing keys preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure client-side utility + static locale data; no trust boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06 | Tampering | units conversion math | accept | Pure deterministic functions on already-validated numeric input; no external input; covered by the behavior test |
| T-12-SC | Tampering | npm installs | mitigate | No packages installed (RESEARCH: zero installs) |
</threat_model>

<verification>
- `npm run typecheck` exits 0 after units.ts change
- locale parity check passes; weeks/week/back/moreOptions present in both files
- toDisplayVolume behavior assertion passes (imperial = kg/KG_PER_LB, no rounding)
</verification>

<success_criteria>
Volume helpers and the complete i18n key set exist before any screen re-skin, so Wave-3 screens import them with zero invention. D-20 (volume conversion), D-21 (i18n sweep), D-07 (streak relabel keys) foundations in place.
</success_criteria>

<output>
Create `.planning/phases/12-history-detail-chart-home-dashboard/12-02-SUMMARY.md` when done
</output>
