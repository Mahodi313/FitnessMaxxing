---
phase: 7
slug: v1-polish-cut
status: draft
shadcn_initialized: false
preset: none
created: 2026-05-15
---

# Phase 7 — UI Design Contract

> Visual and interaction contract for V1 polish-cut surfaces: F11 inline RPE-input on workout-loggning + RPE-suffix in session-history; F12 pass-anteckning capture in `AvslutaOverlay` + read/edit block in session-history; F15 manual theme-toggle in Inställningar (3-läges, AsyncStorage-persisterad, immediate apply without restart). Generated by gsd-ui-researcher; to be verified by gsd-ui-checker.

> **shadcn note:** shadcn/ui is React-DOM only and incompatible with React Native. The locked stack (CLAUDE.md TL;DR) is NativeWind 4 + Tailwind 3 — Phase 7 inherits the Phase 3/4/5/6 UI-SPEC convention `Tool: none`. The shadcn initialization gate is **not applicable**.

> **Contract scope:** Phase 7 is the final UI-only phase before 4-week personal soak. NO new components are built — three existing screens (`workout/[sessionId].tsx`, `history/[sessionId].tsx`, `(tabs)/settings.tsx`) gain three new surfaces. The reusable `<SegmentedControl>` (Phase 6) is consumed verbatim. The reusable inline-overlay-pattern (Phase 4 commit `e07029a`) is consumed verbatim for the new edit-notes overlay. All design tokens (spacing, typography, color, status-bar) are inherited unchanged from Phase 6.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (NativeWind 4 — shadcn is React-DOM only and incompatible with React Native) |
| Preset | not applicable |
| Component library | none — primitives composed from `react-native` (`View`, `Text`, `Pressable`, `TextInput`, `KeyboardAvoidingView`, `ScrollView`, `SafeAreaView`) styled with NativeWind classes; reuses `app/components/segmented-control.tsx` (Phase 6) verbatim for the F15 tema-toggle |
| Icon library | `@expo/vector-icons` Ionicons (inherited from Phase 1/4/6 — D-18) |
| Font | System default (San Francisco on iOS via `react-native`); no custom font loaded — unchanged through Phase 7 |

**Tailwind config inheritance:** `app/tailwind.config.js:9` uses `darkMode: 'class'` with empty `theme.extend`. Phase 7 is the phase that activates the **manual override** of this class via NativeWind's `setColorScheme()` API, but the config itself is **unchanged**. All Tailwind v3 default tokens stay available.

**New libraries entering Phase 7:** None at the package level. `@react-native-async-storage/async-storage@2.2.0` is already installed (used by `LargeSecureStore`); Phase 7 reads/writes the new key `fm:theme` directly. NativeWind 4.2's `useColorScheme()` from `'nativewind'` is already installed (peer of NativeWind 4); Phase 7 migrates 10 import sites from `'react-native'` → `'nativewind'` (D-T4 — mechanical rename, no API change).

**Source-of-truth diff target** (CONTEXT.md `<canonical_refs>` → Source-of-truth diff target):

| File | Change |
|------|--------|
| `app/app/(app)/workout/[sessionId].tsx` | Inline-raden line 476-554: insert `[RPE]`-Controller between Reps and Klart; shrink Klart `w-20`→`w-16`. AvslutaOverlay 806-917: insert `<KeyboardAvoidingView>` + `<TextInput multiline>` + counter. |
| `app/app/(app)/history/[sessionId].tsx` | Insert notes-block above SummaryHeader-chiparna (D-E4); append `· RPE {rpe}` suffix on set-rows when `set.rpe !== null` (D-2); insert edit-overlay (D-E2) using Phase 4 inline-overlay-pattern. |
| `app/app/(app)/(tabs)/settings.tsx` | Replace placeholder text (`Mer kommer i Phase 7.`) with `Tema`-section: heading + `<SegmentedControl>` instance + `useColorScheme().setColorScheme()` wire + `AsyncStorage.setItem('fm:theme', value)`. |
| `app/app/_layout.tsx` | Insert `<ThemeBootstrap />` sibling that reads `AsyncStorage.getItem('fm:theme')` and calls `setColorScheme()` BEFORE `SplashScreen.hideAsync()` (D-T2); switch `<StatusBar style="auto" />` to `style={isDark ? 'light' : 'dark'}` reading from `nativewind` `useColorScheme()` (D-T3). |
| `app/lib/queries/sessions.ts` | Extend `useFinishSession` payload type with `notes: string \| null` (D-N3); add new `useUpdateSessionNotes(sessionId)` hook (D-E3). |
| `app/lib/query/client.ts` | Add 14th `setMutationDefaults(['session','update-notes'], ...)` (D-E3) — onMutate snapshot + setQueryData on `sessionsKeys.detail(id)` + onError rollback + onSettled invalidate `sessionsKeys.listInfinite()`. |
| `app/lib/schemas/sets.ts` | Stretch `setFormSchema.rpe` from `z.coerce.number().nullable().optional()` to `z.preprocess(comma→period→null-on-empty, z.coerce.number().min(0,…).max(10,…).nullable().optional())` (D-R2 + D-R3). |
| `useColorScheme` import migration (10 files) | `'react-native'` → `'nativewind'` (D-T4) — atomic mechanical commit: `app/_layout.tsx`, `(app)/_layout.tsx`, `(app)/(tabs)/_layout.tsx`, `(app)/(tabs)/index.tsx`, `(app)/(tabs)/history.tsx`, `(app)/plans/[id].tsx`, `(app)/plans/[id]/exercise-picker.tsx`, `(app)/history/[sessionId].tsx`, `(app)/exercise/[exerciseId]/chart.tsx`, `components/active-session-banner.tsx`. API-compatible drop-in (returns same `{ colorScheme, setColorScheme }` shape). |
| **NO change** | `app/supabase/migrations/*` (Phase 2 already landed `rpe`+`notes` columns); `app/types/database.ts`; `app/lib/supabase.ts`; `app/lib/schemas/sessions.ts` (already F12-ready); `app/components/segmented-control.tsx` (consumed verbatim); `app/components/offline-banner.tsx`; `app/components/active-session-banner.tsx` (only the import line changes); `app/scripts/test-rls.ts`; `app/tailwind.config.js`. |

---

## Spacing Scale

Declared values (all multiples of 4, mapped to Tailwind v3 default spacing tokens). **Identical scale to Phase 3 + Phase 4 + Phase 5 + Phase 6 UI-SPECs** for cross-phase visual continuity.

| Token | Value | Tailwind class | Usage in Phase 7 |
|-------|-------|----------------|------------------|
| xs | 4px | `p-1`, `gap-1`, `mt-1`, `px-1` | Inline-error padding under RPE-input (`mt-1 px-1` matches Vikt/Reps); RPE TextInput placeholder breath inside `text-center` |
| sm | 8px | `p-2`, `gap-2`, `mt-2`, `mb-2`, `hitSlop={8}` | Pencil-icon `hitSlop` (D-E1); add-anteckning Pressable `gap-2` between `+`-icon and label; gap inside notes-block between text and pencil-icon; `mt-2` between notes-block and SummaryHeader-chiparna; gap between RPE-input and Klart-button `gap-2` (matches existing inline-row) |
| md | 16px | `p-4`, `gap-4`, `mt-4`, `mb-4`, `px-4`, `gap:16` | Notes-block `px-4 py-3 mx-4` (matches existing OfflineBanner / ActiveSessionBanner geometry); `<KeyboardAvoidingView>` inner padding; AvslutaOverlay inner card `gap:16` (existing — preserved); padding around tema-toggle section in Inställningar |
| lg | 24px | `p-6`, `gap-6` | AvslutaOverlay inner card `p-6` (existing — preserved); edit-notes-overlay inner card `p-6` (matches Phase 4 destructive-confirm-overlay geometry); gap between Inställningar email-row and tema-section heading |
| xl | 32px | `p-8`, `mt-8` | Reserved — not used in Phase 7 |
| 2xl | 48px | `p-12`, `pt-12` | Inställningar screen top padding (`pt-12` — existing) inherited |
| 3xl | 64px | `pt-16` | Reserved — not used in Phase 7 |

**RPE-fält fixed-width (D-R1 — load-bearing):** `w-16` (64pt). Vikt + Reps keep `flex-1`; Klart-button shrinks `w-20` (80pt) → `w-16` (64pt) to fit iPhone SE-bredd (320pt content). Klart preserves ≥44pt tap-target via existing `min-h-[56px]`. RPE max 4 chars (`8.5`, `10`, `10.0`) fits 64pt with `text-base font-semibold` + `text-center`.

**Notes TextInput height bounds (D-N2 — load-bearing):** `style={{ minHeight: 80, maxHeight: 160 }}`. Both values are 4-multiples (80 = 20×4, 160 = 40×4). Auto-grows within the interval as the user types; rejects beyond `maxLength=500` chars. `numberOfLines={3}` provides initial visual hint that this is a multi-line field.

**Touch target minimum:** All interactive elements ≥ 44×44pt per Apple HIG (matches Phase 3/4/5/6 contract).
- **RPE TextInput**: `min-h-[56px]` (matches Vikt/Reps in same row) → 56pt vertical hit area; horizontal 64pt — exceeds 44pt floor.
- **Klart-button (post-shrink)**: `w-16 min-h-[56px]` → 64×56pt — exceeds 44×44pt floor by `w` and `h`.
- **Pencil-icon (history-detail edit-affordance, D-E1)**: 18pt Ionicon + `hitSlop={8}` → 34pt visible + 16pt slop = 50pt effective. Icon `accessibilityLabel="Redigera anteckning"`.
- **Add-anteckning Pressable (D-E1)**: `flex-1` row containing `<Ionicons size=18>` + `<Text text-base>` inside `bg-gray-100 dark:bg-gray-800 rounded-lg px-4 py-3 mx-4` shell → ~46pt vertical content + `hitSlop={8}` → ≥54pt effective. Full row Pressable; `accessibilityLabel="Lägg till anteckning"`.
- **Edit-notes-overlay buttons (Avbryt / Spara)**: `py-4` (16px) + `text-base` (16pt) → ~48pt — same primary-CTA dimensions as Phase 4 destructive-confirm.
- **Tema segmented-control segments**: inherits Phase 6 `<SegmentedControl>` `flex-1 py-2 px-3` + parent `p-1` + `hitSlop={{ top: 4, bottom: 4 }}` → 44pt effective vertical.
- **AvslutaOverlay TextInput (notes capture, D-N2)**: `minHeight: 80` → 80pt content + 16pt KeyboardAvoidingView padding → exceeds 44pt floor handily.

Exception: `py-3` / `px-3` (12px, Tailwind spacing-3) used for inner input vertical padding on RPE TextInput, notes-block container, and edit-overlay TextInput. Chosen to match the existing Vikt/Reps inline-row vertical rhythm established in Phase 5; 4-multiple but outside the declared 7-token set. Contained to input-padding contexts only — does not propagate to layout spacing.

---

## Typography

Tailwind v3 default `fontSize` scale, restricted to four roles. **Identical role table to Phase 3/4/5/6 UI-SPECs.**

| Role | Size | Tailwind class | Weight | Tailwind weight class | Line Height |
|------|------|----------------|--------|------------------------|-------------|
| Body | 16px | `text-base` | 400 (regular) | `font-normal` (default) | 1.5 (Tailwind ships `lh-6` = 24px) |
| Label | 14px | `text-sm` | 600 (semibold) | `font-semibold` | 1.43 (Tailwind ships `lh-5` = 20px) |
| Heading | 24px | `text-2xl` | 600 (semibold) | `font-semibold` | 1.33 (Tailwind ships `lh-8` = 32px) |
| Display | 30px | `text-3xl` | 600 (semibold) | `font-semibold` | 1.2 (Tailwind ships `lh-9` = 36px) |

**Counter (notes char-count) sizing:** The `{notes.length}/500` counter uses **`text-sm` (Label role)** — 14px, smaller than Body (16px) so it visually subordinates to the input above it, but still within the standard 4-size scale (no exception entry). Color flips between Muted (`text-gray-500 dark:text-gray-400`) and Destructive (`text-red-600 dark:text-red-400`) at `length > 480` per D-N2. Used in two instances (AvslutaOverlay capture + history edit-overlay) — both consume the same Label size.

**Two weights only:** `font-normal` (400) for body, field input text, RPE-suffix on history set-rows, notes text in history notes-block, and edit-overlay TextInput value; `font-semibold` (600) for labels, headings, the display, primary CTA labels (Klart, Spara), tema-section heading "Tema", currently-selected SegmentedControl segment label, and "Inställningar" screen heading. No `font-bold`, no `font-medium`, no `font-light`.

**Per-screen role mapping (Phase 7 — only the new/changed elements):**

| Surface | Element | Role |
|---------|---------|------|
| `workout/[sessionId].tsx` inline-row | RPE TextInput value | Body bumped to **`text-base font-semibold text-center`** — matches existing Vikt/Reps weight; centered because 64pt narrow column with up-to-4-char value reads better centered (Vikt/Reps stay default-aligned because they have wider content) |
| `workout/[sessionId].tsx` inline-row | RPE TextInput placeholder `"RPE"` | Body (`text-base`) — color `placeholderTextColor="#9CA3AF"` (existing Vikt/Reps convention) |
| `workout/[sessionId].tsx` inline-row | RPE inline error `"RPE 0 eller högre"` / `"RPE 10 eller lägre"` | Body (`text-base font-normal text-red-600 dark:text-red-400 mt-1 px-1`) — matches Vikt/Reps inline-error pattern verbatim |
| `workout/[sessionId].tsx` AvslutaOverlay | Notes TextInput value | Body (`text-base text-gray-900 dark:text-gray-50`) |
| `workout/[sessionId].tsx` AvslutaOverlay | Notes TextInput placeholder `"Anteckningar (valfri)"` | Body — placeholder color `#9CA3AF` |
| `workout/[sessionId].tsx` AvslutaOverlay | Char counter `"123/500"` | **Label (`text-sm`)** — `text-gray-500 dark:text-gray-400` default; flips to `text-red-600 dark:text-red-400` when `notes.length > 480` |
| `history/[sessionId].tsx` set-row | RPE-suffix `· RPE {rpe}` (when not null) | Body (`text-base font-normal text-gray-500 dark:text-gray-400`) — Muted because it's metadata appended after the primary `Set N: weight × reps`; the leading `·` separator (middle dot, U+00B7) is part of the suffix, not standalone |
| `history/[sessionId].tsx` notes-block | Notes display text (when `notes !== null`) | Body (`text-base font-normal text-gray-900 dark:text-gray-50`) — primary text color because the user's anteckning is content, not metadata |
| `history/[sessionId].tsx` notes-block | Add-anteckning label `"Lägg till anteckning"` (when `notes === null`) | Body (`text-base font-normal text-gray-500 dark:text-gray-400`) — Muted because it's an empty-state affordance |
| `history/[sessionId].tsx` edit-overlay | Title `"Redigera anteckning"` | Heading (`text-2xl font-semibold text-gray-900 dark:text-gray-50`) — matches Phase 4/6 destructive-confirm-overlay title weight |
| `history/[sessionId].tsx` edit-overlay | TextInput value | Body (`text-base text-gray-900 dark:text-gray-50`) |
| `history/[sessionId].tsx` edit-overlay | Char counter `"123/500"` | **Label (`text-sm`)** — same color rules as AvslutaOverlay counter |
| `history/[sessionId].tsx` edit-overlay | Avbryt button label | Body (`text-base font-semibold text-gray-900 dark:text-gray-50`) — neutral on `bg-gray-200 dark:bg-gray-700` surface |
| `history/[sessionId].tsx` edit-overlay | Spara button label | Body (`text-base font-semibold text-white`) on accent (`bg-blue-600 dark:bg-blue-500`) |
| `(tabs)/settings.tsx` | Section heading `"Tema"` | Body bumped to **`text-base font-semibold text-gray-900 dark:text-gray-50`** — section header within the Inställningar Display "Inställningar" hierarchy; not a standalone Heading because Inställningar already owns the Display + this is a sub-section label |
| `(tabs)/settings.tsx` | SegmentedControl segment labels (`System`, `Ljust`, `Mörkt`) | Label (`text-sm font-semibold`) — inherited from Phase 6 SegmentedControl primitive; selected `text-gray-900 dark:text-gray-50`, unselected `text-gray-500 dark:text-gray-400` |

**StatusBar dynamic style (D-T3 — load-bearing):** Bumped from `<StatusBar style="auto" />` (Phase 1 convention) → `<StatusBar style={isDark ? 'light' : 'dark'} />` where `isDark = useColorScheme().colorScheme === 'dark'` from `'nativewind'`. The `'auto'` mode reads iOS system theme directly and ignores manual override; explicit `'light'`/`'dark'` driven by NativeWind's resolved colorScheme (which respects the manual override AND falls through to system when set to `'system'`) guarantees status-bar icon contrast across all 9 combinations (3 user-modes × iOS auto/light/dark). This is a typographic-equivalent decision (status-bar icons are essentially typography on the OS-rendered bar) and is the only Phase 1 convention that Phase 7 amends.

---

## Color

60/30/10 split mapped to NativeWind classes that auto-flip in dark mode (F15 convention, Phase 1; **Phase 7 is the phase that adds the manual override mechanism on top of the convention**). **Identical color contract to Phase 3/4/5/6 UI-SPECs** with no new semantic roles — Phase 7 reuses Body/Muted text + Accent + Destructive + Secondary surface for all new elements. The notes-block uses the same Secondary (`bg-gray-100 dark:bg-gray-800`) shell as OfflineBanner / ActiveSessionBanner / chip-shells / segmented-control-track for visual continuity across read-side polish and now V1-cut polish.

| Role | Light value | Dark value | Tailwind class pair | Usage in Phase 7 |
|------|-------------|------------|----------------------|------------------|
| Dominant (60%) | `#FFFFFF` (white) | `#111827` (gray-900) | `bg-white dark:bg-gray-900` | Inställningar screen background (existing); workout/history screen backgrounds (existing); RPE TextInput inner background (matches Vikt/Reps); edit-overlay backdrop is rgba(0,0,0,0.5) overlay on top of Dominant |
| Secondary (30%) | `#F3F4F6` (gray-100) | `#1F2937` (gray-800) | `bg-gray-100 dark:bg-gray-800` | History notes-block shell; AvslutaOverlay inner card (existing); edit-notes-overlay inner card (matches Phase 4/6 destructive-confirm); SegmentedControl track (inherited from Phase 6 primitive); edit-overlay Avbryt-button background `bg-gray-200 dark:bg-gray-700` (slightly darker step within the Secondary family for button-vs-card-surface separation — matches Phase 4/5/6 Avbryt-button convention) |
| Accent (10%) | `#2563EB` (blue-600) | `#60A5FA` (blue-400) | `text-blue-600 dark:text-blue-400` for icons; `bg-blue-600 dark:bg-blue-500` for button surfaces; `border-blue-600 dark:border-blue-500` for focused inputs | **Reserved for: see "Accent reserved for" list below.** RPE TextInput focus border (matches Vikt/Reps `focus:border-blue-600 dark:focus:border-blue-500`); add-anteckning `+`-icon tint when `notes === null`; edit-overlay Spara-button background; SelectedSegment shadow (inherited from Phase 6 SegmentedControl primitive — selectedShadow tokens) |
| Destructive | `#DC2626` (red-600) | `#F87171` (red-400) | `text-red-600 dark:text-red-400` for inline errors; `text-red-600 dark:text-red-400` for over-limit counter | RPE inline error text (`"RPE 0 eller högre"` / `"RPE 10 eller lägre"`); RPE input border on error (`border-red-600 dark:border-red-400` — matches Vikt/Reps); char-counter color when `notes.length > 480` |
| Warning/Info | `bg-yellow-200 dark:bg-yellow-900` + `text-yellow-900 dark:text-yellow-100` + `border border-yellow-400 dark:border-yellow-700` | (same) | (Phase 4-amended class strings) | **OfflineBanner only** — inherited from Phase 4; banner is mounted globally in `(tabs)/_layout.tsx`. ActiveSessionBanner uses Info-blue (Phase 5). Phase 7 introduces NO new banner roles. |
| Body text | `#111827` (gray-900) | `#F9FAFB` (gray-50) | `text-gray-900 dark:text-gray-50` | RPE TextInput value; notes display text in history notes-block; edit-overlay TextInput value; tema-section heading "Tema"; AvslutaOverlay TextInput value; SegmentedControl selected-segment label (inherited from Phase 6 primitive) |
| Muted text | `#6B7280` (gray-500) | `#9CA3AF` (gray-400) | `text-gray-500 dark:text-gray-400` | RPE-suffix on history set-rows (`· RPE 8.5`); add-anteckning label (`Lägg till anteckning`); pencil-icon tint on history notes-block (when `notes !== null`); char-counter default color; SegmentedControl unselected-segment label (inherited); placeholders use direct hex `#9CA3AF` (gray-400 light value — matches existing convention) |

**Accent reserved for (Phase 7 additions to the cross-phase list):**

Carried forward from Phase 6 unchanged:
1. Chart line color (Skia `<Line>` + `<Scatter>` point fill)
2. Chart-press callout ring
3. Chart-ikon on `plans/[id].tsx` plan_exercise rows (D-24)
4. Empty-state icon on `(tabs)/history.tsx` (Ionicons `time-outline` size 64)
5. Empty-state icon on `/exercise/[exerciseId]/chart` (Ionicons `stats-chart-outline` size 64)
6. Tab-bar active tint (`tabBarActiveTintColor`)

**New Phase 7 additions:**
7. RPE TextInput **focus** border (`focus:border-blue-600 dark:focus:border-blue-500`) — matches Vikt/Reps focus border (Phase 5); accent appears only while focused, otherwise neutral border
8. Add-anteckning `+`-icon tint (`color={accent}`) on history notes-block when `notes === null` — telegraphs an interactive empty-state CTA (matches Phase 4/6 empty-state-icon convention)
9. Edit-overlay Spara-button background (`bg-blue-600 dark:bg-blue-500`) — primary CTA inside the edit-overlay (matches Phase 3 sign-in primary CTA + Phase 5 Avsluta primary CTA)
10. SegmentedControl selected-segment shadow (inherited Phase 6 primitive — `selectedShadow` Skia-equivalent iOS shadow on the active segment)

**Accent must NOT be used for:**
- Pencil-icon tint on history notes-block when `notes !== null` (uses Muted — pencil is a discoverable affordance, not a primary CTA; accent would over-emphasize and steal attention from notes content)
- RPE TextInput border in default/unfocused state (uses neutral `border-gray-300 dark:border-gray-700` — matches Vikt/Reps)
- "Tema"-section heading (uses Body — section labels are not interactive)
- Notes display text in history notes-block (uses Body — content, not interactive)
- AvslutaOverlay knapparna `Fortsätt` / `Avsluta` (existing — preserved; `Avsluta` uses Accent per Phase 5 PITFALLS §6.6 — accent-blå INTE röd because Avsluta is positive-completion, not data-loss)
- Edit-overlay Avbryt-button (uses Body text on `bg-gray-200 dark:bg-gray-700` — non-destructive neutral, matches Phase 4 inline-overlay Avbryt convention)

**Destructive color used in Phase 7 (3 places):**
1. **RPE inline error text + border** — `text-red-600 dark:text-red-400` text under field; `border-red-600 dark:border-red-400` on TextInput. Triggered by Zod schema rejection (`< 0` or `> 10`).
2. **Char-counter color when over warning threshold** — `text-red-600 dark:text-red-400` when `notes.length > 480` (warning band before hard 500 max). Returns to Muted when below 480.
3. **(No destructive-action button in Phase 7)** — Phase 7 introduces no destructive actions. Notes-clearing (saving an empty edit-field) is **non-destructive** (recoverable by re-entering text); Spara button uses Accent. Theme-toggle is **non-destructive** (reversible by selecting another segment). Therefore no red button surfaces are added in Phase 7. Inheritance: history-detail's existing delete-pass overlay (Phase 6) keeps its red `Ta bort`-button unchanged.

**Theme toggle propagation (D-T1 + D-T4 — load-bearing across all 6 surfaces above):** All `dark:`-variant resolutions in Phase 7 (and inherited from Phase 1–6) are driven by NativeWind 4's `useColorScheme()` from `'nativewind'`. When the user selects "Mörkt" in Inställningar, `setColorScheme('dark')` flips the root `dark` class instantly — every `dark:` Tailwind variant on screen reflects the change without a re-mount. **No new design tokens** are introduced; the existing Tailwind v3 default palette is exhaustive for the manual-override use case. This is the foundational mechanism that lets Phase 7 ship a 3-läges toggle with zero new color decisions.

**Non-className styling that requires `useColorScheme()` (D-T4 audit):** 6 sites use `useColorScheme()` for runtime hex/style-prop selection (NOT NativeWind class), and ALL of them must be migrated from `'react-native'` to `'nativewind'` so the manual override propagates:
1. `app/app/_layout.tsx` — root Stack `contentStyle.backgroundColor` + `<StatusBar style={...}>` (D-T3)
2. `app/app/(app)/_layout.tsx` — (app) Stack header backdrop (Phase 4 commit `b57d1c2`)
3. `app/app/(app)/(tabs)/_layout.tsx` — Tabs `tabBarActiveTintColor` / `tabBarInactiveTintColor`
4. `app/app/(app)/(tabs)/history.tsx` — RefreshControl + ActivityIndicator tint
5. `app/app/(app)/exercise/[exerciseId]/chart.tsx` — Skia chart line color + tooltip ring
6. `app/components/active-session-banner.tsx` — Ionicon iconColor

The remaining 4 of 10 import sites (`(tabs)/index.tsx`, `plans/[id].tsx`, `plans/[id]/exercise-picker.tsx`, `history/[sessionId].tsx`) currently consume `useColorScheme()` for inline-overlay backdrop or icon-tint — same migration pattern. Drop-in API guarantee: NativeWind 4's `useColorScheme()` returns `{ colorScheme, setColorScheme }`; the existing call-sites only read `.colorScheme` so the rename is mechanical.

**StatusBar contrast guarantee (D-T3):** Three theme-modes (`system` / `light` / `dark`) × three iOS-modes (`auto` / `light` / `dark`) = 9 combinations. With `style={isDark ? 'light' : 'dark'}` driven by NativeWind's resolved colorScheme:
- `theme=light` always → `'dark'` (dark icons on white bg) ✓
- `theme=dark` always → `'light'` (light icons on dark bg) ✓
- `theme=system + iOS=light` → `'dark'` (NativeWind resolves to light) ✓
- `theme=system + iOS=dark` → `'light'` (NativeWind resolves to dark) ✓

All 9 combinations contrast-correct. `style="auto"` would have failed the manual-override case (it reads iOS only, not NativeWind override).

---

## Copywriting Contract

All copy in Swedish per PROJECT.md, CONTEXT.md, and continued from Phase 3 D-15.

### F11 — RPE inline-input on workout/[sessionId]

| Element | Copy |
|---------|------|
| RPE TextInput placeholder | `RPE` |
| RPE TextInput accessibilityLabel | `Upplevd ansträngning, valfri` |
| RPE inline error — value < 0 | `RPE 0 eller högre` |
| RPE inline error — value > 10 | `RPE 10 eller lägre` |
| RPE inline error — non-numeric / multi-comma | (re-uses `setFormSchema.weight_kg`-precedent rejection — schema yields error.message via `z.coerce.number()` default; if Zod default reads in English ("Expected number, received nan"), planner overrides with explicit Swedish `"Ange ett tal mellan 0 och 10"` via `z.coerce.number({ error: () => "Ange ett tal mellan 0 och 10" })`) |

**Empty-handling copy (D-R3):** No copy — empty input is a valid optional value (`set.rpe = null`). User does not see any indicator that they "skipped" RPE.

**Visual "valfri"-signal (D-R4):** Placeholder text `"RPE"` (not `"RPE (valfri)"` — 64pt narrow column does not fit the suffix; the empty state IS the signal). No required-asterisk. The accessibilityLabel `"Upplevd ansträngning, valfri"` carries the optional-status for VoiceOver users.

### F11 — RPE-suffix in history set-rows

| Element | Copy |
|---------|------|
| Suffix when `set.rpe !== null` | `· RPE {rpe}` (e.g. `· RPE 8.5`) — middle-dot separator U+00B7 with one space on each side |
| Format for `rpe` value | Number stringified with no fixed decimals (e.g. `8`, `8.5`, `10`); JS `String(rpe)` is correct because `numeric(3,1)` returns at most one decimal — `8` not `8.0`, `8.5` not `8.50` |
| Suffix when `set.rpe === null` | (none — set-row reads exactly as Phase 5/6: `Set N: 82.5 × 8`) |

### F12 — Pass-anteckning capture in AvslutaOverlay

| Element | Copy |
|---------|------|
| TextInput placeholder | `Anteckningar (valfri)` |
| TextInput accessibilityLabel | `Anteckningar för passet, valfri` |
| Char counter | `{notes.length}/500` (e.g. `123/500`) — always visible under TextInput; color rule per §Color |
| AvslutaOverlay title (existing) | `Avsluta passet?` (preserved — Phase 5 D-31) |
| AvslutaOverlay body (existing) | `Du har loggat {N} set. Vill du avsluta?` (preserved — Phase 5) |
| AvslutaOverlay Fortsätt button (existing) | `Fortsätt` (preserved — Phase 5) |
| AvslutaOverlay Avsluta button (existing) | `Avsluta` (preserved — Phase 5; accent-blå per PITFALLS §6.6) |

**Empty-handling:** Submitting AvslutaOverlay with empty/whitespace-only notes → `notes = null` (D-N3 normalizes via `notes?.trim() ? notes.trim() : null`). User does not see any indicator that they "skipped" notes. No "Skip"-button — the existing Avsluta-button ALWAYS submits the overlay; the anteckning-fältet is purely additive.

### F12 — Pass-anteckning view + edit in history-detail

| Element | Copy |
|---------|------|
| Notes-block when `session.notes !== null` | (display the user's exact text; no prefix label like "Anteckning:" — the visual block IS the affordance) |
| Notes-block when `session.notes === null` (add-affordance) | `Lägg till anteckning` (with leading `+`-icon, accent-tinted) |
| Notes-block accessibilityLabel (when notes exist + pencil-icon) | `Redigera anteckning` (on the pencil Pressable only — the text Pressable below also opens edit) |
| Notes-block accessibilityLabel (when null + add-affordance) | `Lägg till anteckning` |
| Edit-overlay title | `Redigera anteckning` |
| Edit-overlay TextInput placeholder | `Anteckningar (valfri)` (matches AvslutaOverlay capture for consistency) |
| Edit-overlay TextInput accessibilityLabel | `Anteckningar för passet, valfri` |
| Edit-overlay char counter | `{notes.length}/500` — same rules as AvslutaOverlay counter |
| Edit-overlay Avbryt button | `Avbryt` (matches Phase 4 destructive-confirm Avbryt — neutral) |
| Edit-overlay Spara button | `Spara` (matches Phase 3 sign-in/sign-up primary CTA convention) |

**Cross-phase CTA convention (D-15 / Phase 3/4/5/6 inheritance):** `Spara` and `Avbryt` are intentionally single-word visible labels matching the established convention from Phase 3 sign-in/sign-up and Phase 4 destructive-confirm rows. Context is unambiguous from the overlay title (`Redigera anteckning`); accessibilityLabel for `Spara` is `Spara anteckning` (noun present for screen readers).

**Save-empty-text behavior:** Saving the edit-overlay with an empty/whitespace-only TextInput → `notes = null` (D-N3 normalize repeated for the update path in `useUpdateSessionNotes`). The notes-block re-renders the add-affordance (`+ Lägg till anteckning`) on next render. **No "Are you sure?"-confirm** for clearing notes — clearing is recoverable by typing it back; matches Phase 4 WR-05 "non-destructive UI changes do not require confirms".

**Notes-text overflow:** Notes max 500 chars (server-enforced + client-enforced via `maxLength=500`). At ~500 chars, the notes-block height grows to ~10 lines on iPhone SE. No "Visa mer / Visa mindre"-toggle in V1 — full text is always shown. Long-text scroll is absorbed by the parent ScrollView on `history/[sessionId].tsx`.

### F15 — Tema-toggle in Inställningar

| Element | Copy |
|---------|------|
| Section heading | `Tema` |
| Section accessibilityLabel (on SegmentedControl) | `Välj appens tema` |
| Segment 1 label | `System` |
| Segment 2 label | `Ljust` |
| Segment 3 label | `Mörkt` |
| Default selection | `System` (chosen at first launch / when AsyncStorage `fm:theme` key is absent or read fails per D-T2) |
| Toast / confirmation on toggle | (none — the visual change IS the confirmation; instant theme flip per D-T1 is the feedback signal) |
| AsyncStorage write-failure copy | (none — silent per SPEC accepted-risk; logged via `console.warn`) |
| AsyncStorage read-failure copy | (none — silent fallback to `'system'` per D-T2 + T-07-01 mitigation) |

### Core-flow ≤2 min UAT (manual UAT script)

Captured in `07-CONTEXT.md` `<specifics>` block; Plan owns the manual-UAT markdown file. Copy is operational, not user-facing — does not enter the Phase 7 user-visible copy contract.

### Out-of-scope copy (NOT introduced in Phase 7)

- **Per-set notes UI** — `exercise_sets.notes` column exists but no UI surface; no copy needed.
- **RPE in F10-chart "Senaste 10 passen"-rader** — Phase 6 row format `${weight} kg × ${reps}` preserved unchanged; no RPE-suffix added there.
- **RPE-edit on tap-to-edit logged set** — Phase 5 LoggedSetRow tap-to-edit preserved; no RPE field in edit form.
- **Theme-toggle "Auto vid solnedgång" / per-screen overrides** — V2 polish.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | (none — incompatible with React Native; `Tool: none` per Design System table) | not applicable |
| Third-party registries | (none) | not applicable |

**No third-party UI registries are declared for Phase 7.** All UI is composed from `react-native` core primitives + the locally-owned `app/components/segmented-control.tsx` (Phase 6, FIT-66-patched, in-tree audited). No `npx shadcn view` gate is required.

**External library audit (re-confirmation for Phase 7):** Only two libraries new-to-runtime in Phase 7:
1. `nativewind` `useColorScheme()` import — already installed (peer of NativeWind 4); same package is the source of every `dark:`-variant since Phase 1. No new install.
2. `@react-native-async-storage/async-storage` direct read/write of `fm:theme` key — already installed and used by `LargeSecureStore` since Phase 3. No new install.

Both are part of the locked stack in CLAUDE.md TL;DR (verified versions). No registry safety gate triggered.

---

## Phase-7-specific Visual Contracts (in addition to the cross-phase tokens above)

The token tables above carry forward unchanged from Phase 6. The list below captures the **new visual contracts unique to Phase 7** so the planner has prescriptive specs for the three new surfaces.

### A. Inline RPE-row geometry (workout/[sessionId].tsx, D-R1)

Final inline-row layout post-Phase 7 (left to right):

```
[ Vikt (flex-1, min-h-[56px]) ] [ Reps (flex-1, min-h-[56px]) ] [ RPE (w-16, min-h-[56px], text-center) ] [ Klart (w-16, min-h-[56px]) ]
```

Parent: `<View className="flex-row items-center gap-2 mt-3">` (existing — preserved).

**Focal point:** The Klart button — it is the action that commits the set; Vikt/Reps are the inputs that lead to it; RPE is intentionally the most subordinate column (narrow, optional, no error-state highlight unless out-of-range).

iPhone SE width math: 320 (content) − 16 padding × 2 = 288 available − 8 gap × 3 = 264 for inputs. Vikt + Reps = 2 × flex-1 = 132 each. RPE = 64 fixed. Klart = 64 fixed. Total = 132 + 132 + 64 + 64 = 392 — exceeds 264 because flex-1 absorbs the budget; effective Vikt/Reps width = (264 − 64 − 64) / 2 = 68pt each. Acceptable: 68pt fits 3-digit weights (`120`) and 2-digit reps (`12`) at `text-base font-semibold` with the existing `px-3` padding (effective inner = 56pt — still legible). On iPhone 14 Pro (390pt content) the budget is more generous; Vikt/Reps grow to ~103pt each. No iPhone in V1 scope falls below SE width.

**Inline-error stacking:** RPE error (when present) renders BELOW the RPE TextInput within the column-wrapped `<View className="w-16">` (matches Vikt/Reps Controller pattern verbatim). Error pushes the RPE column taller; siblings (Vikt/Reps/Klart) stay aligned at top via `items-center` on the outer flex-row (alignment preserved by the existing items-center; siblings stay vertically centered in the now-taller row — verified pattern in Phase 5 inline-row).

### B. Notes-block geometry (history/[sessionId].tsx, D-E4)

```jsx
<View className="bg-gray-100 dark:bg-gray-800 rounded-lg px-4 py-3 mx-4 mt-2 flex-row items-start gap-2">
  {notes ? (
    <>
      <Text className="flex-1 text-base text-gray-900 dark:text-gray-50">{notes}</Text>
      <Pressable onPress={openEdit} hitSlop={8} accessibilityLabel="Redigera anteckning">
        <Ionicons name="pencil-outline" size={18} color={mutedColor} />
      </Pressable>
    </>
  ) : (
    <Pressable onPress={openEdit} className="flex-row items-center gap-2 flex-1" hitSlop={8} accessibilityLabel="Lägg till anteckning">
      <Ionicons name="add-circle-outline" size={18} color={accentColor} />
      <Text className="text-base text-gray-500 dark:text-gray-400">Lägg till anteckning</Text>
    </Pressable>
  )}
</View>
```

Position: ABOVE the SummaryHeader-chiparna in `history/[sessionId].tsx` (per SPEC §4 acceptance + CONTEXT.md `<canonical_refs>` Source-of-truth diff target).

**Focal point:** When notes are empty, the accent-tinted `+ Lägg till anteckning` add-affordance; when notes exist, the notes Body text itself (the pencil icon is intentionally muted and discoverable, not dominant).

`mutedColor` = `useColorScheme().colorScheme === 'dark' ? '#9CA3AF' : '#6B7280'` — Muted text role hex (Tailwind gray-400 / gray-500).
`accentColor` = `useColorScheme().colorScheme === 'dark' ? '#60A5FA' : '#2563EB'` — Accent role hex (Tailwind blue-400 / blue-600). Both `useColorScheme` imports MUST be from `'nativewind'` per D-T4.

### C. Edit-notes-overlay structure (history/[sessionId].tsx, D-E2)

Backdrop + inner card + KeyboardAvoidingView wrap. Structure mirrors Phase 4 inline-overlay-destructive-confirm-pattern (commit `e07029a`) but swaps the destructive-Ta-bort body for a TextInput body:

```jsx
{showEditNotesOverlay && (
  <Pressable
    onPress={() => setShowEditNotesOverlay(false)}
    className="absolute inset-0 items-center justify-center px-4"
    style={{ backgroundColor: 'rgba(0,0,0,0.5)' }}
  >
    <KeyboardAvoidingView behavior={Platform.OS === 'ios' ? 'padding' : 'height'} style={{ width: '100%', maxWidth: 400 }}>
      <Pressable onPress={(e) => e.stopPropagation()}>
        <View className="bg-gray-100 dark:bg-gray-800 rounded-2xl p-6" style={{ gap: 16 }}>
          <Text className="text-2xl font-semibold text-gray-900 dark:text-gray-50">Redigera anteckning</Text>
          <TextInput
            value={draftNotes}
            onChangeText={setDraftNotes}
            placeholder="Anteckningar (valfri)"
            placeholderTextColor="#9CA3AF"
            multiline
            numberOfLines={3}
            maxLength={500}
            style={{ minHeight: 80, maxHeight: 160 }}
            textAlignVertical="top"
            autoFocus
            accessibilityLabel="Anteckningar för passet, valfri"
            className="rounded-md bg-white dark:bg-gray-900 border border-gray-300 dark:border-gray-700 px-3 py-2 text-base text-gray-900 dark:text-gray-50"
          />
          <Text className={`text-sm text-right ${draftNotes.length > 480 ? 'text-red-600 dark:text-red-400' : 'text-gray-500 dark:text-gray-400'}`}>
            {`${draftNotes.length}/500`}
          </Text>
          <View className="flex-row gap-3">
            <Pressable
              onPress={() => setShowEditNotesOverlay(false)}
              className="flex-1 py-4 rounded-md bg-gray-200 dark:bg-gray-700 items-center justify-center active:opacity-80"
              accessibilityRole="button"
              accessibilityLabel="Avbryt"
            >
              <Text className="text-base font-semibold text-gray-900 dark:text-gray-50">Avbryt</Text>
            </Pressable>
            <Pressable
              onPress={onSave}
              className="flex-1 py-4 rounded-md bg-blue-600 dark:bg-blue-500 items-center justify-center active:opacity-80"
              accessibilityRole="button"
              accessibilityLabel="Spara anteckning"
            >
              <Text className="text-base font-semibold text-white">Spara</Text>
            </Pressable>
          </View>
        </View>
      </Pressable>
    </KeyboardAvoidingView>
  </Pressable>
)}
```

**Focal point:** The TextInput body — `autoFocus` opens the keyboard immediately; the `Redigera anteckning` heading anchors the surface but is visually subordinate to the input area; Spara/Avbryt sit at the bottom as terminal actions.

`autoFocus` defaults to TRUE (CONTEXT.md `<decisions>` Claude's Discretion). If iOS-rendering shows mount-timing collision with KeyboardAvoidingView (e.g. keyboard pops before the card finishes positioning), the planner may set `autoFocus={false}` and instead call `inputRef.current?.focus()` from a `useEffect` after `setShowEditNotesOverlay(true)` resolves — same UX, more deterministic mount-order.

### D. Tema-toggle layout (settings.tsx, F15)

Replace lines 38-40 of current settings.tsx (placeholder Text) with:

```jsx
<View className="gap-2">
  <Text className="text-base font-semibold text-gray-900 dark:text-gray-50">Tema</Text>
  <SegmentedControl
    options={[
      { label: 'System', value: 'system' },
      { label: 'Ljust',  value: 'light'  },
      { label: 'Mörkt',  value: 'dark'   },
    ]}
    value={stored}
    onChange={onChange}
    accessibilityLabel="Välj appens tema"
  />
</View>
```

Position: between the email-row (`text-base text-gray-500 dark:text-gray-400` showing user email) and the `<View className="flex-1" />` spacer above the Logga ut button. Section heading `"Tema"` carries `gap-6` separation from the email-row above (inherited from existing `gap-6` on the outer container).

**Focal point:** The SegmentedControl — it is the only interactive element in the section; the `Tema` label is a plain section header.

`stored` state via `useState<'system' | 'light' | 'dark'>('system')` synced from AsyncStorage on mount; `onChange` calls `setStored(v)` + `setColorScheme(v)` + `AsyncStorage.setItem('fm:theme', v)`. The state-snippet is reproduced verbatim in CONTEXT.md `<specifics>` block.

---

## Accessibility (cross-cutting — Phase 7 additions)

Inherits Phase 3/4/5/6 accessibility floor. New Phase 7 a11y-affirmations:

- **RPE TextInput**: `accessibilityLabel="Upplevd ansträngning, valfri"`; inline error renders with `accessibilityLiveRegion="polite"` (matches Vikt/Reps).
- **Notes TextInput (capture in AvslutaOverlay)**: `accessibilityLabel="Anteckningar för passet, valfri"`. Multi-line announces to VoiceOver as "text field, multi-line". Counter is **NOT** wrapped in `accessibilityLiveRegion` because constant char-by-char announcements would be intrusive — the silent visual color flip at 480 chars is the threshold signal; VoiceOver users reach the 500-char limit naturally via maxLength rejection.
- **Notes-block in history-detail**:
  - When `notes !== null`: text Pressable + pencil Pressable BOTH open edit. The text Pressable carries `accessibilityLabel="Redigera anteckning"` (matches the icon-only Pressable label) so VoiceOver users get one consistent action per content-block. (Planner: alternatively make ONLY the pencil-icon Pressable interactive and let the text be plain `<Text>` — picks the spec that matches Phase 4 destructive-confirm-row convention; default = both interactive for larger touch surface.)
  - When `notes === null`: full row is a single Pressable; `accessibilityLabel="Lägg till anteckning"`.
- **Edit-overlay**: Modal-equivalent overlay (NOT React Native `<Modal>` — inline-overlay per Phase 4 D-13 / commit `e07029a`). Backdrop Pressable carries `accessibilityRole="button"` + `accessibilityLabel="Stäng"` (matches Phase 4 OfflineBanner ✕-close pattern); inner Spara-button `accessibilityLabel="Spara anteckning"`; Avbryt-button `accessibilityLabel="Avbryt"`.
- **SegmentedControl on Settings**: inherits Phase 6 primitive's `accessibilityRole="tablist"` + per-segment `accessibilityRole="tab"` + `accessibilityState={{ selected }}`. Container-level `accessibilityLabel="Välj appens tema"` provides context.
- **StatusBar dynamic style (D-T3)**: not user-facing a11y, but ensures status-bar icon contrast across all 9 (theme × iOS) combinations — load-bearing for users with low-vision who depend on status-bar legibility.

---

## State-Reset Conventions (Phase 7 additions)

Three new pieces of local state must reset on screen blur per Phase 4 commit `af6930c` `useFocusEffect`-cleanup-pattern. The planner picks one of: (a) extend an existing `useFocusEffect` block in the screen, or (b) one new `useFocusEffect` per concern. CONTEXT.md `<decisions>` defers this to planner discretion — both approaches are correct.

| Screen | New State | Reset Trigger |
|--------|-----------|---------------|
| `workout/[sessionId].tsx` AvslutaOverlay | `notes` (string draft inside overlay) | Existing `useFocusEffect` cleanup OR explicit `useEffect(() => () => setNotes(''), [])` inside overlay component |
| `history/[sessionId].tsx` | `showEditNotesOverlay` (boolean overlay-open state) | `useFocusEffect` cleanup — matches Phase 4 modal-state-reset convention |
| `history/[sessionId].tsx` | `draftNotes` (string draft inside edit overlay, seeded with `session.notes ?? ''` on overlay open) | `useFocusEffect` cleanup OR overlay-open `useEffect` re-seeding |

---

## Inheritance Note

Phase 7 introduces **zero new design tokens** — every spacing value, type size, weight, color, and accessibility convention comes from the Phase 1–6 contracts (cross-referenced in detail above). The only Phase 7-unique decisions are:

1. **Geometry**: RPE column = `w-16`, Klart shrink = `w-20`→`w-16`, notes TextInput minHeight=80/maxHeight=160 (D-R1, D-N2 — load-bearing for iPhone SE fit + multi-line UX).
2. **StatusBar mode**: `style="auto"` → `style={isDark ? 'light' : 'dark'}` (D-T3 — load-bearing for manual-override correctness).
3. **Three new copy strings**: `"Tema"`, `"System"`/`"Ljust"`/`"Mörkt"`, `"Lägg till anteckning"` / `"Redigera anteckning"` / `"Anteckningar (valfri)"` / `"Anteckningar för passet, valfri"` / `"RPE"` / `"Upplevd ansträngning, valfri"` / `"RPE 0 eller högre"` / `"RPE 10 eller lägre"`.

Everything else — typography roles, color roles, spacing scale, touch-target floor, status-bar pattern, `useFocusEffect` reset, inline-overlay structure, accessibility floor — is verbatim Phase 1–6 inheritance. **The contract is intentionally derivative because Phase 7 is a polish-cut, not a redesign.**

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
