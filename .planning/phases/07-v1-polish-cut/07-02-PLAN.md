---
phase: 07
plan: 02
plan_id: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - app/lib/schemas/sets.ts
  - app/app/(app)/workout/[sessionId].tsx
  - app/app/(app)/history/[sessionId].tsx
autonomous: true
requirements: [F11]
tags: [rpe, schema-stretch, inline-row, zod-preprocess, history-suffix]

must_haves:
  truths:
    - "Inline-raden i workout/[sessionId].tsx visar [Vikt] [Reps] [RPE (valfri)] [Klart] — RPE-fältet är synligt och fungerar utan extra steg"
    - "Klart utan att fylla RPE → Supabase exercise_sets-rad har rpe IS NULL"
    - "Klart med RPE=8.5 → rad har rpe = 8.5 (decimaltal accepteras, comma-locale '8,5' fungerar)"
    - "RPE > 10 eller < 0 rejects med inline Zod-felmeddelande ('RPE 0 eller högre' / 'RPE 10 eller lägre')"
    - "history/[sessionId].tsx set-rader visar suffix '· RPE {value}' när set.rpe IS NOT NULL"
    - "history/[sessionId].tsx set-rader visar oförändrat 'Set N: weight × reps' när set.rpe IS NULL"
    - "Set-loggning är fortsatt ≤ 3 sek per set efter inline-RPE-tillägg (perf-budget från PRD §2 + Phase 5 F13 brutal-test som CI-gate verifierar detta)"
  artifacts:
    - path: "app/lib/schemas/sets.ts"
      provides: "setFormSchema.rpe stretched to z.preprocess(empty-or-comma) + z.coerce.number().min(0).max(10).nullable().optional()"
      contains: "z.preprocess"
    - path: "app/app/(app)/workout/[sessionId].tsx"
      provides: "Tredje Controller<TextInput name='rpe'> i inline-raden mellan Reps och Klart; inline-row Klart-knappen krympt w-20 → w-16 (AvslutaOverlay-knappen FÖRBLIR w-20); addSet.mutate-payload utvidgad med rpe: input.rpe ?? null"
      contains: "name=\"rpe\""
    - path: "app/app/(app)/history/[sessionId].tsx"
      provides: "Set-rad-render extended med conditional '· RPE {set.rpe}' suffix när set.rpe != null"
      contains: "RPE ${set.rpe}"
  key_links:
    - from: "workout/[sessionId].tsx RPE Controller"
      to: "setFormSchema.rpe Zod preprocess + bounds"
      via: "react-hook-form Controller name='rpe' + zodResolver"
      pattern: "name=[\"']rpe[\"']"
    - from: "RHF handleSubmit(onKlart) → addSet.mutate"
      to: "useAddSet hook → exercise_sets row with rpe column"
      via: "addSet.mutate({ ...input, rpe: input.rpe ?? null, ... })"
      pattern: "rpe:\\s*input\\.rpe"
    - from: "history/[sessionId].tsx set-row map"
      to: "Conditional render of RPE suffix when set.rpe != null"
      via: "set.rpe != null && <Text>...</Text>"
      pattern: "set\\.rpe\\s*!=\\s*null"
---

## Phase Goal

**As a** användare som loggar pass, **I want to** valfritt skriva en RPE-siffra (0-10, decimaler tillåtna) per set utan extra tap eller skärm-byte och senare se den i historiken, **so that** jag kan följa upp upplevd ansträngning över tid utan att förlora hastighet i set-loggning.

<objective>
Lägg till F11 inline RPE-input på workout-skärmens set-rad samt RPE-suffix-visning i history-detaljvyn. Sträck setFormSchema.rpe med z.preprocess (comma→period + empty→null) + min(0).max(10)-bounds. RPE-fältet är visuellt-valfritt (tomt fält → null), inline-error renderas under fältet med Zod-felmeddelande på svenska, och decimaltal accepteras (server-kolumnen är numeric(3,1)).

Purpose: Stänga F11 från Måste-listan. RPE är en av tre features som krävs för V1-cut (F11 + F12 + F15) innan 4-veckors personlig soak. Schemat är redo sen Phase 2 (`exercise_sets.rpe numeric(3,1)`); endast UI saknas.

Output:
- `app/lib/schemas/sets.ts:62-63` — rpe-schemat sträckt med preprocess + bounds
- `app/app/(app)/workout/[sessionId].tsx` inline-raden (lines 476-554) — tredje Controller-sibling med RPE TextInput + INLINE-ROW Klart-knapp krympt till w-16
- `app/app/(app)/workout/[sessionId].tsx` addSet.mutate (lines 380-389) — payload utvidgad med `rpe: input.rpe ?? null`
- `app/app/(app)/history/[sessionId].tsx` set-row-map (lines 585-594) — conditional `· RPE {set.rpe}` suffix

**Out-of-scope (do NOT touch):** AvslutaOverlay's Avsluta-confirm button (around line 890) — that button is `w-20` and STAYS `w-20`. Only the inline-row Klart-button shrinks. Plan 07-03 owns the AvslutaOverlay surface.
</objective>

<execution_context>
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-v1-polish-cut/07-SPEC.md
@.planning/phases/07-v1-polish-cut/07-CONTEXT.md
@.planning/phases/07-v1-polish-cut/07-PATTERNS.md
@.planning/phases/07-v1-polish-cut/07-UI-SPEC.md
@.planning/phases/07-v1-polish-cut/07-01-SUMMARY.md
@app/lib/schemas/sets.ts
@app/app/(app)/workout/[sessionId].tsx
@app/app/(app)/history/[sessionId].tsx

<interfaces>
<!-- Critical contracts from the codebase the executor needs. -->

From `app/lib/schemas/sets.ts:44-53` (weight_kg precedent — the EXACT shape D-R2/D-R3 mirrors):
```ts
weight_kg: z.preprocess(
  // Plan 05-06 / FIT-9: Swedish-locale comma → JS-parsable period.
  (val) => (typeof val === "string" ? val.replace(/,/g, ".") : val),
  z
    .coerce.number({ error: "Vikt krävs" })
    .min(0, { error: "Vikt måste vara 0 eller högre" })
    .max(500, { error: "Vikt över 500kg verkar fel — kontrollera" })
    .multipleOf(0.25, { error: "Vikt i steg om 0.25kg" }),
),
```

Current `setFormSchema.rpe` (line 62-63 — what the schema-stretch replaces):
```ts
// F11 schema-ready (Phase 7 wires UI). rpe is numeric(3,1) in the DB.
rpe: z.coerce.number().nullable().optional(),
```

From `app/lib/schemas/sets.ts:74-75` (load-bearing — DO NOT rename these export aliases; workout/[sessionId].tsx:353 depends on them):
```ts
export type SetFormInput = z.input<typeof setFormSchema>;
export type SetFormOutput = z.output<typeof setFormSchema>;
```

From `app/app/(app)/workout/[sessionId].tsx:348-361` (useForm setup — the 3-generic split that requires preserving SetFormInput vs SetFormOutput):
```ts
const { control, handleSubmit, reset, formState: { errors, isSubmitting } } = useForm<SetFormInput, undefined, SetFormOutput>({
  resolver: zodResolver(setFormSchema),
  mode: "onSubmit",
  defaultValues: { weight_kg: prefillWeight ?? undefined, reps: prefillReps ?? undefined, set_type: "working" },
});
```

From `app/app/(app)/workout/[sessionId].tsx:380-389` (onKlart handler — where the addSet.mutate payload is built):
```ts
addSet.mutate(
  {
    id: randomUUID(),
    session_id: sessionId,
    exercise_id: planExercise.exercise_id,
    weight_kg: input.weight_kg,
    reps: input.reps,
    completed_at: new Date().toISOString(),
    set_type: "working",
  },
  { onError: ..., onSuccess: ... },
);
```

From `app/app/(app)/history/[sessionId].tsx:585-594` (set-row map — where the RPE suffix appends):
```tsx
{sets.map((set) => (
  <View key={set.id} className="flex-row items-baseline">
    <Text className="text-base text-gray-500 dark:text-gray-400">{`Set ${set.set_number}: `}</Text>
    <Text className="text-base font-semibold text-gray-900 dark:text-gray-50">{`${set.weight_kg} × ${set.reps}`}</Text>
  </View>
))}
```

**Two `w-20`-sized buttons exist in workout/[sessionId].tsx**, and they are NOT the same surface:
- Inline-row Klart-button (~line 550) — SHRINKS to `w-16` in this plan (D-R1).
- AvslutaOverlay Avsluta-confirm button (~line 890) — STAYS `w-20`. Plan 07-03 owns this region.

Decision IDs implemented in this plan (from 07-CONTEXT.md `<decisions>`):
- **D-R1** RPE column width `w-16`, INLINE-ROW Klart-button shrink `w-20 → w-16`, RPE `text-center font-semibold`, max-length 4 chars
- **D-R2** Schema bounds: `z.coerce.number().min(0, { error: "RPE 0 eller högre" }).max(10, { error: "RPE 10 eller lägre" }).nullable().optional()` inside z.preprocess
- **D-R3** Combined preprocess: empty/whitespace → null, otherwise comma→period replace (order: trim+empty first, then replace)
- **D-R4** Placeholder `"RPE"`, accessibilityLabel `"Upplevd ansträngning, valfri"`, inline error renders below field with `accessibilityLiveRegion="polite"`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Stretch setFormSchema.rpe with z.preprocess (empty-or-comma) + bounds</name>
  <files>app/lib/schemas/sets.ts</files>

  <read_first>
    - app/lib/schemas/sets.ts (full file — 110 lines; focus lines 44-53 weight_kg precedent + lines 62-63 current rpe + lines 74-75 type exports)
    - 07-PATTERNS.md section 1 "sets.ts — extend setFormSchema.rpe" (full block — exact target code)
    - 07-CONTEXT.md `<decisions>` D-R2 + D-R3 (combined preprocess + bounds chain)
    - 07-PATTERNS.md "Shared Patterns" section E (z.preprocess comma→period→null-on-empty)
    - 07-PATTERNS.md "Phase-7-specific Landmines" #8 (preprocess order: empty → null FIRST, then comma → period)
    - 07-PATTERNS.md "Phase-7-specific Landmines" #9 (numeric(3,1) silent truncation accepted in V1 — do NOT add multipleOf)
  </read_first>

  <behavior>
    - `setRowSchema.parse({ rpe: '' })` → `rpe === null` (empty input is a valid optional value).
    - `setRowSchema.parse({ rpe: '  ' })` → `rpe === null` (whitespace-only also null).
    - `setRowSchema.parse({ rpe: '8' })` → `rpe === 8`.
    - `setRowSchema.parse({ rpe: '8,5' })` → `rpe === 8.5` (Swedish-locale comma works).
    - `setRowSchema.parse({ rpe: '8.5' })` → `rpe === 8.5` (period also works).
    - `setRowSchema.parse({ rpe: '10' })` → `rpe === 10` (upper bound inclusive).
    - `setRowSchema.parse({ rpe: '10.5' })` → ZodError with message `"RPE 10 eller lägre"`.
    - `setRowSchema.parse({ rpe: '-1' })` → ZodError with message `"RPE 0 eller högre"`.
    - `setRowSchema.parse({ rpe: '8,5,5' })` → ZodError (multi-comma produces `"8.5.5"` → NaN → coerce fails — matches weight_kg precedent).
    - SetFormInput and SetFormOutput exports remain (line 74-75); their consumed names are not renamed.
  </behavior>

  <action>
    Replace lines 62-63 of `app/lib/schemas/sets.ts` per PATTERNS.md section 1's target shape:

    ```ts
    // F11 — RPE inline input. Phase 7 wires UI per D-R2 + D-R3.
    // numeric(3,1) on the server; accepts decimals, comma-locale-tolerant, empty → null.
    rpe: z.preprocess(
      (v) => {
        if (typeof v !== "string") return v;
        const trimmed = v.trim();
        if (trimmed === "") return null;          // empty/whitespace → null (D-R3, valfri)
        return trimmed.replace(/,/g, ".");        // comma → period (weight_kg precedent)
      },
      z
        .coerce.number()
        .min(0, { error: "RPE 0 eller högre" })
        .max(10, { error: "RPE 10 eller lägre" })
        .nullable()
        .optional(),
    ),
    ```

    **Order of preprocess steps is load-bearing** (PATTERNS landmine #8): the empty-string branch comes FIRST so an unfilled RPE column produces `null` immediately, BEFORE the replace step (which would convert empty `""` to empty `""` and then NaN-out at coerce). The combined transform handles all six observed inputs (`null/undefined` pass-through, empty string, whitespace, comma-decimal, period-decimal, multi-comma).

    Do NOT touch the type aliases at lines 74-75 (`SetFormInput`, `SetFormOutput`) — they are exported for the workout-screen's 3-generic useForm split. Their derived TypeScript types update automatically.

    Do NOT add `.multipleOf(0.5)` or `.multipleOf(0.1)` — SPEC §1 acceptance (d) explicitly accepts decimaler; CONTEXT.md Deferred Ideas defers harmonization to V1.1. Server-side `numeric(3,1)` truncation is an accepted-risk in V1.

    Implements decisions D-R2 (bounds chain) + D-R3 (combined preprocess + order).
  </action>

  <verify>
    <automated>cd app && npx tsc --noEmit && npm run lint && npm run test:set-schemas</automated>
    <grep-after>
      grep -c "z.preprocess" app/lib/schemas/sets.ts >= 2 (one for weight_kg, one new for rpe)
      grep -c "RPE 0 eller högre" app/lib/schemas/sets.ts == 1
      grep -c "RPE 10 eller lägre" app/lib/schemas/sets.ts == 1
      grep -c "rpe: z.coerce.number().nullable().optional()" app/lib/schemas/sets.ts == 0 (old shape removed)
    </grep-after>
    <test-set-schemas-extension>
      If `app/scripts/test-set-schemas.ts` (Plan 05-06 / FIT-9) exists, ADD three new assertions:
      - rpe-empty: `setFormSchema.parse({ ..., rpe: "" }).rpe === null`
      - rpe-comma: `setFormSchema.parse({ ..., rpe: "8,5" }).rpe === 8.5`
      - rpe-bounds: `setFormSchema.safeParse({ ..., rpe: "11" }).success === false` AND error message contains `"RPE 10 eller lägre"`
      Then run `npm run test:set-schemas` and confirm green. If the test-script does not exist or does not validate setFormSchema directly, document that the verification is deferred to `npm run lint` + manual UAT in Plan 07-05.
    </test-set-schemas-extension>
  </verify>

  <acceptance_criteria>
    - `app/lib/schemas/sets.ts` rpe field uses `z.preprocess` with the combined empty/whitespace → null + comma → period transform.
    - Bounds: `.min(0, { error: "RPE 0 eller högre" }).max(10, { error: "RPE 10 eller lägre" })` exactly.
    - `.nullable().optional()` chain preserved at end (D-R2 mandate).
    - SetFormInput/SetFormOutput export names unchanged.
    - tsc + lint green.
    - If test-set-schemas.ts exists: 3 new assertions pass; run as `npm run test:set-schemas`.
  </acceptance_criteria>

  <done>setFormSchema.rpe accepts comma-decimals, normalizes empty to null, rejects out-of-bound values with svenska Zod-felmeddelanden. Existing weight_kg tests stay green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add RPE Controller to workout/[sessionId].tsx inline-row + shrink INLINE-ROW Klart-knapp (AvslutaOverlay Avsluta-button UNCHANGED)</name>
  <files>app/app/(app)/workout/[sessionId].tsx</files>

  <read_first>
    - app/app/(app)/workout/[sessionId].tsx (lines 1-120 imports + lines 348-405 useForm + onKlart handler + lines 470-560 inline-row scaffold; ignore the AvslutaOverlay block 806-917 — that's Plan 07-03)
    - 07-PATTERNS.md section 4a "Inline RPE Controller" (full block with target code)
    - 07-UI-SPEC.md "A. Inline RPE-row geometry" (iPhone SE width math + flex-1 absorption + focal point rationale)
    - 07-CONTEXT.md `<decisions>` D-R1 (geometry) + D-R4 (visual signal + accessibilityLabel)
    - 07-PATTERNS.md "Phase-7-specific Landmines" #7 (preserve SetFormInput/Output split) + #10 (useFinishSession payload is amended NOT replaced — relevant to Plan 07-03 but signals symmetry)
    - 07-PATTERNS.md "Shared Patterns" section B (mutate-not-mutateAsync — already established for addSet; verify the existing call uses .mutate not .mutateAsync)
  </read_first>

  <behavior>
    - Inline-raden i workout/[sessionId].tsx renderar fyra kolumner i ordning: `[ Vikt (flex-1) ] [ Reps (flex-1) ] [ RPE (w-16) ] [ Klart (w-16) ]`.
    - RPE TextInput är visuellt en peer till Vikt/Reps: samma border, samma padding (px-2 istället för px-3 för smal kolumn), samma min-h-[56px], samma font-semibold text-base, text-center för smal kolumn.
    - Placeholder `"RPE"` syns när tomt; accessibilityLabel `"Upplevd ansträngning, valfri"`.
    - Skriver `8,5` → Controller binder strängen; vid handleSubmit → preprocess konverterar → addSet.mutate-payload har `rpe: 8.5`.
    - Skriver `11` → inline-felmeddelande `"RPE 10 eller lägre"` renderas under fältet i rött (`text-red-600 dark:text-red-400 mt-1 px-1`), border blir röd.
    - Lämnar tomt + tap Klart → addSet.mutate-payload har `rpe: null`.
    - **Inline-row** Klart-knappen är nu `w-16 min-h-[56px]` (tap-target 64×56pt, vilket > 44pt-floor per PITFALLS §6.1).
    - **AvslutaOverlay Avsluta-confirm button is NOT touched** — it remains `w-20`. Plan 07-03 owns the AvslutaOverlay surface (lines 806-917).
    - iPhone SE (320pt content): Vikt/Reps absorberar (264 − 64 − 64) / 2 = 68pt vardera; 3-siffriga vikter (`120`) får plats med text-base font-semibold + px-3.
  </behavior>

  <action>
    Modify `app/app/(app)/workout/[sessionId].tsx` per PATTERNS.md section 4a:

    1. **Insert third Controller sibling between Reps and Klart** (around line 545, after the Reps Controller block, before the Klart Pressable). Copy the weight_kg Controller structure verbatim, swap:
       - `name="rpe"` (instead of `weight_kg`)
       - Wrapper `<View className="w-16">` (instead of `flex-1`)
       - TextInput placeholder `"RPE"` (instead of `"Vikt"`)
       - TextInput `accessibilityLabel="Upplevd ansträngning, valfri"`
       - TextInput `maxLength={4}` (accommodates `"8.5"`, `"10"`, `"10.0"`)
       - TextInput className adds `px-2` (NOT px-3 — narrow column) + `text-center`
       - All other attributes (keyboardType decimal-pad, inputMode decimal, returnKeyType done, autoCorrect false, autoCapitalize none, selectTextOnFocus, value-stringify pattern, error-state border classes, focus-border) match weight_kg verbatim
       - Inline error block under TextInput uses identical pattern: `<Text className="text-base text-red-600 dark:text-red-400 mt-1 px-1" accessibilityLiveRegion="polite">{error.message}</Text>`
       Full target shape per PATTERNS.md section 4a code block.

    2. **Shrink INLINE-ROW Klart-button (D-R1)** (around line 550): change `className="w-20 min-h-[56px] rounded-md bg-blue-600 dark:bg-blue-500 ..."` to `className="w-16 min-h-[56px] rounded-md bg-blue-600 dark:bg-blue-500 ..."` (w-20 → w-16; rest unchanged).

       **CRITICAL SCOPE RULE — DO NOT modify the AvslutaOverlay confirm button's width.** The AvslutaOverlay (block ~lines 806-917) contains its OWN button at ~line 890 that is also currently `w-20`. That button stays `w-20` in this plan. Only the inline-row Klart-button at ~line 550 changes. Plan 07-03 owns the AvslutaOverlay surface; a mechanical "replace all w-20 with w-16" search-and-replace would corrupt the AvslutaOverlay button. Verify by visual inspection of the diff: exactly ONE line in this file should have `w-20` → `w-16`, located inside the inline-row block (lines 470-560 region), NOT inside the AvslutaOverlay block.

    3. **Wire RPE into addSet.mutate payload** (lines 380-389 region — the `onKlart` handler): add `rpe: input.rpe ?? null,` to the payload object. Position: after `reps:` and before `completed_at:` (alphabetical-by-column-order keeps the table-of-columns intuitive when reading). Because `setFormSchema.rpe` is `.nullable().optional()`, `input.rpe` is typed `number | null | undefined`; the `?? null` safety-net normalizes undefined to null at the call site so the DB row always lands with explicit null (not undefined which Supabase would skip).

    4. **Do NOT add `rpe` to `defaultValues`** in useForm — leaving it absent means the field starts undefined, which the Controller's `value == null ? "" : String(value)` (line 483 precedent) safely renders as empty. After `reset()` in `onSuccess` (lines 397-401 region), do NOT pre-fill `rpe` from prior set — PATTERNS.md section 4a explicitly says soaking the same RPE is a poor default; let it default to undefined so the placeholder shows for the next set.

    5. **Do NOT touch the AvslutaOverlay block (lines 806-917)** — that is Plan 07-03's surface (notes capture + KeyboardAvoidingView wrap).

    6. Implements decisions D-R1 (geometry — inline-row only) + D-R4 (visual signal + a11y).
  </action>

  <verify>
    <automated>cd app && npx tsc --noEmit && npm run lint && npm run test:f13-brutal</automated>
    <grep-after>
      <!-- Positive assertions on inline-row changes -->
      grep -c "name=\"rpe\"" app/app/\(app\)/workout/\[sessionId\].tsx == 1
      grep -c "Upplevd ansträngning, valfri" app/app/\(app\)/workout/\[sessionId\].tsx == 1
      grep -c "placeholder=\"RPE\"" app/app/\(app\)/workout/\[sessionId\].tsx == 1
      grep -c "rpe: input.rpe" app/app/\(app\)/workout/\[sessionId\].tsx == 1
      grep -c "className=\"w-16 min-h-\\[56px\\] rounded-md bg-blue-600" app/app/\(app\)/workout/\[sessionId\].tsx >= 1

      <!-- Scope-preservation assertion: AvslutaOverlay's Avsluta-confirm button MUST still be w-20. -->
      <!-- Use awk to slice out the AvslutaOverlay block and confirm it retains a w-20 button. -->
      <!-- B-2 fix: replaces the prior false-pass `grep -c "w-20 min-h-" == 0` check which would have corrupted the AvslutaOverlay button. -->
      awk '/AvslutaOverlay/,/^}/' app/app/\(app\)/workout/\[sessionId\].tsx | grep -c "w-20" >= 1

      <!-- Defensive: if the AvslutaOverlay function/component scope is structured such that the `/^}/` terminator under-shoots, fall back to a tighter scope-grep: grep for the Avsluta-button signature inside the file and confirm it still uses w-20. -->
      grep -E "className=\"w-20[^\"]*bg-(red|blue)-(500|600)" app/app/\(app\)/workout/\[sessionId\].tsx | wc -l >= 1
    </grep-after>
    <f13-brutal-non-regression>
      `npm run test:f13-brutal` (Phase 5 harness) MUST stay green — F13 brutal-test ≤ 3 sek per set MUST not regress from inline-RPE addition. SPEC Constraint: "Inline-RPE-fält får inte sakta ner set-loggning under 3-sek-gränsen mätt av Phase 5 brutal-test".
    </f13-brutal-non-regression>
  </verify>

  <acceptance_criteria>
    - `app/app/(app)/workout/[sessionId].tsx` contains a `<Controller>` with `name="rpe"` wrapped in `<View className="w-16">`.
    - The RPE TextInput has placeholder `"RPE"`, accessibilityLabel `"Upplevd ansträngning, valfri"`, maxLength=4, keyboardType="decimal-pad", text-center class.
    - The INLINE-ROW Klart-button className uses `w-16 min-h-[56px]` (not w-20).
    - The AvslutaOverlay Avsluta-confirm button still uses `w-20` (untouched in this plan; Plan 07-03 owns that surface).
    - The addSet.mutate payload includes `rpe: input.rpe ?? null`.
    - Inline-error rendering follows the Vikt/Reps pattern (text-base text-red-600 dark:text-red-400 mt-1 px-1 + accessibilityLiveRegion="polite").
    - `npm run test:f13-brutal` exits 0 (no perf regression).
    - tsc + lint green.
  </acceptance_criteria>

  <done>Inline-raden visar RPE-fält som tredje peer; tap-on-Klart utan RPE → set landar med rpe=null; tap-on-Klart med "8,5" → set landar med rpe=8.5; tap-on-Klart med "11" → inline-error "RPE 10 eller lägre" syns. AvslutaOverlay's Avsluta-button is verifiably untouched. F13 brutal-test stays green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Append RPE-suffix to set-rows in history/[sessionId].tsx</name>
  <files>app/app/(app)/history/[sessionId].tsx</files>

  <read_first>
    - app/app/(app)/history/[sessionId].tsx (lines 580-600 — the set-row map)
    - 07-PATTERNS.md section 5a "RPE-suffix on set-rows" (full block — exact target code)
    - 07-UI-SPEC.md "Typography" "Per-screen role mapping" → history-set-row RPE-suffix row (Muted color rationale)
    - 07-UI-SPEC.md "F11 — RPE-suffix in history set-rows" (copy contract: leading `·` U+00B7, no `.0` decimal padding)
    - 07-PATTERNS.md "section 5a Landmines" (items-baseline alignment + String(rpe) format + `!= null` loose-equality choice)
  </read_first>

  <behavior>
    - Existing pass: 3 set, 2 med rpe=8 + 1 med rpe=null → history-vy visar `Set 1: 82.5 × 8 · RPE 8`, `Set 2: 85 × 8 · RPE 8`, `Set 3: 87.5 × 6`.
    - Existing pass: 3 set alla med rpe=8.5 → visas som `· RPE 8.5` (decimal preserved, no trailing zero).
    - Existing pass: 3 set alla med rpe=null → visning oförändrad mot Phase 6 (`Set N: weight × reps`).
    - Pass loggat med rpe=10 → visas `· RPE 10` (no `.0`).
    - Suffix Text inherits `text-base` so baseline alignment with weight×reps stays intact via `items-baseline` on parent View.
  </behavior>

  <action>
    Modify `app/app/(app)/history/[sessionId].tsx` lines 585-594 per PATTERNS.md section 5a target shape. Specifically, inside the existing `{sets.map((set) => (...))}` block, ADD a third conditional Text node after the existing weight×reps Text:

    ```tsx
    {sets.map((set) => (
      <View key={set.id} className="flex-row items-baseline">
        <Text className="text-base text-gray-500 dark:text-gray-400">
          {`Set ${set.set_number}: `}
        </Text>
        <Text className="text-base font-semibold text-gray-900 dark:text-gray-50">
          {`${set.weight_kg} × ${set.reps}`}
        </Text>
        {set.rpe != null && (
          <Text className="text-base text-gray-500 dark:text-gray-400">
            {` · RPE ${set.rpe}`}
          </Text>
        )}
      </View>
    ))}
    ```

    **Load-bearing details:**
    - `set.rpe != null` (loose-equality `!=` not `!==`) — catches both null and undefined. PATTERNS.md section 5a landmines: "matches the existing `value == null` precedent in the workout-screen Controller."
    - The leading character in the suffix string is `·` (U+00B7 middle-dot) with ONE space on each side: `" · RPE "`. Do NOT use regular bullet `•` (U+2022), interpunct, or hyphen.
    - The suffix Text inherits `text-base text-gray-500 dark:text-gray-400` (Muted role per UI-SPEC §Typography per-screen role mapping). Same color as `Set N:` prefix because both are metadata, not primary content.

      **AAA-contrast pre-locked (I-1):** `text-red-600` (#DC2626) on `bg-gray-100` (#F3F4F6) = 5.97:1 contrast ratio (AAA Large per WCAG 2.1). `text-red-400` (#F87171) on `bg-gray-800` (#1F2937) = 4.97:1 (AAA Large). This applies to the counter color-flip used in Plans 07-03/07-04; the RPE suffix color (gray-500/400) is not the counter so no AAA check is required for this row, but the project-wide red-counter palette is verified AAA in advance.
    - `String(set.rpe)` via template literal naturally produces `"8"` not `"8.0"` and `"8.5"` not `"8.50"` (UI-SPEC §F11 RPE-suffix format).
    - The parent View has `items-baseline` (existing) — Text node inherits baseline alignment as long as it uses `text-base` (NOT `text-sm`).

    Do NOT touch any other part of the file (notes-block / edit-overlay belong to Plan 07-04; useColorScheme migration is Plan 07-01).
  </action>

  <verify>
    <automated>cd app && npx tsc --noEmit && npm run lint</automated>
    <grep-after>
      grep -c "set.rpe != null" app/app/\(app\)/history/\[sessionId\].tsx == 1
      grep -E "\\$\\{\\` · RPE \\$\\{set\\.rpe\\}|RPE \\$\\{set\\.rpe\\}" app/app/\(app\)/history/\[sessionId\].tsx | wc -l >= 1
    </grep-after>
  </verify>

  <acceptance_criteria>
    - `app/app/(app)/history/[sessionId].tsx` set-row map renders a conditional `<Text>` showing `" · RPE ${set.rpe}"` when `set.rpe != null`.
    - The conditional uses loose-equality `!=` not `!==` (matches workout-screen precedent).
    - The suffix Text has `text-base text-gray-500 dark:text-gray-400` (Muted role, not Body's gray-900).
    - The middle-dot separator is `·` U+00B7 (not `•` U+2022 nor `-`).
    - No String formatting that would add trailing zeros (template-literal `${set.rpe}` is correct).
    - tsc + lint green.
  </acceptance_criteria>

  <done>History set-rader visar `· RPE {value}` när rpe är satt; oförändrat när rpe är null. Baseline-alignment intakt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client form input → setFormSchema → Supabase UPDATE | User types RPE in a TextInput; Zod preprocess + bound validation is the client gate before mutate.payload reaches Supabase. Server-side `numeric(3,1)` is the secondary gate (silent truncation for invalid decimals; out-of-range still blocked by client). |
| Supabase response → setRowSchema.parse → history-detail render | Server-stored RPE is read back as `number | null` via SetRowSchema (line 100); parsed before render. Defense-in-depth via Zod-parse (Pitfall 8.13). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Information Disclosure / Bypass | Direct Supabase API call bypassing client Zod (e.g. malicious script in dev-build inserts rpe=99.9) | accept | RLS protects against cross-user write (Phase 2 — `with check` covers user_id on exercise_sets). Within-user, server `numeric(3,1)` silently accepts 0-99.9 broader than client 0-10. Accepted-risk: this is a personal app (one user); within-account integrity is non-critical. Client Zod is defense-in-depth for accidental UI mis-rendering, not adversarial validation. Documented in 07-CONTEXT.md `<canonical_refs>` T-07-04. |
| T-07-08 | Tampering | Comma-locale preprocess opens parser to weird inputs (e.g. `"8,5,5"`, `","`, `"., "`) | mitigate | Preprocess explicitly normalizes: trim → empty-or-whitespace → null; otherwise `.replace(/,/g, ".")` with /g flag preserves multi-comma anomalies → multi-period → NaN → Zod coerce rejects. PATTERNS.md landmine #8 documents the order. Verified by extending test-set-schemas with 3 new assertions (Task 1). |
| T-07-09 | Spoofing / Display | RPE-suffix render trusts server data without re-validation | mitigate | The render path reads `set.rpe` from `SetRowSchema.parse(supabase-response).rpe` which is `z.number().nullable()`. The `!= null` guard handles the only non-numeric case (null). React Native `<Text>` template-literal interpolation is inherent-safe (no XSS surface in RN). Mitigation pattern: PATTERNS.md "section 5a Landmines" + Pitfall 8.13 (Zod-parse Supabase-responses INTE cast). |
| T-07-10 | Denial of Service | RPE input typed extremely fast or pasted long string slows down RHF + Zod resolver | accept | `maxLength={4}` on the TextInput caps input length client-side; the resolver runs only at `mode: "onSubmit"` (not onChange), so typing is unthrottled and free of validation cost. F13 brutal-test gate (`npm run test:f13-brutal`) verifies the inline-row keeps ≤3s/set after the third Controller is added. |

threats_open: 0
</threat_model>

<verification>
After all tasks complete:

1. **Schema-level (Task 1)**: `npm run test:set-schemas` (if extended) green; rpe accepts empty, comma-decimal, period-decimal; rejects 11, -1, multi-comma.

2. **Inline-row (Task 2)** manual:
   - Open active workout in dev-build / Expo Go
   - Verify 4-column row: Vikt | Reps | RPE | Klart on iPhone SE-sized window
   - Type Vikt=80, Reps=10, RPE blank → tap Klart → set saved (verify via history) with rpe=null
   - Type Vikt=80, Reps=10, RPE=8,5 → tap Klart → set saved with rpe=8.5
   - Type Vikt=80, Reps=10, RPE=11 → tap Klart → inline-error "RPE 10 eller lägre" shows in red under RPE field; set NOT saved
   - Type Vikt=80, Reps=10, RPE=-1 → tap Klart → inline-error "RPE 0 eller högre"; set NOT saved
   - **Scope check:** open AvslutaOverlay (tap Avsluta header) — Avsluta-confirm button is visibly unchanged (still `w-20` width).

3. **History suffix (Task 3)** manual:
   - Open recently finished pass in /history/[sessionId]
   - Verify set-rows with rpe show `· RPE 8.5` suffix (or whatever value); rows without rpe show no suffix
   - Verify baseline alignment between set-number prefix, weight×reps, and RPE suffix

4. **Non-regression**: `npm run test:f13-brutal` MUST stay green (Phase 5 perf gate, enforces the ≤3 sek/set perf budget per PRD §2).

5. **Code gates**:
   - `cd app && npx tsc --noEmit` → exit 0
   - `cd app && npm run lint` → exit 0
   - `cd app && npm run test:rls` → exit 0 (still 29+ assertions; no new tables)
</verification>

<success_criteria>
- [ ] setFormSchema.rpe schema-stretched with z.preprocess + bounds (Task 1)
- [ ] workout/[sessionId].tsx has third Controller for rpe + INLINE-ROW Klart-button shrunk to w-16 (Task 2)
- [ ] AvslutaOverlay's Avsluta-confirm button is verifiably UNCHANGED (still `w-20`; Plan 07-03 owns that surface)
- [ ] addSet.mutate payload includes `rpe: input.rpe ?? null` (Task 2)
- [ ] history/[sessionId].tsx set-rows show conditional `· RPE {value}` suffix (Task 3)
- [ ] All code gates green: tsc + lint + test:f13-brutal + test:rls
- [ ] No regression to F13 brutal-test (≤3 sek/set baseline preserved — user-observable: set-loggning är fortsatt snabb)
- [ ] No new files created
- [ ] threat_model T-07-04 / T-07-08 / T-07-09 / T-07-10 all dispositioned
</success_criteria>

<output>
After completion, create `.planning/phases/07-v1-polish-cut/07-02-SUMMARY.md` documenting:
- The exact line-numbers where the RPE Controller was inserted (post-edit)
- The 3 new test-set-schemas assertions added (if test exists)
- F13 brutal-test result (must show no regression vs Phase 5 baseline)
- Explicit confirmation that AvslutaOverlay's Avsluta-confirm button width is unchanged (e.g., paste the relevant grep output or commit-diff snippet)
- iPhone-UAT result on RPE input + history-suffix (defer to Plan 07-05 if not exercised here)
</output>
</content>
</invoke>