---
phase: 09-auth-settings-preferences
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - app/app/(auth)/sign-in.tsx
  - app/app/(auth)/sign-up.tsx
autonomous: false
requirements: [SKIN-01, I18N-02]

must_haves:
  truths:
    - "Sign-in and sign-up match the Forge design in light + dark"
    - "Auth visible labels render through t() so the live language toggle reaches the auth screens"
    - "Field-level validation errors render inline beneath each ForgeField"
    - "Server auth failures render as a form-level message above the submit button"
    - "Submit button shows an inline spinner and is disabled during the async auth call"
    - "The Phase 3 RHF+Zod validation, error.code switch, and copy are unchanged"
  artifacts:
    - path: "app/app/(auth)/sign-in.tsx"
      provides: "Forge-reskinned sign-in (logic unchanged)"
      contains: "ForgeField"
    - path: "app/app/(auth)/sign-up.tsx"
      provides: "Forge-reskinned sign-up (logic unchanged)"
      contains: "ForgeButton"
  key_links:
    - from: "app/app/(auth)/sign-in.tsx"
      to: "react-i18next useTranslation"
      via: "t() on visible labels"
      pattern: "useTranslation|t\\("
    - from: "app/app/(auth)/sign-in.tsx"
      to: "app/components/ui/ForgeButton.tsx"
      via: "loading prop during isSubmitting"
      pattern: "loading"
  decisions:
    - "D-12 — auth visible labels go through t() so the live language toggle reaches auth, no restart"
    - "D-17 — pure visual re-skin of sign-in/sign-up; RHF + Zod validation and copy unchanged"
    - "D-18 — field errors inline beneath each ForgeField; server failures form-level above the CTA"
    - "D-19 — submit button shows inline spinner and is disabled during the async auth call"
---

<objective>
Pure visual re-skin (D-17) of the two auth screens (`sign-in.tsx`, `sign-up.tsx`) to the Forge design using `forge-screens.jsx` `FSignIn`/`FSignUp` as the layout reference: swap raw `TextInput`/`Pressable` for `ForgeField`/`ForgeButton`, route all visible labels through `t()` (so the live language toggle from Plan 02 reaches auth), render field errors inline beneath each field (D-18) and server failures as a form-level message above the CTA (D-18), and show the submit spinner+disabled during async (D-19).

Purpose: Auth is the first screen a user sees; it must match Forge in light + dark and respond to the live language override. The Phase 3 RHF + Zod validation, the `error.code` switch mapping, and the copy stay UNCHANGED — this is presentation only.
Output: Forge-reskinned `sign-in.tsx` + `sign-up.tsx` with `t()`-routed labels, inline field errors, form-level server errors, and loading-state CTA.
</objective>

<execution_context>
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-auth-settings-preferences/09-CONTEXT.md
@.planning/phases/09-auth-settings-preferences/09-PATTERNS.md
@.planning/phases/09-auth-settings-preferences/09-UI-SPEC.md
@.planning/phases/09-auth-settings-preferences/09-01-SUMMARY.md

<interfaces>
<!-- Phase 8 primitives + Plan 01 keys. Use directly. -->

From app/components/ui/ForgeField.tsx (Phase 8):
  ForgeField — props: icon ("mail"|"lock"|...), state ("default"|"focused"|"error"), secureTextEntry; uses placeholderTextColor prop (NOT placeholder:text-* class); a11y label fallback built-in.

From app/components/ui/ForgeButton.tsx (Phase 8):
  ForgeButton — variant ("primary"|"secondary"), size ("lg"|"md"), loading (built-in ActivityIndicator + disabled, lines 148-149).

Existing keys in locales (already present, reuse — no new auth keys): welcome, welcomeSub, signIn, signingIn, signUp, email, password, forgotPassword, noAccount.
New keys added by Plan 01 (for password-visibility a11y): showPassword, hidePassword.

KEEP VERBATIM (Phase 3 logic — do NOT touch):
  - sign-in.tsx lines 45-53: useForm({ resolver: zodResolver(signInSchema), mode: "onSubmit" })
  - sign-in.tsx lines 55-105: onSubmit + error.code switch (banner-error / field-error split)
  Hardcoded Swedish to REPLACE with t(): "Logga in", "Email", "Inget konto?", etc. (lines 124-263)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Forge re-skin sign-in.tsx (logic verbatim, labels through t())</name>
  <files>app/app/(auth)/sign-in.tsx</files>
  <read_first>
    - app/app/(auth)/sign-in.tsx (the file being re-skinned — RHF/Zod block lines 45-53, onSubmit+error.code switch lines 55-105, hardcoded Swedish lines 124-263, raw TextInput lines 170-186/208-223, CTA Pressable lines 238-248)
    - app/components/ui/ForgeField.tsx (icon/state/secureTextEntry props + placeholderTextColor)
    - app/components/ui/ForgeButton.tsx (variant/size/loading props, lines 148-149)
    - app/design v2/Sources/design/forge-screens.jsx (FSignIn line 6 — layout/brand-mark/hero/field-stack/CTA reference)
    - .planning/phases/09-auth-settings-preferences/09-PATTERNS.md (sign-in/sign-up re-skin section: keep-verbatim list + re-skin transforms)
    - .planning/phases/09-auth-settings-preferences/09-UI-SPEC.md (§Interaction Contract auth rows + §Color accent reserved-for + §Typography auth roles + §Copywriting auth key map)
  </read_first>
  <action>
    Re-skin `app/app/(auth)/sign-in.tsx` visually only (D-17). KEEP VERBATIM: the RHF `useForm({ resolver: zodResolver(signInSchema), mode: "onSubmit" })` (lines 45-53), the `onSubmit` + `error.code` switch mapping (lines 55-105), the banner-error / field-error split, and all copy. Do NOT touch validation, schemas, or copy semantics.
    Transforms:
    - Replace raw `<TextInput className="...bg-gray-100...">` (lines 170-186, 208-223) with `<ForgeField icon="mail" state={errors.email ? "error" : "default"} ...>` (email) and `<ForgeField icon="lock" secureTextEntry state={errors.password ? "error" : "default"} ...>` (password). The password field gets an `eye` visibility toggle carrying `accessibilityLabel={t('showPassword')}` when masked / `t('hidePassword')` when revealed, hit target ≥44px.
    - Replace the CTA `<Pressable className="...bg-blue-600...">` (lines 238-248) with `<ForgeButton variant="primary" size="lg" loading={isSubmitting} label={t('signIn')} onPress={handleSubmit(onSubmit)} />` — the `loading` prop is the built-in ActivityIndicator + disabled (D-19, prevents double-submit).
    - Route ALL visible labels through `t()` via `useTranslation()` (existing keys: `welcome`, `welcomeSub`, `signIn`, `email`, `password`, `forgotPassword`, `noAccount`, `signUp`). Without this the live language toggle (D-12) won't affect auth (Pitfall 3). Call `t()` inside render — never capture in module scope.
    - Field-level error → inline `text-forge-danger` line beneath each `ForgeField` (D-18); server auth failure → a single form-level `text-forge-danger` message above the CTA (D-18), dismissible. Server-error literal copy MAY stay inline per D-17 "copy unchanged" (RESEARCH Open Question 2 — visible labels keyed, server-error literals inline is acceptable).
    - Apply Forge tokens with light+dark parity on every surface (`bg-forge-bg-light dark:bg-forge-bg`, etc.); brand-mark tile uses the gradFrom→gradTo brand gradient (auth header). Apply optical values (`24px 28px 32px` padding, 14px field gap, 6px sign-up-link gap) as inline `style`, not arbitrary Tailwind classes (NativeWind purge).
    - Preserve FIT-66 safety on any new Pressable (eye toggle): pressed feedback via `style={({pressed}) => ...}`, never `active:opacity-*`/`shadow-*`.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit && npm run lint && npm run check:locale-parity</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ForgeField\|ForgeButton" app/app/(auth)/sign-in.tsx` matches (primitives swapped in).
    - `grep -n "useTranslation\|t('signIn')\|t(\"signIn\")" app/app/(auth)/sign-in.tsx` matches (labels keyed).
    - `grep -n "loading={isSubmitting}\|loading=" app/app/(auth)/sign-in.tsx` matches (D-19 loading CTA).
    - `grep -n "zodResolver(signInSchema)\|mode: \"onSubmit\"" app/app/(auth)/sign-in.tsx` STILL matches (logic verbatim).
    - `grep -n "bg-blue-600\|bg-gray-100" app/app/(auth)/sign-in.tsx` returns NO matches (raw styling removed).
    - `grep -n "active:opacity\|shadow-\[" app/app/(auth)/sign-in.tsx` returns NO matches (FIT-66).
    - `npx tsc --noEmit`, `npm run lint`, `npm run check:locale-parity` clean.
  </acceptance_criteria>
  <done>sign-in.tsx Forge-reskinned with ForgeField/ForgeButton, t()-keyed labels, inline+form-level errors, loading CTA; Phase 3 logic verbatim; gates green.</done>
</task>

<task type="auto">
  <name>Task 2: Forge re-skin sign-up.tsx (mirror sign-in transforms, logic verbatim)</name>
  <files>app/app/(auth)/sign-up.tsx</files>
  <read_first>
    - app/app/(auth)/sign-up.tsx (the file being re-skinned — its RHF/Zod + onSubmit + error.code logic to keep verbatim)
    - app/app/(auth)/sign-in.tsx (the just-reskinned sibling — mirror its ForgeField/ForgeButton/t() structure exactly)
    - app/design v2/Sources/design/forge-screens.jsx (FSignUp/FSignIn reference)
    - .planning/phases/09-auth-settings-preferences/09-PATTERNS.md (sign-up mirrors sign-in exactly)
    - .planning/phases/09-auth-settings-preferences/09-UI-SPEC.md (§Copywriting: sign-up CTA `t('signUp')`, nav prompt `t('noAccount')`/`t('signIn')`)
  </read_first>
  <action>
    Re-skin `app/app/(auth)/sign-up.tsx` mirroring the sign-in transforms exactly (D-17, logic verbatim). KEEP VERBATIM the RHF `useForm` + `onSubmit` + `error.code` switch + the banner/field error split + copy. Swap raw `TextInput` → `ForgeField` (email `icon="mail"`, password `icon="lock"` + `eye` toggle with `t('showPassword')`/`t('hidePassword')` a11y), CTA `Pressable` → `ForgeButton variant="primary" size="lg" loading={isSubmitting} label={t('signUp')}`. Route visible labels through `t()` (`signUp`, `email`, `password`, and the cross-nav prompt to sign-in). Field errors inline (D-18); server failures form-level above CTA (D-18). Forge tokens light+dark parity; optical values inline `style`; FIT-66-safe pressed styles.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit && npm run lint && npm run check:locale-parity</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ForgeField\|ForgeButton" app/app/(auth)/sign-up.tsx` matches.
    - `grep -n "useTranslation" app/app/(auth)/sign-up.tsx` matches; visible labels keyed.
    - `grep -n "loading=" app/app/(auth)/sign-up.tsx` matches (D-19).
    - sign-up RHF resolver + onSubmit + error.code switch unchanged (logic verbatim — confirm against pre-edit file).
    - `grep -n "bg-blue-600\|bg-gray-100\|active:opacity" app/app/(auth)/sign-up.tsx` returns NO matches.
    - `npx tsc --noEmit`, `npm run lint`, `npm run check:locale-parity` clean.
  </acceptance_criteria>
  <done>sign-up.tsx Forge-reskinned mirroring sign-in; logic verbatim; gates green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Device UAT — auth re-skin parity light+dark + live language + error states (SKIN-01)</name>
  <what-built>
    Both auth screens re-skinned to Forge with t()-keyed labels, inline field errors, form-level server errors, and a loading-state CTA.
  </what-built>
  <read_first>
    - .planning/phases/09-auth-settings-preferences/09-VALIDATION.md (Manual-Only: SKIN-01 row)
    - app/design v2/Sources/design/forge-screens.jsx (FSignIn/FSignUp — visual reference for comparison)
  </read_first>
  <how-to-verify>
    1. Open sign-in in Expo Go. Compare to FSignIn: brand-mark tile, hero + sub, two ForgeFields (mail/lock icons), forgot-password link, primary CTA, sign-up nav link. Toggle theme — light + dark parity holds.
    2. Tap the password eye toggle — masking flips; VoiceOver announces show/hide password.
    3. Submit with an invalid email / empty password — inline errors render beneath the offending field; they clear on re-type.
    4. Submit valid-shaped but wrong credentials — a single form-level error renders above the CTA (Phase 3 copy).
    5. During the async submit — the CTA shows a spinner and is disabled (no double-submit).
    6. Navigate to sign-up — same parity, same behaviors.
    7. From Settings, switch language to English, return to auth (sign out if needed) — auth labels are now English (live i18n reaches auth).
  </how-to-verify>
  <verify>
    <human-check>All 7 steps pass; auth matches Forge in light+dark, errors/loading correct, live language reaches auth.</human-check>
  </verify>
  <acceptance_criteria>
    - Sign-in + sign-up match FSignIn/FSignUp in light AND dark (SKIN-01).
    - Field errors inline; server failure form-level above CTA (D-18).
    - CTA spinner+disabled during async (D-19).
    - Auth labels follow the language toggle (I18N-02 reaches auth).
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
  <done>Device UAT signed off for SKIN-01 + auth live-language.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → auth form | email/password cross into RHF+Zod validation |
| client → Supabase auth | signInWithPassword / signUp crosses to the auth API |
| language toggle → auth labels | t() values re-render on changeLanguage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-12 | Tampering/Info-disclosure | auth form input | mitigate | Phase 3 RHF + Zod validation UNCHANGED (D-17); re-skin is presentational only — no validation regression |
| T-09-13 | Info-disclosure | server auth error copy | mitigate | Preserve Phase 3 generic invalid-credentials copy (V2.1.4) — do not leak which field failed; form-level message reuses existing copy |
| T-09-14 | Spoofing | double-submit / replay | mitigate | ForgeButton loading prop disables CTA during async (D-19) — prevents duplicate auth calls |
| T-09-15 | Tampering | session handling during re-skin | accept | Re-skin does NOT touch supabase.auth.* or LargeSecureStore; session/M3 path unchanged (D-17) |
</threat_model>

<verification>
- `npx tsc --noEmit && npm run lint && npm run check:locale-parity` green.
- Device UAT (Task 3) signed off for SKIN-01 + auth live-language.
- `npm run test:f13-brutal` stays green (presentational — hot path untouched).
</verification>

<success_criteria>
- Sign-in and sign-up match Forge in light + dark.
- Auth labels render through t() and follow the live language toggle.
- Field errors inline; server failures form-level; CTA loading state correct.
- Phase 3 auth logic, validation, and copy unchanged.
</success_criteria>

<output>
Create `.planning/phases/09-auth-settings-preferences/09-03-SUMMARY.md` when done.
</output>
