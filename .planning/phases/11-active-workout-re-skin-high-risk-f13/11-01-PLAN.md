---
phase: 11-active-workout-re-skin-high-risk-f13
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/app/(app)/workout/[sessionId].tsx
  - app/locales/sv.json
  - app/locales/en.json
autonomous: true
requirements: [SKIN-04]
must_haves:
  decisions: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-09, D-13, D-14]
  truths:
    - "Active-workout screen renders a single vertical ScrollView of stacked Forge exercise cards (no Övning 1/6 pagination, no Up next card)"
    - "Custom in-content Forge header is rendered with circular back button, centered live-timer pill, and accent Avsluta button; native Stack header is hidden"
    - "Header timer ticks live (MM:SS) off session.started_at every second"
    - "Each exercise card shows the Forge per-card progress-dot strip plus an N / M counter, replacing the old 3/4 set klart chip"
    - "Logged sets render as a Forge set-table (# / weight / reps / RPE / action) with set-number badge, kg unit suffix, and an always-rendered RPE column showing – when null"
    - "Each logged-set row has a trailing ✕ delete affordance (swipe-to-delete gesture removed); tapping a row still enters inline edit"
    - "All sets render the plain checkCircle success icon — no PR banner, no trophy column, no inert PR slots"
    - "Empty-state and loading/restoring screens are re-skinned to Forge tokens and routed through t()"
    - "Every hardcoded Swedish literal on the workout screen is replaced by a t() call; sv.json and en.json have matching new keys"
  artifacts:
    - path: "app/app/(app)/workout/[sessionId].tsx"
      provides: "Re-skinned workout header, progress dots, logged-set table, empty/loading states, i18n sweep"
      contains: "headerShown: false"
    - path: "app/locales/sv.json"
      provides: "Swedish workout/overlay keys"
      contains: "removeSet"
    - path: "app/locales/en.json"
      provides: "English workout/overlay keys at parity with sv.json"
      contains: "removeSet"
  key_links:
    - from: "app/app/(app)/workout/[sessionId].tsx header timer"
      to: "session.started_at"
      via: "copied formatElapsed + setInterval(1000) ticker"
      pattern: "formatElapsed"
    - from: "app/app/(app)/workout/[sessionId].tsx logged-set row ✕"
      to: "removeSet.mutate"
      via: "trailing delete Pressable onPress (unchanged mutation call)"
      pattern: "removeSet\\.mutate"
---

<objective>
Re-skin the structural shell of the active-workout hot-path screen to the Forge design: custom header with live timer (D-07/D-09), per-card progress dots (D-02), the structured logged-set table with ✕-delete (D-03/D-04/D-05), PR markup omitted (D-06), the defensive empty-state and loading copy (D-13), and a full i18n sweep (D-14). This plan also OWNS all new locale keys for the entire phase so downstream plans (11-02, 11-03) only consume them.

Purpose: SKIN-04's structure (header, set log, progress dots) is the dominant visual surface of the most-used screen. This plan restyles it AROUND the frozen write path (D-17) — never touching mutation wiring.
Output: Re-skinned `workout/[sessionId].tsx` structural surfaces + the complete phase locale key set in both languages.
</objective>

<execution_context>
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-active-workout-re-skin-high-risk-f13/11-CONTEXT.md
@.planning/phases/11-active-workout-re-skin-high-risk-f13/11-PATTERNS.md
@.planning/phases/11-active-workout-re-skin-high-risk-f13/11-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Custom Forge header + live timer + i18n + locale keys (D-07/D-09/D-14)</name>
  <read_first>
    - app/app/(app)/workout/[sessionId].tsx (the file being modified — read fully; note MUST-NOT-CHANGE zones in PATTERNS.md: imports/hooks L69-90, hydration gate L145-158, loading gate L163-174, useFocusEffect L119-125, useLocalSearchParams narrowing L111-113, second OfflineBanner L206)
    - app/components/active-session-banner.tsx (source of formatElapsed L39-46 + the now/setInterval ticker L62-69 + tabular-nums fontVariant L122 + useTranslation pattern L31/L51)
    - app/design v2/Sources/design/forge-screens.jsx FWorkout header L341-360 (visual target: 40px circle surface back, accentSoft timer pill, 36px accent Avsluta pill)
    - app/components/ui/Icon.tsx + app/components/ui/index.ts (Icon names chevronLeft, check; ForgeButton, ForgeCard barrel)
    - app/locales/en.json and app/locales/sv.json (existing flat-key convention; confirm resume/finishSession/resumeSession/sessionSaved/exercises ALREADY exist — reuse, do not duplicate)
    - app/tailwind.config.js forge.* block (token classes + radius scale rounded-forge-sm/md/lg/xl)
  </read_first>
  <action>
    Add `import { useTranslation } from "react-i18next"` and `const { t } = useTranslation();` to WorkoutScreen, mirroring active-session-banner.tsx.
    Copy the `formatElapsed(ms)` function (mm:ss / h:mm:ss) from active-session-banner.tsx into this file (it is module-private there). Add the live ticker inside WorkoutScreen: `const startedAt = session?.started_at; const [now, setNow] = useState(() => Date.now());` + a `useEffect` keyed on `startedAt` that calls `setInterval(() => setNow(Date.now()), 1000)` and clears on unmount; derive `const timer = startedAt ? formatElapsed(now - new Date(startedAt).getTime()) : null;`.
    Replace the `Stack.Screen` at L182-200 (and the two inside the gates at L150/L166) so `headerShown: false` on all three. Render an in-content Forge header View at the top of the screen body: left = 40px circular Pressable (className box styling: `w-10 h-10 rounded-forge-md items-center justify-center border bg-forge-surface-light dark:bg-forge-surface border-forge-border-light dark:border-forge-border`, hitSlop to 44px) with `<Icon name="chevronLeft" />` calling the existing back navigation; center = accentSoft timer pill (`bg-forge-accentSoft-light dark:bg-forge-accentSoft border border-forge-border-light dark:border-forge-border`) with a 6px accent dot + the `timer` text styled `style={{ fontVariant: ["tabular-nums"] }}` accent ink; right = accent pill button (className `bg-forge-accent-light dark:bg-forge-accent`, accent-shadow in style() per FIT-66 exception) with label `t("finish")` opening the existing Avsluta overlay.
    Apply the NativeWind box-decoration rule: all bg/border/radius/size in className, only shadow/opacity/animated values in the style() callback.
    The custom header owns its safe-area top inset (useSafeAreaInsets or SafeAreaView edges top) — the native header no longer provides it.
    i18n sweep for this plan's surfaces: replace literals "Pass", "Avsluta" (header), "Laddar…", "Återställer pass…" and any header/loading copy with `t()` calls.
    Add ALL phase locale keys NOW to both app/locales/sv.json and app/locales/en.json (flat keys, alphabetical-ish, 1:1 parity), so 11-02 and 11-03 only consume them: `done`, `finish`, `weight`, `reps`, `rpe`, `previous` (interpolated "Förra: {{w}} × {{r}}" / "Previous: {{w}} × {{r}}"), `removeSet` ("Ta bort set" / "Remove set"), `finishWorkoutQ` ("Avsluta passet?" / "Finish workout?"), `finishBody` (interpolated "{{count}} set sparade på {{time}}. Lägg gärna en notering om hur passet kändes." / "{{count}} sets logged in {{time}}. Add a note about how the session felt."), `continue` ("Fortsätt" / "Continue"), `nothingToLog` ("Inget att logga" / "Nothing to log"), `nothingToLogBody` (per UI-SPEC copy table), `restoringWorkout` ("Återställer pass…" / "Restoring workout…"), `loading` ("Laddar…" / "Loading…"), `finishError` ("Kunde inte avsluta passet. Försök igen." / "Couldn't finish the workout. Try again."), `sets` ("Set" / "Set"), `min` ("min" / "min"), and the column-header keys `colWeight`/`colReps`/`colRpe` (or reuse `weight`/`reps`/`rpe`). Do NOT re-add existing keys (resume, finishSession, resumeSession, sessionSaved, exercises, kg).
    Do NOT touch any MUST-NOT-CHANGE zone listed in read_first.
  </action>
  <acceptance_criteria>
    - Source assertion: `headerShown: false` appears for the workout route; no `headerShown: true` and no `title: "Pass"` remain in the file.
    - Source assertion: `formatElapsed` is defined in this file and the header renders a `timer` value derived from `session.started_at`; the ticker uses `setInterval(... , 1000)` with a clear-on-unmount return.
    - Source assertion: header timer text carries `fontVariant: ["tabular-nums"]`.
    - Source assertion: `import { useTranslation }` present; header/loading literals ("Pass", "Avsluta", "Laddar…", "Återställer pass…") are gone, replaced by `t(...)`.
    - CLI: `cd app && npm run check:locale-parity` exits 0 (sv.json/en.json key sets identical).
    - Source assertion: the MUST-NOT-CHANGE zones are byte-identical — `useAddSet`, `useFinishSession`, second `<OfflineBanner />`, `useFocusEffect` cleanup, and `useLocalSearchParams` narrowing are unchanged.
    - CLI: `cd app && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd app && npm run check:locale-parity && npx tsc --noEmit</automated>
  </verify>
  <done>Native header hidden; in-content Forge header with live timer renders; full phase locale key set present at parity; header/loading copy i18n'd; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Per-card progress dots + Forge logged-set table + ✕-delete + empty-state (D-02/D-03/D-04/D-05/D-06/D-13)</name>
  <read_first>
    - app/app/(app)/workout/[sessionId].tsx (ExerciseCard L305+, LoggedSetRow L618-695, EditableSetRow L700-796, LastValueChip L802-824, WorkoutBody empty-state L247-269, formatTargetChip L1015+; MUST-NOT-CHANGE: removeSet.mutate L657, updateSet.mutate L643-648, EditableSetRow RHF/keyboard wiring, LoggedSetRow useFocusEffect L628-632)
    - app/design v2/Sources/design/forge-screens.jsx FWorkout progress dots L373-387, logged-set table L415-466 (grid 32px 1fr 1fr 56px 36px; column headers L421-433; set-number badge L441-446; numerals + kg suffix L447-449)
    - app/app/(app)/(tabs)/index.tsx empty-state structure L305-357 (mirror for the Forge empty-state tile)
    - app/components/ui/Icon.tsx (names: checkCircle, close, list; trophy exists but OMIT per D-06)
    - app/tailwind.config.js forge.* tokens + radius scale
    - memory feedback_nativewind_box_deco_via_classname.md (box styling in className, NOT style())
  </read_first>
  <action>
    Per-card progress dots (D-02): delete the counter chip (counterChipText green/grey pill, ExerciseCard L414-461) and render the FWorkout dot strip instead — a flex row of 6px-tall bars (`flex-1 h-1.5 rounded-[3px]`; the 6px height is a declared spacing exception): done bars `bg-forge-accent-light dark:bg-forge-accent`, current bar `bg-forge-accentSoft-light dark:bg-forge-accentSoft border border-forge-accent`, remaining bars `bg-forge-surface2-light dark:bg-forge-surface2`, plus a trailing "N / M" counter in `text-forge-text2-light dark:text-forge-text2` with `fontVariant: ["tabular-nums"]`. Do NOT render an "Övning 1/6" global label or an "Up next" card (D-01). Keep `formatTargetChip` for the plan-target chip if retained.
    Logged-set table (D-03/D-04/D-05): restyle LoggedSetRow into a grid row `32px 1fr 1fr 56px 36px` (# · weight · reps · RPE · action). Render 10px uppercase column headers once per card (`# · {t("colWeight")} · {t("colReps")} · RPE`). Set-number badge = accent circle with accentText numeral. Weight/reps use display-font numerals with a `kg` unit suffix (reuse existing `kg` key). RPE column ALWAYS rendered, showing a muted `–` (en dash, `text-forge-text3`) when the set has no RPE value (D-05).
    REMOVE swipe-to-delete: delete the `ReanimatedSwipeable` wrapper and replace it with a trailing `<Icon name="close" />` ✕-delete Pressable (muted text2/text3, hitSlop expanded to 44px, `accessibilityLabel={t("removeSet")}`) that calls the UNCHANGED `removeSet.mutate({ id, session_id })`. If `GestureHandlerRootView`/`ReanimatedSwipeable` imports become unused, drop them. No delete confirmation (recoverable — mirror v1, per Claude's Discretion).
    Preserve tap-row-to-edit: tapping the row still sets editing → EditableSetRow (keep the setIsEditing flow L637-654 and EditableSetRow's RHF/keyboard wiring). Restyle EditableSetRow to Forge tokens but DO NOT change its keyboardType/inputMode/returnKeyType/mode:onSubmit wiring (that is 11-02's domain to verify; here, just retoken its chrome).
    Success column = plain `<Icon name="checkCircle" color={forge-success} />`. OMIT the trophy entirely (D-06) — no trophy import on this surface, no inert PR column.
    Empty-state (D-13): restyle WorkoutBody empty-state to Forge — `<Icon name="list" />` in a surface2 tile (mirror index.tsx L305-357), `t("nothingToLog")` heading + `t("nothingToLogBody")` body + a `ForgeButton variant="primary"` back button. i18n any remaining table/empty literals ("Vikt", "Reps", "Ta bort", empty copy).
    All box styling (bg/border/radius/size) in className; style() only for shadow/opacity.
  </action>
  <acceptance_criteria>
    - Source assertion: no `ReanimatedSwipeable` usage remains; a `<Icon name="close" />` ✕-delete Pressable with `accessibilityLabel={t("removeSet")}` calls `removeSet.mutate`.
    - Source assertion: `removeSet.mutate` and `updateSet.mutate` call sites are unchanged (same payload shape).
    - Source assertion: no `trophy` Icon is rendered on the logged-set rows or progress area; the success icon is `checkCircle`.
    - Source assertion: the old counter chip (counterChipText) is removed; a progress-dot strip with a trailing N / M counter renders.
    - Source assertion: RPE column renders unconditionally; a `–` placeholder appears when RPE is null.
    - Source assertion: empty-state uses `t("nothingToLog")` / `t("nothingToLogBody")` and a Forge back button; no hardcoded "Inget set är loggat" Swedish literal remains.
    - Behavior assertion: tapping a logged-set row still enters inline edit (EditableSetRow path preserved).
    - CLI: `cd app && npx tsc --noEmit` exits 0 and `npx expo lint` reports no new errors.
  </acceptance_criteria>
  <verify>
    <automated>cd app && npx tsc --noEmit && npx expo lint</automated>
  </verify>
  <done>Progress dots replace the counter chip; logged sets render as the Forge table with ✕-delete and always-on RPE; trophy omitted; empty-state re-skinned and i18n'd; mutation calls untouched; tsc + lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client UI ↔ frozen mutation layer | Re-skin renders around useAddSet/useRemoveSet/useFinishSession; no new call into the boundary |
| client ↔ Supabase | No new query/mutation/RLS surface introduced (presentational only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | logged-set ✕-delete affordance | accept | Calls the existing `removeSet.mutate({ id, session_id })` unchanged; RLS `session:${sessionId}` scope unchanged; no new authz path. Recoverable (re-add). |
| T-11-02 | Information Disclosure | header timer + i18n strings | accept | Timer is derived client-side from already-authorized `session.started_at`; no new data fetched or exposed. Locale strings carry no user data. |
| T-11-03 | Denial of Service | live setInterval(1000) ticker | accept | Single 1s interval cleared on unmount via useEffect return; bounded, no leak; does not touch the write path. |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed (Reanimated 4 + expo-haptics already in stack per PATTERNS.md). No legitimacy gate needed; slopcheck N/A. |
</threat_model>

<verification>
- `cd app && npm run check:locale-parity` exits 0 (phase locale keys at parity).
- `cd app && npx tsc --noEmit` exits 0.
- `cd app && npx expo lint` reports no new errors.
- Source audit: MUST-NOT-CHANGE zones (mutation hooks, second OfflineBanner, useFocusEffect cleanup) byte-identical.
- Service-role audit unaffected (no new server-only imports): `git grep "service_role\|SERVICE_ROLE"` matches only the allowlisted files.
</verification>

<success_criteria>
- Active-workout screen renders the Forge custom header (back / live timer pill / accent Avsluta), per-card progress dots, and the structured logged-set table with ✕-delete and always-on RPE.
- All sets render the plain checkCircle (no PR banner/trophy).
- Empty-state and loading copy are Forge-skinned and i18n'd.
- All phase locale keys exist at sv/en parity.
- Frozen write path untouched; tsc + lint + locale-parity green.
</success_criteria>

<output>
Create `.planning/phases/11-active-workout-re-skin-high-risk-f13/11-01-SUMMARY.md` when done.
</output>
