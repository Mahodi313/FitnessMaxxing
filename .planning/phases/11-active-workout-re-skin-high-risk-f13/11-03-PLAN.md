---
phase: 11-active-workout-re-skin-high-risk-f13
plan: 03
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - app/app/(app)/(tabs)/index.tsx
autonomous: true
requirements: [SKIN-05, MOTN-04]
must_haves:
  decisions: [D-11, D-15, D-16]
  truths:
    - "The draft-resume overlay is re-skinned to FDraftResumeOverlay: pulsing-dot icon block, meta strip (clock + plan name + N set + Live pill), primary accent Återuppta/Resume button (play icon) above a danger ghost Avsluta sessionen/End session button"
    - "The End session button is danger-colored (the one place red is correct — data-loss-adjacent), not the prior neutral surface2"
    - "The saved-toast is re-skinned to FSavedToast: a forge-success pill with a white check, bottom-centered above the tab bar, copy t(\"sessionSaved\")"
    - "Both overlays animate open with the §07 spring (backdrop opacity 0→0.5 + card translateY 24→0) and stay inline-rendered (no Modal portal)"
    - "The offline-critical overlay/toast logic (cold-start detection, prev-value transition watcher, 2s timer, force-decision backdrop, finishSession.mutate) is preserved byte-for-byte"
  artifacts:
    - path: "app/app/(app)/(tabs)/index.tsx"
      provides: "Re-skinned draft-resume overlay + saved-toast (chrome only), §07 spring, danger ghost End session"
      contains: "FadeIn"
  key_links:
    - from: "DraftResumeOverlay End session button"
      to: "handleAvslutaSession → finishSession.mutate"
      via: "danger ghost button onPress (unchanged mutate call, NOT mutateAsync)"
      pattern: "finishSession\\.mutate"
    - from: "saved-toast render"
      to: "previousActiveRef transition watcher + 2s timeout"
      via: "edge-triggered toast (logic unchanged, chrome re-skinned)"
      pattern: "sessionSaved"
---

<objective>
Re-skin the two overlays that Phase 10 left verbatim inside `(tabs)/index.tsx` (lines 25-27 warned this): the draft-resume overlay → FDraftResumeOverlay with a danger ghost End-session button (D-16), and the saved-toast → FSavedToast success pill (SKIN-05). Add the §07 overlay spring (MOTN-04, D-11) while keeping both inline-rendered (D-15). Re-skin CHROME ONLY — the cold-start detection, prev-value transition watcher, 2s timer, and force-decision backdrop are offline-critical and stay untouched.

Purpose: SKIN-05 requires the three session overlays to match the Forge design and stay inline-rendered. The active-session banner is already Forge (Phase 10, carry-forward-complete); the finish overlay is 11-02; this plan closes the remaining two overlays.
Output: Re-skinned draft-resume overlay + saved-toast in `(tabs)/index.tsx`, logic preserved.
</objective>

<execution_context>
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-active-workout-re-skin-high-risk-f13/11-CONTEXT.md
@.planning/phases/11-active-workout-re-skin-high-risk-f13/11-PATTERNS.md
@.planning/phases/11-active-workout-re-skin-high-risk-f13/11-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Re-skin DraftResumeOverlay to FDraftResumeOverlay + danger ghost End session + §07 spring (D-16/D-15/D-11)</name>
  <read_first>
    - app/app/(app)/(tabs)/index.tsx (DraftResumeOverlay subcomponent ~L516-623; lines 25-27 warning. MUST-NOT-CHANGE: cold-start detection L121-133 coldStartSessionId/isColdStartDraft/shouldShowDraftOverlay; handleAvslutaSession L532-542 finishSession.mutate NOT mutateAsync + onDismiss after; onStartShouldSetResponder touch-claim L553; NO-onPress backdrop force-decision L549-551; current End-session neutral surface2 color L587 — CHANGE to danger ghost)
    - app/design v2/Sources/design/forge-screens.jsx FDraftResumeOverlay L1817-1928 (pulsing-dot icon block L1850-1865, meta strip clock + plan + N set + Live pill L1876-1901, primary Resume accent+play 56px L1905-1914, danger End-session ghost L1915-1922)
    - app/components/ui/ForgeButton.tsx (variant="destructive" = transparent + danger text/border — exact match for End session; variant="primary" = filled accent + auto shadow for Resume) + Icon.tsx (play, clock)
    - app/tailwind.config.js forge.* tokens
    - app/design v2/Sources/design/Forge Design Spec.html §07 (spring damping 18 / stiffness 220, ~240ms; backdrop 0→0.5 + card translateY 24→0)
    - memory feedback_nativewind_box_deco_via_classname.md
  </read_first>
  <action>
    Bring DraftResumeOverlay (currently ~80% Forge via the `tk` token map) to the full FDraftResumeOverlay mock: a pulsing-dot icon block (L1850-1865), a meta strip with `<Icon name="clock" />` + plan name + "{N} set" + a "Live" pill (L1876-1901), then a primary "Återuppta/Resume" button (`ForgeButton variant="primary"` with `icon="play"`, full-width 56px) ABOVE a secondary "Avsluta sessionen/End session" button. Change the End-session button from the current neutral `surface2` (L587) to a DANGER GHOST button — `ForgeButton variant="destructive"` (transparent bg, danger text/border) per D-16 (data-loss-adjacent is the one place red is correct). Use the existing `resume`/`resumeSession`/`finishSession` locale keys (already present) for copy.
    Add the MOTN-04 §07 spring entrance: backdrop opacity 0→0.5 + card translateY 24→0 (damping 18 / stiffness 220, ~240ms) via Reanimated, INLINE-rendered (absolute scrim + flex, NO Modal portal — D-15).
    PRESERVE byte-for-byte: cold-start detection (coldStartSessionId/isColdStartDraft/shouldShowDraftOverlay), `handleAvslutaSession` (`finishSession.mutate` NOT mutateAsync + `onDismiss()` after), the `onStartShouldSetResponder` touch-claim, and the NO-onPress force-decision backdrop (orphan must be resolved — no dismiss).
    All box styling (bg/border/radius/size) in className; style() only for shadow/opacity/animated transforms.
  </action>
  <acceptance_criteria>
    - Source assertion: the End-session button uses `variant="destructive"` (or danger text/border tokens) — the prior neutral `surface2` styling is gone.
    - Source assertion: the Resume button uses `variant="primary"` with a `play` icon; a meta strip with a `clock` icon + "Live" pill renders.
    - Source assertion: cold-start detection (`shouldShowDraftOverlay`, `isColdStartDraft`), `handleAvslutaSession` calling `finishSession.mutate` (NOT mutateAsync), the touch-claim, and the no-onPress backdrop are all unchanged.
    - Source assertion: no `Modal` import/usage added — the overlay stays inline (absolute scrim + flex); a §07 spring entrance (damping 18 / stiffness 220) is applied.
    - CLI: `cd app && npx tsc --noEmit` exits 0 and `npx expo lint` reports no new errors.
  </acceptance_criteria>
  <verify>
    <automated>cd app && npx tsc --noEmit && npx expo lint</automated>
  </verify>
  <done>Draft-resume overlay matches FDraftResumeOverlay with a danger ghost End-session button, accent Resume, §07 spring, inline-rendered; offline-critical logic preserved; tsc + lint clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Re-skin saved-toast to FSavedToast success pill + optional §07 spring (D-11)</name>
  <read_first>
    - app/app/(app)/(tabs)/index.tsx (saved-toast: state ~L115, edge-trigger watcher L139-148 previousActiveRef + 2s setTimeout, render ~L477-505 surface pill with Icon check + FadeIn/FadeOut L478-480. MUST-NOT-CHANGE: the previousActiveRef transition watcher + 2s timer logic L139-148)
    - app/design v2/Sources/design/forge-screens.jsx FSavedToast L1933+ (success-colored pill, white check in a translucent circle L1943-1958, bottom-centered above tab bar)
    - app/components/ui/Icon.tsx (check) + app/tailwind.config.js forge-success token
    - app/design v2/Sources/design/Forge Design Spec.html §07 (spring curve)
  </read_first>
  <action>
    Re-skin the saved-toast (render L477-505) from the current `surface` pill to the FSavedToast mock: a `forge-success` background pill (`bg-forge-success-light dark:bg-forge-success`) with a white `<Icon name="check" />` in a translucent circle, bottom-centered above the tab bar, copy `t("sessionSaved")` (existing key, unchanged). Keep the `Animated.View entering={FadeIn} exiting={FadeOut}` wrapper (L478-480); optionally upgrade the entrance to the MOTN-04 §07 spring (damping 18 / stiffness 220) per Claude's Discretion — either is acceptable as long as it stays inline (no Modal).
    PRESERVE byte-for-byte: the toast edge-trigger logic — the `previousActiveRef` transition watcher and the 2s `setTimeout` (L139-148). Restyle chrome ONLY.
    All box styling in className; style() only for shadow/opacity/animated values.
  </action>
  <acceptance_criteria>
    - Source assertion: the toast pill uses `forge-success` background tokens (not the prior neutral surface) with a white check Icon; copy is `t("sessionSaved")` (unchanged key).
    - Source assertion: the `previousActiveRef` transition watcher and 2s `setTimeout` edge-trigger logic (L139-148) are unchanged.
    - Source assertion: the toast stays inline (FadeIn/FadeOut Animated.View or §07 spring) — no `Modal` added.
    - CLI: `cd app && npx tsc --noEmit` exits 0 and `npx expo lint` reports no new errors.
  </acceptance_criteria>
  <verify>
    <automated>cd app && npx tsc --noEmit && npx expo lint</automated>
  </verify>
  <done>Saved-toast is a forge-success pill (white check, bottom-centered) with unchanged sessionSaved copy and unchanged edge-trigger logic; inline-rendered; tsc + lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client UI ↔ frozen mutation layer | DraftResumeOverlay End-session restyled around finishSession.mutate; no new boundary call |
| client ↔ Supabase | No new query/mutation/RLS surface (presentational re-skin) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-08 | Tampering | DraftResumeOverlay End-session (finishSession.mutate) | accept | Mutation call unchanged (D-17); `mutate` not `mutateAsync`; force-decision backdrop preserved so an orphan draft cannot be silently dismissed; RLS validates server-side. |
| T-11-09 | Information Disclosure | draft-resume meta strip (plan name / N set / time) | accept | Rendered from the user's own already-authorized draft session data; no new fetch, no cross-user exposure. |
| T-11-10 | Denial of Service | overlay/toast §07 spring + FadeIn/FadeOut | accept | Inline Reanimated entrance; does not touch the write path or the 2s timer logic; bounded. |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (Reanimated 4 already installed per PATTERNS.md). Legitimacy gate N/A. |
</threat_model>

<verification>
- `cd app && npx tsc --noEmit` exits 0; `npx expo lint` reports no new errors.
- Source audit: cold-start detection, `handleAvslutaSession` (mutate not mutateAsync), touch-claim, force-decision backdrop, and the toast 2s edge-trigger logic are all unchanged.
- No `Modal` import added — both overlays stay inline (D-15).
- Service-role audit unaffected (no new server-only imports).
</verification>

<success_criteria>
- Draft-resume overlay matches FDraftResumeOverlay with a danger ghost End-session button (D-16), accent Resume, and the §07 spring.
- Saved-toast matches FSavedToast (forge-success pill, bottom-centered).
- Both overlays stay inline-rendered (no Modal portal, D-15).
- Offline-critical overlay/toast logic preserved; tsc + lint green.
</success_criteria>

<output>
Create `.planning/phases/11-active-workout-re-skin-high-risk-f13/11-03-SUMMARY.md` when done.
</output>
