---
phase: 11-active-workout-re-skin-high-risk-f13
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - app/app/(app)/workout/[sessionId].tsx
autonomous: false   # Task 3 is checkpoint:human-verify (blocking F13 verification)
requirements: [SKIN-04, SKIN-08, MOTN-01, MOTN-04, MOTN-05]
must_haves:
  decisions: [D-08, D-10, D-11, D-12, D-15, D-16, D-17]
  truths:
    - "Set-input row matches the Forge mock: 56px accent-bordered fields with a large display value and a small uppercase unit label underneath (KG/REPS/RPE), plus a full-width accent Klart/Done button with a check icon"
    - "All v1 keyboard wiring survives: decimal-pad/number-pad, inputMode, selectTextOnFocus, returnKeyType:done, RHF mode:onSubmit, and the session-prefill → F7 fallback prefill"
    - "Logging a set plays the set-logged motion (row slides in from bottom + check scales 0.8→1) and fires a Medium haptic — fire-and-forget, after the mutate, never blocking the optimistic write"
    - "The set-logged haptic is gated behind the fm:haptics pref (default on); the visual animation is NOT gated"
    - "The Avsluta finish overlay is re-skinned to FFinishOverlay with a 3-cell client-derived stats row (sets / volume kg / duration min) and animates open with the §07 spring, staying inline-rendered (no Modal portal)"
    - "The finish overlay primary Avsluta button is accent (not red); backdrop-tap dismiss and the manual keyboard-height lift are preserved"
    - "npm run test:f13-brutal stays green after the re-skin"
  artifacts:
    - path: "app/app/(app)/workout/[sessionId].tsx"
      provides: "Re-skinned set-input row, set-logged motion+haptic, Forge finish overlay with stats, overlay spring"
      contains: "impactAsync"
  key_links:
    - from: "set-logged feedback in ExerciseCard onKlart/onSuccess"
      to: "fm:haptics pref"
      via: "getPref('fm:haptics').then(on => on && Haptics.impactAsync(Medium))"
      pattern: "getPref\\(\"fm:haptics\"\\)"
    - from: "AvslutaOverlay finish button"
      to: "finishSession.mutate"
      via: "handleConfirm calling mutate (NOT mutateAsync) with unchanged notes payload"
      pattern: "finishSession\\.mutate"
    - from: "finish overlay stats row"
      to: "useSetsForSessionQuery data"
      via: "client-side reduce (count / Σ weight×reps / elapsed since started_at)"
      pattern: "reduce"
---

<objective>
Re-skin the set-input row to the Forge mock while preserving every keyboard/RHF wiring detail (D-10), add the non-blocking set-logged motion + gated haptic (MOTN-01/MOTN-05, D-11/D-12), and re-skin the Avsluta finish overlay with the new client-derived 3-cell stats row and the §07 overlay spring (D-08, MOTN-04), keeping it accent-not-red (D-16) and inline-rendered (D-15). This plan finishes on a BLOCKING F13 verification that asserts the ≤3s budget and keyboard wiring survive (SKIN-08).

Purpose: This is the hot-path tap loop. The re-skin must FEEL as fast as v1; motion/haptic is additive polish that never gates the optimistic write. The frozen write path (D-17) is restyled around, never touched.
Output: Re-skinned input row + finish overlay + motion/haptic in `workout/[sessionId].tsx`, verified green against `test:f13-brutal`.
</objective>

<execution_context>
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-active-workout-re-skin-high-risk-f13/11-CONTEXT.md
@.planning/phases/11-active-workout-re-skin-high-risk-f13/11-PATTERNS.md
@.planning/phases/11-active-workout-re-skin-high-risk-f13/11-UI-SPEC.md
@.planning/phases/11-active-workout-re-skin-high-risk-f13/11-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Forge set-input row re-skin — preserve all keyboard/RHF wiring (D-10)</name>
  <read_first>
    - app/app/(app)/workout/[sessionId].tsx (input row L478-591: 3 TextInput + Klart Pressable; RHF wiring L349-373 mode:onSubmit + defaultValues prefill + re-hydrate useEffect; prefill logic L339-344 sessionPrefill ?? f7Prefill; LastValueChip L802-824. MUST-NOT-CHANGE: every TextInput keyboard prop L489-494/L523-528/L558-564 — keyboardType decimal-pad/number-pad, inputMode, returnKeyType:done, selectTextOnFocus, autoCorrect/autoCapitalize; onKlart L377-410 addSet.mutate + reset() prefill in onSuccess)
    - app/design v2/Sources/design/forge-screens.jsx FWorkout input row L468-495 + ForgeInput L526-545 (accent-tinted field bg, large display value + small uppercase unit label underneath, 50px full-width Klart button with leading check)
    - app/components/ui/ForgeField.tsx (NOTE per PATTERNS.md: ForgeField does NOT expose inputMode/selectTextOnFocus/returnKeyType — do NOT swap the hot-path fields to ForgeField; restyle the raw TextInputs)
    - app/components/ui/Icon.tsx (check icon)
    - app/tailwind.config.js forge.* tokens + radius scale
    - memory feedback_nativewind_box_deco_via_classname.md
  </read_first>
  <action>
    Restyle the always-visible input row (L478-591) to the FWorkout/ForgeInput target. Restyle the RAW TextInputs in place — do NOT swap to ForgeField (it cannot carry the hot-path keyboard props). Each of the three fields becomes a ~56px accent-bordered card cell (className box styling: `h-14 rounded-forge-md border border-forge-accent/... bg-[rgba(...accent 0.04-0.05...)]`) containing a large ~22px display-font value (font-display-semibold, tabular-nums) with a small uppercase unit label underneath — `KG`, `REPS`, `RPE` (10-11px, letter-spacing wide, text2/text3). Grid the three fields `1fr 1fr 60px` (rpe narrower).
    Header of the row: "SET N" accent uppercase label + the prev-value "Förra: {w} × {r}" using `t("previous", { w, r })` — fold the existing LastValueChip data INTO this row header per the mock instead of the separate chip below (the F7 prev-value lives here now).
    Klart button: a full-width 50px hand-rolled Pressable (className `h-[50px] w-full rounded-forge-md bg-forge-accent-light dark:bg-forge-accent items-center justify-center flex-row gap-2`, accent-shadow in style() per FIT-66 exception, opacity on pressed in style()) with a leading `<Icon name="check" />` + `t("done")`. (ForgeButton size="md" 52px is an acceptable fallback per Claude's Discretion if the 2px does not matter.)
    PRESERVE byte-for-byte: every TextInput's keyboardType/inputMode/returnKeyType/selectTextOnFocus/autoCorrect/autoCapitalize; the RHF `useForm({ mode: "onSubmit", defaultValues })`; the prefill `useEffect` re-hydrate; the `sessionPrefill ?? f7Prefill` logic; the `onKlart` `addSet.mutate({...}, { onSuccess })` (mutate NOT mutateAsync) and the `reset()` prefill inside onSuccess.
    All bg/border/radius/size in className; only shadow/opacity/animated values in style().
    i18n: route "Vikt"/"Reps"/"Klart"/"Förra:" through t() (keys added by 11-01).
  </action>
  <acceptance_criteria>
    - Source assertion: the three set-input TextInputs still carry their original keyboardType (decimal-pad/number-pad), inputMode, returnKeyType="done", selectTextOnFocus — grep confirms these props survive on the restyled fields.
    - Source assertion: `useForm` still uses `mode: "onSubmit"`; the prefill re-hydrate useEffect and `sessionPrefill ?? f7Prefill` expression are unchanged.
    - Source assertion: `onKlart` still calls `addSet.mutate(` (NOT `mutateAsync`) and `reset()` runs in `onSuccess`.
    - Source assertion: the Klart CTA renders `t("done")` with a check Icon; field unit labels render KG/REPS/RPE uppercase micro-labels.
    - Source assertion: prev-value renders via `t("previous", ...)` in the input-row header (LastValueChip data folded in).
    - CLI: `cd app && npx tsc --noEmit` exits 0 and `npx expo lint` reports no new errors.
  </acceptance_criteria>
  <verify>
    <automated>cd app && npx tsc --noEmit && npx expo lint</automated>
  </verify>
  <done>Input row matches the Forge mock (tall accent fields + value/unit-label + full-width Klart); all keyboard/RHF/prefill wiring preserved; addSet.mutate path untouched; tsc + lint clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Set-logged motion + gated haptic + Forge finish overlay with stats + spring (MOTN-01/04/05, D-08/D-11/D-12/D-15/D-16)</name>
  <read_first>
    - app/app/(app)/workout/[sessionId].tsx (onKlart L377-410 — add motion/haptic AFTER mutate; AvslutaOverlay L843-1012 incl. keyboard-height listener L865-881 + paddingBottom L932-934 + notes state/counter L967-985 + notes-reset L859 + backdrop-tap dismiss L937 + inner-Pressable touch-claim L947-950; finishSession.mutate L895-906 in handleConfirm; useSetsForSessionQuery in scope for stats)
    - app/design v2/Sources/design/forge-screens.jsx FFinishOverlay L1590-1700 + FFOStat L1684-1701 (trophy-block hero gradient icon, 26px heading, body, notes textarea + N/500 counter, 3-cell stats row, neutral Fortsätt + accent Avsluta)
    - app/app/(app)/(tabs)/index.tsx LinearGradient usage L199-213 (for the overlay's own trophy hero block — trophy IS allowed here, it is the overlay icon, NOT the omitted PR banner)
    - app/lib/prefs.ts getPref L75 + app/app/(app)/(tabs)/settings.tsx fm:haptics read pattern; expo-haptics (installed)
    - app/design v2/Sources/design/Forge Design Spec.html §07 Motion L525 (set-logged row/check/impactMedium; overlay backdrop 0→0.5 + card translateY 24→0; default spring damping 18 / stiffness 220, ~220-240ms)
    - app/app/(app)/(tabs)/index.tsx toast FadeIn/FadeOut precedent L478-480 + app/app/(app)/exercise/[exerciseId]/chart.tsx useSharedValue precedent L66 (Reanimated 4 patterns)
    - app/components/ui/ForgeField.tsx (multiline — viable for the notes textarea where wiring is simpler) + Icon.tsx
  </read_first>
  <action>
    Set-logged motion + haptic (MOTN-01/MOTN-05, D-11/D-12): in `onKlart`, AFTER the existing `addSet.mutate(...)` call (never before, never awaited), add fire-and-forget feedback. Haptic — gate behind the pref: `import * as Haptics from "expo-haptics"; import { getPref } from "@/lib/prefs";` then `void getPref("fm:haptics").then((on) => { if (on) void Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Medium); });`. Visual — the newly logged row slides in from bottom + its check icon scales 0.8→1; implement via a Reanimated `entering` animation (e.g. SlideInDown) or a useSharedValue + withSpring using the §07 default curve (damping 18, stiffness 220, ~220ms). The visual is NOT gated by fm:haptics. Neither the haptic nor the animation may block or precede the mutate.
    Finish overlay re-skin (D-08/D-15/D-16): restyle AvslutaOverlay's inner card to FFinishOverlay. Render: a trophy-block hero (LinearGradient like index.tsx L199-213 with `<Icon name="trophy" />` — this is the overlay's OWN icon, NOT the omitted PR banner), a 26px display heading `t("finishWorkoutQ")`, a body line `t("finishBody", { count, time })`, the existing notes textarea + "N/500" counter (keep the notes state/counter L967-985 and notes-reset L859), and a NEW 3-cell stats row (FFOStat): cell 1 = `{loggedSetCount}` / `t("sets")`, cell 2 = `{Σ weight×reps}` / `kg`, cell 3 = `{MM:SS elapsed since started_at}` / `t("min")`, all derived client-side by reducing over `useSetsForSessionQuery` data already in scope; numerals tabular. Buttons: neutral "Fortsätt/Continue" (`surface3`) + accent "Avsluta/Finish" (filled accent + check — D-16, NOT red).
    PRESERVE: backdrop-tap dismiss (`onPress={onCancel}` L937), inner-Pressable touch-claim (L947-950), the manual keyboard-height lift (`justifyContent`/`paddingBottom = keyboardHeight + 16` L932-934), notes-reset-on-unmount (L859), and `finishSession.mutate` (mutate NOT mutateAsync, notes payload unchanged, synchronous onFinish() navigation after).
    Overlay spring (MOTN-04): animate the overlay open — backdrop opacity 0→0.5 + card translateY 24→0 with the §07 spring (damping 18 / stiffness 220, ~240ms), INLINE-rendered (absolute scrim + flex, NO Modal portal — D-15).
    All box styling in className; style() only for shadow/opacity/animated transforms.
  </action>
  <acceptance_criteria>
    - Source assertion: `onKlart` calls `addSet.mutate(` BEFORE any haptic/animation trigger; the haptic is `Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Medium)` guarded by `getPref("fm:haptics")` and is fire-and-forget (void, not awaited before mutate).
    - Source assertion: the set-logged visual animation (entering/SlideInDown or useSharedValue+withSpring) is NOT wrapped in the fm:haptics guard.
    - Source assertion: the finish overlay renders a 3-cell stats row whose values come from a client-side `reduce` over the sets query (sets count / Σ weight×reps / elapsed); a `kg` and `t("min")` / `t("sets")` label appear.
    - Source assertion: `finishSession.mutate` is still used (NOT mutateAsync) with the unchanged notes payload; backdrop-tap `onCancel`, touch-claim, notes-reset, and `paddingBottom = keyboardHeight + 16` are all present.
    - Source assertion: the overlay is inline (absolute scrim + flex) — no `Modal` import/usage added; the primary finish button uses accent tokens, not danger.
    - CLI: `cd app && npx tsc --noEmit` exits 0 and `npx expo lint` reports no new errors.
  </acceptance_criteria>
  <verify>
    <automated>cd app && npx tsc --noEmit && npx expo lint</automated>
  </verify>
  <done>Set-logged slide+scale animation plays with a fm:haptics-gated Medium haptic, both fire-and-forget after mutate; finish overlay re-skinned to FFinishOverlay with a client-derived stats row, accent (not red) finish button, §07 spring, inline-rendered; all preserved logic intact; tsc + lint clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: BLOCKING — F13 ≤3s budget + keyboard-wiring survival verification (SKIN-08)</name>
  <read_first>
    - app/scripts/verify-f13-brutal-test.ts (DB-integrity check — verifies recently-logged sets persisted to Supabase with correct set_count; NOT a UI test, so it stays green only if the mutation path is untouched)
    - app/app/(app)/workout/[sessionId].tsx (confirm the MUST-NOT-CHANGE zones survived Tasks 1-2)
    - CLAUDE.md "Project / Constraints" (Core Value never lose a set; ≤3s log budget; offline-first)
  </read_first>
  <what-built>
    Tasks 1-2 re-skinned the set-input row, added non-blocking set-logged motion + gated haptic, and re-skinned the finish overlay — all AROUND the frozen write path (D-17). This task proves the ≤3s "never lose a set" budget and the keyboard wiring survived.
  </what-built>
  <action>
    Run the automated DB-integrity gate and the source-assertion gates first (Claude does these before pausing):
    1. Source-assert the keyboard/RHF wiring survived: grep `workout/[sessionId].tsx` confirms `decimal-pad`/`number-pad`, `inputMode`, `selectTextOnFocus`, `returnKeyType`, `mode: "onSubmit"`, and the `sessionPrefill ?? f7Prefill` prefill are all still present.
    2. Source-assert `mutate(` is used and `mutateAsync(` is NOT used for addSet/finishSession.
    3. In Expo Go on a real iPhone, log several sets across at least two exercises (this populates recent sets), then run `cd app && npm run test:f13-brutal` and confirm it exits 0 (recently-logged sets persisted with correct set_count).
  </action>
  <how-to-verify>
    1. Open the app in Expo Go on iPhone, start a pass, and log 3-4 sets across two exercises.
    2. Confirm each set logs in ≤3s from Klart-tap to the row appearing (the set-logged slide+haptic should feel additive, never a delay).
    3. Toggle Settings → haptics OFF, log a set: NO haptic fires but the visual animation still plays. Toggle ON: haptic returns.
    4. Open the Avsluta overlay: it springs open inline (no modal jump), the 3-cell stats row shows correct set count / volume / duration, the Avsluta button is accent (orange), not red.
    5. Run `cd app && npm run test:f13-brutal` and confirm exit 0.
  </how-to-verify>
  <verify>
    <automated>cd app && npm run test:f13-brutal && npx tsc --noEmit</automated>
  </verify>
  <resume-signal>Type "approved" once test:f13-brutal exits 0 and the ≤3s log feel + haptic-toggle behavior are confirmed on device, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client UI ↔ frozen mutation layer | onKlart/handleConfirm restyled around useAddSet/useFinishSession; no new call into the boundary |
| client ↔ fm:haptics pref store | getPref reads a local boolean (Zod-catch) for the haptic gate; no PII |
| client-side stats derivation | finish-overlay stats reduce over already-authorized session sets; no new fetch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Information Disclosure | finish-overlay client-side stats (sets/volume/duration) | accept | Derived from `useSetsForSessionQuery` data already authorized for this user under the `session:${sessionId}` RLS scope; no new query, no cross-user aggregation (API3). |
| T-11-05 | Information Disclosure | fm:haptics pref read | accept | Local device preference (boolean, Zod-catch), no PII, never networked. |
| T-11-06 | Denial of Service | set-logged Reanimated animation + haptic | accept | Fire-and-forget AFTER mutate; never blocks/precedes the optimistic write (F13). Animation runs on the UI thread off the JS write path; haptic is voided. |
| T-11-07 | Tampering | finishSession.mutate notes payload | accept | Payload unchanged (D-17); `mutate` not `mutateAsync`; RLS validates on the server. No new fields. |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (Reanimated 4 + expo-haptics already installed per PATTERNS.md). Legitimacy gate N/A. |
</threat_model>

<verification>
- `cd app && npm run test:f13-brutal` exits 0 (DB-integrity: recently-logged sets persisted with correct set_count — proves the mutation path is intact).
- `cd app && npx tsc --noEmit` exits 0; `npx expo lint` reports no new errors.
- Source audit: keyboard wiring (decimal-pad/number-pad, inputMode, selectTextOnFocus, returnKeyType, mode:onSubmit, prefill) and `mutate`-not-`mutateAsync` survive on addSet + finishSession.
- On-device: ≤3s log feel preserved; haptic respects the fm:haptics toggle; overlay springs inline (no modal portal).
</verification>

<success_criteria>
- Set-input row matches the Forge mock with all keyboard/RHF/prefill wiring intact.
- Logging a set plays the slide+scale animation and a fm:haptics-gated Medium haptic, fire-and-forget after the optimistic mutate.
- Finish overlay re-skinned to FFinishOverlay with a client-derived 3-cell stats row, accent (not red) finish button, §07 spring, inline-rendered.
- `npm run test:f13-brutal` green; ≤3s budget confirmed on device.
</success_criteria>

<output>
Create `.planning/phases/11-active-workout-re-skin-high-risk-f13/11-02-SUMMARY.md` when done.
</output>
