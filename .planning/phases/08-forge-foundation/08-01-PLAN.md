---
phase: 08-forge-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/package.json
  - app/lib/i18n.ts
  - app/locales/sv.json
  - app/locales/en.json
  - app/lib/font-store.ts
  - app/lib/utils/format.ts
  - app/scripts/check-locale-parity.ts
autonomous: false
requirements: [I18N-01, I18N-04, DSGN-03]
user_setup: []

must_haves:
  truths:
    - "react-i18next is initialized at module load with sv + en flat-key resources, flat keys mirroring the I18N object 1:1 in a single default namespace (D-10)"
    - "sv.json and en.json each contain the full I18N map (188 keys) with identical key sets, transcribed from lib.jsx with function-valued strings as {{n}} interpolations (D-09)"
    - "fmtNum formats 1234.5 as '1 234,5' (sv) and '1,234.5' (en); fmtDate formats per locale"
    - "check-locale-parity.ts exits 0 when sv/en key sets match, non-zero otherwise"
    - "useFontStore exposes fontsReady + localeReady booleans with setters, no persistence"
  artifacts:
    - path: "app/lib/i18n.ts"
      provides: "i18next module-singleton init (single default namespace, sv fallback)"
      contains: "initReactI18next"
    - path: "app/locales/sv.json"
      provides: "Swedish flat-key translation resource (188 keys)"
    - path: "app/locales/en.json"
      provides: "English flat-key translation resource (188 keys)"
    - path: "app/lib/font-store.ts"
      provides: "Zustand slice: fontsReady + localeReady flags"
      contains: "create"
    - path: "app/lib/utils/format.ts"
      provides: "fmtNum (Intl) + fmtDate (date-fns) + tnum style helper"
      contains: "Intl.NumberFormat"
    - path: "app/scripts/check-locale-parity.ts"
      provides: "Automatable sv/en key-parity gate"
  key_links:
    - from: "app/lib/i18n.ts"
      to: "app/locales/sv.json + en.json"
      via: "import + resources block"
      pattern: "resources:\\s*\\{"
    - from: "app/scripts/check-locale-parity.ts"
      to: "app/locales/*.json"
      via: "Object.keys comparison"
      pattern: "Object\\.keys"
---

<objective>
Stand up the i18n engine, the full bilingual locale resources, the locale-aware
formatting helpers, the tabular-numeral style helper, and the tiny font-store
slice — plus install every net-new dependency this phase needs. This is the
data + engine layer the app-shell wiring (Plan 02) and the gallery (Plan 05) consume.

Purpose: I18N-01 (text from translation resources), I18N-04 (locale-aware
number/date formatting), and the DSGN-03 tabular-numeral helper all originate here.
Building i18n.ts + font-store.ts first means Plan 02's _layout.tsx wiring imports
ready-made modules instead of a scavenger hunt.

Output: app/lib/i18n.ts, app/locales/{sv,en}.json, app/lib/font-store.ts,
app/lib/utils/format.ts, app/scripts/check-locale-parity.ts, and 5 new deps in package.json.
</objective>

<execution_context>
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-forge-foundation/08-CONTEXT.md
@.planning/phases/08-forge-foundation/08-RESEARCH.md
@.planning/phases/08-forge-foundation/08-PATTERNS.md

<interfaces>
<!-- Zustand-no-persist shape to copy verbatim (persistence-store.ts) -->
From app/lib/persistence-store.ts:
  export const usePersistenceStore = create<PersistenceState>((set) => ({
    hydrated: false,
    setHydrated: (v) => set({ hydrated: v }),
  }));

<!-- date-fns import idiom already used in the repo -->
From app/app/(app)/exercise/[exerciseId]/chart.tsx (lines 52-53):
  import { format } from "date-fns";
  import { sv } from "date-fns/locale";

<!-- I18N source-of-truth line ranges (web React object — TRANSCRIBE, do not import) -->
lib.jsx I18N.sv = lines 8-119 ; I18N.en = lines 120-222
Exactly 2 function-valued keys (verified): setsSavedBody (line 71/178), pbSub (line 76/183).
All other keys (incl. noSetsBody) are plain strings. Each locale has 188 keys.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify + install net-new dependencies (supply-chain gate)</name>
  <read_first>
    - .planning/phases/08-forge-foundation/08-RESEARCH.md (§"Package Legitimacy Audit" — all 5 tagged [ASSUMED]; §"Installation"; §Pitfall 2 expo-localization SDK-56 trap)
    - app/package.json (confirm none already present; confirm SDK ~54.0.33 pin)
  </read_first>
  <what-built>Nothing yet — this gate verifies legitimacy BEFORE the first install.</what-built>
  <how-to-verify>
    All five net-new packages are tagged [ASSUMED] in RESEARCH (slopcheck unavailable). Confirm each on its official registry page before install:
    1. i18next — npmjs.com/package/i18next (i18next-org, 10M+/wk)
    2. react-i18next — npmjs.com/package/react-i18next (i18next-org)
    3. expo-localization — npmjs.com/package/expo-localization (Expo first-party)
    4. @expo-google-fonts/inter — npmjs.com/package/@expo-google-fonts/inter (Expo first-party)
    5. react-native-svg — npmjs.com/package/react-native-svg (software-mansion, 5M+/wk) — per D-11
    Confirm each is the genuine first-party package (not a typosquat).
  </how-to-verify>
  <action>
    After approval, install from `app/` cwd in this exact split (pure-JS via npm, native via expo so SDK-54 pins resolve — per RESEARCH §Installation + Pitfall 2):
    `npm install i18next react-i18next @expo-google-fonts/inter`
    then `npx expo install expo-localization react-native-svg`.
    Do NOT run `npm install expo-localization` (pulls SDK-56 latest 56.0.6 → native mismatch). Do NOT bump tailwindcss/nativewind/skia. Restart Metro is the executor's note, not an install step.
  </action>
  <resume-signal>Type "approved" to install, or name any package that looks wrong.</resume-signal>
  <acceptance_criteria>
    - All 5 packages confirmed on official registry pages (i18next-org / Expo / Software Mansion).
    - After install: `app/package.json` lists i18next, react-i18next, expo-localization, @expo-google-fonts/inter, react-native-svg.
    - `npx tsc --noEmit` from app/ exits 0 (no broken peer/types).
  </acceptance_criteria>
  <done>5 deps installed via the correct npm/expo split; tsc green; no version bumps to locked packages.</done>
</task>

<task type="auto">
  <name>Task 2: Transcribe locales + write i18n engine + parity script</name>
  <read_first>
    - app/design v2/Sources/design/lib.jsx (I18N.sv lines 8-119, I18N.en lines 120-222 — the transcription SOURCE; web object, copy strings verbatim)
    - app/lib/supabase.ts (module-singleton + module-scope side-effect convention — i18n.ts mirrors this)
    - app/lib/auth-store.ts lines 1-37 (file-header rationale comment convention)
    - app/scripts/test-rls.ts (existing tsx-script header + Node-only pattern for check-locale-parity.ts)
    - .planning/phases/08-forge-foundation/08-PATTERNS.md (§app/lib/i18n.ts, §app/locales/*.json — D-09/D-10 transcription rules)
  </read_first>
  <action>
    Create `app/locales/sv.json` and `app/locales/en.json`: transcribe the full I18N.sv / I18N.en objects from lib.jsx into FLAT-key JSON, single namespace, keys mirroring the object 1:1 (D-09/D-10). The 2 function-valued keys become i18next interpolations: `setsSavedBody` → "{{n}} set sparade. Avsluta passet?" (sv) / "{{n}} sets saved. Finish workout?" (en); `pbSub` → "{{kg}} kg × {{reps}} reps" (both). Every other key is a verbatim string copy. Both files MUST have the identical 188-key set. Do NOT author new copy.
    Create `app/lib/i18n.ts` per PATTERNS §i18n.ts: module-scope `i18n.use(initReactI18next).init({ resources: { sv: { translation: sv }, en: { translation: en } }, lng: Localization.getLocales()[0]?.languageCode ?? 'sv', fallbackLng: 'sv', interpolation: { escapeValue: false }, compatibilityJSON: 'v4' })`, `export default i18n`. Open with the file-header rationale comment (cite D-09/D-10, I18N-01).
    Create `app/scripts/check-locale-parity.ts` (tsx, Node-only, test-rls.ts header style): import both JSON files, assert `Object.keys(sv)` ≡ `Object.keys(en)` as sets; print the symmetric difference and `process.exit(1)` on mismatch, else print PASS + count and exit 0. Add an npm script `"check:locale-parity": "tsx scripts/check-locale-parity.ts"` to app/package.json.
  </action>
  <verify>
    <automated>cd app && npx tsx scripts/check-locale-parity.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && npx tsx scripts/check-locale-parity.ts` prints PASS with 188 keys and exits 0.
    - `grep -c "setsSavedBody\|pbSub" app/locales/sv.json` returns 2; the values contain `{{n}}` and `{{kg}}`/`{{reps}}` respectively.
    - `app/lib/i18n.ts` contains `initReactI18next`, `fallbackLng: 'sv'`, `escapeValue: false`.
    - `cd app && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Both locale files at 188 identical keys; i18n engine module compiles; parity script green.</done>
</task>

<task type="auto">
  <name>Task 3: font-store slice + locale-aware format helpers + tnum</name>
  <read_first>
    - app/lib/persistence-store.ts (full — exact Zustand-no-persist shape to copy)
    - app/app/(app)/exercise/[exerciseId]/chart.tsx lines 52-53 (date-fns import idiom)
    - .planning/phases/08-forge-foundation/08-RESEARCH.md (§"Number / date formatting", §"Tabular numerals in RN" — tnum helper, Intl-not-the-lib.jsx-hack)
    - .planning/phases/08-forge-foundation/08-PATTERNS.md (§font-store.ts, §format.ts)
  </read_first>
  <action>
    Create `app/lib/font-store.ts`: copy persistence-store.ts shape exactly, with TWO booleans + setters — `fontsReady`/`setFontsReady` and `localeReady`/`setLocaleReady`, both default false. No persistence (ARCHITECTURE §2). File-header comment citing DSGN-02 + the splash-gate wiring (Plan 02 consumer).
    Create `app/lib/utils/format.ts` per RESEARCH §Number/date formatting: `fmtNum(n, locale: 'sv'|'en')` using `new Intl.NumberFormat(locale==='sv'?'sv-SE':'en-US').format(n)` (Hermes-native, no polyfill); `fmtDate(d, locale, fmt='d MMM yyyy')` using date-fns `format` + `{ locale: locale==='sv'?sv:enUS }` from `date-fns/locale`; and `export const tnum = { fontVariant: ['tabular-nums'] as const }` (DSGN-03 — RN ignores web-only ss01; tabular-nums delivers alignment). Do NOT reuse lib.jsx's `.replace(/,/g,' ')` fmtNum hack.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `app/lib/font-store.ts` exports `useFontStore` with fontsReady, localeReady, setFontsReady, setLocaleReady.
    - `app/lib/utils/format.ts` contains `Intl.NumberFormat`, imports `sv, enUS` from `date-fns/locale`, exports `tnum` with `fontVariant: ['tabular-nums']`.
    - `cd app && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>font-store + format helpers compile; tnum exported; no lib.jsx number hack.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bundled JSON → i18n engine | Static, first-party locale resources; no runtime external input in this plan |
| npm/expo registry → build | New dependency code enters the build at install time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering (supply chain) | npm/expo install of 5 net-new deps | mitigate | Task 1 blocking-human checkpoint: verify each [ASSUMED] package on official registry (i18next-org / Expo / Software Mansion) before install; pin via `npx expo install` for native modules; never auto-approve. |
| T-08-02 | Tampering/Injection | i18next interpolation (`{{n}}`, `{{kg}}`, `{{reps}}`) | accept | `escapeValue:false` is safe: only app-controlled numeric values interpolate in Phase 8, and RN `<Text>` renders no markup (no XSS surface). Documented for later phases. |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck unavailable → all deps [ASSUMED]; Task 1 blocking-human gate covers every install; `[SLOP]`/`[SUS]`: none found. |
</threat_model>

<verification>
- `cd app && npx tsx scripts/check-locale-parity.ts` → PASS, 188 keys.
- `cd app && npx tsc --noEmit` → exits 0.
- `cd app && npm run lint` → clean.
- F13 untouched: this plan adds only new files + deps; no `@/lib/query/*` import touched.
</verification>

<success_criteria>
- i18next initialized with sv+en flat resources, sv fallback (I18N-01).
- sv.json/en.json at 188 identical keys; 2 interpolation keys correct (D-09).
- fmtNum/fmtDate locale-correct; tnum helper exported (I18N-04, DSGN-03).
- 5 deps installed via correct npm/expo split, no locked-version bumps.
</success_criteria>

<output>
Create `.planning/phases/08-forge-foundation/08-01-SUMMARY.md` when done
</output>
