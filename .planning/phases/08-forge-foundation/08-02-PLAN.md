---
phase: 08-forge-foundation
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - app/tailwind.config.js
  - app/assets/fonts/InterDisplay-Regular.otf
  - app/assets/fonts/InterDisplay-SemiBold.otf
  - app/assets/fonts/InterDisplay-Bold.otf
  - app/assets/fonts/JetBrainsMono-Regular.ttf
  - app/assets/fonts/OFL.txt
  - app/app/_layout.tsx
autonomous: false
requirements: [DSGN-01, DSGN-02]
user_setup: []

must_haves:
  truths:
    - "forge.* color tokens (light + dark) resolve as NativeWind classes app-wide"
    - "font-display / font-display-semibold / font-display-bold / font-mono classes map to the 4 loaded font families"
    - "Inter Display + JetBrains Mono load via expo-font; the splash is held until fontsReady AND localeReady AND auth resolve"
    - "A corrupt fm:language value falls back to 'sv' and never hangs the splash"
    - "borderRadius forge-sm/md/lg/xl tokens exist (10/14/20/28)"
  artifacts:
    - path: "app/tailwind.config.js"
      provides: "forge.* colors + fontFamily (display weights + mono) + borderRadius extends"
      contains: "forge"
    - path: "app/assets/fonts/InterDisplay-Bold.otf"
      provides: "Bundled Inter Display Bold face (DSGN-02)"
    - path: "app/assets/fonts/JetBrainsMono-Regular.ttf"
      provides: "Bundled mono numeric face"
    - path: "app/app/_layout.tsx"
      provides: "FontBootstrap + LocaleBootstrap + i18n side-effect import + extended splash gate"
      contains: "FontBootstrap"
  key_links:
    - from: "app/app/_layout.tsx"
      to: "app/lib/font-store.ts"
      via: "useFontStore reads fontsReady + localeReady in splash gate"
      pattern: "useFontStore"
    - from: "app/app/_layout.tsx"
      to: "app/lib/i18n.ts"
      via: "module-scope side-effect import"
      pattern: "import \"@/lib/i18n\""
    - from: "app/tailwind.config.js"
      to: "components consuming forge-* classes"
      via: "theme.extend.colors.forge"
      pattern: "forge:\\s*\\{"
---

<objective>
Migrate the Forge token palette into tailwind.config.js, bundle the 4 font files,
and wire FontBootstrap + LocaleBootstrap + the i18n side-effect import + the
extended splash gate into the root _layout.tsx. After this plan, every later
component can use forge-* classes and the 4 font families, the app boots with
fonts loaded and the saved locale applied, and no screen behavior changes.

Purpose: DSGN-01 (tokens available app-wide), DSGN-02 (custom type system loaded,
splash held). This is the load-bearing app-shell change; F13 imports must NOT move.

Output: token block in tailwind.config.js, app/assets/fonts/* (4 faces + OFL.txt),
modified app/app/_layout.tsx.
</objective>

<execution_context>
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-forge-foundation/08-RESEARCH.md
@.planning/phases/08-forge-foundation/08-PATTERNS.md
@.planning/phases/08-forge-foundation/08-UI-SPEC.md

<interfaces>
<!-- THEMES.forge — VERBATIM values to mirror (lib.jsx lines 234-276) -->
radius: { sm:10, md:14, lg:20, xl:28 }
dark:  bg #000000 · surface #0E0E10 · surface2 #18181B · surface3 #222226 · text #FFFFFF
       text2 rgba(255,255,255,0.62) · text3 rgba(255,255,255,0.38) · accent #FF5A1F
       accentText #FFFFFF · accentSoft rgba(255,90,31,0.14) · success #30D158 · warn #FFD60A
       danger #FF453A · border rgba(255,255,255,0.08) · borderStrong rgba(255,255,255,0.14)
       gradFrom #FF7A2E · gradTo #FF2D55 · tabBg rgba(20,20,22,0.85)
light: bg #FAFAF7 · surface #FFFFFF · surface2 #F2F1EC · surface3 #E8E7E1 · text #0A0A0A
       text2 #4D4D4D · text3 #8B8B8B · accent #E14E10 · accentText #FFFFFF
       accentSoft rgba(225,78,16,0.10) · success #1E9E45 · warn #B68000 · danger #D70015
       border rgba(0,0,0,0.07) · borderStrong rgba(0,0,0,0.14) · gradFrom #FF7A2E
       gradTo #FF3D5E · tabBg rgba(255,255,255,0.85)

<!-- font-store API created in Plan 01 -->
From app/lib/font-store.ts:
  useFontStore: { fontsReady, localeReady, setFontsReady, setLocaleReady }

<!-- _layout.tsx already imports: AsyncStorage, z, useColorScheme, useEffect.
     ThemeBootstrap (lines 84-100) + SplashScreenController (lines 64-74) are the templates.
     LOAD-BEARING — DO NOT REORDER: lines 30-32 query imports. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Forge token block in tailwind.config.js</name>
  <read_first>
    - app/tailwind.config.js (full — only theme.extend changes; darkMode/content/presets stay)
    - app/design v2/Sources/design/lib.jsx lines 234-276 (THEMES.forge — verbatim token source)
    - .planning/phases/08-forge-foundation/08-RESEARCH.md (§Pattern 1 token migration + the OQ-3 light-mode gotcha + Pitfall 4)
    - .planning/phases/08-forge-foundation/08-PATTERNS.md (§tailwind.config.js — OQ-3 solid-hex + OQ-4 3-weight decisions, do NOT re-derive)
  </read_first>
  <action>
    Extend `theme.extend` only (leave content/presets/darkMode untouched). Add flat `forge.*` colors with DEFAULT (dark) + light entries verbatim from THEMES.forge: bg, surface, surface2, surface3, text, accent, accentText, success, warn, danger, gradFrom, gradTo.
    Per OQ-3 (the most error-prone part): for the rgba tokens add SOLID-HEX `forge.text2`/`text3`/`border`/`borderStrong`/`accentSoft` with light = the light solid hex (text2 #4D4D4D, text3 #8B8B8B) and DEFAULT = the dark rgba expressed so it round-trips — i.e. provide both `forge.text2` (DEFAULT dark rgba, light #4D4D4D) etc. so light mode uses solid warm-grays and dark uses the opacity-of-white values. Add a comment that dark secondary text may also be expressed via `text-forge-text/62`-style modifiers at use-site; the solid token exists so LIGHT mode is correct.
    Per OQ-4: add `fontFamily` extends — `display: ['InterDisplay','Inter','System']`, `'display-semibold': ['InterDisplay-SemiBold','Inter','System']`, `'display-bold': ['InterDisplay-Bold','Inter','System']`, `mono: ['JetBrainsMono','Menlo']`. (RN does not synthesize weights — 3 separate families.)
    Add `borderRadius`: forge-sm 10px, forge-md 14px, forge-lg 20px, forge-xl 28px.
    Add `forge.tabBg` (DEFAULT rgba(20,20,22,0.85), light rgba(255,255,255,0.85)) for the TabBar shell (Plan 04).
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "forge:" app/tailwind.config.js` ≥ 1; config still has `darkMode: "class"` and `presets`.
    - Token block contains `#FF5A1F`, `#E14E10`, `#4D4D4D`, `#8B8B8B`, `forge-sm`, and the 3 display fontFamily keys (`display`, `display-semibold`, `display-bold`).
    - `cd app && npx tsc --noEmit` exits 0 (config is valid JS).
  </acceptance_criteria>
  <done>forge.* colors + 3 display families + mono + 4 radii present; light-mode solid-hex tokens correct per OQ-3; coexists with existing gray/blue classes.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Source + commit the 4 OFL font files</name>
  <read_first>
    - .planning/phases/08-forge-foundation/08-RESEARCH.md (§"Font sourcing detail" — rsms/inter v4.1 OTF, JetBrains/JetBrainsMono TTF, OFL 1.1, exact filenames)
    - .planning/research/ARCHITECTURE.md §2 (font filename contract)
  </read_first>
  <what-built>Claude cannot download binary font assets from release zips into the repo autonomously; this is the one genuinely human step.</what-built>
  <how-to-verify>
    Place exactly these 4 files in `app/assets/fonts/` (filenames must match the Font.loadAsync keys in Task 3):
    1. InterDisplay-Regular.otf — from github.com/rsms/inter releases v4.x (extras/otf or "Inter Desktop")
    2. InterDisplay-SemiBold.otf — same source
    3. InterDisplay-Bold.otf — same source
    4. JetBrainsMono-Regular.ttf — from github.com/JetBrains/JetBrainsMono releases
    Also add `app/assets/fonts/OFL.txt` (both projects ship OFL 1.1 — bundle the license). Commit all 5 (must NOT be gitignored).
    If any Inter Display weight is unavailable: per D-05, the matching standard Inter weight from @expo-google-fonts/inter (installed Plan 01) is the fallback — Task 3 registers it under the SAME family key. Note which (if any) fell back.
  </how-to-verify>
  <action>
    After files are placed, the executor confirms presence and that they are tracked by git (not ignored). No code in this task.
  </action>
  <resume-signal>Type "fonts placed" (and note any D-05 fallbacks), or "use Inter fallback for all" to skip self-hosting.</resume-signal>
  <acceptance_criteria>
    - `ls app/assets/fonts/` shows the 4 font files + OFL.txt.
    - `git check-ignore app/assets/fonts/InterDisplay-Bold.otf` returns nothing (not ignored).
  </acceptance_criteria>
  <done>4 font faces + OFL license committed to app/assets/fonts/; any D-05 fallbacks recorded in the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 3: Wire FontBootstrap + LocaleBootstrap + i18n import + splash gate in _layout.tsx</name>
  <read_first>
    - app/app/_layout.tsx (full — ThemeBootstrap lines 84-100 + SplashScreenController lines 64-74 are the exact templates; lines 30-32 are LOAD-BEARING query imports)
    - app/lib/font-store.ts (useFontStore API from Plan 01)
    - .planning/phases/08-forge-foundation/08-PATTERNS.md (§app/app/_layout.tsx — the 5-step wiring recipe + the F13 DO-NOT-TOUCH list)
    - .planning/phases/08-forge-foundation/08-RESEARCH.md (§Pattern 2 fail-open font load, §Pattern 3 LocaleBootstrap, §Pitfall 7 splash-never-clears)
  </read_first>
  <action>
    Five edits, per PATTERNS §_layout.tsx:
    1. Add `import "@/lib/i18n";` AFTER the `@/lib/query/*` imports (lines 30-32) — those are LOAD-BEARING and must not be reordered (CLAUDE.md: i18n import goes after the query side-effect imports).
    2. Add `FontBootstrap` component (copy ThemeBootstrap shape): `Font.loadAsync({ 'InterDisplay': require('../assets/fonts/InterDisplay-Regular.otf'), 'InterDisplay-SemiBold': require('../assets/fonts/InterDisplay-SemiBold.otf'), 'InterDisplay-Bold': require('../assets/fonts/InterDisplay-Bold.otf'), 'JetBrainsMono': require('../assets/fonts/JetBrainsMono-Regular.ttf') }).then(()=>setFontsReady(true)).catch(()=>setFontsReady(true))` — FAIL-OPEN (D-05, Pitfall 7). If any weight fell back to @expo-google-fonts/inter (Task 2), register that import under the SAME family key.
    3. Add `LocaleBootstrap` component: reuse ThemeBootstrap's `z.enum([...]).catch(default).parse(v)` idiom on `fm:language` — `AsyncStorage.getItem('fm:language').then(v => i18n.changeLanguage(z.enum(['sv','en']).catch('sv').parse(v))).catch(()=>{}).finally(()=>setLocaleReady(true))`. Import i18n default + useFontStore.
    4. Extend `SplashScreenController`: gate becomes `status !== 'loading' && fontsReady && localeReady`; read fontsReady/localeReady from useFontStore; add both to the effect dep array.
    5. Mount `<FontBootstrap />` and `<LocaleBootstrap />` as siblings next to `<ThemeBootstrap />` inside PersistQueryClientProvider.
    DO NOT TOUCH: `@/lib/query/client|persister|network` imports or the PersistQueryClientProvider onSuccess/onError callbacks (F13 gate).
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit && npm run lint && npm run test:f13-brutal</automated>
  </verify>
  <acceptance_criteria>
    - `app/app/_layout.tsx` contains `FontBootstrap`, `LocaleBootstrap`, `import "@/lib/i18n"`, and a splash gate referencing `fontsReady` AND `localeReady`.
    - The i18n import appears AFTER `import "@/lib/query/network"`.
    - `grep -c "@/lib/query/client\|@/lib/query/persister\|@/lib/query/network" app/app/_layout.tsx` is unchanged (3) — F13 imports intact.
    - `npm run test:f13-brutal` stays green.
    - `cd app && npx tsc --noEmit && npm run lint` exit 0.
  </acceptance_criteria>
  <done>Fonts load fail-open; saved locale applied before splash clears; corrupt fm:language → 'sv'; F13 brutal-test green; query imports untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AsyncStorage `fm:language` → LocaleBootstrap | Persisted device value, can be corrupted/injected |
| font release zip → bundled asset | Binary fonts enter the build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Tampering | `fm:language` read in LocaleBootstrap | mitigate | `z.enum(['sv','en']).catch('sv').parse(v)` (mirrors ThemeBootstrap T-07-01); corrupt value → 'sv', never crashes/hangs. |
| T-08-04 | Denial of Service (self) | splash gate on fontsReady/localeReady | mitigate | Both bootstraps set their ready flag on BOTH success and failure (fail-open `.catch`/`.finally`) per Pitfall 7 — a missing font / corrupt pref cannot hang the splash forever. |
| T-08-05 | Tampering | malicious/corrupt font file | mitigate | Task 2: source Inter/JetBrains from official OFL releases only; commit the exact reviewed files; OFL.txt bundled. |
</threat_model>

<verification>
- `cd app && npx tsc --noEmit && npm run lint` → exit 0.
- `cd app && npm run test:f13-brutal` → green (additive change did not disturb offline queue).
- Manual gallery-precursor smoke (Plan 05 owns the gallery): app boots, no splash hang, theme + locale apply.
- F13 DO-NOT-TOUCH list honored: query imports + provider callbacks unchanged.
</verification>

<success_criteria>
- forge.* tokens + 3 display families + mono + 4 radii live as NativeWind classes (DSGN-01).
- 4 font faces load via expo-font, splash held until fontsReady && localeReady && auth (DSGN-02).
- Corrupt fm:language → 'sv'; no splash hang (T-08-03/04).
- F13 brutal-test green; query module imports untouched.
</success_criteria>

<output>
Create `.planning/phases/08-forge-foundation/08-02-SUMMARY.md` when done
</output>
