---
phase: 08-forge-foundation
plan: 04
type: execute
wave: 4
depends_on: ["08-02", "08-03"]
files_modified:
  - app/components/ui/ForgeButton.tsx
  - app/components/ui/ForgeField.tsx
  - app/components/ui/ForgeCard.tsx
  - app/components/ui/ForgeStat.tsx
  - app/components/ui/ForgeChip.tsx
  - app/components/ui/SettingsRow.tsx
  - app/components/ui/TabBar.tsx
autonomous: true
requirements: [DSGN-04]
user_setup: []

must_haves:
  truths:
    - "ForgeButton renders all 4 variants × 3 sizes with token-driven styling, light + dark"
    - "ForgeField renders default/focused/error states with optional leading Icon, controlled via value/onChangeText"
    - "ForgeCard, ForgeStat (tnum), ForgeChip render per the variant/size enums from forge-screens.jsx"
    - "SettingsRow + SettingsSection render with icon tile, label, value, chevron and toggle variants"
    - "TabBar shell renders standalone (plans/history/settings) and is NOT wired into the live <Tabs>"
    - "Every pressable primitive uses Pressable style-callback for pressed state + explicit shadow object (FIT-66), never active:/shadow- classes"
  artifacts:
    - path: "app/components/ui/ForgeButton.tsx"
      provides: "variant primary|secondary|ghost|destructive × size lg|md|sm"
      contains: "variant"
    - path: "app/components/ui/ForgeField.tsx"
      provides: "controlled input with default|focused|error states"
    - path: "app/components/ui/ForgeStat.tsx"
      provides: "label + tnum value + delta pill"
      contains: "tnum"
    - path: "app/components/ui/SettingsRow.tsx"
      provides: "SettingsRow + SettingsSection (toggle/chevron/control variants)"
    - path: "app/components/ui/TabBar.tsx"
      provides: "presentational tab shell (gallery-only, not wired to expo-router)"
  key_links:
    - from: "app/components/ui/ForgeField.tsx"
      to: "app/components/ui/Icon.tsx"
      via: "leading Icon slot"
      pattern: "Icon"
    - from: "app/components/ui/ForgeStat.tsx"
      to: "app/lib/utils/format.ts"
      via: "tnum style on the numeral"
      pattern: "tnum"
---

<objective>
Build the seven Forge component-library primitives (ForgeButton, ForgeField,
ForgeCard, ForgeStat, ForgeChip, SettingsRow + SettingsSection, TabBar shell) as
token-driven, explicit-prop RN components consuming the Plan 02 tokens and the
Plan 03 Icon. These are what every Phase 9-12 re-skin imports.

Purpose: DSGN-04 (core component library). Explicit variant/size props per D-02,
enums derived from forge-screens.jsx usage. TabBar is build-only (OQ-5) — rendered
in the gallery, NOT wired into the live <Tabs> (no navigation behavior change).
Every pressable obeys the FIT-66 rule (no active:/shadow- NativeWind classes).

Output: app/components/ui/{ForgeButton,ForgeField,ForgeCard,ForgeStat,ForgeChip,SettingsRow,TabBar}.tsx.
</objective>

<execution_context>
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-forge-foundation/08-RESEARCH.md
@.planning/phases/08-forge-foundation/08-PATTERNS.md
@.planning/phases/08-forge-foundation/08-UI-SPEC.md

<interfaces>
<!-- The canonical token-driven Pressable + FIT-66 reference in this repo -->
From app/components/segmented-control.tsx:
  - conditional-className idiom (lines 93-101)
  - selectedShadow explicit iOS shadow object (lines 64-69)
  - pressed feedback via style={({ pressed }) => [...]} (lines 98-101)
  - a11y props (lines 80-91): accessibilityRole/State/Label, hitSlop

<!-- Card-shell + dark: flip + full-row Pressable analog -->
From app/components/active-session-banner.tsx:
  - card container dark-flip (line 58)
  - label-over-value stack (lines 62-72)
  - full-row Pressable + a11y + trailing chevron (lines 54-75)

<!-- Plan 03 Icon API -->
From app/components/ui/Icon.tsx: <Icon name=... size=... color=... strokeWidth=... />

<!-- Plan 01 tnum helper -->
From app/lib/utils/format.ts: export const tnum = { fontVariant: ['tabular-nums'] }

<!-- SPEC source line ranges (web React — re-author in RN) -->
forge-screens.jsx: ForgeField 69 · ForgeChip 205 · ForgeStat 318 · SettingsSection 951 · SettingsRow 967
lib.jsx: TabBar ~580-612 (DO NOT wire to live (tabs)/_layout.tsx)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ForgeButton + ForgeField (interactive primitives, FIT-66)</name>
  <read_first>
    - app/components/segmented-control.tsx (FULL — token-driven Pressable, conditional-className lines 93-101, selectedShadow lines 64-69, pressed-callback lines 98-101, a11y lines 80-91)
    - app/design v2/Sources/design/forge-screens.jsx (ForgeField line 69; ForgeButton variant/size usage across FSignIn/FWorkout/overlays)
    - .planning/phases/08-forge-foundation/08-UI-SPEC.md (§ForgeButton variant→style + size→height; §ForgeField states/props)
    - .planning/phases/08-forge-foundation/08-PATTERNS.md (§ForgeButton FIT-66 critical block; §ForgeField)
  </read_first>
  <action>
    Create `app/components/ui/ForgeButton.tsx`: props `variant: 'primary'|'secondary'|'ghost'|'destructive'`, `size: 'lg'|'md'|'sm'`, `icon?`, `iconPosition?: 'leading'|'trailing'`, `loading?`, `disabled?`, `fullWidth?`, `onPress`. Select classes via conditional-className idiom (segmented-control pattern). variant→style per UI-SPEC: primary = `bg-forge-accent` + accentText + accent shadow (explicit iOS shadow OBJECT, not Tailwind shadow class — FIT-66); secondary = `bg-forge-surface3` + text-forge-text; ghost = transparent + `border-forge-border` + text-forge-text; destructive = transparent + `border-forge-border` + `text-forge-danger`. size→height: lg 56-64 (rounded-forge-lg), md 50-52 (rounded-forge-md), sm 36 (pill). Pressed feedback via `Pressable style={({pressed}) => [...]}` (NEVER `active:opacity-*`). Label weight 600, letterSpacing -0.2, System family. a11y: accessibilityRole="button" + accessibilityState disabled/busy + accessibilityLabel.
    Create `app/components/ui/ForgeField.tsx`: controlled `TextInput` with props `icon?`, `placeholder`, `value`, `onChangeText`, `secureTextEntry?`, `keyboardType?`, `state?: 'default'|'focused'|'error'`, `multiline?`. Style per UI-SPEC: height 56 (single) / minHeight 92-110 (multiline), `rounded-forge-md`, `bg-forge-surface`, `border-forge-border`; focused = `border-2 border-forge-accent`; error = danger border; leading `<Icon>` slot tinted text3; value 16px text, placeholder text3. a11y label from placeholder.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `ForgeButton.tsx` exports the 4-variant + 3-size unions; contains a `style={({ pressed })` callback; `grep -c "active:opacity\|shadow-lg\|shadow-md\|shadow-xl\|shadow-sm" app/components/ui/ForgeButton.tsx` returns 0 (FIT-66).
    - `ForgeField.tsx` uses controlled `value`/`onChangeText` and a `state` prop driving border classes.
    - `cd app && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Button (4×3) + Field (3 states) compile; FIT-66 pressed/shadow rule honored; a11y present.</done>
</task>

<task type="auto">
  <name>Task 2: ForgeCard + ForgeStat + ForgeChip (static primitives)</name>
  <read_first>
    - app/components/active-session-banner.tsx (card-shell dark-flip line 58; label-over-value stack lines 62-72)
    - app/components/segmented-control.tsx (chip-pill idiom)
    - app/design v2/Sources/design/forge-screens.jsx (ForgeChip line 205, ForgeStat line 318)
    - app/lib/utils/format.ts (tnum helper from Plan 01)
    - .planning/phases/08-forge-foundation/08-UI-SPEC.md (§ForgeCard, §ForgeStat, §ForgeChip props/enums)
    - .planning/phases/08-forge-foundation/08-PATTERNS.md (§ForgeCard/ForgeStat/ForgeChip)
  </read_first>
  <action>
    Create `app/components/ui/ForgeCard.tsx`: props `padding?: 'sm'|'md'|'lg'` (default lg), `radius?: 'md'|'lg'|'xl'` (default lg), `interactive?` (adds chevron + press feedback via Pressable style-callback, FIT-66), `tint?: 'surface'|'accentSoft'` (default surface), children. Style: `bg-forge-surface` (or `bg-forge-accentSoft` when tint=accentSoft) + `border-forge-border` + `rounded-forge-lg`, with `dark:` siblings on every color class.
    Create `app/components/ui/ForgeStat.tsx`: props `label` (UPPERCASE eyebrow), `value`, `unit?`, `delta?: { value, direction: 'up'|'down' }`, `size?: 'sm'|'md'|'lg'`, `align?: 'left'|'center'|'right'`. Value uses `font-display-bold` + `style={tnum}`; size→value font sm 18 / md 22 / lg 28-30. Label 10-11px UPPERCASE text3 weight 600. delta = success/danger pill.
    Create `app/components/ui/ForgeChip.tsx`: props `icon?` (accent, 12px strokeWidth 2 via Icon), `variant?: 'default'|'accent'|'success'`, children. Style: inline `flex-row items-center`, padding `px-[9px] py-1`, `rounded-lg` (8px), `bg-forge-surface2`, `border-forge-border`, 12px weight-600 text2.
    File-header comments on all three (DSGN-04, UI-SPEC + forge-screens.jsx line refs).
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `ForgeStat.tsx` imports `tnum` from `@/lib/utils/format` and applies it to the value Text with `font-display-bold`.
    - `ForgeCard.tsx` supports `tint` and `interactive`; `ForgeChip.tsx` supports the 3-variant union.
    - Every color class on the three has a `dark:` sibling (light+dark parity).
    - `cd app && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Card/Stat/Chip compile with full prop enums; tnum on Stat value; light+dark parity.</done>
</task>

<task type="auto">
  <name>Task 3: SettingsRow + SettingsSection + TabBar shell (gallery-only)</name>
  <read_first>
    - app/components/active-session-banner.tsx (full-row Pressable + a11y + trailing chevron lines 54-75)
    - app/components/segmented-control.tsx (toggle pill / FIT-66 pressed-style-callback)
    - app/app/(app)/(tabs)/_layout.tsx (LIVE expo-router <Tabs> — READ for structure only; DO NOT modify or wire to it)
    - app/design v2/Sources/design/forge-screens.jsx (SettingsSection line 951, SettingsRow line 967)
    - app/design v2/Sources/design/lib.jsx (TabBar ~580-612)
    - .planning/phases/08-forge-foundation/08-UI-SPEC.md (§SettingsRow, §TabBar shell — scope note: re-skin appearance only)
    - .planning/phases/08-forge-foundation/08-PATTERNS.md (§SettingsRow, §TabBar — OQ-5 build-only, Pitfall 6)
  </read_first>
  <action>
    Create `app/components/ui/SettingsRow.tsx` exporting `SettingsRow` AND `SettingsSection`. SettingsRow props: `icon` (in accent-soft tile), `label`, `value?`, `chevron?`, `toggle?` (+ `toggleValue`, `onToggle`), `control?` (inline slot), `onPress?`, `last?`. Style: `min-height 52`, `px-4 py-[14px]`, bottom `border-forge-border` (suppressed when last), icon tile 28×28 `rounded-lg bg-forge-accentSoft` + 15px accent `<Icon>`, label 15px weight 500. Toggle = 44×26 pill (`bg-forge-accent` on, 22px white knob) implemented as a Pressable with style-callback (FIT-66, no active: class). SettingsSection = UPPERCASE label + rounded `bg-forge-surface` group container. a11y: row accessibilityRole="button", toggle accessibilityRole="switch" + accessibilityState checked.
    Create `app/components/ui/TabBar.tsx` as a STANDALONE presentational component (OQ-5 / Pitfall 6 — do NOT swap the live <Tabs> tabBar prop this phase). Items: plans (icon barbell, label `t('plans')`), history (icon clock, `t('history')`), settings (icon settings, `t('settings')`). Style: floating, `bg-forge-tabBg` + blur, top `border-forge-border`, paddingTop 10 / paddingBottom 28. Active = `text-forge-accent` + icon strokeWidth 2 + label weight 600; inactive = text3 + strokeWidth 1.6 + weight 500; label 10.5px. Accept an `active` prop + `onSelect` so the gallery can demo it; it renders in the gallery only.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `SettingsRow.tsx` exports both `SettingsRow` and `SettingsSection`; toggle uses a Pressable style-callback (grep for `active:opacity` returns 0).
    - `TabBar.tsx` uses `t(` for labels and accepts an `active`/`onSelect` prop; it is NOT imported by `app/app/(app)/(tabs)/_layout.tsx` (grep that file for `ui/TabBar` returns 0 — live tabs untouched).
    - `cd app && npx tsc --noEmit && npm run lint` exit 0.
  </acceptance_criteria>
  <done>SettingsRow/Section + TabBar shell compile; toggle obeys FIT-66; live <Tabs> not wired (OQ-5).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| component props → render | App-controlled props; no external/user input in Phase 8 |
| TabBar shell vs live navigation | Build-only component must NOT alter live routing behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | Tampering (stability) | pressable primitives using NativeWind active:/shadow- | mitigate | FIT-66: pressed feedback via `Pressable` style-callback + explicit iOS shadow object on ForgeButton/interactive ForgeCard/SettingsRow toggle; never `active:opacity-*`/`shadow-*` classes (css-interop recursion crash). |
| T-08-09 | Elevation/behavior drift | TabBar wired to live <Tabs> | mitigate | OQ-5: TabBar is standalone presentational, rendered in gallery only; live `(tabs)/_layout.tsx` not modified (no navigation-behavior change, preserves phase boundary). |
</threat_model>

<verification>
- `cd app && npx tsc --noEmit && npm run lint` → exit 0.
- `grep -rc "ui/TabBar" app/app/(app)/(tabs)/_layout.tsx` → 0 (live tabs untouched).
- Manual (Plan 05 gallery UAT): each primitive in each variant/size, light + dark.
- No `active:opacity`/`shadow-*` classes on any pressable (FIT-66).
</verification>

<success_criteria>
- 7 primitives compile with explicit variant/size prop enums per D-02 (DSGN-04).
- FIT-66 honored across all pressables; light+dark parity on all.
- TabBar build-only; live <Tabs> not wired (OQ-5).
</success_criteria>

<output>
Create `.planning/phases/08-forge-foundation/08-04-SUMMARY.md` when done
</output>
