---
phase: 08-forge-foundation
plan: 05
type: execute
wave: 5
depends_on: ["08-03", "08-04"]
files_modified:
  - app/app/(app)/_forge-gallery.tsx
autonomous: false
requirements: [DSGN-01, DSGN-02, DSGN-03, DSGN-04, DSGN-05, DSGN-06, I18N-01, I18N-04]
user_setup: []

must_haves:
  truths:
    - "The gallery renders every forge.* color swatch in light + dark"
    - "The gallery renders the full type scale (display + mono) and a tabular-numeral alignment proof"
    - "The gallery renders every primitive × variant × size: ForgeButton ×4×3, ForgeField states, ForgeCard, ForgeStat, ForgeChip, SettingsRow, TabBar shell, ProgressRing (static), Sparkline (static), Logo gradient+white, AppIcon"
    - "An sv↔en toggle flips at least one sample string live, and a number/date sample reformats per locale"
    - "The gallery is reachable via router.push in dev, __DEV__-guarded, and never appears in the live tab bar"
  artifacts:
    - path: "app/app/(app)/_forge-gallery.tsx"
      provides: "Dev-only Forge component gallery (the manual UAT surface, D-06/D-07)"
      contains: "__DEV__"
  key_links:
    - from: "app/app/(app)/_forge-gallery.tsx"
      to: "app/components/ui/*"
      via: "imports + renders every primitive"
      pattern: "@/components/ui"
    - from: "app/app/(app)/_forge-gallery.tsx"
      to: "react-i18next"
      via: "useTranslation + i18n.changeLanguage for the sv↔en toggle"
      pattern: "useTranslation|changeLanguage"
---

<objective>
Build the dev-only Forge gallery route — the single new visible surface in this
phase and the manual-UAT test surface (D-06/D-07). It renders every token, the
type scale, every primitive × variant × size, the static Skia ring + sparkline,
the brand components, and an sv↔en toggle, in both light and dark. It proves all
8 phase requirements end-to-end and stays in the repo as a living reference.

Purpose: closes ROADMAP success criteria 1-4 by making them observable. Gated out
of normal navigation (leading-underscore route outside (tabs), __DEV__-guarded,
reachable via router.push in dev) per OQ-6 / Pitfall 6.

Output: app/app/(app)/_forge-gallery.tsx.
</objective>

<execution_context>
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-forge-foundation/08-RESEARCH.md
@.planning/phases/08-forge-foundation/08-PATTERNS.md
@.planning/phases/08-forge-foundation/08-UI-SPEC.md
@.planning/phases/08-forge-foundation/08-VALIDATION.md

<interfaces>
<!-- Screen-container idiom to copy -->
From app/app/(app)/(tabs)/settings.tsx + chart.tsx (lines 48-50):
  ScrollView + SafeAreaView + useColorScheme screen shell

<!-- All primitives available from Plans 03 + 04 -->
@/components/ui/Icon · Logo · AppIcon · ProgressRing · Sparkline
@/components/ui/ForgeButton · ForgeField · ForgeCard · ForgeStat · ForgeChip · SettingsRow (+ SettingsSection) · TabBar

<!-- i18n from Plan 01/02 -->
useTranslation() from react-i18next ; i18n.changeLanguage('sv'|'en')
fmtNum/fmtDate from @/lib/utils/format

<!-- Gallery gating (OQ-6 / Pitfall 6): file at (app)/_forge-gallery (OUTSIDE (tabs)),
     leading underscore keeps expo-router from making it a tab child;
     __DEV__-guard the screen body; reach via router.push('/_forge-gallery') in dev. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the dev-only gallery route</name>
  <read_first>
    - app/app/(app)/(tabs)/settings.tsx (ScrollView screen shell analog)
    - app/app/(app)/exercise/[exerciseId]/chart.tsx lines 48-50 (SafeAreaView + useColorScheme container)
    - .planning/phases/08-forge-foundation/08-UI-SPEC.md (§Verification Surface — the exact section checklist; §Component Inventory for every variant/size)
    - .planning/phases/08-forge-foundation/08-PATTERNS.md (§_forge-gallery.tsx — settings.tsx analog, OQ-6 gating, Pitfall 6)
    - .planning/phases/08-forge-foundation/08-RESEARCH.md (§Pitfall 6 gallery-in-nav; defaults for ProgressRing/Sparkline sample values)
  </read_first>
  <action>
    Create `app/app/(app)/_forge-gallery.tsx` — a `ScrollView` screen (settings.tsx shell + SafeAreaView + useColorScheme). Wrap the screen body in a `__DEV__` guard (render a tiny "dev only" notice + nothing else in non-dev) per OQ-6. Render these sections (UI-SPEC §Verification Surface), each labeled:
    1. Color swatches — every forge.* token (bg/surface/surface2/surface3/text/text2/text3/accent/accentSoft/success/danger/border + gradient), each a labeled chip; the screen must read correctly in BOTH themes (rely on dark: classes — the section proves DSGN-01).
    2. Type scale — display samples in `font-display`/`font-display-semibold`/`font-display-bold` + mono samples in `font-mono`; a column of stacked numbers using `style={tnum}` to prove tabular alignment (DSGN-03).
    3. Component matrix — ForgeButton all 4 variants × 3 sizes; ForgeField default/focused/error (+ multiline); ForgeCard (surface + accentSoft, interactive); ForgeStat sm/md/lg with a delta pill; ForgeChip 3 variants; SettingsSection wrapping SettingsRow rows (chevron, value, toggle).
    4. Brand + Skia — Logo gradient + white; AppIcon; ProgressRing at value 0.25/0.7/1.0 (one with gradient + center children); Sparkline with a sample number[] (DSGN-05/06).
    5. i18n — `useTranslation()`; render a few sample `t()` strings; a sv↔en toggle (two ForgeButtons or a segmented control) calling `i18n.changeLanguage`; a number sample via `fmtNum` and a date via `fmtDate` that reformat per active locale (I18N-01/04).
    Add a `__DEV__`-guarded entry point so the route is reachable in dev (e.g. a dev link/button on an existing dev-accessible screen OR document the `router.push('/_forge-gallery')` invocation in the SUMMARY — do NOT add it to the tab bar). The gallery's own section/swatch labels are dev-only scaffolding (exempt from the i18n copy contract per UI-SPEC §Copywriting).
    Confirm the route does NOT register as a tab: it lives at `(app)/_forge-gallery` (outside (tabs)); verify it never appears in the live tab bar.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `app/app/(app)/_forge-gallery.tsx` imports from `@/components/ui` (all primitives) and `react-i18next`; contains `__DEV__` and `changeLanguage`.
    - It renders ProgressRing at multiple values and Sparkline with sample data; renders ForgeButton across variants/sizes.
    - File is under `(app)/` and NOT under `(tabs)/`; `grep -rc "_forge-gallery" app/app/(app)/(tabs)/` returns 0.
    - `cd app && npx tsc --noEmit && npm run lint` exit 0.
  </acceptance_criteria>
  <done>Gallery renders all sections, sv↔en toggle works, __DEV__-guarded, not in the live tab bar.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Manual device UAT of the gallery (D-07 phase gate)</name>
  <read_first>
    - .planning/phases/08-forge-foundation/08-VALIDATION.md (§Manual-Only Verifications — the per-requirement gallery observations)
    - .planning/phases/08-forge-foundation/08-UI-SPEC.md (§Verification Surface)
  </read_first>
  <what-built>The dev-only gallery rendering every token, font, primitive, Skia ring/sparkline, brand component, and an sv↔en toggle — the D-07 test surface.</what-built>
  <how-to-verify>
    In Expo Go on a physical iPhone, open the gallery via `router.push('/_forge-gallery')` (dev). Confirm, in BOTH light and dark (flip the device/theme toggle):
    1. DSGN-01: every color swatch renders and switches with the theme.
    2. DSGN-02: display headings + mono cells use the bundled faces (not system); if any weight fell back to standard Inter (D-05), note it.
    3. DSGN-03: the stacked-number column aligns digit-for-digit (tabular).
    4. DSGN-04: every ForgeButton variant×size, ForgeField state, ForgeCard, ForgeStat, ForgeChip, SettingsRow, TabBar shell render correctly in both themes.
    5. DSGN-05: ProgressRing at the sample values + Sparkline render via Skia (static — no animation expected).
    6. DSGN-06: Logo (gradient + white) + AppIcon render.
    7. I18N-01/04: the sv↔en toggle flips the sample string live; numbers show `1 234,5` (sv) vs `1,234.5` (en); the date reformats per locale.
    Also confirm there is NO "forge-gallery" entry in the live tab bar.
  </how-to-verify>
  <action>
    No code. Record the UAT result (and any D-05 font fallbacks) in 08-05-SUMMARY.md. If a section fails, file a Linear UI issue (type=ui, priority=high) and loop back to the owning plan.
  </action>
  <resume-signal>Type "approved" if every section passes in both themes + both locales, or describe the failing section.</resume-signal>
  <acceptance_criteria>
    - All 7 requirement observations pass in light + dark on a physical iPhone.
    - sv↔en toggle flips a string + reformats number/date.
    - No gallery entry in the live tab bar.
  </acceptance_criteria>
  <done>Manual gallery UAT signed off; results + any font fallbacks recorded in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev route → release build | Gallery must not be reachable / must show no real data in a release/TestFlight build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10 | Information disclosure (minor) | dev-only gallery reachable in release | mitigate | Route lives outside (tabs) with leading-underscore (not in nav) AND the screen body is `__DEV__`-guarded; it renders only SAMPLE data behind the existing (app) session guard. Defense-in-depth, low severity. |
| T-08-11 | Tampering | i18n sv↔en toggle in gallery | accept | Toggle calls `i18n.changeLanguage('sv'|'en')` with a hardcoded literal union — no free-text into the engine; `escapeValue:false` safe in RN <Text>. |
</threat_model>

<verification>
- `cd app && npx tsc --noEmit && npm run lint` → exit 0.
- `cd app && npm run test:f13-brutal` → green (final phase regression gate; additive change must not disturb the offline queue).
- `cd app && npx tsx scripts/check-locale-parity.ts` → PASS (locale completeness still green).
- Manual gallery UAT (Task 2) signed off in both themes + both locales.
- Gallery not in live tab bar.
</verification>

<success_criteria>
- Gallery renders all sections proving DSGN-01..06 + I18N-01/04 (ROADMAP success criteria 1-4).
- sv↔en toggle + locale number/date formatting observable.
- __DEV__-guarded, outside (tabs), not in live nav.
- Manual device UAT signed off; F13 brutal-test green.
</success_criteria>

<output>
Create `.planning/phases/08-forge-foundation/08-05-SUMMARY.md` when done
</output>
