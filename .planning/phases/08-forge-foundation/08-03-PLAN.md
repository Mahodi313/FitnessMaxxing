---
phase: 08-forge-foundation
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - app/components/ui/Icon.tsx
  - app/components/ui/Logo.tsx
  - app/components/ui/AppIcon.tsx
  - app/components/ui/ProgressRing.tsx
  - app/components/ui/Sparkline.tsx
autonomous: true
requirements: [DSGN-05, DSGN-06]
user_setup: []

must_haves:
  truths:
    - "Icon renders any name from the lib.jsx enum as a react-native-svg stroke path, tintable via color/strokeWidth"
    - "Logo renders the Ascend mark in gradient and white variants"
    - "AppIcon renders a gradient squircle wrapping a white Logo as a component only — no real app.json icon/splash asset swap this phase (D-03)"
    - "ProgressRing renders a static Skia arc at value 0..1 with optional sweep gradient and center children, with NO mount/draw animation (D-08 — animation is Phase 12)"
    - "Sparkline renders a static Skia line + gradient fill + last-point dot from number[] data"
  artifacts:
    - path: "app/components/ui/Icon.tsx"
      provides: "Shared react-native-svg icon set with name enum"
      contains: "react-native-svg"
    - path: "app/components/ui/Logo.tsx"
      provides: "Ascend brand mark (gradient | white)"
    - path: "app/components/ui/AppIcon.tsx"
      provides: "Gradient-squircle app-icon component (no app.json swap)"
    - path: "app/components/ui/ProgressRing.tsx"
      provides: "Static Skia activity ring (DSGN-05)"
      contains: "Canvas"
    - path: "app/components/ui/Sparkline.tsx"
      provides: "Static Skia sparkline (DSGN-05)"
      contains: "Canvas"
  key_links:
    - from: "app/components/ui/AppIcon.tsx"
      to: "app/components/ui/Logo.tsx"
      via: "renders white Logo inside the squircle"
      pattern: "Logo"
    - from: "app/components/ui/ProgressRing.tsx"
      to: "@shopify/react-native-skia"
      via: "Canvas + Path arc"
      pattern: "@shopify/react-native-skia"
---

<objective>
Build the vector + Skia primitive layer: the shared Icon set (react-native-svg),
the Ascend Logo + AppIcon brand components, and the two STATIC Skia primitives
ProgressRing and Sparkline. These are the brand/data-viz building blocks the
Forge component library (Plan 04) and the gallery (Plan 05) render.

Purpose: DSGN-05 (ProgressRing + Sparkline via the already-installed Skia, no new
charting dep) and DSGN-06 (Logo + AppIcon as token-driven components). STATIC only
— mount/draw animation is Phase 12 (D-08), do NOT add reanimated shared values.

Output: app/components/ui/{Icon,Logo,AppIcon,ProgressRing,Sparkline}.tsx.
</objective>

<execution_context>
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-forge-foundation/08-RESEARCH.md
@.planning/phases/08-forge-foundation/08-PATTERNS.md
@.planning/phases/08-forge-foundation/08-UI-SPEC.md

<interfaces>
<!-- Skia import idiom already in the repo -->
From app/app/(app)/exercise/[exerciseId]/chart.tsx (lines 60-66):
  import { Circle, matchFont, RoundedRect, Text as SkiaText } from "@shopify/react-native-skia";
  // Add for these primitives: Canvas, Path, Skia, SweepGradient, LinearGradient, vec
  // DO NOT import useDerivedValue/useSharedValue (chart.tsx line 66) — STATIC only (D-08)

<!-- SPEC source line ranges (web React — re-author in RN) -->
lib.jsx: Icon = line 381 (path strings transcribe 1:1 to <Path d>)
         ProgressRing = lines 424-452 ; Sparkline = lines 457-488
         Logo = lines 667-688 ; AppIcon = lines 691-702

<!-- forge tokens available from Plan 02: bg-forge-*, text-forge-*, rounded-forge-* -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Icon set (react-native-svg) + Logo + AppIcon</name>
  <read_first>
    - app/design v2/Sources/design/lib.jsx (Icon line 381 + path strings; Logo lines 667-688; AppIcon lines 691-702 — the SVG spec, web React)
    - app/components/active-session-banner.tsx (Ionicons `name`/`color`/`size` prop-shape analog to mirror)
    - .planning/phases/08-forge-foundation/08-UI-SPEC.md (§Icon name enum, §Logo, §AppIcon props/defaults; brand-discipline: barbell=content, Ascend=brand-only)
    - .planning/phases/08-forge-foundation/08-PATTERNS.md (§Icon.tsx, §Logo/AppIcon — D-11 react-native-svg, no analog)
  </read_first>
  <action>
    Create `app/components/ui/Icon.tsx`: a `react-native-svg` (`<Svg><Path d=.../></Svg>`) component with props `name` (enum per UI-SPEC: barbell, ascend, flame, clock, chart, settings, plus, check, checkCircle, chevronRight/Left/Down, ellipsis, close, grip, play, pause, trash, pencil, spark, trophy, user, bell, globe, scale, arrowUp, arrowRight, wifi, wifiOff, eye, lock, mail, list), `size` (default 20), `color` (default a forge text token, NOT literal 'currentColor'), `strokeWidth` (default 1.8), `fill?` (default 'none'). 24×24 viewBox, `strokeLinecap`/`strokeLinejoin="round"`. Transcribe each `<path d="...">` string 1:1 from lib.jsx Icon.
    Create `app/components/ui/Logo.tsx`: Ascend mark (two rising rounded bars + peak dot, strokeWidth 7) via react-native-svg `<Path>`/`<Circle>`. Props `size` (default 64), `variant: 'gradient' | 'white'`, `from?`/`to?` (default #FF7A2E → #FF2D55). `gradient` = `<LinearGradient>` stroke; `white` = `#fff` stroke.
    Create `app/components/ui/AppIcon.tsx`: gradient-squircle `<View>` (NativeWind bg gradient + `borderRadius = size*0.28`) containing a white `<Logo size={size*0.62} variant="white" />`, soft brand-colored shadow via explicit iOS shadow style object (NOT a Tailwind shadow class — FIT-66). Props `size` (default 56), `radius?`, `from?`/`to?`. Component-only (D-03 — no app.json swap).
    Open every file with the file-header rationale comment (cite DSGN-06, UI-SPEC, lib.jsx line refs).
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `Icon.tsx` imports from `react-native-svg`, exports a `name` union including `barbell` and `ascend`, defaults strokeWidth 1.8.
    - `Logo.tsx` exports `variant: 'gradient' | 'white'`; `AppIcon.tsx` renders `Logo` and uses an explicit shadow style object (no `shadow-` className).
    - `cd app && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Icon enum renders 1:1 path strings; Logo gradient+white; AppIcon squircle wraps white Logo; FIT-66 shadow rule honored.</done>
</task>

<task type="auto">
  <name>Task 2: Static Skia ProgressRing + Sparkline</name>
  <read_first>
    - app/app/(app)/exercise/[exerciseId]/chart.tsx lines 60-66 (the only Skia consumer in the repo — import idiom + Canvas mental model; do NOT copy its reanimated imports)
    - app/design v2/Sources/design/lib.jsx (ProgressRing lines 424-452, Sparkline lines 457-488 — the SVG spec to port to Skia Canvas)
    - .planning/phases/08-forge-foundation/08-RESEARCH.md (§Pattern 4 ProgressRing, §Pattern 5 Sparkline, §Pitfall 5 Canvas sizing/overflow, defaults)
    - .planning/phases/08-forge-foundation/08-PATTERNS.md (§ProgressRing/Sparkline — STATIC, D-08)
    - .planning/phases/08-forge-foundation/08-UI-SPEC.md (§ProgressRing, §Sparkline props/defaults)
  </read_first>
  <action>
    Create `app/components/ui/ProgressRing.tsx` per RESEARCH §Pattern 4: a `<Canvas style={{width:size,height:size}}>` with a background circle `Path` (full circle, stroke) and a foreground arc `Path` built via `addArc(rect, -90, 360*clamp(value,0,1))` rendered `style="stroke" strokeCap="round"`. Optional sweep gradient via `<SweepGradient c={vec(cx,cy)} colors={gradient}>` child of the fg Path. Center `children` overlay via an absolutely-positioned `<View>` sibling of the Canvas. Props: `size` (default 80), `stroke` (default 8), `value: 0..1`, `color` (default accent #FF5A1F), `trackColor` (default rgba(255,255,255,0.08) — accept a light override), `gradient?: [from,to]`, `children`. STATIC — NO useSharedValue/withTiming (D-08).
    Create `app/components/ui/Sparkline.tsx` per RESEARCH §Pattern 5: normalize `data` min/max with `pad = strokeWidth + 2`; build a line `Path` via `moveTo`/`lineTo`; a closed fill `Path` to the baseline with `<LinearGradient start=vec(0,0) end=vec(0,height)>` color@0.35→transparent; last-point dot `<Circle r={strokeWidth+1.5}>` + halo `<Circle r={strokeWidth+4.5} opacity={0.18}>`. Props: `data: number[]`, `width` (default 200), `height` (default 48), `color` (default accent), `fill?` (default true), `showDot?` (default true), `strokeWidth?` (default 2). Size the Canvas explicitly and pad for the halo so it doesn't clip (Pitfall 5). STATIC — no draw-on-mount animation.
    File-header rationale comments citing DSGN-05, D-08, lib.jsx line refs.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Both files import `Canvas` + `Path` from `@shopify/react-native-skia`; NEITHER imports `useSharedValue`/`useDerivedValue`/`withTiming` (grep returns 0).
    - `ProgressRing.tsx` clamps value 0..1 and supports a `gradient` prop; `Sparkline.tsx` accepts `data: number[]`.
    - `cd app && npx tsc --noEmit && npm run lint` exit 0.
  </acceptance_criteria>
  <done>Static Skia ring + sparkline compile; no animation primitives; uses already-installed Skia (no new charting dep).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| component props → render | All inputs are app-controlled (sample data / numeric value); no external/user input in Phase 8 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Denial of Service (render) | ProgressRing value / Sparkline data | mitigate | Clamp `value` to 0..1; guard empty/single-element `data` (no NaN min/max division). Prevents a malformed prop from crashing the Canvas. |
| T-08-07 | Information disclosure | rendered sample data | accept | Gallery/components render only SAMPLE data; no real user/PII data flows through these primitives in Phase 8. |
</threat_model>

<verification>
- `cd app && npx tsc --noEmit && npm run lint` → exit 0.
- Manual (deferred to Plan 05 gallery UAT): ring at several values, sparkline with sample data, Logo gradient+white, AppIcon — all render in light + dark.
- No reanimated animation imports in the two Skia primitives (STATIC, D-08).
</verification>

<success_criteria>
- Icon name enum renders 1:1 react-native-svg paths; Logo + AppIcon brand components (DSGN-06).
- ProgressRing + Sparkline render statically via installed Skia, no new charting dep (DSGN-05).
- FIT-66 shadow rule honored on AppIcon; no animation primitives present.
</success_criteria>

<output>
Create `.planning/phases/08-forge-foundation/08-03-SUMMARY.md` when done
</output>
