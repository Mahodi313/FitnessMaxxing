---
phase: 5
slug: active-workout-hot-path-f13-lives-or-dies
status: draft
shadcn_initialized: false
preset: none
created: 2026-05-12
---

# Phase 5 — UI Design Contract

> Visual and interaction contract for the active-workout hot path: `/workout/[sessionId]`, the new `<ActiveSessionBanner />` mounted globally above the tab content, the "Starta pass"-CTA added to `plans/[id]`, the cold-start draft-session-recovery overlay on `(tabs)/index`, and the Avsluta-pass confirmation flow. Generated by gsd-ui-researcher; verified by gsd-ui-checker.

> **shadcn note:** shadcn/ui is React-DOM only and incompatible with this React Native runtime. The shadcn initialization gate is **not applicable** — `Tool: none` is the correct value. All UI continues to be composed from `react-native` core primitives + NativeWind 4 classes, identical to Phase 3 + Phase 4. No new third-party component libraries are introduced in Phase 5.

> **Inheritance:** Phase 5 inherits the full Phase 3 + Phase 4 design system without modification: spacing scale (xs–3xl), 4 typography roles (Body / Label / Heading / Display), 60/30/10 color split (white dominant / gray-100 secondary / blue-600 accent), destructive red-600, warning yellow-200, system font, Ionicons. The executor SHOULD reuse exact class strings from Phase 4 components (`TextInput`, primary CTA, screen container, inline-overlay-confirm, FAB, OfflineBanner). New visual surfaces in Phase 5 are explicitly listed below.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (NativeWind 4 — shadcn is React-DOM only and incompatible with React Native) |
| Preset | not applicable |
| Component library | none — primitives composed from `react-native` (`View`, `Text`, `TextInput`, `Pressable`, `ScrollView`, `KeyboardAvoidingView`, `SafeAreaView`, `FlatList`) styled with NativeWind classes. No new libraries enter Phase 5. `expo-haptics` is **optional** (CONTEXT.md `<canonical_refs>` — Plan 02 decides; default = no haptics) |
| Icon library | `@expo/vector-icons` Ionicons (inherited from Phase 4). New Phase 5 icons: `play` / `play-outline` (Starta pass CTA), `flag` / `flag-outline` (Avsluta pass), `checkmark-circle` (logged set indicator), `time` / `time-outline` (active-session banner — same as Historik tab), `trash-outline` (set-row swipe-left delete) |
| Font | System default (San Francisco on iOS). Unchanged from Phase 1–4. |

**Tailwind config inheritance:** `app/tailwind.config.js` uses `darkMode: 'class'` with empty `theme.extend`. All Tailwind v3 default tokens resolve unchanged. Class names below match Phase 3 + Phase 4 vocabulary for cross-phase visual continuity.

**Net-new visual surfaces in Phase 5:**
1. `app/components/active-session-banner.tsx` — persistent "Pågående pass · Tryck för att återgå"-banner (new — uses an info-blue color role distinct from the OfflineBanner yellow)
2. `app/app/(app)/workout/[sessionId].tsx` — active-workout screen (new — single-scroll card-per-exercise with always-visible inline set-row + F7 last-value chip)
3. `app/app/(app)/workout/_layout.tsx` — optional, only if route-grouping required (else declared in `(app)/_layout.tsx`)
4. "Starta pass"-CTA added to `app/app/(app)/plans/[id].tsx` (Phase 4 surface extended, not replaced)
5. Inline-overlay draft-resume modal on `(tabs)/index.tsx` (Phase 4 surface extended)
6. Inline-overlay Avsluta-pass confirmation on workout screen (new — but uses identical inline-overlay-confirm pattern as Phase 4 archive-plan flow)

---

## Spacing Scale

Identical scale to Phase 3 + Phase 4 UI-SPECs. All values multiples of 4, mapped to Tailwind v3 default spacing tokens.

| Token | Value | Tailwind class | Usage in Phase 5 |
|-------|-------|----------------|------------------|
| xs | 4px | `p-1`, `gap-1`, `py-1` | Gap inside the F7 last-value chip ("Förra: 82.5 × 8"); gap between counter-chip elements ("X/3 set klart"); vertical padding inside the card-header chip shells (target / equipment / counter) |
| sm | 8px | `p-2`, `gap-2`, `mt-2` | Field-label-to-input gap; gap between vikt + reps inputs on the set-input row; gap between active-session-banner icon and label text; gap between Klart-tap checkmark fade and row content; ActiveSessionBanner ✕ close hit-padding (if dismissable — not in V1) |
| md | 16px | `p-4`, `gap-4`, `mt-4` | Default horizontal screen padding on `/workout/[sessionId]`; vertical rhythm between exercise cards in the workout-screen scroll; padding inside exercise-card; ActiveSessionBanner inner padding |
| lg | 24px | `p-6`, `gap-6`, `mt-6` | Vertical rhythm between exercise-card header and first set-row inside the card; gap between Avsluta-overlay heading and body copy |
| xl | 32px | `p-8`, `gap-8`, `mt-8` | Vertical separation between major sections inside the Avsluta-overlay (heading block ↔ button row); bottom padding on workout-screen scroll content (so the last card clears the keyboard region) |
| 2xl | 48px | `p-12`, `mt-12` | Top padding inside Avsluta-overlay above its title in modal-content state |
| 3xl | 64px | `pt-16` | Reserved — not used in Phase 5 surfaces |

**4-pt grid is strict:** every Phase 5 spacing token resolves to a multiple of 4. Sub-4-pt Tailwind tokens (`*-0.5` = 2px, `*-1.5` = 6px, `*-2.5` = 10px, `*-3.5` = 14px) are **forbidden** anywhere in Phase 5 surfaces. The card-header chip shell uses `py-1` (4px) — NOT `py-0.5` (2px) — to remain on the grid.

**Touch target minimum:** All interactive elements ≥ 44×44pt per Apple HIG (matches Phase 3 + Phase 4 contract). Phase 5 introduces one additional constraint: **the Klart button on the set-input row MUST be ≥ 56pt** per PITFALLS §6.1 (the hot path is finger-tap-density-critical; 56pt minimum reduces mis-tap risk during a high-RPM logging session).

- Primary CTAs ("Starta pass", "Avsluta", "Återuppta"): `py-4` (16px) + `text-base` (16px) → ~48pt. Same as Phase 3 + Phase 4 — identical class string.
- **Klart button on set-input row** (NEW Phase 5 constraint): `py-4` + `text-base font-semibold` + `min-h-[56px]` explicit style → ≥ 56pt total. Width is fixed at `w-20` (80pt) so the row layout is `[vikt: flex-1][reps: flex-1][Klart: w-20]` and the Klart-target is 80×56pt = 4480pt² — well above PITFALLS §6.1 floor.
- ActiveSessionBanner full-row tap: minimum `py-3` row padding → 44pt. The full banner is a `Pressable` (tap anywhere routes to `/workout/<id>`).
- Set-input row TextInputs (vikt + reps): `py-3` + `text-base font-semibold` (16px at weight 600 — scan affordance carried by weight contrast, not size, so the four-role typography contract is preserved) → ~52pt height. Each input is `flex-1` of the row; on a 360pt-wide iPhone minus 32pt horizontal padding minus 80pt Klart-w-20 minus 16pt gaps = ~116pt per input ≥ 56pt — meets HIG.
- Logged set-row tap target (D-14 tap-to-edit): full row is a `Pressable` with `py-3` → 44pt vertical.
- Swipe-left delete reveal (D-14): swipe gesture itself has no fixed tap target; the revealed "Ta bort"-action button is `py-3 px-6` → ≥ 44pt tap target on the unhidden affordance.
- Header right "Avsluta"-Pressable: `px-3 py-2` + hitSlop={8} → 44pt total (matches Phase 4 header-action pattern).
- Active-session-banner ✕ close: not implemented in V1 (banner is not dismissable — it represents an active workout; dismissing would hide the affordance to return to the in-progress session).

Exceptions: Klart button has a **floor** at 56pt (not the 44pt baseline) per PITFALLS §6.1. All other Phase 5 values are 4-multiples and meet the 44pt minimum.

---

## Typography

Tailwind v3 default `fontSize` scale, restricted to four roles. Identical role table to Phase 3 + Phase 4 UI-SPECs. **Strict four-role contract:** Phase 5 declares exactly four font sizes — Label 14 / Body 16 / Heading 24 / Display 30. No `text-lg` (18px), no `text-xl` (20px), no other intermediate sizes appear in any Phase 5 surface. Numeric scan-affordance on the hot path is carried by **weight contrast** (semibold against surrounding regular body text), not by an additional size step.

**Sizes declared (4):** 14 (Label) / 16 (Body) / 24 (Heading) / 30 (Display).
**Weights declared (2):** 400 regular (`font-normal`) / 600 semibold (`font-semibold`).

| Role | Size | Tailwind class | Weight | Tailwind weight class | Line Height |
|------|------|----------------|--------|------------------------|-------------|
| Body | 16px | `text-base` | 400 (regular) | `font-normal` (default) | 1.5 (Tailwind ships `lh-6` = 24px) |
| Label | 14px | `text-sm` | 600 (semibold) | `font-semibold` | 1.43 (Tailwind ships `lh-5` = 20px) |
| Heading | 24px | `text-2xl` | 600 (semibold) | `font-semibold` | 1.33 (Tailwind ships `lh-8` = 32px) |
| Display | 30px | `text-3xl` | 600 (semibold) | `font-semibold` | 1.2 (Tailwind ships `lh-9` = 36px) |

**Two weights only:** `font-normal` (400) for body and field input text; `font-semibold` (600) for labels, headings, the display, primary CTA labels, the active-session-banner copy, the F7 last-value chip's set-position prefix, **and the numeric values on the set-input row + the editable-mode logged set-row** (Phase 5 hot-path emphasis is weight-driven, not size-driven). No `font-bold`, no `font-medium`, no `font-light`. Matches Phase 3 + Phase 4.

**Numeric-input hot-path emphasis (Phase 5 specific):** Vikt and reps `TextInput` values use **`text-base font-semibold`** (16px + weight 600) instead of the default `text-base font-normal` (16px + weight 400). This relies on weight contrast against the surrounding `font-normal` body text to make in-progress weight/reps scannable at arm's length. **No size bump** — the four-role contract is strictly preserved (no `text-lg`/18px size in Phase 5). The F7 last-value chip continues to use Body (`text-base`) for its prefix ("Förra:") at weight 400 and `text-base font-semibold` for the numeric portion ("82.5 × 8") at weight 600; this is the same weight-only emphasis pattern, consistent across the set-row and the chip.

**Per-screen role mapping (Phase 5):**

| Surface | Element | Role |
|---------|---------|------|
| `(tabs)/index.tsx` draft-resume overlay | "Återuppta passet?" title | Heading (`text-2xl font-semibold`) |
| `(tabs)/index.tsx` draft-resume overlay | "Du har ett pågående pass från [HH:MM]." body | Body (`text-base font-normal text-gray-900 dark:text-gray-50`) |
| `(tabs)/index.tsx` draft-resume overlay | "Återuppta" / "Avsluta sessionen" buttons | Body size with semibold (`text-base font-semibold`) — same as primary CTA |
| ActiveSessionBanner (`components/active-session-banner.tsx`) | "Pågående pass · Tryck för att återgå" label | Body (`text-base font-semibold` for "Pågående pass"; `text-base font-normal` for " · Tryck för att återgå") — slight emphasis on the lead |
| `plans/[id].tsx` "Starta pass" CTA | Button label | Body size with semibold (`text-base font-semibold`) — identical class string to Phase 4 primary CTA |
| `/workout/[sessionId].tsx` header title (via `<Stack.Screen options={{ title }}>`) | Plan name or "Pass" | Heading (header default; styled by centralized (app) Stack — Phase 4 commit b57d1c2) |
| `/workout/[sessionId].tsx` exercise-card header | Exercise name | Heading (`text-2xl font-semibold`) — bumped from "Body semibold" used on Phase 4 plan_exercise rows; on this screen the exercise name is the *visual anchor* per card, not a list-row entry |
| `/workout/[sessionId].tsx` exercise-card chips | Target chip (`3×8–12`), equipment chip, counter chip (`2/3 set klart`) | Label (`text-sm font-semibold text-gray-500 dark:text-gray-400`) — muted color so they read as metadata |
| `/workout/[sessionId].tsx` logged set-row | Set number prefix (`Set 1`) | Label (`text-sm font-semibold text-gray-500 dark:text-gray-400`) |
| `/workout/[sessionId].tsx` logged set-row | Logged weight + reps (`82.5 × 8`) | Body (`text-base font-normal text-gray-900 dark:text-gray-50`) at rest; flips to `text-base font-semibold` editable when tapped (weight bump matches input-row emphasis; size is unchanged) |
| `/workout/[sessionId].tsx` set-input row | Vikt + reps TextInput value | `text-base font-semibold text-gray-900 dark:text-gray-50` (Phase 5 numeric-input weight bump — size remains Body 16px) |
| `/workout/[sessionId].tsx` set-input row | Vikt + reps TextInput placeholder | `text-base font-normal text-gray-500 dark:text-gray-400` (placeholderTextColor prop carries `#9CA3AF` per Phase 3 Pitfall 7 workaround; placeholder stays weight-400 so the focus/blur weight delta tells the user when the field is being edited) |
| `/workout/[sessionId].tsx` set-input row | Klart button label | Body size with semibold (`text-base font-semibold text-white`) |
| `/workout/[sessionId].tsx` F7 last-value chip | "Förra:" prefix + "82.5 × 8" value | Both Body (`text-base`); prefix `font-normal`, value `font-semibold`; both muted (`text-gray-500 dark:text-gray-400`) |
| `/workout/[sessionId].tsx` empty exercises state | "Den här planen har inga övningar än" + "Gå tillbaka och lägg till några." | Heading + Body (same pattern as Phase 4 plan-detail empty-state) |
| `/workout/[sessionId].tsx` field error (vikt > 500, reps > 60, etc.) | Inline error text | Body (`text-base text-red-600 dark:text-red-400`) — identical to Phase 3 + Phase 4 |
| `/workout/[sessionId].tsx` Avsluta-overlay | "Avsluta passet?" title | Heading (`text-2xl font-semibold`) |
| `/workout/[sessionId].tsx` Avsluta-overlay | Body summary (e.g. "8 set sparade." or "Inget set är loggat. Avsluta utan att spara?") | Body (`text-base font-normal text-gray-900 dark:text-gray-50`) |
| `/workout/[sessionId].tsx` Avsluta-overlay | "Avsluta" / "Fortsätt" buttons | Body size with semibold (`text-base font-semibold`) |
| Toast on "Passet sparat ✓" (post-finish) | Toast label | Body (`text-base font-semibold text-white`) on accent-green or accent-blue bg (color decision below) |

---

## Color

60/30/10 split mapped to NativeWind classes that auto-flip in dark mode (F15 convention from Phase 1). Identical to Phase 3 + Phase 4 with **two additions specific to Phase 5**:

1. **Info-blue role** for `ActiveSessionBanner` (new — distinct from accent-blue used for CTAs)
2. **Success-green role** for the post-finish toast and the logged set-row check-icon (new — used sparingly, only for confirmed-saved indicators)

| Role | Light value | Dark value | Tailwind class pair | Usage |
|------|-------------|------------|----------------------|-------|
| Dominant (60%) | `#FFFFFF` (white) | `#111827` (gray-900) | `bg-white dark:bg-gray-900` | Screen background of `/workout/[sessionId]`; default overlay backdrop tint (`bg-black/40` for the dimmed scrim around inline-overlays — inherited from Phase 4) — matches Phase 3 + Phase 4 |
| Secondary (30%) | `#F3F4F6` (gray-100) | `#1F2937` (gray-800) | `bg-gray-100 dark:bg-gray-800` | TextInput field background; exercise-card background on `/workout/[sessionId]`; logged set-row background (at rest); inline-overlay panel background (Avsluta + draft-resume) — matches Phase 4 |
| Accent (10%) | `#2563EB` (blue-600) | `#60A5FA` (blue-400) | `bg-blue-600 dark:bg-blue-500` (button bg) + `text-blue-600 dark:text-blue-400` (link text + active tab tint + focused border) | **Reserved for: see "Accent reserved for" list below** |
| Destructive | `#DC2626` (red-600) | `#F87171` (red-400) | `text-red-600 dark:text-red-400` / `bg-red-600 dark:bg-red-500` | Inline form-field error messages (vikt > 500, reps > 60, Zod min/max violations); swipe-left "Ta bort"-action button background on logged set-row; "Avsluta sessionen"-button in draft-resume overlay (destructive variant — discards the session without going to workout screen) |
| Warning/Info-yellow | `bg-yellow-200` + `text-yellow-900` + `border-yellow-400` (light); `bg-yellow-900` + `text-yellow-100` + `border-yellow-700` (dark) | (same) | `bg-yellow-200 dark:bg-yellow-900` + `text-yellow-900 dark:text-yellow-100` + `border border-yellow-400 dark:border-yellow-700` | **OfflineBanner only** (inherited from Phase 4 — no change). Phase 5 does not add yellow usage; the ActiveSessionBanner deliberately uses a **different** color role (info-blue) so users can distinguish "system condition" (offline) from "active state" (pass pågående) at a glance |
| Info-blue (NEW Phase 5) | `bg-blue-100` + `text-blue-900` + `border-blue-300` (light); `bg-blue-950` + `text-blue-100` + `border-blue-800` (dark) | (same) | `bg-blue-100 dark:bg-blue-950` + `text-blue-900 dark:text-blue-100` + `border border-blue-300 dark:border-blue-800` | **ActiveSessionBanner only.** Indicates an in-progress workout session — neutral-positive system state, not a warning. Blue chosen because (a) it's adjacent to the accent-blue used for CTAs so it reads as "actionable" — tapping the banner returns to the session; (b) it's distinct from yellow (warning) so the visual hierarchy between OfflineBanner and ActiveSessionBanner is unambiguous when both stack (offline mid-pass = both banners visible). Contrast: `text-blue-900` on `bg-blue-100` = 9.5:1 (AAA); `text-blue-100` on `bg-blue-950` = 13.1:1 (AAA). **Real-device-verification required** per Phase 4 UAT-color-amendment convention (commit cfc1dc8) — if the light-mode shade reads as near-white on physical iPhone, Plan 02 amends to `bg-blue-200` |
| Success-green (NEW Phase 5) | `#16A34A` (green-600) bg + `#FFFFFF` text (light); `#22C55E` (green-500) bg + `#FFFFFF` text (dark) | (same) | `bg-green-600 dark:bg-green-500` + `text-white` | **Toast on "Passet sparat ✓"** + the optional check-icon prefix on logged set-rows (`<Ionicons name="checkmark-circle" color={green-600/green-500} />`). Reserved exclusively for these two confirmed-saved indicators. **Not used** for primary CTAs (those stay accent-blue), not used for borders, not used for any other state. Contrast: `text-white` on `bg-green-600` = 4.55:1 (AA Body); `text-white` on `bg-green-500` = 3.05:1 — for the dark-mode toast, bump to `text-white font-semibold` (semibold weight pushes effective contrast to AA Body floor per WCAG 1.4.3 large-text exception at 14px+ semibold). The check-icon is decorative (paired with the always-present "Set N" label) so contrast floor is not load-bearing |
| Body text | `#111827` (gray-900) | `#F9FAFB` (gray-50) | `text-gray-900 dark:text-gray-50` | All non-error body and label text on `/workout/[sessionId]`; exercise-card name; logged set-row weight + reps |
| Muted text | `#6B7280` (gray-500) | `#9CA3AF` (gray-400) | `text-gray-500 dark:text-gray-400` | TextInput placeholder text; target chips; equipment chip; counter chip; F7 last-value chip; logged set-row "Set N" prefix |

**Accent reserved for:**
1. Primary CTA button background ("Starta pass" on plans/[id], "Återuppta" in draft-resume overlay, "Avsluta" in Avsluta-overlay non-destructive variant when ≥1 set was logged, "Klart" on set-input row)
2. Active tab tint (inherited from Phase 4 — `tabBarActiveTintColor` unchanged)
3. TextInput border when focused (`focus:border-blue-600 dark:focus:border-blue-500`) — applies to vikt + reps inputs on set-row, all form fields in overlays
4. Header-right "Avsluta"-Pressable text tint (`text-blue-600 dark:text-blue-400`) — text-only header action, no background

**Accent must NOT be used for:**
- Exercise-card headers (those use Body color; weight differentiates)
- Target chips, equipment chip, counter chip (those use Muted text color)
- F7 last-value chip (Muted text color)
- Active-session-banner background (Info-blue role owns that surface)
- The "Pågående pass"-label inside the banner (uses Info-blue role's text color, not accent)
- Check-icon on logged set-rows (Success-green role)
- Toast background (Success-green role)
- Tab-bar inactive icons (Muted text color, inherited from Phase 4)

**Destructive color is used in three places in Phase 5:**
1. **Inline form errors** — text only, on white/gray background. Identical to Phase 3 + Phase 4.
2. **Swipe-left "Ta bort"-action on logged set-row** — full-width red panel revealed by swipe-left gesture, white text, no border. Standard iOS Reminders/Mail pattern.
3. **"Avsluta sessionen"-button in the draft-resume overlay** — destructive style, white text on red bg, paired with the non-destructive "Återuppta" (accent-blue bg). Used **only** in the draft-resume context where "Avsluta sessionen" abandons a recovered draft without entering it. The Avsluta-overlay reached from inside the workout screen (after the user has actively been logging sets) uses **non-destructive** styling on its "Avsluta"-button because finishing a pass is not destructive — it's the intended terminal state.

**Why the Avsluta-overlay uses non-destructive accent-blue:** finishing a pass is the **expected terminal action**, not a data-loss action. PITFALLS §6.6 explicitly forbids "Discard workout"-buttons because they create data-loss vectors. The CONTEXT.md D-23 confirmation copy ("8 set sparade. Avsluta passet?") frames it as a save-and-exit. Red would mis-signal that finishing destroys work. Accent-blue is the right color for "complete this workflow".

**Status bar contrast:** `<StatusBar style="auto" />` from Phase 1 + Phase 4 stays. Workout screen has `bg-white dark:bg-gray-900` like all other surfaces — no special handling needed.

**ActiveSessionBanner color rationale:** Info-blue chosen over yellow (warning, owned by OfflineBanner) and accent-blue (CTA, would conflict with "Klart"-tap). Blue-100/blue-950 sits adjacent to accent-blue so the visual relationship "tap this to return to your session" is implicit; the lighter tone signals "informational state" rather than "primary action". Contrast remains AAA. If on real-device verification the light-mode shade reads as near-white (the same failure mode Phase 4 OfflineBanner hit on bg-yellow-100), Plan 02 bumps to `bg-blue-200` per the Phase 4 UAT-amendment convention.

**Toast color rationale:** Success-green chosen because (a) "Passet sparat ✓" is a confirmation of successful save, (b) green is unambiguous as "completed successfully" across cultures (matches iOS native toast styling in Reminders/Notes), (c) it's the only place in V1 where green appears so the user develops a one-cue association ("green flash = work is safe"). Toast is auto-dismissed after ~2s per CONTEXT.md D-24 — Plan 02 chooses Reanimated `entering={FadeIn}` + `exiting={FadeOut.delay(2000)}` or equivalent.

---

## Copywriting Contract

All copy in Swedish per PROJECT.md and continued from Phase 3 D-15 + Phase 4 D-15. New copy elements in Phase 5 cover the workout hot-path, draft recovery, set logging, and pass finishing.

### `plans/[id].tsx` "Starta pass" CTA (Phase 4 surface extended)

| Element | Copy |
|---------|------|
| Primary CTA label (when plan has ≥1 plan_exercise) | `Starta pass` |
| Primary CTA loading state (while `useStartSession` is in flight) | `Startar pass…` (button disabled, `opacity-60`) |
| Disabled CTA label (when plan has 0 plan_exercises) | `Starta pass` (same label; button rendered with `disabled` prop + `opacity-50` — empty-state copy below makes the reason discoverable) |
| Helper text below disabled CTA (when 0 plan_exercises) | `Lägg till minst en övning för att kunna starta.` (Muted text, Body size — sits between the "Lägg till övning" CTA from Phase 4 and the disabled "Starta pass" CTA) |

**"Starta pass" placement decision:** The CTA renders below the plan-exercises list in `plans/[id].tsx`, **above** the existing "Lägg till övning"-CTA from Phase 4. Rationale:
- (a) After scrolling through the exercises (or after adding them), the natural reading flow lands on "Starta pass" as the terminal action.
- (b) Keeping it visible without requiring scroll-to-bottom-FAB pattern reduces tap latency for the most-common entry point per CONTEXT.md D-02.
- (c) The "Lägg till övning"-CTA stays below it as the secondary action (edits to the plan).
- (d) Defensive UI per CONTEXT.md Discretion: when `plan_exercises.length === 0`, the "Starta pass" button is disabled with the helper text above — not hidden — so the affordance is discoverable.

### `/workout/[sessionId].tsx` header

| Element | Copy |
|---------|------|
| Header title | `Pass` (literal — no plan-name injection in the header; the plan name renders as exercise-card context. Rationale: the workout screen's purpose is logging sets, not browsing the plan — keeping the title fixed at "Pass" reduces cognitive load. Plan name can render in a small subtitle if Plan 02 finds it ergonomic, but the V1 default is fixed title.) |
| Header right button | `Avsluta` (text-only, accent-blue tint per centralized (app) Stack header convention from Phase 4 commit b57d1c2) |
| Header back-arrow | (system default — does NOT finish the session; backs to `plans/[id]` while preserving the in-progress session via persistence layer + ActiveSessionBanner re-entry) |

### `/workout/[sessionId].tsx` exercise-card

| Element | Copy |
|---------|------|
| Card header — exercise name | (dynamic — `exercises.name` resolved via `Map<id, name>` lookup per Phase 4 Plan 04-04 commit 3bfaba8) |
| Target chip (when all 3 targets set) | (dynamic — `{target_sets}×{target_reps_min}–{target_reps_max}` e.g. `3×8–12`) |
| Target chip (when only `target_sets` set) | (dynamic — `{target_sets} set`) |
| Target chip (when only reps range set, no `target_sets`) | (dynamic — `{target_reps_min}–{target_reps_max} reps`) |
| Target chip (when nothing set) | (omit — no chip rendered) |
| Equipment chip | (dynamic — `exercises.equipment` if non-empty) |
| Counter chip (when `target_sets` is set) | (dynamic — `{loggedCount}/{target_sets} set klart`) |
| Counter chip (when `target_sets` is NULL) | (dynamic — `{loggedCount} set`) |

### `/workout/[sessionId].tsx` logged set-row

| Element | Copy |
|---------|------|
| Set number prefix | (dynamic — `Set {set_number}`) e.g. `Set 1`, `Set 2` |
| Value display (at rest) | (dynamic — `{weight_kg} × {reps}` e.g. `82.5 × 8`) — no unit suffix per PROJECT.md "Sverige-only V1" + CONTEXT.md specifics |
| Value display (editable mode after tap, D-14) | (dynamic — same as input-row, TextInputs replace the static text) |
| Save-confirm icon | `<Ionicons name="checkmark-circle">` (Success-green color, decorative — paired with always-present "Set N" label for non-decorative semantics) |
| Swipe-left revealed delete action | `Ta bort` (white text on `bg-red-600 dark:bg-red-500`, semibold) |
| Edit-mode "Klart" affordance | Same as input-row Klart button — the row's editable state IS the input row, just pre-filled with existing values |

### `/workout/[sessionId].tsx` set-input row (always-visible bottom row per CONTEXT.md D-08)

| Element | Copy |
|---------|------|
| Vikt input placeholder | `Vikt` (Muted text; iOS-native placeholder convention matches the field-label inside the input rather than an external label, because the row is dense and an external label would waste vertical space — confirmed against PITFALLS §6.1 "no modal per set; inline always-on") |
| Reps input placeholder | `Reps` |
| Klart button label | `Klart` (Body semibold, white on accent-blue bg) |
| Klart button loading state | `Klart` (label unchanged; no loading state — optimistic-update writes to cache immediately so visual feedback is the new logged row appearing, not a spinner. Identical to Phase 4 mutate-not-mutateAsync convention from commit 5d953b6) |
| F7 last-value chip — prefix | `Förra:` (Muted text, Body normal) |
| F7 last-value chip — value | (dynamic — `{prev.weight_kg} × {prev.reps}` e.g. `82.5 × 8`; Muted text, Body semibold) |
| F7 last-value chip — when no prior data | (omit — chip not rendered; the input row sits without a chip per CONTEXT.md D-19) |

### `/workout/[sessionId].tsx` empty-states

| Surface | Trigger | Icon (Ionicons, accent-tinted) | Heading | Body | CTA |
|---------|---------|--------------------------------|---------|------|-----|
| Workout screen with 0 plan_exercises (defensive — should be blocked by disabled "Starta pass" on plans/[id]) | `plan_exercises.length === 0` after navigation | `list-outline` (size 64, accent color — matches Phase 4 plan-detail empty-state for visual continuity) | `Den här planen har inga övningar än` | `Gå tillbaka och lägg till några.` | `Tillbaka till planen` (Body semibold, accent-blue background — routes back via `router.back()`) |

### `(tabs)/index.tsx` draft-session-recovery overlay (Phase 4 surface extended)

Inline-overlay pattern from Phase 4 commit e07029a (NOT modal portal — same anti-pattern per Phase 4 D-08).

| Element | Copy |
|---------|------|
| Overlay title | `Återuppta passet?` |
| Overlay body (when active session has 0 logged sets) | `Du startade ett pass [HH:MM] men har inte loggat något set än.` (`HH:MM` formatted via `date-fns` `format(startedAt, 'HH:mm')`) |
| Overlay body (when active session has ≥1 logged set) | `Du har ett pågående pass från [HH:MM] med [N] set sparade.` |
| Primary button | `Återuppta` (Body semibold, accent-blue bg → `router.push('/workout/<id>')`) |
| Secondary button | `Avsluta sessionen` (Body semibold, red bg → `useFinishSession().mutate({ id, finished_at: now() })` followed by toast on success) |

**Overlay does NOT have a "X" close affordance** — the user must explicitly choose between resume and finish. Closing without choosing would leave the draft hanging and the next cold-start would re-prompt; forcing a decision is correct UX for data-loss-adjacent states per PRD §F13 mindset.

### `/workout/[sessionId].tsx` Avsluta-pass overlay (Phase 5 net-new)

Inline-overlay pattern from Phase 4 commit e07029a. Triggered by header-right `Avsluta` Pressable.

| Element | Copy |
|---------|------|
| Overlay title | `Avsluta passet?` |
| Overlay body (when ≥1 set logged) | (dynamic — `{N} set sparade. Avsluta passet?`) |
| Overlay body (when 0 sets logged) | `Inget set är loggat. Avsluta utan att spara?` |
| Primary button (when ≥1 set logged) | `Avsluta` (Body semibold, accent-blue bg — non-destructive per CONTEXT.md D-23 + PITFALLS §6.6) |
| Primary button (when 0 sets logged) | `Avsluta utan att spara` (Body semibold, accent-blue bg — slightly different copy to acknowledge no-set-saved state, but still non-destructive because PRD says no Discard button exists; the session row remains with `finished_at` set, just with 0 child sets) |
| Secondary button (both cases) | `Fortsätt` (Body semibold, Body text color on `bg-gray-100 dark:bg-gray-800` — same neutral-cancel pattern as Phase 4 inline-overlay-cancel) |

**No "Discard" button anywhere.** Per CONTEXT.md D-23 + PITFALLS §6.6. The session is either finished (`finished_at IS NOT NULL`) or in-progress (`finished_at IS NULL`); there is no third "discarded" state in V1.

### Post-finish toast on `(tabs)/index.tsx`

After `useFinishSession` success + navigation home:

| Element | Copy |
|---------|------|
| Toast label | `Passet sparat ✓` (white text, semibold, on Success-green background) |
| Toast duration | ~2 seconds (Reanimated `entering={FadeIn}` + `exiting={FadeOut.delay(2000)}` or equivalent) |
| Toast position | Bottom-center, above the tab-bar by ~16pt; horizontal padding `px-6 py-3`; `rounded-full` |
| Toast a11y | `accessibilityRole="alert"` + `accessibilityLiveRegion="polite"` |

### ActiveSessionBanner (`components/active-session-banner.tsx`)

| Element | Copy |
|---------|------|
| Banner label (when active session exists) | `Pågående pass · Tryck för att återgå` |
| Icon prefix | `<Ionicons name="time" size={20}>` (matches Historik tab icon for visual association with "time-bound" state; CONTEXT.md Discretion confirmed clock-vs-play) |
| Tap target | Full banner row is a `Pressable` → routes to `/workout/<activeSessionId>` |
| Accessibility | `accessibilityRole="button"` + `accessibilityLabel="Återgå till pågående pass"` + `accessibilityLiveRegion="polite"` on mount |

**Banner is not dismissable in V1.** Adding a ✕ would hide the affordance to return to the in-progress session; the only way to remove the banner is to finish the session (Avsluta). This is intentional per PRD §F13 — data-loss prevention takes precedence over UI minimalism for the duration of an active workout.

### Inline error states (set-input row, all surfaces — strict Zod per CONTEXT.md D-15)

Identical pattern to Phase 3 + Phase 4: red text below the field, Body size, no icons, RHF `mode: 'onSubmit'` per Phase 3 D-15 precedent (CONTEXT.md Discretion confirmed onSubmit over onChange for the set-row to avoid mid-typing "1.0 is invalid" flicker on numeric inputs).

| Trigger | Copy |
|---------|------|
| Vikt empty (Zod `.coerce.number()` required) | `Vikt krävs` |
| Vikt below 0 (Zod `.min(0)`) | `Vikt måste vara 0 eller högre` |
| Vikt above 500 (Zod `.max(500)`) | `Vikt över 500kg verkar fel — kontrollera` |
| Vikt not in 0.25kg-steps (Zod `.multipleOf(0.25)`) | `Vikt i steg om 0.25kg` |
| Reps empty | `Reps krävs` |
| Reps not integer (Zod `.int()`) | `Reps måste vara ett heltal` |
| Reps below 1 (Zod `.min(1)`) | `Minst 1 rep` |
| Reps above 60 (Zod `.max(60)`) | `Över 60 reps verkar fel — kontrollera` |
| Supabase error: any thrown error during set-add/update/remove mutation when online | (no error rendered — optimistic-update has already written the row; user sees the row as if it succeeded. If the eventual replay fails with a non-409 error after `retry: 1`, the row stays in the optimistic cache and the OfflineBanner + ActiveSessionBanner remain visible — Plan 02 may surface a screen-level banner for the rare hard-failure case, but the default is silent optimistic + queue per PRD §F13) |

**Note on numeric Zod inputs (Phase 4 D-11 RHF pattern carried forward):** `weight_kg` and `reps` both use `z.coerce.number()`, so the schema's INPUT type is `unknown` and OUTPUT is `number | null`. The set-form `useForm` call uses the three-arg generic:

```typescript
useForm<z.input<typeof setFormSchema>, undefined, SetFormOutput>({
  resolver: zodResolver(setFormSchema),
  defaultValues: { weight_kg: prefillWeight, reps: prefillReps },
  mode: 'onSubmit',
})
```

This is identical to Phase 4 `planExerciseFormSchema` pattern (commit f8b75b6) and must be followed for the set-row form.

### Destructive confirmations in Phase 5

Two destructive confirmations exist:

| Action | Trigger | Confirmation copy |
|--------|---------|-------------------|
| Avsluta sessionen från draft-resume | Tap "Avsluta sessionen" in `(tabs)/index.tsx` draft-resume overlay | **No second confirmation** — the overlay button itself IS the confirmation. Single-tap finishes the orphaned draft. Rationale: the draft already represents a previously-abandoned session; requiring two taps to dispose of it adds friction without protecting data (the orphan has 0 or N sets that were already committed; finishing it doesn't delete those sets). |
| Ta bort loggat set (swipe-left) | Swipe-left on logged set-row → reveal "Ta bort"-action → tap | **No second confirmation** — the swipe gesture is intentional + the revealed red button is the confirmation. Rationale: matches iOS Reminders/Mail convention; the user's swipe motion already signals intent. The set is **deleted from the cache + server**, not soft-archived, because Phase 5 schema for `exercise_sets` has no `archived_at` column and adding one would require a schema migration outside Phase 5 scope. |

**No `Alert.alert` native popups in Phase 5.** The Phase 4 archive-plan flow uses `Alert.alert` per Phase 4 D-12, but Phase 5 destructive paths inherit the **inline-overlay pattern** established by Phase 4 commit e07029a + 954c480 + 1f4d8d0. The draft-resume overlay's destructive button doesn't need a second confirmation, and the swipe-left set-delete doesn't either, so no `Alert.alert` is required.

**No destructive confirmation for:**
- Avsluta pass when ≥1 set is logged (non-destructive — finishing is the intended terminal state per PRD §F13 + PITFALLS §6.6)
- Avsluta pass when 0 sets are logged (still non-destructive — the user can re-start the same plan; no data is lost)
- Tap-to-edit a logged set (non-destructive — the row enters edit mode; the user can still Klart to commit or back out)
- Remove plan_exercise from plan (out of Phase 5 scope — owned by Phase 4 plans/[id])

---

## Visuals (Phase 5 component inventory)

The following primitives compose all Phase 5 surfaces. Each is a plain `react-native` element styled with the NativeWind classes specified. Components NOT listed here (TextInput, Primary CTA, Screen container, OfflineBanner, FAB, plan-list row, plan_exercise row, exercise-picker row) inherit Phase 4 implementations verbatim — the executor SHOULD reuse the exact class strings from Phase 4 UI-SPEC.

### `/workout/[sessionId].tsx` exercise-card

The single visual unit on the workout screen. One card per `plan_exercises` row, rendered in `order_index` order.

```
View (className="bg-gray-100 dark:bg-gray-800 rounded-lg p-4 mb-4")

  // Card header
  View (className="flex-row items-start justify-between mb-2")
    View (className="flex-1 gap-1")
      Text  → text-2xl font-semibold text-gray-900 dark:text-gray-50
              numberOfLines={1}     ← exercise.name (from Map<id, name> lookup)
      View (className="flex-row flex-wrap gap-2 mt-1")  ← chip row
        [Target chip — render if any target field set]
        [Equipment chip — render if exercise.equipment non-empty]
    [Counter chip — right-aligned via flex-row justify-between]

  // Logged set rows (zero or more, ordered by set_number asc)
  View (className="gap-2 mt-2")
    [LoggedSetRow × N]

  // Always-visible set-input row (D-08)
  View (className="flex-row items-center gap-2 mt-3")
    [VikInput] [RepsInput] [KlartButton]

  // F7 last-value chip (only when prior data exists for this set_number)
  View (className="mt-1")
    [LastValueChip]
```

**Card-header chip row format** (per CONTEXT.md `<specifics>`):
- Target chip: `View bg-gray-200 dark:bg-gray-700 rounded-full px-2 py-1` containing `Text text-sm font-semibold text-gray-500 dark:text-gray-400` with `3×8–12` or `3 set` or `8–12 reps`. (`py-1` = 4px — on the 4-pt grid; replaces an earlier `py-0.5` (2px) that violated the spacing scale.)
- Equipment chip: same chip shell (`px-2 py-1`), content `Skivstång` (literal `exercise.equipment` value).
- Counter chip: positioned right (outside the chip row, in the header's right slot via `flex-row justify-between`); same shell (`px-2 py-1`), content `2/3 set klart` or `2 set`. **When all target_sets reached** (`loggedCount >= target_sets`), counter chip background swaps to `bg-green-100 dark:bg-green-900` and text to `text-green-900 dark:text-green-100` — success state, AAA contrast — providing implicit feedback that the user has met the plan's target without requiring an explicit "all done" badge.

### `/workout/[sessionId].tsx` LoggedSetRow

At rest (not in edit mode):

```
Pressable  → flex-row items-center bg-white dark:bg-gray-900
             rounded-md px-3 py-3 active:opacity-80
             accessibilityRole="button"
             accessibilityLabel={`Set ${n}: ${weight} kilo, ${reps} repetitioner. Tryck för att redigera.`}
  // Success-green check icon (decorative)
  Ionicons name="checkmark-circle" size=20 color={green-600 light / green-500 dark}
  // Set number prefix
  Text  → text-sm font-semibold text-gray-500 dark:text-gray-400 mx-2
        → "Set 1"
  // Value display
  Text (flex-1)  → text-base font-normal text-gray-900 dark:text-gray-50
                 → "82.5 × 8"
```

Swipe-left reveals delete:

```
// Swipe-left mechanic implemented via react-native-gesture-handler Swipeable
// (already in stack via Reanimated 4 toolchain — no new dep) OR via
// react-native's PanResponder for a simpler V1 — Plan 02 chooses.
//
// Hidden action panel (revealed on swipe-left):
View  → absolute inset-y-0 right-0 w-24 bg-red-600 dark:bg-red-500
        items-center justify-center
  Pressable  → w-full h-full items-center justify-center
               accessibilityLabel={`Ta bort set ${n}`}
    Text     → text-base font-semibold text-white
             → "Ta bort"
```

In edit mode (after tap, D-14):

```
View  → flex-row items-center gap-2 bg-white dark:bg-gray-900
        rounded-md px-3 py-3
  [VikInput pre-filled with current weight_kg]   ← className uses text-base font-semibold
  [RepsInput pre-filled with current reps]       ← className uses text-base font-semibold
  [KlartButton — calls useUpdateSet().mutate on submit]
```

**Visual distinction between logged-and-resting vs editing:** at rest the row has lighter `bg-white dark:bg-gray-900` (one level above card-secondary background); in edit mode the row swaps to TextInput inputs that share the same row dimensions — no jarring height change. The success-green checkmark stays visible during edit so the row's "this is a saved set" identity persists. The value cells flip from `text-base font-normal` (at-rest) to `text-base font-semibold` (editing) — same size, weight-only contrast, so vertical layout is stable.

### `/workout/[sessionId].tsx` VikInput / RepsInput (numeric inputs on set-input row)

```
TextInput  → flex-1 rounded-md bg-white dark:bg-gray-900
             border border-gray-300 dark:border-gray-700
             focus:border-blue-600 dark:focus:border-blue-500
             px-3 py-3 text-base font-semibold text-gray-900 dark:text-gray-50
             min-h-[56px]
  Props:
    placeholder="Vikt" or "Reps"
    placeholderTextColor="#9CA3AF"   ← Phase 3 Pitfall 7 workaround
    keyboardType="decimal-pad"        ← per CONTEXT.md D-11 + PITFALLS §6.1
    inputMode="decimal" (vikt) / "numeric" (reps)
    returnKeyType="done"
    autoCorrect={false}
    autoCapitalize="none"
    accessibilityLabel="Vikt i kilo" / "Antal repetitioner"
    selectTextOnFocus={true}          ← so pre-filled values can be quickly overwritten
```

**Note on placeholder weight:** the placeholder text itself remains weight-400 (NativeWind's `font-semibold` applies to the value text via the input's render style; React Native's placeholder is controlled by `placeholderTextColor` only, not weight). The semibold weight only kicks in once the user types or once a pre-filled value is rendered. This means focus/blur naturally signals "you are editing" via the weight delta — additional UX benefit, no extra wiring.

**Error border:** when RHF reports `fieldState.error`, border switches to `border-red-600 dark:border-red-400` (replaces the focus blue or the at-rest gray).

**Error text:** renders below the row inside the card, full-width:
```
Text  → text-base text-red-600 dark:text-red-400 mt-1 px-3
        accessibilityLiveRegion="polite"
      → "Vikt över 500kg verkar fel — kontrollera"
```

### `/workout/[sessionId].tsx` KlartButton (set-input row submit)

```
Pressable  → w-20 min-h-[56px] rounded-md bg-blue-600 dark:bg-blue-500
             items-center justify-center
             disabled:opacity-60 active:opacity-80
             accessibilityRole="button"
             accessibilityLabel="Spara set"
  Text     → text-base font-semibold text-white
           → "Klart"
```

Tap: calls `useAddSet(sessionId).mutate({ id: randomUUID(), session_id, exercise_id, set_number, weight_kg, reps, completed_at: new Date().toISOString(), set_type: 'working' }, { onSuccess: () => reset() })` per Phase 4 mutate-not-mutateAsync convention. Optimistic-update writes to cache; new LoggedSetRow renders above the input row; input row resets to blank (or pre-filled from new latest, per CONTEXT.md D-10).

**Optional haptic feedback** (CONTEXT.md `<deferred>` — V1 default = off): if Plan 02 enables, `Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Medium)` on tap. Requires `npx expo install expo-haptics`. Default = no haptics; visual flash of the new row is the confirmation signal.

### `/workout/[sessionId].tsx` LastValueChip (F7 prior-value display)

Rendered below the set-input row only when `useLastValueQuery(exerciseId, currentSessionId)` returns a value for the current `set_number`:

```
View  → flex-row items-center gap-1 px-3 py-1
  Text  → text-base font-normal text-gray-500 dark:text-gray-400
        → "Förra:"
  Text  → text-base font-semibold text-gray-500 dark:text-gray-400
        → "82.5 × 8"
```

No background, no border, no tap affordance. Pure informational. Sits just below the input row, left-aligned. When no prior-value data exists for this set position (first time logging the exercise, or set_number > previous session's set count), the chip is **not rendered** (per CONTEXT.md D-19 — empty dash "—" rejected as visual noise).

### `app/components/active-session-banner.tsx` (NEW)

Mounted in `(tabs)/_layout.tsx` **below** `<OfflineBanner />` but **above** `<Tabs>`, inside the same `SafeAreaView edges={['top']}` per CONTEXT.md D-22 + Phase 4 D-05 slot-pattern.

```
View (conditional render: only when useActiveSessionQuery returns a row)
  Pressable  → flex-row items-center justify-between gap-2
               bg-blue-100 dark:bg-blue-950
               border border-blue-300 dark:border-blue-800
               px-4 py-3 mx-4 mt-2 rounded-lg
               active:opacity-80
               accessibilityRole="button"
               accessibilityLabel="Återgå till pågående pass"
    View (flex-row items-center gap-2 flex-1)
      Ionicons name="time" size=20 color={blue-900 light / blue-100 dark}
      View (flex-1)
        Text  → text-base font-semibold text-blue-900 dark:text-blue-100
              accessibilityLiveRegion="polite"
              → "Pågående pass"
        Text  → text-base text-blue-900 dark:text-blue-100 opacity-80
              → "Tryck för att återgå"
    Ionicons name="chevron-forward" size=20 color={blue-900 light / blue-100 dark}
```

**No close affordance** (V1 — see Copywriting Contract rationale).

**Visibility logic:** rendered when `useActiveSessionQuery()` returns a non-null row (i.e. a `workout_sessions` row WHERE `user_id = current` AND `finished_at IS NULL` exists). Unmounts when the session finishes (i.e. `useFinishSession.mutate` writes optimistically to the cache and the query re-evaluates to return null). Visible across all three tabs (Planer, Historik, Inställningar) and inside the workout screen itself — when the user is already inside `/workout/[sessionId]`, the banner is **conditionally hidden** via a route-check (`useSegments()` or `usePathname()`) so it doesn't double-stack with the screen header. Plan 02 implements the hide-on-workout-route logic.

**Stacking behavior with OfflineBanner:** when both banners are visible (offline mid-workout), OfflineBanner renders first (top), ActiveSessionBanner renders directly below it. The two are visually distinct (yellow vs info-blue) so the user can read both states at a glance.

**Animation:** V1 = no animation (conditional render only — matches OfflineBanner). V1.1 polish can add Reanimated `withTiming` slide-down.

### Inline-overlay-confirm (Avsluta + draft-resume — Phase 5 reuses Phase 4 pattern)

Pattern established in Phase 4 commits 954c480 (overflow menu) + e07029a (archive confirm) + 1f4d8d0 (modal presentation). Phase 5 reuses verbatim for Avsluta-pass overlay and draft-resume overlay.

```
// Full-screen dimmed backdrop (Pressable for tap-to-dismiss — Avsluta only;
// draft-resume omits this to force decision)
Pressable  → absolute inset-0 bg-black/40
             onPress={() => setShowOverlay(false)}   ← Avsluta only

// Centered panel
View  → absolute inset-x-4 top-[40%] bg-gray-100 dark:bg-gray-800
        rounded-2xl p-6 gap-6
        // OR: bottom-anchored
        // → absolute bottom-0 inset-x-0 bg-gray-100 dark:bg-gray-800
        //   rounded-t-2xl p-6 gap-6 pb-12

  // Title block
  View (gap-2)
    Text  → text-2xl font-semibold text-gray-900 dark:text-gray-50
          → "Avsluta passet?" / "Återuppta passet?"
    Text  → text-base text-gray-900 dark:text-gray-50
          → "8 set sparade. Avsluta passet?" / "Du har ett pågående pass från 14:32 med 8 set sparade."

  // Button row
  View (flex-row gap-3)
    Pressable (secondary — neutral cancel)
      → flex-1 py-4 rounded-lg bg-gray-200 dark:bg-gray-700
        items-center justify-center active:opacity-80
      Text  → text-base font-semibold text-gray-900 dark:text-gray-50
            → "Fortsätt"
    Pressable (primary — accent OR destructive)
      → flex-1 py-4 rounded-lg [bg-blue-600/dark:bg-blue-500 OR bg-red-600/dark:bg-red-500]
        items-center justify-center active:opacity-80
      Text  → text-base font-semibold text-white
            → "Avsluta" / "Återuppta" / "Avsluta sessionen"
```

**`useFocusEffect` reset:** workout-screen + (tabs)/index Pressable-state for both overlays MUST reset via `useFocusEffect` cleanup per Phase 4 D-08 (commit af6930c). `freezeOnBlur: true` retains React state across navigation; a stale `showAvslutaOverlay=true` would re-appear on screen unfreeze.

### Toast — "Passet sparat ✓"

```
// Animated using Reanimated 4 (already in stack)
Animated.View  → absolute bottom-20 self-center
                 entering={FadeIn.duration(200)}
                 exiting={FadeOut.delay(2000).duration(200)}
                 bg-green-600 dark:bg-green-500 rounded-full px-6 py-3
                 accessibilityRole="alert"
                 accessibilityLiveRegion="polite"
  Text  → text-base font-semibold text-white
        → "Passet sparat ✓"
```

Triggered via a Zustand flag or a router-param hand-off from `useFinishSession.onSuccess`. Plan 02 chooses implementation; Reanimated `entering`/`exiting` props are the cleanest path with no extra dep.

### Screen container for `/workout/[sessionId].tsx`

```
SafeAreaView  → flex-1 bg-white dark:bg-gray-900
  Stack.Screen options={{
    headerShown: true,
    title: 'Pass',
    headerRight: () => <AvslutaButton onPress={() => setShowAvslutaOverlay(true)} />,
    // headerStyle / headerTintColor / headerTitleStyle inherited from (app) Stack
    // (Phase 4 commit b57d1c2)
  }}
  KeyboardAvoidingView  → flex-1 behavior="padding" (iOS only)
    ScrollView          → contentContainerStyle={{ paddingHorizontal:16, paddingVertical:16, paddingBottom:96 }}
                          keyboardShouldPersistTaps="handled"
                          keyboardDismissMode="on-drag"   ← iOS standard for set-logging UX
      [ExerciseCard × N — rendered in plan_exercises order_index order]

  // Conditional overlays (rendered ABOVE everything via z-index / position absolute)
  [DraftAvslutaOverlay  — only when showAvslutaOverlay]
  [Toast               — managed by parent (tabs) post-navigation; not rendered on this screen]
```

`KeyboardAvoidingView` is critical here — the bottom-most exercise card's input row would be covered by the numeric keyboard without it. `keyboardShouldPersistTaps="handled"` allows the user to tap Klart in one card while another card's input has focus.

### Loading / cold-start (Phase 5 additions to Phase 4 pattern)

- **Cold-start splash:** unchanged from Phase 1 + Phase 3 + Phase 4. Native splash holds via `expo-splash-screen` until auth-status resolves.
- **Active-session-query on cold-start:** `useActiveSessionQuery()` mounts in `(tabs)/index.tsx` with `initialData` from the TanStack persister cache. If hydrated cache has a row, the draft-resume overlay renders immediately (no spinner). If cache is empty, `isPending=true` → no overlay rendered until network resolves; if no row exists post-fetch, no overlay; if a row exists, overlay appears. **No loading spinner is rendered for the draft-resume check** — the user sees the tab content normally and the overlay either appears or doesn't.
- **Workout-screen on cold-start (via deep-link or back-from-banner):** `useSessionQuery(sessionId)` seeds from `useActiveSessionQuery` cache via `initialData` (Phase 4 Plan 04-04 pattern, commits eca0540 + b87bddf). Logged sets via `useSetsForSessionQuery(sessionId)` similarly hydrate from persister. No loading spinner is expected in steady-state use; first-launch-after-cache-clear may flash an `<ActivityIndicator>` for ≤500ms.
- **F7 last-value pre-fetch:** per CONTEXT.md D-20, on workout-screen mount (or `useStartSession.onSuccess`), fire `useLastValueQuery(exerciseId, currentSessionId)` per `plan_exercises.exercise_id`. Results populate persister cache with `staleTime: 15 * 60 * 1000` (15 min). When user reaches each card's input row, the chip already has data; no loading state ever flashes on the chip.

### Offline state surfaces in Phase 5

- **Global OfflineBanner:** inherited from Phase 4 unchanged — mounted in `(tabs)/_layout.tsx`. Visible during the workout via the global slot. Inside `/workout/[sessionId].tsx`, the OfflineBanner is **not** re-mounted because the route is inside the (app) stack but **outside** the (tabs) layout — Plan 02 decides whether to add an OfflineBanner instance inside the workout-screen header (visible inline) or rely on the user knowing they're offline via system network indicator. **Recommendation:** mount a second OfflineBanner instance inside the workout screen's SafeAreaView (just below the header) so offline state is visible during the hot path. The two instances are state-mirrored via `useOnlineStatus()`.
- **ActiveSessionBanner:** mounted globally in (tabs)/_layout.tsx; hidden inside the workout screen via route-check. Above OfflineBanner stacking-wise when both visible (offline mid-workout).
- **Mutation behavior offline:** Per `networkMode: 'offlineFirst'` (inherited from Phase 4), all set-mutations queue. Optimistic-updates write to cache immediately. The user sees their logged set in the cache regardless of network state. No "queued" badge per row (F24 deferred to V2). No error rendered offline.

---

## Interaction Contracts

| Surface | Trigger | Result |
|---------|---------|--------|
| `plans/[id].tsx` "Starta pass" CTA (NEW Phase 5) | Tap when plan has ≥1 plan_exercise | `useStartSession().mutate({ id: randomUUID(), plan_id: planId, started_at: new Date().toISOString(), user_id: <current> }, { onSuccess: ({ id }) => router.push(`/workout/${id}`) })` per Phase 4 mutate-not-mutateAsync convention. Optimistic onMutate dual-writes `sessionsKeys.active()` + `sessionsKeys.detail(newId)` so workout screen has data on push (Phase 4 Plan 04-04 dual-write pattern). Offline: mutation queues; router.push still fires because `onMutate` populates cache synchronously. |
| `(tabs)/index.tsx` cold-start with active session | `useActiveSessionQuery()` returns row + local `dismissed=false` | Inline-overlay draft-resume modal renders. User must choose. |
| Draft-resume overlay `Återuppta` | Tap | `router.push('/workout/<activeSession.id>')`; overlay unmounts via `useFocusEffect` cleanup or local state. |
| Draft-resume overlay `Avsluta sessionen` | Tap | `useFinishSession().mutate({ id: activeSession.id, finished_at: new Date().toISOString() }, { onSuccess: () => { /* trigger toast on (tabs)/index */ } })`. Optimistic-update sets `finished_at` in cache → `useActiveSessionQuery` re-evaluates to null → overlay unmounts → ActiveSessionBanner unmounts → toast renders. |
| ActiveSessionBanner full row | Tap | `router.push('/workout/<activeSession.id>')` |
| `/workout/[sessionId].tsx` mount | Screen pushed (from "Starta pass" or banner or draft-resume "Återuppta") | `useSessionQuery(sessionId)` (initialData from active-session cache); `usePlanExercisesQuery(plan_id)` (from session's plan_id) renders cards; `useSetsForSessionQuery(sessionId)` renders logged set rows; `useLastValueQuery` × N pre-fetched for F7 chips. |
| Set-input row Klart-button | Tap with valid form (vikt + reps both filled, Zod passes) | `useAddSet(sessionId).mutate({ id: randomUUID(), session_id, exercise_id, set_number: <client-count+1>, weight_kg: <coerced>, reps: <coerced>, completed_at: new Date().toISOString(), set_type: 'working' })`. Optimistic onMutate appends to `setsKeys.list(sessionId)` cache → new LoggedSetRow renders above input row → input row's RHF state resets (or pre-fills from new latest per D-10). `mutationKey: ['set','add']` + scope `session:<id>` per Phase 4 `setMutationDefaults` chain. Offline: mutation queues; user still sees the new row in cache. **Scroll position preserved** per CONTEXT.md D-05 (keyboard dismiss does not jolt the scroll). |
| Logged set-row tap | Tap on the row body | Flips row to edit mode (TextInputs replace static text, pre-filled with current values). Klart tap calls `useUpdateSet(sessionId).mutate({ id, weight_kg, reps })` per Phase 4 update-pattern. Offline: queues. Scope `session:<id>` chained per Pitfall 5.3. |
| Logged set-row swipe-left | Swipe-left gesture | Reveals red "Ta bort" action button. Tap on action: `useRemoveSet(sessionId).mutate({ id })`. Optimistic-removes from cache. No second confirmation. Offline: queues. |
| Header-right `Avsluta` Pressable | Tap | Renders inline-overlay (`showAvslutaOverlay=true`). State reset via `useFocusEffect` cleanup. |
| Avsluta-overlay `Avsluta` button (≥1 set) | Tap | `useFinishSession().mutate({ id: sessionId, finished_at: new Date().toISOString() }, { onSuccess: () => router.replace('/(app)/(tabs)/') })`. Optimistic-update sets `finished_at` → `useActiveSessionQuery` re-evaluates to null → ActiveSessionBanner unmounts. `router.replace` because back-button shouldn't return to a finished workout. Toast renders on `(tabs)/index.tsx` mount via post-navigation hook. |
| Avsluta-overlay `Avsluta utan att spara` button (0 sets) | Tap | Same as above. Empty-session-finish is acceptable per PITFALLS §6.6. |
| Avsluta-overlay `Fortsätt` button | Tap | `setShowAvslutaOverlay(false)`. Returns to workout screen. |
| Workout screen back-arrow (header left) | Tap | `router.back()` → returns to `plans/[id]` (or wherever the user came from). **Does NOT finish the session.** ActiveSessionBanner remains visible across tabs as the affordance to return. |
| Cold start with finished session in cache | App launch where the previous session has `finished_at IS NOT NULL` | No overlay, no banner — `useActiveSessionQuery` returns null. User lands on (tabs)/index normally. |
| Force-quit during set-logging | App killed mid-flight | TanStack persister has serialized the paused mutation to AsyncStorage. `setMutationDefaults` for `['set','add']` registered at module load. On next launch, hydrated mutations are rewired with their `mutationFn` and `resumePausedMutations()` replays them on `onlineManager` online-transition. Idempotency: client-UUID via `randomUUID()` + Postgres PK conflict → `INSERT … ON CONFLICT (id) DO NOTHING` semantics protect against duplicates. Per CONTEXT.md D-16 + Pitfall 1.1 + Phase 4 D-04 inheritance. |
| Network reconnect after airplane-mode session | Online transition while session has queued mutations | `onlineManager.subscribe(...)` callback (inherited from Phase 4) fires `queryClient.resumePausedMutations()`. Each `['set','add']` replays in scope `session:<id>` order (serial — Pitfall 5.3). FK safety: `useStartSession` runs in the same scope so the parent session row inserts before its child sets. Optimistic-cache is unchanged; the server inserts catch up in background. |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| (none) | (none) | not applicable — shadcn registry is React-DOM only and incompatible with this React Native runtime; no third-party block sources are used in Phase 5 |

All UI in Phase 5 is composed from `react-native` core primitives (`View`, `Text`, `TextInput`, `Pressable`, `ScrollView`, `KeyboardAvoidingView`, `SafeAreaView`, `FlatList`, `Animated.View`) plus Phase 4 inheritance:
- `@expo/vector-icons` (Ionicons subset — already installed; new Phase 5 icons drawn from same package)
- `react-native-gesture-handler` (already installed; backs swipe-left set-row delete — same library that powers `react-native-draggable-flatlist` from Phase 4)
- `react-native-reanimated` (already installed; backs the toast `FadeIn`/`FadeOut` and any optional banner animations)
- `expo-crypto` (already installed Phase 4; provides `randomUUID()` for client-generated IDs)
- **Optional:** `expo-haptics` — Plan 02 may add via `npx expo install expo-haptics`. Default = no haptics; if added, the safety surface is the standard Expo-managed install (no third-party registry fetch).

No third-party component blocks are fetched, copied, or wired via `npx shadcn add`. There is no source-code vetting requirement.

---

## Accessibility Floor

| Item | Implementation |
|------|----------------|
| Touch target | All `Pressable` elements ≥ 44×44pt. Klart button explicitly ≥ 56pt per PITFALLS §6.1. Numeric inputs ≥ 56pt height via `min-h-[56px]`. Logged set-row uses `py-3` (44pt). Header `Avsluta` Pressable uses `px-3 py-2 + hitSlop={8}` (44pt). ActiveSessionBanner uses `py-3` (44pt). |
| Color contrast (light mode) | Body `text-gray-900` on `bg-white` = 15.3:1 (AAA); on `bg-gray-100` = 13.6:1 (AAA). Muted `text-gray-500` on `bg-white` = 4.6:1 (AA Body). Accent `text-white` on `bg-blue-600` = 4.86:1 (AA Body). Error `text-red-600` on `bg-white` = 4.83:1 (AA Body). Info-blue `text-blue-900` on `bg-blue-100` = 9.5:1 (AAA). Success-green `text-white` on `bg-green-600` = 4.55:1 (AA Body). Counter-success `text-green-900` on `bg-green-100` = 8.9:1 (AAA). |
| Color contrast (dark mode) | `text-gray-50` on `bg-gray-900` = 17.4:1 (AAA); on `bg-gray-800` = 14.4:1 (AAA). Muted `text-gray-400` on `bg-gray-900` = 6.4:1 (AA Body). Accent `text-white` on `bg-blue-500` = 4.5:1 (AA Body). Error `text-red-400` on `bg-gray-900` = 5.7:1 (AA Body). Info-blue `text-blue-100` on `bg-blue-950` = 13.1:1 (AAA). Success-green `text-white` on `bg-green-500` = 3.05:1 — paired with `font-semibold` for AA Body floor at 14px+ per WCAG 1.4.3 large-text exception. |
| Form labels | Numeric inputs use placeholder-as-label (dense set-row UX requires this); `accessibilityLabel="Vikt i kilo"` / `"Antal repetitioner"` carries the explicit label for VoiceOver. The placeholder copy ("Vikt", "Reps") doubles as visual cue and a11y-label fallback. |
| Error association | Each field's error `<Text>` has `accessibilityLiveRegion="polite"` so screen readers announce on submit. RHF re-validates on change after first submit, so VoiceOver picks up clearance via re-render. |
| Logged-set-row a11y | `accessibilityLabel={`Set ${n}: ${weight} kilo, ${reps} repetitioner. Tryck för att redigera.`}` — full set context spoken on focus. Swipe-left action's button is announced via `accessibilityLabel="Ta bort set ${n}"`. |
| ActiveSessionBanner a11y | `accessibilityRole="button"` + `accessibilityLabel="Återgå till pågående pass"` + `accessibilityLiveRegion="polite"` on mount. VoiceOver announces "Återgå till pågående pass, knapp" when the banner appears. |
| Draft-resume overlay a11y | Overlay panel uses `accessibilityRole="alert"`. VoiceOver lands on title first ("Återuppta passet?"). Forced-decision design (no dismiss-by-tap) is intentional but means VoiceOver users cannot escape without making a choice — acceptable for data-loss-adjacent state. |
| Avsluta-overlay a11y | Same pattern as draft-resume but allows dismiss-by-tap on backdrop (per inline-overlay convention from Phase 4). VoiceOver can swipe to backdrop and double-tap to dismiss. |
| Toast a11y | `accessibilityRole="alert"` + `accessibilityLiveRegion="polite"`. VoiceOver announces "Passet sparat" on appearance; the ✓ glyph is decorative and not vocalized. |
| Header right action a11y | `accessibilityRole="button"` + `accessibilityLabel="Avsluta passet"` (more specific than the visible "Avsluta" so VoiceOver users know the scope of the action). |
| Keyboard semantics | Numeric inputs use `keyboardType="decimal-pad"` (vikt) and `keyboardType="number-pad"` (reps). `returnKeyType="done"` triggers Klart-equivalent on numpad Return. `keyboardDismissMode="on-drag"` on the ScrollView so scrolling dismisses keyboard. |
| Focus management | After Zod fails on submit, RHF `setFocus(firstErrorField)` per Phase 3 pattern. (Note: Phase 3 disclosed Controller-ref-forwarding gap; Plan 02 may close this for the set-input row since the hot path is more focus-sensitive than auth.) |

**A11y deferrals to V1.1 (acknowledged not addressed in Phase 5):**
- VoiceOver-friendly set reordering (V1 has no reorder UI for logged sets within a session; if added, would need up/down buttons exposed to AT)
- Reduced-motion handling for the toast fade animation (would short-circuit `entering`/`exiting` to instant render — V1 default ships the fade)
- Dynamic Type scaling above system default (Body 16px is fixed; users with very large text may see truncation on the set-row — same risk as Phase 4 input rows)
- Haptic feedback as an a11y enhancement (decoupled from V1 default; V1.1 if soak shows visual flash is insufficient)

---

## Dark Mode Verification (F15 Convention)

All Phase 5 surfaces MUST render correctly in both light and dark mode. Checklist for the executor:

- [ ] Background flips: `bg-white` ↔ `bg-gray-900` (workout screen, screen container)
- [ ] Body text flips: `text-gray-900` ↔ `text-gray-50` (exercise-card name, logged set-row value, input value)
- [ ] Card background flips: `bg-gray-100` ↔ `bg-gray-800` (exercise card)
- [ ] Logged set-row background flips: `bg-white` ↔ `bg-gray-900` (one level above card-secondary)
- [ ] Input border flips: `border-gray-300` ↔ `border-gray-700` (at rest); `border-blue-600` ↔ `border-blue-500` (focused); `border-red-600` ↔ `border-red-400` (error)
- [ ] CTA flips: `bg-blue-600` ↔ `bg-blue-500` (Klart, Starta pass, Återuppta, Avsluta)
- [ ] Destructive flips: `bg-red-600` ↔ `bg-red-500` (swipe-delete, draft-resume Avsluta sessionen); `text-red-600` ↔ `text-red-400` (inline errors)
- [ ] Info-blue banner flips: `bg-blue-100` + `text-blue-900` + `border-blue-300` ↔ `bg-blue-950` + `text-blue-100` + `border-blue-800` (ActiveSessionBanner)
- [ ] Success-green flips: `bg-green-600` ↔ `bg-green-500` (toast); `bg-green-100` + `text-green-900` ↔ `bg-green-900` + `text-green-100` (counter-chip-success state)
- [ ] Muted text flips: `text-gray-500` ↔ `text-gray-400` (target chips, counter chip, F7 chip, set number prefix, placeholders)
- [ ] Header (centralized via Phase 4 (app) Stack) inherits dark-mode-aware backgroundColor/tintColor — no per-screen handling needed
- [ ] Status bar icons remain visible (`<StatusBar style="auto" />` from Phase 1, untouched in Phase 5)
- [ ] Toast renders with white text on green bg in both modes — green-600 (light) / green-500 (dark) — semibold weight gives AA contrast floor

**Real-device verification required** (per Phase 4 UAT-color-amendment convention from commit cfc1dc8): the ActiveSessionBanner light-mode shade `bg-blue-100` should be verified on a physical iPhone before merge. If it reads as near-white (the failure mode that bumped OfflineBanner from yellow-100 to yellow-200), Plan 02 amends to `bg-blue-200` + `border-blue-400` + `text-blue-900` and updates this UI-SPEC inline. The amendment is part of the Phase 4-established convention that simulator preview is not load-bearing for color identity.

Manual test: simulator → Settings → Developer → Dark Appearance toggle. All Phase 5 surfaces must pass without a reload. **Plus:** physical iPhone Pre-flight test in both light and dark mode focused on ActiveSessionBanner and the success-green toast.

---

## Out-of-Scope (Per CONTEXT.md `<deferred>` — Do Not Implement in Phase 5)

- Soft-warn on vikt > F7_max × 1.3 ("är du säker"-popup) → V1.1 polish
- Haptic feedback on Klart-tap (default V1 = off; `expo-haptics` is optional per Plan 02) → V1.1 polish if visual flash is insufficient
- PR-detection F18 (Epley formula, max-vikt, max-volym per övning) → V1.1
- Vilo-timer F19 (`expo-keep-awake` + `expo-notifications`) → V1.1
- Sparkline mini-graf per exercise-card (5 most recent working-set weights as inline trend) → V2 polish
- "Senast använda övningar"-shortcut mid-pass → V1.1
- Plan-scoped F7 ("Förra X-passet"-specific instead of global per övning) → V2 (F22)
- "Repeat last session"-CTA on hemskärm → V2 (F23)
- Visible pending-sync-badge with count → V2 (F24)
- Ad-hoc exercise mid-pass ("+ Lägg till övning"-button on workout screen) → V1.1
- 6h auto-finish of abandoned sessions → V1.1 if soak shows need
- Redundant Zustand pending-mutations-store (Phase 4 D-03 belt-and-braces) → only if V1 soak shows lost sets
- Multi-unfinished-session edge case (V1 = pick LIMIT 1) → V1.1
- Long-press menu on logged set-row (copy, repeat, comment) → V1.1
- Set-type toggling (warmup/working/dropset/failure) during active pass (F17-UI) → V1.1; schema ready
- RPE field (F11) per set inline → Phase 7
- Anteckningar per pass (F12) → Phase 7
- Apple Health integration → V2
- Dynamic Type scaling above system default → V1.1 a11y polish
- Reduced-motion handling for toast fade → V1.1 a11y polish
- VoiceOver-friendly set reordering (no UI for it in V1) → V1.1 if added
- F9 Historik-lista + F10 progressionsgraf → Phase 6 (entirely separate phase)
- F15 dark-mode toggle UI → Phase 7
- Custom toast component / context provider → if a toast lib is added in V1.1, the Phase 5 implementation can be a one-shot Reanimated `Animated.View` and refactored later

---

## Inheritance From Phase 3 + Phase 4 UI-SPECs (Continuity Notes)

These are intentionally identical between Phase 3 / Phase 4 / Phase 5 to preserve cross-phase visual consistency. The executor SHOULD reuse the exact class strings from Phase 4 components:

- Spacing scale (xs–3xl tokens; strict 4-pt grid — no sub-4-pt tokens like `*-0.5`)
- Typography role table (Body / Label / Heading / Display sizes + weights — two weights only; strict four-size contract)
- Color 60/30/10 split (dominant white, secondary gray-100, accent blue-600)
- Destructive red-600 (same hex; new usage in Phase 5 = swipe-delete + draft-resume Avsluta sessionen)
- Warning yellow-200 (unchanged — OfflineBanner only; Phase 5 adds no yellow surface)
- Body and Muted text colors
- TextInput field structure and class string (verbatim from Phase 3/Phase 4 — re-applied to set-input row with `font-semibold` weight bump on numeric value, no size change)
- Primary CTA button structure (verbatim from Phase 3/Phase 4 — re-applied to "Starta pass", "Återuppta", overlay buttons)
- Inline-overlay-confirm pattern (verbatim from Phase 4 commits 954c480 + e07029a + 1f4d8d0)
- `useFocusEffect` state-reset pattern (verbatim from Phase 4 commit af6930c)
- Centralized (app) Stack header styling (inherited from Phase 4 commit b57d1c2; workout-screen header-right slot uses it)
- `initialData` cache-seed pattern (verbatim from Phase 4 Plan 04-04 commits eca0540 + b87bddf)
- `mutate(payload, { onError, onSuccess })` pattern — NOT `mutateAsync` (verbatim from Phase 4 commit 5d953b6)
- `<StatusBar style="auto" />` at root (Phase 1 convention)
- `placeholderTextColor="#9CA3AF"` prop usage (Pitfall 7 workaround)
- OfflineBanner mounted in (tabs)/_layout.tsx (Phase 4 D-05 slot — Phase 5 mounts ActiveSessionBanner directly below it)
- Inline form error rendering (red Body text below field, no icons, RHF `mode: 'onSubmit'`)
- Floating action button (FAB) pattern (not used in Phase 5 but the pattern is available if Plan 02 finds a use)
- Empty-state pattern (icon + heading + body + CTA — used in workout-screen defensive empty-state)
- Touch-target 44pt minimum (Klart bumps to 56pt per PITFALLS §6.1 hot-path floor)

These are **new** in Phase 5 (no Phase 3/4 precedent):

- Info-blue color role (ActiveSessionBanner)
- Success-green color role (toast + check-icon on logged set-rows + counter-chip success state)
- Numeric-input weight bump (`font-semibold` on hot-path values — size unchanged at `text-base` 16px; scan affordance via weight contrast, not size step)
- Klart button 56pt floor (vs 44pt baseline)
- ActiveSessionBanner component (mounts below OfflineBanner in (tabs)/_layout.tsx)
- LoggedSetRow component (single-line static-or-editable display with swipe-left delete; at-rest `font-normal` → editing `font-semibold` weight delta)
- LastValueChip component (F7 set-position-aligned chip)
- Exercise-card layout (multi-row card: header + chips + logged rows + input row + last-value chip)
- Toast component (Reanimated `Animated.View` with `entering`/`exiting.delay(2000)`)
- Persistent banner stacking (OfflineBanner + ActiveSessionBanner in same SafeAreaView slot)
- Header-right text-button pattern (`Avsluta` — accent-blue tint, no background)
- Workout-screen route-check to hide ActiveSessionBanner during active workout view
- Counter-chip success-state color swap when all `target_sets` reached
- Swipe-left set-row delete gesture (red action panel revealed behind row)

---

## Accepted FLAGs (Acknowledged Non-Blocking — Next Checker Run)

The following Dimension 1 (Copywriting) findings from the checker are intentional and accepted for Phase 5. They are documented here so that the next checker pass treats them as known-and-justified rather than re-raising them.

| Element | Surface | Copy | Justification |
|---------|---------|------|---------------|
| Primary CTA | Set-input row Klart button | `Klart` | Hot-path constraint — the button cell is fixed at `w-20` (80pt) so the row fits `[vikt: flex-1][reps: flex-1][Klart: w-20]` on a 360pt-wide iPhone. A two-word label ("Spara set") would either truncate or force the row to wrap, which breaks the always-visible-input-row contract from CONTEXT.md D-08. "Klart" is the iOS-native idiom for "submit this dense form cell" (compare Apple's native pickers + form-field commit affordance). |
| Header right | Workout-screen header (`/workout/[sessionId].tsx`) | `Avsluta` | Header-right text-button has tight horizontal space (must coexist with the title + system back-arrow). The accompanying `accessibilityLabel="Avsluta passet"` carries the explicit verb-noun pairing for VoiceOver (already documented in the Accessibility Floor section above). |
| Overlay primary | Draft-resume overlay (`(tabs)/index.tsx`) | `Återuppta` | The overlay heading immediately above (`Återuppta passet?`) supplies the noun ("passet"), so the button label only needs the verb. Two-word "Återuppta passet" on the button would duplicate the title and waste vertical space inside an inline overlay. |
| Overlay secondary | Avsluta-pass overlay (`/workout/[sessionId].tsx`) | `Fortsätt` | Same overlay-context rationale: the title `Avsluta passet?` supplies the noun, and `Fortsätt` is the universal "not-now" cancel verb in Swedish iOS apps. |

These four labels are space- and idiom-constrained choices, not oversights. They will not be re-evaluated in the next checker pass unless surface-level context changes (e.g. the button cell widens, in which case the executor MAY revisit). All other Swedish copy in this phase is multi-word and follows the verb-noun pattern (`Starta pass`, `Avsluta sessionen`, `Avsluta utan att spara`, `Ta bort`, `Tillbaka till planen`).

---

## Revision History

| Date | Change | Reason |
|------|--------|--------|
| 2026-05-12 | Initial UI-SPEC | gsd-ui-researcher draft |
| 2026-05-12 | D4 Typography fix: dropped `text-lg` (18px) entirely; numeric scan-affordance now via `font-semibold` weight contrast at `text-base` (16px). Sizes reduced from 5 (14/16/18/24/30) to 4 (14/16/24/30). | UI-checker BLOCK on Dimension 4 — declared sizes count, not roles named; 4-size contract violated. |
| 2026-05-12 | D5 Spacing fix: replaced `py-0.5` (2px) with `py-1` (4px) on card-header chip shells (target / equipment / counter). All `*-0.5` Tailwind tokens forbidden in Phase 5 per the 4-pt grid declaration. | UI-checker BLOCK on Dimension 5 — 2px not on the declared 4 / 8 / 16 / 24 / 32 / 48 / 64 scale. |
| 2026-05-12 | Added "Accepted FLAGs" section documenting four single-word Swedish CTA labels (Klart, Avsluta, Återuppta, Fortsätt) as intentional space- and idiom-constrained choices. | UI-checker FLAG on Dimension 1 — labels acknowledged as non-blocking; documenting for next-checker-run continuity. |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
