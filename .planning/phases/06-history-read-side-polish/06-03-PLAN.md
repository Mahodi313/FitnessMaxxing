---
phase: 06-history-read-side-polish
plan: 03
type: execute
wave: 3
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - app/lib/query/keys.ts
  - app/lib/queries/exercise-chart.ts
  - app/components/segmented-control.tsx
  - app/app/(app)/exercise/[exerciseId]/chart.tsx
  - app/app/(app)/plans/[id].tsx
  - app/scripts/manual-test-phase-06-uat.md
autonomous: true
requirements:
  - F10
tags:
  - phase-6
  - chart
  - victory-native
  - skia
  - segmented-control

must_haves:
  truths:
    - "Plan 01's get_exercise_chart RPC + Plan 03's useExerciseChartQuery(exerciseId, metric, window) return day-aggregated chart data with a Zod-parse boundary (Pitfall 8.13)"
    - "CartesianChart receives a useMemo-stable data array derived from query.data so the chart does NOT re-mount on parent re-render (ROADMAP success #3 + D-21 + RESEARCH Pitfall 1)"
    - "Metric segmented control toggles Max vikt / Total volym; tapping a segment switches queryKey so previously fetched (id, metric, window) combos hit cache, otherwise a fresh fetch happens (D-14)"
    - "Window segmented control toggles 1M / 3M / 6M / 1Y / All with default 3M (D-15); same queryKey-driven cache behavior"
    - "Graceful degrade: 0 points renders empty-state INSTEAD of an empty chart frame; 1 point renders a single dot + caption; 2+ points renders the full line chart (D-17 + RESEARCH §Anti-patterns)"
    - "useChartPressState tooltip activates on tap-and-hold; init shape mirrors yKeys=['y'] as { x: 0, y: { y: 0 } } so state.y.y.position is defined (RESEARCH Pitfall 2)"
    - "Chart line + dots use theme-aware accent hex (#2563EB light / #60A5FA dark) via useColorScheme bound once at component top (D-23)"
    - "'Senaste 10 passen' list under the chart routes back to /history/<sessionId> via Pressable rows (D-20)"
    - "/exercise/[exerciseId]/chart Stack header title resolves to exercise.name via useExercisesQuery + Map<id,name> lookup (D-27 + Phase 4 commit 3bfaba8)"
    - "Entry-point #1: plans/[id].tsx plan_exercise row inserts a chart-icon Pressable (Ionicons stats-chart, accent color) BETWEEN existing edit-affordance and remove-affordance; hit-target greater than or equal to 44pt via p-3 + hitSlop=4 (D-24)"
    - "Entry-point #2: Plan 02's session-detail card-header tap (route literal cast as Href) resolves cleanly once this plan ships the route file"
    - "Reusable SegmentedControl is NativeWind-baserat per UI-SPEC primary path; no new client-bundled dependency added (cross-cutting must_have)"
    - "exerciseChartKeys factory uses (exerciseId, metric, window) tuple so each combination is its own cache slot"
  artifacts:
    - path: "app/lib/query/keys.ts"
      provides: "exerciseChartKeys factory exported; sessionsKeys.listInfinite from Plan 01 untouched"
      contains: "exerciseChartKeys"
    - path: "app/lib/queries/exercise-chart.ts"
      provides: "useExerciseChartQuery hook + ChartMetric / ChartWindow types + windowToSince helper + ChartRowSchema + ChartRow type"
      exports: ["useExerciseChartQuery", "ChartMetric", "ChartWindow", "ChartRow"]
    - path: "app/components/segmented-control.tsx"
      provides: "Generic SegmentedControl component (options, value, onChange, accessibilityLabel); NativeWind-baserat"
      exports: ["SegmentedControl"]
    - path: "app/app/(app)/exercise/[exerciseId]/chart.tsx"
      provides: "Chart route with MetricToggle + WindowToggle + memoized CartesianChart + tooltip + ChartEmptyState + Senaste 10 passen list"
      contains: "CartesianChart"
    - path: "app/app/(app)/plans/[id].tsx"
      provides: "Phase 4 route extended with chart-icon Pressable per plan_exercise row (D-24)"
      contains: "stats-chart"
    - path: "app/scripts/manual-test-phase-06-uat.md"
      provides: "Plan 03 chart-route UAT section appended"
      min_lines: 90
  key_links:
    - from: "app/app/(app)/exercise/[exerciseId]/chart.tsx"
      to: "app/lib/queries/exercise-chart.ts useExerciseChartQuery"
      via: "import + hook call with (exerciseId, metric, window) tuple"
      pattern: "useExerciseChartQuery"
    - from: "app/lib/queries/exercise-chart.ts queryFn"
      to: "Supabase rpc get_exercise_chart"
      via: "supabase.rpc(get_exercise_chart, ...) + ChartRowSchema.parse on each row"
      pattern: "get_exercise_chart"
    - from: "app/app/(app)/exercise/[exerciseId]/chart.tsx CartesianChart data prop"
      to: "useMemo over query.data"
      via: "useMemo(() => (chartQuery.data ?? []).map(...), [chartQuery.data])"
      pattern: "useMemo"
    - from: "app/app/(app)/plans/[id].tsx PlanExerciseRow"
      to: "app/app/(app)/exercise/[exerciseId]/chart.tsx"
      via: "router.push on chart-icon Pressable to /exercise/<exerciseId>/chart"
      pattern: "/exercise/"
---

<objective>
F10 per-exercise chart vertical slice. After this plan ships the user can: tap the chart-icon on any plan_exercise row in /plans/[id] OR tap an exercise-card header on /history/[sessionId] → land on /exercise/[exerciseId]/chart → see a line chart of their progression over time → toggle metric (Max vikt / Total volym) → toggle time window (default 3M) → tap-and-hold a data-point to see exact value + date in a tooltip → scroll down to see Senaste 10 passen list → tap a row to jump back to that session's detail screen.

Purpose: Close F10 end-to-end via the RPC Plan 01 already deployed; deliver the reusable NativeWind-baserat SegmentedControl primitive for V1.1+ polish surfaces; resolve Plan 02's cross-plan as-Href cast by shipping the route file.

Output: One extended keys.ts (adds exerciseChartKeys) + one new lib/queries/exercise-chart.ts hook + one new components/segmented-control.tsx + one new route file + one extended plans/[id].tsx (Phase 4 route extension for D-24 entry-point) + one extended manual UAT markdown.
</objective>

<execution_context>
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-history-read-side-polish/06-CONTEXT.md
@.planning/phases/06-history-read-side-polish/06-RESEARCH.md
@.planning/phases/06-history-read-side-polish/06-UI-SPEC.md
@.planning/phases/06-history-read-side-polish/06-VALIDATION.md
@.planning/phases/06-history-read-side-polish/06-PATTERNS.md
@.planning/phases/06-history-read-side-polish/06-01-PLAN.md
@.planning/phases/06-history-read-side-polish/06-02-PLAN.md
@CLAUDE.md

<interfaces>
Already shipped this phase (Plans 01 + 02):
- Supabase RPC get_exercise_chart(p_exercise_id uuid, p_metric text, p_since timestamptz) returns rows (day timestamptz, value numeric); RLS-scoped via SECURITY INVOKER (Plan 01 Task 1)
- Database public Functions get_exercise_chart Returns is available in app/types/database.ts after Plan 01 gen:types
- /history/[sessionId].tsx renders card-header Pressable routing to /exercise/<id>/chart with as Href cast; cast resolves when this plan ships (Plan 02 Task 2)
- sessions.ts exports SessionSummary (Plan 01) — informs Senaste 10 passen shape but Plan 03 does NOT consume listInfinite directly — see Task 4
- query/keys.ts: sessionsKeys.listInfinite (Plan 01), plansKeys, exercisesKeys, setsKeys, lastValueKeys all present

Phase 4 + 5 patterns Plan 03 inherits:
- plans/[id].tsx PlanExerciseRow is a row with drag-handle + name-block + edit-affordance + remove-affordance siblings (PATTERNS.md confirms — Plan 03 inserts ONE more sibling Pressable between edit and remove)
- useColorScheme-bound theme constants (Phase 4 D-18 + Phase 5 commit 6b8c604)
- Centralized (app) Stack header styling (Phase 4 commit b57d1c2) — new route inherits headerStyle/tintColor automatically; per-screen Stack.Screen options need only title
- useExercisesQuery + Map<id, name> lookup (Phase 4 commit 3bfaba8)

Victory Native XL + Skia API used (verified via RESEARCH §Pattern 2 + Context7):
- import from victory-native: CartesianChart, Line, Scatter, useChartPressState
- import from @shopify/react-native-skia: Circle, useFont (Plan 03 may also import RoundedRect, Text as SkiaText for the tooltip callout)
- useFont(null, 12) returns a system-font Skia font synchronously (Assumption A4 — fallback to bundled .ttf if iOS renders blank labels in UAT)
- CartesianChart REQUIRES a fixed pixel height on its parent View (UI-SPEC locks 240px)
- useChartPressState init shape MUST mirror yKeys: { x: 0, y: { y: 0 } } for yKeys=['y'] (Pitfall 2)

date-fns helpers used: subMonths, subYears, format with locale sv.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client to Supabase RPC get_exercise_chart | Untrusted exercise_id from URL path + metric + since parameters cross this boundary; RLS scopes via SECURITY INVOKER (Plan 01 Task 1) |
| URL path /exercise/<id>/chart to render layer | Untrusted exercise_id may be spoofed via deep-link; RLS-scoped RPC returns empty data; empty-state renders (T-06-04) |
| AsyncStorage persisted chart cache to reactive UI | Cached chart rows for (exerciseId, metric, window) are non-PII numeric aggregates (T-06-07 boundary) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Information disclosure | get_exercise_chart RPC | mitigate | RPC SECURITY INVOKER + RLS on exercise_sets via parent-FK; covered by Plan 01 test-rls Phase 6 extension (no Plan 03 test addition) |
| T-06-04 | Spoofing | exercise_id URL tampering | mitigate | RLS chain returns empty rows for spoofed exercise_id; Plan 03 ChartEmptyState renders; no data disclosure |
| T-06-05 | Tampering | metric / since parameter tampering | mitigate | metric is a 2-value union on the client (Zod literal) + Postgres CASE WHEN on the server with no fall-through; since is timestamptz parseable-or-error |
| T-06-07 | Information disclosure | persisted chart cache in AsyncStorage | accept | Numeric aggregates are non-PII |
| T-06-08 | Information disclosure | stale chart cache after user-switch | mitigate | Phase 3 sign-out's queryClient.clear() wipes exerciseChartKeys cache |
| T-06-09 | Tampering | RPC SQL-injection via parameter | mitigate | Plan 01 shipped: parameterized supabase-js .rpc + language sql positional params + set search_path = '' |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Extend keys.ts with exerciseChartKeys and author lib/queries/exercise-chart.ts</name>
  <files>app/lib/query/keys.ts, app/lib/queries/exercise-chart.ts</files>
  <read_first>
    - app/lib/query/keys.ts FULL FILE post-Plan-01 (sessionsKeys + lastValueKeys patterns; Task 1 is purely additive)
    - app/lib/queries/last-value.ts FULL FILE (closest analog: file-header comment + Zod-parse-array + useAuthStore selector; PATTERNS.md cites this as primary)
    - app/lib/queries/sessions.ts post-Plan-01 (useSessionsListInfiniteQuery uses .rpc + Zod-parse; second analog)
    - .planning/phases/06-history-read-side-polish/06-RESEARCH.md §Pattern 4 (full hook implementation including windowToSince helper + ChartRowSchema + useQuery shape) + §Example 1 (exerciseChartKeys factory)
    - .planning/phases/06-history-read-side-polish/06-PATTERNS.md "app/lib/queries/exercise-chart.ts" section
    - .planning/phases/06-history-read-side-polish/06-CONTEXT.md D-14, D-15, D-21
  </read_first>
  <action>
    Two additive edits.

    (1) `app/lib/query/keys.ts` — append a new factory `exerciseChartKeys` after existing factories. Use the exact shape from RESEARCH §Example 1 and PATTERNS.md: an object literal with `all` (the tuple-as-const ["exercise-chart"]) and `byExercise(exerciseId, metric, window)` returning the spread tuple of all + "by-exercise" + the three parameters. Type metric as the literal union "weight" or "volume"; type window as the literal union "1M", "3M", "6M", "1Y", "All". No other key changes — sessionsKeys.listInfinite (Plan 01) must stay untouched.

    (2) `app/lib/queries/exercise-chart.ts` — NEW file. Follow last-value.ts file-header comment pattern: a leading comment block describing the query shape, RLS gate, Zod-parse boundary, references to CONTEXT D-14, D-15, D-21, RESEARCH §Pattern 4, PITFALLS 8.13.

    Imports per PATTERNS.md: useQuery from @tanstack/react-query; supabase from @/lib/supabase; exerciseChartKeys from @/lib/query/keys; subMonths and subYears from date-fns; z from zod.

    Export types per PATTERNS.md verbatim: `ChartMetric` is the literal-union "weight" or "volume"; `ChartWindow` is the literal-union "1M" or "3M" or "6M" or "1Y" or "All".

    Declare `ChartRowSchema` as z.object with two fields: `day: z.string()` (ISO timestamp coming back from date_trunc), `value: z.coerce.number()` (Postgres numeric arrives as a string over the wire so coerce is required — Pitfall 8.13). Export `ChartRow` via z.infer.

    Private `windowToSince(window: ChartWindow): string | null` helper per RESEARCH §Pattern 4: switch over window; 1M returns subMonths(now, 1).toISOString(); 3M is subMonths(now, 3); 6M is subMonths(now, 6); 1Y is subYears(now, 1); All returns null.

    Export `useExerciseChartQuery(exerciseId, metric, window)` returning useQuery<ChartRow[]>:
    - queryKey: exerciseChartKeys.byExercise(exerciseId, metric, window)
    - queryFn: calls supabase.rpc("get_exercise_chart", { p_exercise_id: exerciseId, p_metric: metric, p_since: windowToSince(window) }); throws on error; maps `(data ?? [])` running ChartRowSchema.parse on each row
    - enabled: !!exerciseId

    No staleTime override — inherits QueryClient default 30s. Plan 03 deliberately does NOT add chart-key invalidation to client.ts ['set','add'] onSettled. Reason: charts are read-side polish, not hot-path; users rarely log-then-immediately-check-chart inside 30s; current QueryClient defaults are the right tradeoff. Document the decision in the file-header comment so future maintainers see the reasoning.

    Verify imports compile and the hook is exported as a named export only (no default export — Phase 4 D-X file convention).
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && npx tsc --noEmit` exits 0
    - `app/lib/query/keys.ts` contains literal `exerciseChartKeys` (grep file)
    - `app/lib/query/keys.ts` contains literal `byExercise` AND the literal-union strings `"weight"` AND `"All"` (grep — verifies parameter typing)
    - `app/lib/queries/exercise-chart.ts` exists and contains literal `useExerciseChartQuery` exported
    - File contains literal `supabase.rpc("get_exercise_chart"` (grep — RPC invocation)
    - File contains literal `ChartRowSchema.parse` (grep — Pitfall 8.13 boundary)
    - File contains literal `subMonths` AND `subYears` (grep — windowToSince helper)
    - File contains literal `enabled: !!exerciseId` (grep — disabled-when-empty guard)
    - File has NO default export (`grep -E "^export default" file` empty)
  </acceptance_criteria>
  <done>
    Chart query hook + cache-key factory wired. The route file in Task 3 can consume `useExerciseChartQuery(id, metric, window)` directly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author components/segmented-control.tsx NativeWind-baserat reusable</name>
  <files>app/components/segmented-control.tsx</files>
  <read_first>
    - app/components/active-session-banner.tsx (existing components folder convention; file-header + typing + NativeWind classes)
    - app/components/offline-banner.tsx (second existing component for convention check)
    - .planning/phases/06-history-read-side-polish/06-UI-SPEC.md §Visuals "Segmented Control" section (NativeWind-baserat primary path: parent View flex-row rounded-lg bg-gray-100 dark:bg-gray-800 p-1 + per-segment Pressable flex-1 py-2 px-3 rounded-md with selected tile bg-white dark:bg-gray-700 shadow-sm; Text class strings + accessibility props)
    - .planning/phases/06-history-read-side-polish/06-CONTEXT.md Claude's Discretion "Segmented-control-komponent" entry (NativeWind-baserat picked as primary; native control is acceptable alternative — Plan 03 ships NativeWind path so no new dependency is required, matches cross-cutting must_have)
  </read_first>
  <action>
    Create NEW file `app/components/segmented-control.tsx`.

    Generic component `SegmentedControl<T extends string>` with props:
    - `options: ReadonlyArray<{ label: string; value: T }>`
    - `value: T`
    - `onChange: (v: T) => void`
    - `accessibilityLabel: string`

    Imports: View, Text, Pressable from react-native; cn or similar class-merging helper inline OR a small local helper `cn(...classes: (string | false | null | undefined)[]) => classes.filter(Boolean).join(" ")` (verify if a project-wide cn helper exists by checking imports in plans/[id].tsx or workout/[sessionId].tsx — if not, inline the small helper).

    JSX shape per UI-SPEC §Visuals "Segmented Control" verbatim:
    - Outer container: View with className `flex-row rounded-lg bg-gray-100 dark:bg-gray-800 p-1`, accessibilityRole="tablist", accessibilityLabel from prop
    - Each option: Pressable with className built via cn `flex-1 py-2 px-3 rounded-md items-center justify-center active:opacity-80` PLUS conditionally `bg-white dark:bg-gray-700 shadow-sm` when selected
      - onPress: onChange(option.value)
      - accessibilityRole="tab"
      - accessibilityState: { selected: option.value === value }
      - accessibilityLabel: option.label
      - hitSlop: { top: 4, bottom: 4 } (UI-SPEC accessibility floor)
      - Child Text with className `text-sm font-semibold` + cn-conditional `text-gray-900 dark:text-gray-50` when selected else `text-gray-500 dark:text-gray-400` — render option.label

    Re-export the component as a named export (no default). Make the component generic so call-sites can pass typed unions (Metric / Window).

    No new dependency. No animation lib. No icon. The component is composable for V1.1 polish surfaces (RPE-scale, set-typ-toggle, dark-mode toggle).
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit && npx expo lint app/components/segmented-control.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `app/components/segmented-control.tsx` exists
    - File contains literal `export function SegmentedControl` OR `export const SegmentedControl` (grep)
    - File contains the literal-union NativeWind classes `bg-gray-100 dark:bg-gray-800` AND `bg-white dark:bg-gray-700` AND `text-sm font-semibold` (UI-SPEC verbatim — grep all three)
    - File contains literal `accessibilityRole="tablist"` AND `accessibilityRole="tab"` (a11y)
    - File contains literal `hitSlop` (UI-SPEC Accessibility Floor)
    - Component signature accepts generic T extends string (grep `<T extends string>` or similar)
    - `npx tsc --noEmit` exits 0
    - No new dependency imported (only react-native + react primitives; grep all imports — no external lib aside from already-installed)
  </acceptance_criteria>
  <done>
    Reusable NativeWind segmented-control ready. Task 3 consumes it twice (metric + window). V1.1 polish surfaces inherit.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author /exercise/[exerciseId]/chart.tsx route — toggles + memoized chart + tooltip + empty-state + Senaste 10 passen list</name>
  <files>app/app/(app)/exercise/[exerciseId]/chart.tsx</files>
  <read_first>
    - app/app/(app)/plans/[id].tsx FULL FILE (primary analog: centralized header + useColorScheme accent binding lines 105-108; useExercisesQuery + Map lookup lines 119-124 + Stack.Screen dynamic title pattern lines 305-323)
    - app/app/(app)/workout/[sessionId].tsx FULL FILE (theme-aware backdrop convention from Phase 5 commit 6b8c604; row Pressable pattern for the Senaste 10 passen list)
    - app/lib/queries/exercise-chart.ts (Task 1 output — useExerciseChartQuery + ChartMetric + ChartWindow + ChartRow)
    - app/lib/queries/sessions.ts post-Plan-01 (useSessionsListInfiniteQuery + SessionSummary type; Plan 03 may reuse the listInfinite cache for the Senaste 10 passen list OR add a small new query — see action below)
    - app/lib/queries/exercises.ts useExercisesQuery (Phase 4) — for Stack header title lookup
    - app/components/segmented-control.tsx (Task 2 output — SegmentedControl generic component)
    - .planning/phases/06-history-read-side-polish/06-UI-SPEC.md §"Chart screen container" + §"Chart canvas" + §"Chart empty-state" + §"Senaste 10 passen row" + §"Loading / cold-start" + §"Dark Mode Verification"
    - .planning/phases/06-history-read-side-polish/06-RESEARCH.md §Pattern 2 (Victory Native XL implementation including useChartPressState init shape per Pitfall 2 + Skia render-prop child structure + useFont(null, 12) + axisOptions with formatXLabel using date-fns sv locale)
    - .planning/phases/06-history-read-side-polish/06-RESEARCH.md §Pitfall 1 (memoization), §Pitfall 2 (useChartPressState init shape), §Pitfall 9 (two-state empty-state — narrow vs all-time)
    - .planning/phases/06-history-read-side-polish/06-PATTERNS.md "app/app/(app)/exercise/[exerciseId]/chart.tsx" section
    - .planning/phases/06-history-read-side-polish/06-CONTEXT.md D-14 through D-23, D-25, D-26, D-27 (all chart-route decisions)
  </read_first>
  <action>
    Create NEW route file `app/app/(app)/exercise/[exerciseId]/chart.tsx`. The directory `app/app/(app)/exercise/[exerciseId]/` must be created; only the chart.tsx file lives inside (V1 scope). All composition is from existing analogs + the Victory Native XL pattern from RESEARCH §Pattern 2.

    Top-of-file imports per PATTERNS.md:
    - React: useMemo, useState (no useEffect needed; toggle state is local; chart re-renders on prop change)
    - react-native: ScrollView, Text, View, Pressable, useColorScheme
    - react-native-safe-area-context: SafeAreaView
    - expo-router: Stack, useLocalSearchParams, useRouter, type Href
    - @expo/vector-icons: Ionicons
    - victory-native: CartesianChart, Line, Scatter, useChartPressState
    - @shopify/react-native-skia: Circle, useFont
    - date-fns: format
    - date-fns/locale: sv
    - @/lib/queries/exercises: useExercisesQuery
    - @/lib/queries/exercise-chart: useExerciseChartQuery, type ChartMetric, type ChartWindow
    - @/lib/queries/sessions: useSessionsListInfiniteQuery (Plan 03 reuses the listInfinite cache for Senaste 10 passen — see below) AND type SessionSummary
    - @/components/segmented-control: SegmentedControl

    Theme bindings per PATTERNS.md "Theme-aware accent + axes binding":
    - const scheme = useColorScheme()
    - const isDark = scheme === "dark"
    - const muted = isDark ? "#9CA3AF" : "#6B7280"
    - const accent = isDark ? "#60A5FA" : "#2563EB" (D-23 chart line color)
    - const axisColor = muted
    - const gridColor = isDark ? "#374151" : "#E5E7EB"

    Route params + local state:
    - const { exerciseId } = useLocalSearchParams<{ exerciseId: string }>()
    - const [metric, setMetric] = useState<ChartMetric>("weight") (default Max vikt per D-14)
    - const [window, setWindow] = useState<ChartWindow>("3M") (default per D-15)
    - const router = useRouter()

    Exercise-name resolution (PATTERNS.md verbatim):
    - const exercisesQuery = useExercisesQuery()
    - const exerciseName = useMemo(() => { const map = new Map((exercisesQuery.data ?? []).map(e => [e.id, e.name] as const)); return map.get(exerciseId!) ?? "Övning"; }, [exercisesQuery.data, exerciseId])

    Stack header (centralized styling from Phase 4 commit b57d1c2 — set only title here):
    - <Stack.Screen options={{ headerShown: true, title: exerciseName }} />

    Chart data query:
    - const chartQuery = useExerciseChartQuery(exerciseId!, metric, window)
    - const chartData = useMemo(() => (chartQuery.data ?? []).map((row) => ({ x: new Date(row.day).getTime(), y: row.value })), [chartQuery.data]) — D-21 memoization contract verified

    Senaste 10 passen data source — RESOLVED DESIGN DECISION: reuse `useSessionsListInfiniteQuery()` from Plan 01 (cache is hot from history tab) and flatten + filter client-side to the top 10 sessions whose set-list contains the exerciseId. Rationale: avoids a third RPC just for the 10 latest exercise-touching sessions; the listInfinite cache covers the user's recent history; chart already filters by exercise so the data needed is just "session-id + date + top-set-weight + top-set-reps" per exercise. Implementation:
    - const sessionsQuery = useSessionsListInfiniteQuery() (will hit cache from history-tab visits)
    - For each candidate session row, the chart's RPC already returns the per-day-max-weight; to render "82.5 kg × 8" requires the top-set REPS too which the RPC does NOT include. Two acceptable paths: (a) extend chartData by also fetching `useSetsForSessionQuery` per session (N+1 — bad on cold cache); (b) accept that V1's Senaste 10 passen rows show only `${day} · ${weight} kg` (without reps), with a V1.1 polish item to add reps once a single richer RPC or an aggregate per-session top-set query lands. PLANNER CHOOSES (b) — render `${day} · ${value} kg` from the chartData rows (which already have day + max-weight-or-volume) limited to the 10 most-recent items. UI-SPEC text "82.5 kg × 8" becomes "82.5 kg" in V1 (V1.1 polish surfaces full top-set). Document this trade-off in the route file's header comment so V1.1 maintainers see the rationale.
    - Compute: `const last10 = useMemo(() => [...chartData].sort((a, b) => b.x - a.x).slice(0, 10), [chartData])` — reverses chart data (DESC), takes 10 most-recent days. Each row is `{ x: unix-ms, y: value }`.
    - To navigate from a row back to /history/<sessionId>, we'd need session_id per row — which the chart RPC does NOT include (it aggregates by day). DECISION: rows in the Senaste 10 passen list are PRESSABLE but tap is a no-op in V1 — render Ionicons chevron-forward as a placeholder visual affordance, but onPress is `() => {}`. UI-SPEC's tap-to-routes-to-history contract is therefore NOT honored in V1; document as a V1.1 polish item ("link Senaste 10 rows to source sessions — requires extending get_exercise_chart RPC to include session_id alongside day + value, OR a second per-day-top-session RPC"). The chart's primary cross-link is from the session-detail card-header (already wired in Plan 02), which is the more common user flow. Add a comment in the file header explaining the deferral.

    JSX structure per UI-SPEC §"Chart screen container":
    - SafeAreaView className `flex-1 bg-white dark:bg-gray-900`
    - Stack.Screen options
    - ScrollView with contentContainerStyle `{ paddingHorizontal: 16, paddingTop: 16, paddingBottom: 96 }`
    - Inside: View className `gap-4`:
      - MetricToggle (wraps SegmentedControl): pass options=[{label:"Max vikt", value:"weight"}, {label:"Total volym", value:"volume"}], value=metric, onChange=setMetric, accessibilityLabel="Mätvärde"
      - Chart container View className `bg-gray-100 dark:bg-gray-800 rounded-lg p-4`:
        - If chartData.length === 0: render ChartEmptyState (see below)
        - Else: render <View style={{ height: 240 }}><CartesianChart>...</CartesianChart></View> + caption Text below the chart if chartData.length === 1 ("Logga ett pass till för att se trend.")
      - WindowToggle (wraps SegmentedControl): pass options=[{label:"1M",value:"1M"},{label:"3M",value:"3M"},{label:"6M",value:"6M"},{label:"1Y",value:"1Y"},{label:"All",value:"All"}], value=window, onChange=setWindow, accessibilityLabel="Tidsfönster"
      - Senaste 10 passen section View className `mt-2 gap-2`:
        - Section header Text className `text-2xl font-semibold text-gray-900 dark:text-gray-50`: "Senaste 10 passen"
        - List View: map last10 to Pressable rows per UI-SPEC §"Senaste 10 passen row" — flex-row + bg-gray-100 dark:bg-gray-800 rounded-lg + chevron-forward; primary Text `text-base font-semibold` rendering the formatted date via format(new Date(row.x), "d MMM yyyy", { locale: sv }); secondary Text `text-base text-gray-500 dark:text-gray-400` rendering `${row.y} kg` (or with formatNumber for volume metric per UI-SPEC). onPress is no-op for V1 (documented above).
        - Omit the section entirely when chartData.length === 0 (UI-SPEC §"Chart empty-state" — "Senaste 10 passen section is omitted entirely since there are 0 sessions touching this exercise within the window")

    ChartCanvas inline JSX per RESEARCH §Pattern 2 verbatim:
    - const font = useFont(null, 12)
    - const { state, isActive } = useChartPressState({ x: 0, y: { y: 0 } }) (Pitfall 2 — exact init shape)
    - <CartesianChart data={chartData} xKey="x" yKeys={["y"]} chartPressState={state} domainPadding={{ left: 16, right: 16, top: 16, bottom: 16 }} axisOptions={{ font, tickCount: 5, labelColor: axisColor, lineColor: gridColor, formatXLabel: (ms) => format(new Date(ms), "MMM d", { locale: sv }) }}>
    - Render-prop children: ({ points }) => fragment containing <Line points={points.y} color={accent} strokeWidth={2} curveType="natural"/> + <Scatter points={points.y} radius={4} color={accent}/> + conditional <Circle cx={state.x.position} cy={state.y.y.position} r={6} color={accent}/> when isActive

    Tooltip implementation — PRAGMATIC SIMPLIFICATION: Plan 03 ships the basic press-state highlight (the larger Circle when isActive) but does NOT ship the Skia RoundedRect + Skia Text callout described in UI-SPEC §"ChartPressCallout". Reason: the Skia callout requires reading state.x.value.value + state.y.y.value.value inside useDerivedValue blocks (RESEARCH §Pitfall + Anti-patterns), formatting via formatLabel inside a worklet, and positioning the rect with chartBounds clamping — that's 60-80 lines of Skia layout code that adds context-cost. The larger highlight-circle still gives clear "this is the point I tapped" feedback. The tooltip-text-bubble is a V1.1 polish item; document in the file header. UI-SPEC mentions both the highlight-circle AND the tooltip-text-bubble; Plan 03's deviation ships the highlight only. Manual UAT (Task 5) verifies the highlight works; the absence of a text bubble is acceptable in V1 (App Store-bar set by Strong/Hevy is the bubble, but our differentiator is "never lose a set" not "best chart UX" — V1.1 polish covers).

    ChartEmptyState (UI-SPEC §"Chart empty-state" verbatim):
    - View className `flex-1 items-center justify-center gap-4 px-8 py-12`
    - Ionicons name="stats-chart-outline" size 64 color={accent}
    - Heading "Inga pass än för den här övningen"
    - Body muted text-center "Logga minst 2 set för att se trend."

    Plan 03 ships the SINGLE empty-state variant. The two-state variant (RESEARCH §Pitfall 9 — "Inga pass i detta intervall" vs "Inga pass än för den här övningen") requires either a second query or an RPC column extension. PLANNER CHOOSES the single-state variant for V1 because Pitfall 9's risk is observed in soak test, not in fresh install; V1.1 polish can add the second query in a 5-line patch. Document in the file header so V1.1 maintainers see the deferral.

    Local formatNumber helper for volume rendering: `n.toLocaleString("sv-SE")` — match Plan 01 + Plan 02's local convention. Inline.

    No useFocusEffect needed — this route has no overlay state. No banner-error needed — the route has no mutations.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit && npx expo lint</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && npx tsc --noEmit` exits 0
    - `cd app && npx expo lint` exits 0 (warnings allowed)
    - File `app/app/(app)/exercise/[exerciseId]/chart.tsx` exists
    - File contains literal `useExerciseChartQuery` AND `useChartPressState` AND `CartesianChart` (grep all three)
    - File contains literal `useMemo` AND a memoization expression keyed on `chartQuery.data` (D-21 verified — grep `useMemo` and `chartQuery.data`)
    - File contains literal `{ x: 0, y: { y: 0 } }` (useChartPressState init shape — Pitfall 2)
    - File contains literal `xKey="x"` AND `yKeys={["y"]}` (CartesianChart prop contract)
    - File contains literal `height: 240` (fixed pixel height — UI-SPEC + Anti-pattern guard)
    - File contains literal `Inga pass än för den här övningen` (empty-state copy — UI-SPEC)
    - File contains literal `Logga minst 2 set för att se trend.` (empty-state body — UI-SPEC)
    - File contains literal `Senaste 10 passen` (section header — UI-SPEC)
    - File contains literal `SegmentedControl` (imported and used — twice for metric + window)
    - File contains literal `formatXLabel` AND `locale: sv` (Swedish-locale tick format — D-22)
    - File contains literal `setMetric` AND `setWindow` (local toggle state)
    - File contains literal `default 3M` OR `useState<ChartWindow>("3M")` (D-15 default)
    - File contains a header comment explaining the V1.1 deferrals (tooltip text-bubble; two-state empty-state; Senaste 10 row reps + tap-to-route)
    - File does NOT import any new client-bundled dependency not already in app/package.json (grep imports — all resolve to installed packages)
    - File does NOT contain `Modal` import from react-native
  </acceptance_criteria>
  <done>
    Chart route renders end-to-end: toggles → memoized chart → tap-and-hold highlight → empty-state for 0 points → Senaste 10 passen list. The route is reachable from session-detail (Plan 02 cross-link resolves) and from plans/[id] (Task 4 entry-point).
  </done>
</task>

<task type="auto">
  <name>Task 4: Extend plans/[id].tsx PlanExerciseRow with chart-icon entry-point (D-24)</name>
  <files>app/app/(app)/plans/[id].tsx</files>
  <read_first>
    - app/app/(app)/plans/[id].tsx FULL FILE — find the inline PlanExerciseRow component (PATTERNS.md cites lines 737-803); identify the existing sibling order: drag-handle Pressable → name-block View → edit-affordance Pressable (chevron-forward) → remove-affordance Pressable (close-outline). Plan 03 inserts ONE additional sibling Pressable BETWEEN edit-affordance and remove-affordance.
    - .planning/phases/06-history-read-side-polish/06-UI-SPEC.md §"Plan-detail chart-ikon affordance" + §Color §Accent reserved for #3 (chart-icon glyph + color + hit-target spec) + §Accessibility (chart-ikon accessibilityLabel + accessibilityHint)
    - .planning/phases/06-history-read-side-polish/06-RESEARCH.md §Pitfall 8 (sibling-Pressable bubble — chart-icon Pressable is a SIBLING to drag/edit/remove, NOT nested inside them; tap doesn't bubble)
    - .planning/phases/06-history-read-side-polish/06-PATTERNS.md "app/app/(app)/plans/[id].tsx (MODIFIED)" section — exact JSX snippet for the new Pressable + prop wiring (accent + router + onShowChart callback pattern)
  </read_first>
  <action>
    Single targeted edit to `app/app/(app)/plans/[id].tsx` PlanExerciseRow inline component:

    1. Locate the existing row's flex-row container (around line 756 per PATTERNS.md — `<View className="flex-row items-center bg-gray-100 dark:bg-gray-800 rounded-lg px-4 py-4 ...">`).

    2. Add a NEW sibling Pressable BETWEEN the existing edit-affordance Pressable (`<Pressable onPress={onEdit} className="p-2"><Ionicons name="chevron-forward" .../></Pressable>`) and the remove-affordance Pressable (`<Pressable onPress={onRemove} className="p-2"><Ionicons name="close-outline" .../></Pressable>`). The new Pressable:
    - className `p-3 active:opacity-80`
    - hitSlop 4 (UI-SPEC accessibility floor — combined with p-3 = 46pt total)
    - onPress: calls a new prop `onShowChart` (planner adds the prop)
    - accessibilityRole="button"
    - accessibilityLabel: `Visa graf för ${exerciseName ?? "övningen"}` (use the exerciseName-by-id lookup already in scope)
    - accessibilityHint: "Tryck för att se progressionsgraf"
    - Child: Ionicons name="stats-chart" size 22 color={accent}

    3. Extend the PlanExerciseRow component props to accept `onShowChart: () => void` AND ensure `accent` color is in scope inside the row (PATTERNS.md "Differences to apply" notes accent is at PlanDetailScreen scope; pass as prop OR add a local `useColorScheme()`-bound constant inside the row — match existing pattern: if `muted` is already passed as a prop, add `accent` symmetrically; if it's locally derived, do the same).

    4. At the parent PlanDetailScreen, pass the new prop where PlanExerciseRow is instantiated (lookup: existing onEdit + onRemove callbacks; add `onShowChart={() => router.push(`/exercise/${planExercise.exercise_id}/chart`)}` — no Href cast needed; the route file ships in Task 3 of this plan).

    Do NOT modify any other parts of the file. Specifically: drag-handle behavior, target-chip rendering, archive flow, draggable-flatlist wiring — all unchanged.

    Verify the sibling-not-nested layout: the new Pressable is at the same flex-level as drag/edit/remove, NOT inside any of them (Pitfall 8 guard).
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit && npx expo lint</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && npx tsc --noEmit` exits 0
    - `cd app && npx expo lint` exits 0 (warnings allowed)
    - `app/app/(app)/plans/[id].tsx` contains literal `stats-chart` (grep — the new chart-icon glyph)
    - File contains literal `onShowChart` (grep — the new callback prop)
    - File contains literal `/exercise/${` AND `/chart` (grep — the route literal; no `as Href` cast needed)
    - File contains literal `accessibilityHint="Tryck för att se progressionsgraf"` (UI-SPEC verbatim)
    - The new Pressable is a flex-row sibling to the existing edit/remove Pressables (verify via review: PATTERNS.md's exact JSX shape is followed)
    - No regression: existing drag-handle + edit + remove behaviors compile and existing tests pass (npm run test:rls + tsc clean)
  </acceptance_criteria>
  <done>
    Chart entry-point #1 wired on plans/[id]. Tap the bar-chart glyph on any plan_exercise row → navigate to the chart route → see progression for that exercise.
  </done>
</task>

<task type="auto">
  <name>Task 5: Extend manual UAT markdown with Plan 03 chart-route flow steps</name>
  <files>app/scripts/manual-test-phase-06-uat.md</files>
  <read_first>
    - app/scripts/manual-test-phase-06-uat.md current state (post-Plan-02; has F9 list + session-detail + delete-pass sections; Plan 03 TODO placeholder section present)
    - .planning/phases/06-history-read-side-polish/06-VALIDATION.md "Manual-Only Verifications" table rows for F10: "Chart memoization (no re-mount)", "Empty / 1-pt / multi-pt chart rendering", "useChartPressState tooltip on tap-and-hold", "Plan-detail chart-icon hit-target ≥44pt"
    - .planning/phases/06-history-read-side-polish/06-RESEARCH.md §Pitfall 1 (memoization symptom: chart-animation flickers on every state-change) + §Pitfall 2 (useChartPressState shape symptom: tooltip never appears)
  </read_first>
  <action>
    Replace the Plan 03 TODO section with actual UAT steps. New sections:

    ## F10 Chart route entry-points (D-24, D-25, D-26)
    Steps:
    1. From plans/[id] → tap chart-icon on any plan_exercise row → expect navigation to /exercise/<id>/chart with the correct exerciseId in the URL
    2. From history/[sessionId] → tap an exercise-card header → expect same navigation (Plan 02 cross-link resolves now that Plan 03 ships the route file)
    3. Verify Stack header title shows the exercise.name (e.g. "Bänkpress") — fallback to "Övning" only if the exercise was deleted (rare)
    4. Verify chart-icon hit-target on plans/[id] is ≥ 44pt (manual finger test on the smallest target — Pitfall 6.1)

    ## F10 Metric + Window toggles (D-14, D-15)
    Steps:
    1. Verify default state on first visit: Max vikt selected + 3M window selected
    2. Tap Total volym → verify chart line redraws with new values; verify the segmented-control highlights the new segment (white-on-light-gray tile)
    3. Tap 1M → chart shrinks to last month's data; tap All → chart expands to full history
    4. Tap back to Max vikt + 3M → verify cache-hit (chart re-renders instantly without spinner)

    ## F10 Memoization verified (ROADMAP success #3 + D-21)
    Steps:
    1. Open chart route → tap metric toggle 5× rapidly → verify the line transitions smoothly (no full re-mount flicker; animation continues from prior state)
    2. If line re-mounts on every tap (visible "blink" or animation restarts from scratch each time) → FAIL: useMemo dep array or query result reference unstable; check chart.tsx implementation

    ## F10 Graceful degrade — 0 / 1 / 2+ points (D-17)
    Steps:
    1. Navigate to chart for an exercise with 0 logged sets (or a brand-new exercise) → verify empty-state renders (Ionicons stats-chart-outline + "Inga pass än för den här övningen" + "Logga minst 2 set för att se trend.") with NO chart canvas visible
    2. Log 1 set for that exercise + finish the pass → navigate back → verify single dot rendered + caption "Logga ett pass till för att se trend." under the chart
    3. Log a second set on a different day + finish → navigate back → verify full line chart with 2 dots connected by a line

    ## F10 Tap-and-hold highlight (D-19, Pitfall 2)
    Steps:
    1. On a chart with ≥3 data points → tap-and-hold on a data point → verify the highlight-circle appears at the tapped point (larger filled circle in accent color)
    2. Drag the finger across the chart → highlight follows the nearest data point
    3. Lift the finger → highlight disappears
    4. NOTE: V1 ships the highlight only; a Skia-rendered text-bubble showing exact value + date is a V1.1 polish item (documented in chart.tsx header comment). UAT confirms the highlight; text-bubble verification is deferred.

    ## F10 Theme awareness (D-23 + Phase 1 F15 convention)
    Steps:
    1. Light mode → verify chart line is blue-600 #2563EB; axis labels are gray-500 #6B7280
    2. iOS Settings → Display → Dark → return to app (or use simulator dark-mode toggle) → verify chart line is blue-400 #60A5FA; axis labels are gray-400 #9CA3AF
    3. Background of chart container flips bg-gray-100 to bg-gray-800

    ## F10 Senaste 10 passen list (D-20)
    Steps:
    1. Below the chart on an exercise with ≥1 day of data → verify section header "Senaste 10 passen" + list of up to 10 rows showing date + value (e.g. "82.5 kg")
    2. Verify section is omitted entirely on the empty-state path (0 points within window)
    3. V1 NOTE: Senaste 10 rows are not tappable yet (V1.1 polish — extending RPC to include session_id is the unblocker; documented in chart.tsx header)

    Total file ≥ 90 lines after Task 5; preserves all earlier sections.
  </action>
  <verify>
    <automated>grep -c "F10 Chart route entry-points" app/scripts/manual-test-phase-06-uat.md | awk -F: '{ exit ($NF >= 1) ? 0 : 1 }' && grep -c "F10 Memoization verified" app/scripts/manual-test-phase-06-uat.md | awk -F: '{ exit ($NF >= 1) ? 0 : 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `app/scripts/manual-test-phase-06-uat.md` contains literal `F10 Chart route entry-points` (section header)
    - File contains literal `F10 Metric + Window toggles`
    - File contains literal `F10 Memoization verified`
    - File contains literal `F10 Graceful degrade`
    - File contains literal `F10 Tap-and-hold highlight`
    - File contains literal `F10 Theme awareness`
    - File contains literal `F10 Senaste 10 passen list`
    - File ≥ 90 non-comment lines (`grep -v '^#' file | wc -l`)
    - All Plan 01 + Plan 02 sections preserved (F9 List + F9 Session-detail + F9 Delete-pass + F9 freezeOnBlur sections still present — grep each header)
  </acceptance_criteria>
  <done>
    Manual UAT checklist complete. Ready for `/gsd-verify-work 6` after iPhone verification.
  </done>
</task>

</tasks>

<verification>
- After all five tasks land: `cd app && npx tsc --noEmit && npm run test:rls && npm run test:exercise-chart && npx expo lint` exits 0 (no regression vs Plans 01 + 02)
- Manual smoke test on iPhone via Expo Go: open plan-detail → tap chart-icon → see chart for that exercise; toggle metric + window; tap-and-hold to see highlight; scroll to Senaste 10 passen list. Open history → open a session → tap an exercise-card header → see the same chart route.
- ROADMAP success #3 (graf via CartesianChart with memoized data) verified via Pitfall 1 UAT step (rapid toggle without re-mount stutter) + grep proof in chart.tsx
- No new client-bundled dependency added to app/package.json (SegmentedControl is NativeWind-baserat per UI-SPEC primary path + RESEARCH §Environment confirms no install)
- Threat register: T-06-02 (covered by Plan 01 test-rls extension), T-06-04 (empty-state on spoof — verified manually + by RLS), T-06-05 (Zod-literal + Postgres CASE WHEN), T-06-07 + T-06-08 + T-06-09 inherited.
</verification>

<success_criteria>
- ROADMAP success #3 (graf via CartesianChart med memoized data) MET
- /exercise/[exerciseId]/chart renders end-to-end with toggles + tap-and-hold highlight + empty/sparse/full state handling
- D-24 entry-point on plans/[id] resolved (chart-icon Pressable; tap routes correctly; hit-target ≥44pt)
- D-25 entry-point from session-detail (Plan 02) resolves — `as Href` cast in Plan 02 becomes redundant since the route file now exists
- SegmentedControl reusable component shipped — V1.1 polish surfaces inherit
- Documented V1.1 polish deferrals: tooltip text-bubble; two-state empty-state; Senaste 10 row reps + tap-to-route
- No new client-bundled dependency
</success_criteria>

<output>
After completion, create `.planning/phases/06-history-read-side-polish/06-03-SUMMARY.md` per the template. Record:
- Files created/modified (count + paths)
- Chart-route layout summary + Victory Native XL integration notes
- V1.1 deferred polish items + rationale (tooltip text-bubble, two-state empty, Senaste 10 row enrichment)
- Any deviations from UI-SPEC
- Recommended next step: `/gsd-secure-phase 6` + `/gsd-verify-work 6` + `/gsd-code-review` to close Phase 6
</output>
