---
phase: 06-history-read-side-polish
plan: 03
type: execute
wave: 4
depends_on:
  - "06-01a"
  - "06-01b"
  - "06-02"
files_modified:
  - app/lib/query/keys.ts
  - app/lib/queries/exercise-chart.ts
  - app/components/segmented-control.tsx
  - app/app/(app)/exercise/[exerciseId]/chart.tsx
  - app/app/(app)/plans/[id].tsx
  - app/scripts/manual-test-phase-06-uat.md
autonomous: true
requirements:
  - F10
tags:
  - phase-6
  - chart
  - victory-native
  - skia
  - segmented-control

must_haves:
  truths:
    - "Plan 06-01a's get_exercise_chart RPC + Plan 06-03's useExerciseChartQuery(exerciseId, metric, window) return day-aggregated chart data with a Zod-parse boundary (Pitfall 8.13)"
    - "Plan 06-01a's get_exercise_top_sets RPC + Plan 06-03's useExerciseTopSetsQuery(exerciseId, since, limit) return ONE row PER source session with (session_id, completed_at, weight_kg, reps); the chart-route's Senaste 10 passen list consumes this hook so each row's secondary text shows the literal `${weight_kg} kg × ${reps}` and each Pressable row's onPress routes to /history/<session_id> (D-20 — BLOCKER-2 fix)"
    - "CartesianChart renders a line chart via <CartesianChart> + <Line> per D-16 (NOT bar/area) and receives a useMemo-stable data array derived from chartQuery.data so the chart does NOT re-mount on parent re-render; the useMemo dep array is EXACTLY [chartQuery.data] (D-21 + RESEARCH Pitfall 1 + WARN-7 fix)"
    - "Metric segmented control toggles Max vikt / Total volym; tapping a segment switches queryKey so previously fetched (id, metric, window) combos hit cache (D-14)"
    - "Window segmented control toggles 1M / 3M / 6M / 1Y / All with default 3M (D-15); same queryKey-driven cache behavior. Per D-22 the chart axes use Y-axis auto-scale and X-axis date-labels capped at tickCount 5 via date-fns format"
    - "Two-state empty rendering (D-17 + UI-SPEC line 258 + RESEARCH Pitfall 9 + A2 — BLOCKER-3 fix): when chartData.length === 0 AND a SECOND `useExerciseChartQuery(id, metric, 'All')` query returns ≥1 point, the empty-state heading is 'Inga pass i detta intervall' + body 'Byt till All för att se hela historiken.'; when BOTH return 0, the heading is 'Inga pass än för den här övningen' + body 'Logga minst 2 set för att se trend.' Each Zod-parsed empty-state copy string is reachable in the file source for grep verification."
    - "1-point sparse state renders the single dot + caption 'Logga ett pass till för att se trend.' (D-17)"
    - "useChartPressState activates on tap-and-hold AND renders the FULL Skia tooltip callout per UI-SPEC §ChartPressCallout (RoundedRect + two Skia Text lines for value + date, with chartBounds clamping). NOT a placeholder highlight — the executor ships the value-line `${weight} kg` or `${formatNumber(volume)} kg` AND the date-line via Skia primitives inside the CartesianChart render-prop child (D-19 + UI-SPEC lines 555-574 + RESEARCH §Pattern 2 — BLOCKER-1 fix)"
    - "useChartPressState init shape mirrors yKeys=['y'] as { x: 0, y: { y: 0 } } so state.y.y.position is defined (RESEARCH Pitfall 2)"
    - "Chart line + dots + tooltip-text use theme-aware accent hex (#2563EB light / #60A5FA dark) via useColorScheme bound once at component top (D-23)"
    - "Per D-26 the route literal is /exercise/[exerciseId]/chart (URL-state, exercise-id in path). /exercise/[exerciseId]/chart Stack header title resolves to exercise.name via useExercisesQuery + Map<id,name> lookup (D-27 + Phase 4 commit 3bfaba8)"
    - "Entry-point #1: plans/[id].tsx plan_exercise row inserts a chart-icon Pressable (Ionicons stats-chart, accent color) BETWEEN existing edit-affordance and remove-affordance siblings (per UI-SPEC §Visuals JSX lines 671-684 — note: line 287 prose says 'rightmost' but the JSX shape locks 'between edit and remove'; deviation documented in task notes per WARN-5). hitSlop={6} per UI-SPEC line 286 + line 677 (WARN-6 fix); hit-target ≥44pt via p-3 (D-24)"
    - "Entry-point #2: Plan 06-02's session-detail card-header tap (route literal cast as Href) resolves cleanly once this plan ships the route file"
    - "Reusable SegmentedControl is NativeWind-baserat per UI-SPEC primary path; no new client-bundled dependency added (cross-cutting must_have)"
    - "exerciseChartKeys factory uses (exerciseId, metric, window) tuple so each combination is its own cache slot; exerciseTopSetsKeys factory uses (exerciseId, window) tuple"
    - "Informational -- per D-28 the workout-hot-path chart-icon (mid-pass chart access) is DEFERRED to V1.1; covered by absence (no chart-icon is added inside the active workout screen by this plan)"
  artifacts:
    - path: "app/lib/query/keys.ts"
      provides: "exerciseChartKeys factory + exerciseTopSetsKeys factory exported; sessionsKeys.listInfinite from Plan 06-01b untouched"
      contains: "exerciseChartKeys"
    - path: "app/lib/queries/exercise-chart.ts"
      provides: "useExerciseChartQuery hook + useExerciseTopSetsQuery hook + ChartMetric / ChartWindow types + windowToSince helper + ChartRowSchema + ChartRow type + TopSetRowSchema + TopSetRow type"
      exports: ["useExerciseChartQuery", "useExerciseTopSetsQuery", "ChartMetric", "ChartWindow", "ChartRow", "TopSetRow"]
    - path: "app/components/segmented-control.tsx"
      provides: "Generic SegmentedControl<T extends string> component (options, value, onChange, accessibilityLabel); NativeWind-baserat"
      exports: ["SegmentedControl"]
    - path: "app/app/(app)/exercise/[exerciseId]/chart.tsx"
      provides: "Chart route with MetricToggle + WindowToggle + memoized CartesianChart + Skia tooltip callout + two-state ChartEmptyState + tappable Senaste 10 passen list"
      contains: "CartesianChart"
      min_lines: 150
    - path: "app/app/(app)/plans/[id].tsx"
      provides: "Phase 4 route extended with chart-icon Pressable per plan_exercise row (D-24); hitSlop={6} per UI-SPEC"
      contains: "stats-chart"
    - path: "app/scripts/manual-test-phase-06-uat.md"
      provides: "Plan 03 chart-route UAT section appended"
      min_lines: 90
  key_links:
    - from: "app/app/(app)/exercise/[exerciseId]/chart.tsx"
      to: "app/lib/queries/exercise-chart.ts useExerciseChartQuery"
      via: "import + hook call with (exerciseId, metric, window) tuple"
      pattern: "useExerciseChartQuery"
    - from: "app/app/(app)/exercise/[exerciseId]/chart.tsx Senaste 10 passen list"
      to: "app/lib/queries/exercise-chart.ts useExerciseTopSetsQuery"
      via: "import + hook call mapped to Pressable rows"
      pattern: "useExerciseTopSetsQuery"
    - from: "app/lib/queries/exercise-chart.ts queryFn"
      to: "Supabase rpc get_exercise_chart + get_exercise_top_sets"
      via: "supabase.rpc(...) + ChartRowSchema.parse / TopSetRowSchema.parse on each row"
      pattern: "get_exercise_chart|get_exercise_top_sets"
    - from: "app/app/(app)/exercise/[exerciseId]/chart.tsx CartesianChart data prop"
      to: "useMemo over chartQuery.data with dep array EXACTLY [chartQuery.data]"
      via: "useMemo(() => (chartQuery.data ?? []).map(...), [chartQuery.data])"
      pattern: "useMemo"
    - from: "app/app/(app)/exercise/[exerciseId]/chart.tsx Senaste 10 passen row onPress"
      to: "/history/[sessionId] route (Plan 06-02 owns)"
      via: "router.push(`/history/${row.session_id}`)"
      pattern: "router\\.push\\(`/history/\\$\\{"
    - from: "app/app/(app)/plans/[id].tsx PlanExerciseRow chart-icon Pressable"
      to: "app/app/(app)/exercise/[exerciseId]/chart.tsx"
      via: "router.push on chart-icon Pressable to /exercise/<exerciseId>/chart"
      pattern: "/exercise/"
---

<objective>
F10 per-exercise chart vertical slice. After this plan ships the user can: tap the chart-icon on any plan_exercise row in /plans/[id] OR tap an exercise-card header on /history/[sessionId] → land on /exercise/[exerciseId]/chart → see a line chart of their progression over time → toggle metric (Max vikt / Total volym) → toggle time window (default 3M) → tap-and-hold a data-point to see exact value + date in a Skia-rendered tooltip → scroll down to see Senaste 10 passen list → tap a row to jump back to that session's detail screen.

Purpose: Close F10 end-to-end via the TWO RPCs Plan 06-01a deployed (`get_exercise_chart` + `get_exercise_top_sets`). The dual-RPC design closes BLOCKER-2 (Senaste 10 rows route to source sessions with reps preserved) and the two-state empty-state via a second `useExerciseChartQuery` call closes BLOCKER-3. The full Skia tooltip callout closes BLOCKER-1. Deliver the reusable NativeWind SegmentedControl primitive for V1.1+ polish surfaces; resolve Plan 06-02's cross-plan as-Href cast by shipping the route file.

Output: One extended keys.ts (adds exerciseChartKeys + exerciseTopSetsKeys) + one new lib/queries/exercise-chart.ts hook file (two hooks + types) + one new components/segmented-control.tsx + one new route file + one extended plans/[id].tsx (D-24 entry-point) + one extended manual UAT markdown.
</objective>

<execution_context>
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-history-read-side-polish/06-CONTEXT.md
@.planning/phases/06-history-read-side-polish/06-RESEARCH.md
@.planning/phases/06-history-read-side-polish/06-UI-SPEC.md
@.planning/phases/06-history-read-side-polish/06-VALIDATION.md
@.planning/phases/06-history-read-side-polish/06-PATTERNS.md
@.planning/phases/06-history-read-side-polish/06-01a-PLAN.md
@.planning/phases/06-history-read-side-polish/06-01b-PLAN.md
@.planning/phases/06-history-read-side-polish/06-02-PLAN.md
@CLAUDE.md

<interfaces>
Already shipped this phase (Plans 06-01a, 06-01b, 06-02):
- Supabase RPC `get_exercise_chart(p_exercise_id uuid, p_metric text, p_since timestamptz)` returns rows (day timestamptz, value numeric); RLS-scoped (Plan 06-01a Task 1)
- Supabase RPC `get_exercise_top_sets(p_exercise_id uuid, p_since timestamptz, p_limit int default 10)` returns rows (session_id uuid, completed_at timestamptz, weight_kg numeric, reps int); RLS-scoped (Plan 06-01a Task 1)
- `Database['public']['Functions']['get_exercise_chart']['Returns']` AND `get_exercise_top_sets['Returns']` are emitted in app/types/database.ts
- `/history/[sessionId].tsx` renders card-header Pressable routing to `/exercise/<id>/chart` with `as Href` cast (Plan 06-02 Task 2); cast resolves when THIS plan ships
- sessions.ts exports SessionSummary (Plan 06-01b) — Plan 06-03 does NOT consume listInfinite directly anymore (BLOCKER-2 fix uses the new top-sets RPC instead)
- query/keys.ts: sessionsKeys.listInfinite (Plan 06-01b); plansKeys, exercisesKeys, setsKeys, lastValueKeys all present

Phase 4 + 5 patterns Plan 06-03 inherits:
- plans/[id].tsx PlanExerciseRow row sibling structure: drag-handle + name-block + edit-affordance + remove-affordance
- useColorScheme-bound theme constants (Phase 4 D-18 + Phase 5 commit 6b8c604)
- Centralized (app) Stack header-styling (Phase 4 commit b57d1c2)
- useExercisesQuery + Map<id, name> lookup (Phase 4 commit 3bfaba8)

Victory Native XL + Skia API used (verified via RESEARCH §Pattern 2 + Context7):
- import from victory-native: CartesianChart, Line, Scatter, useChartPressState
- import from @shopify/react-native-skia: Circle, RoundedRect, Text as SkiaText, useFont, matchFont
- useFont(null, 12) returns a system-font Skia font synchronously (Assumption A4 — fallback to bundled .ttf if iOS renders blank labels)
- CartesianChart REQUIRES a fixed pixel height on its parent View (UI-SPEC locks 240px)
- useChartPressState init shape MUST mirror yKeys: { x: 0, y: { y: 0 } } for yKeys=['y'] (Pitfall 2)
- pressState.x.value + pressState.y.y.value are Reanimated SharedValue<number>; read via useDerivedValue inside the Skia tooltip Text node (RESEARCH §Pattern 2 closing paragraph)

UI-SPEC contracts this plan implements:
- Lines 258 + 260-266 — two-state empty + tooltip copy contract (BLOCKER-3, BLOCKER-2 secondary copy)
- Lines 286-287 — hitSlop={6} + chart-icon row position (WARN-5, WARN-6)
- Lines 555-574 — ChartPressCallout (BLOCKER-1)
- Lines 599-615 — Senaste 10 passen row JSX (BLOCKER-2)
- Lines 671-684 — plan_exercise chart-icon position (between edit + remove); WARN-5 fix
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client to Supabase RPC get_exercise_chart + get_exercise_top_sets | Untrusted exercise_id from URL path + metric + since parameters cross this boundary; RLS scopes via SECURITY INVOKER (Plan 06-01a Task 1) |
| URL path /exercise/<id>/chart to render layer | Untrusted exercise_id may be spoofed via deep-link; RLS-scoped RPCs return empty data; two-state empty-state renders (T-06-04) |
| AsyncStorage persisted chart cache to reactive UI | Cached chart + top-sets rows for (exerciseId, metric, window) are non-PII numeric aggregates + UUIDs (T-06-07 boundary) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Information disclosure | get_exercise_chart + get_exercise_top_sets RPCs | mitigate | RPCs SECURITY INVOKER + RLS on exercise_sets via parent-FK; covered by Plan 06-01a test-rls Phase 6 extension (5 assertions including the new top-sets cross-user assertion) |
| T-06-04 | Spoofing | exercise_id URL tampering | mitigate | RLS chain returns empty rows for spoofed exercise_id; two-state empty-state renders; no data disclosure |
| T-06-05 | Tampering | metric / since / limit parameter tampering | mitigate | metric is a 2-value union on the client (Zod literal) + Postgres CASE WHEN with no fall-through; since is timestamptz parseable-or-error; limit is int with sane default |
| T-06-07 | Information disclosure | persisted chart + top-sets cache in AsyncStorage | accept | Numeric aggregates + session UUIDs are non-PII |
| T-06-08 | Information disclosure | stale chart cache after user-switch | mitigate | Phase 3 sign-out's queryClient.clear() wipes exerciseChartKeys + exerciseTopSetsKeys cache |
| T-06-09 | Tampering | RPC SQL-injection via parameter | mitigate | Plan 06-01a shipped: parameterized supabase-js .rpc + language sql positional params + set search_path = '' |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Extend keys.ts + author lib/queries/exercise-chart.ts (TWO hooks)</name>
  <files>app/lib/query/keys.ts, app/lib/queries/exercise-chart.ts</files>
  <read_first>
    - app/lib/query/keys.ts FULL FILE post-Plan-06-01b (sessionsKeys.listInfinite + other factories; Task 1 is purely additive)
    - app/lib/queries/last-value.ts FULL FILE (closest analog: file-header comment + Zod-parse-array + useAuthStore selector)
    - app/lib/queries/sessions.ts post-Plan-06-01b (useSessionsListInfiniteQuery uses .rpc + Zod-parse; second analog)
    - .planning/phases/06-history-read-side-polish/06-RESEARCH.md §Pattern 4 (verbatim hook implementation for useExerciseChartQuery including windowToSince helper + ChartRowSchema + useQuery shape) + §Example 1 (exerciseChartKeys factory)
    - .planning/phases/06-history-read-side-polish/06-PATTERNS.md "app/lib/queries/exercise-chart.ts" section
    - .planning/phases/06-history-read-side-polish/06-CONTEXT.md D-14, D-15, D-20, D-21
    - .planning/phases/06-history-read-side-polish/06-01a-PLAN.md (the RPC signatures for both get_exercise_chart and get_exercise_top_sets)
  </read_first>
  <action>
    Two additive edits.

    (1) `app/lib/query/keys.ts` — append TWO new factories after the existing ones. Use the exact shape from RESEARCH §Example 1 and PATTERNS.md.

    ```ts
    // Phase 6 — F10 per-exercise chart with metric + window cache slots.
    export const exerciseChartKeys = {
      all: ["exercise-chart"] as const,
      byExercise: (
        exerciseId: string,
        metric: "weight" | "volume",
        window: "1M" | "3M" | "6M" | "1Y" | "All",
      ) => [...exerciseChartKeys.all, "by-exercise", exerciseId, metric, window] as const,
    };

    // Phase 6 — F10 "Senaste 10 passen" top-sets per source-session.
    export const exerciseTopSetsKeys = {
      all: ["exercise-top-sets"] as const,
      byExercise: (
        exerciseId: string,
        window: "1M" | "3M" | "6M" | "1Y" | "All",
      ) => [...exerciseTopSetsKeys.all, "by-exercise", exerciseId, window] as const,
    };
    ```

    Type metric as the literal union "weight" | "volume"; type window as the literal union "1M" | "3M" | "6M" | "1Y" | "All". The top-sets factory excludes `metric` from the key because top-sets are metric-independent (same source session regardless of which metric the chart is rendering). No other key changes; sessionsKeys.listInfinite (Plan 06-01b) untouched.

    (2) `app/lib/queries/exercise-chart.ts` — NEW file. Follow last-value.ts file-header comment pattern: a leading comment block describing both queries, RLS gate, Zod-parse boundary, references to CONTEXT D-14, D-15, D-17, D-19, D-20, D-21, RESEARCH §Pattern 4, PITFALLS 8.13.

    Imports: `useQuery` from `@tanstack/react-query`; `supabase` from `@/lib/supabase`; `exerciseChartKeys, exerciseTopSetsKeys` from `@/lib/query/keys`; `subMonths, subYears` from `date-fns`; `z` from `zod`.

    Export types per PATTERNS.md verbatim:
    - `ChartMetric = "weight" | "volume"`
    - `ChartWindow = "1M" | "3M" | "6M" | "1Y" | "All"`

    Declare `ChartRowSchema = z.object({ day: z.string(), value: z.coerce.number() })` and export `ChartRow = z.infer<typeof ChartRowSchema>`.

    Declare `TopSetRowSchema = z.object({ session_id: z.string().uuid(), completed_at: z.string(), weight_kg: z.coerce.number(), reps: z.coerce.number().int() })` and export `TopSetRow = z.infer<typeof TopSetRowSchema>`. The `.int()` constraint on `reps` matches the Postgres `int` column type (BLOCKER-2 contract: reps MUST be preserved as int through the wire).

    Private `windowToSince(window: ChartWindow): string | null` helper per RESEARCH §Pattern 4: switch over window; 1M returns `subMonths(now, 1).toISOString()`; 3M is `subMonths(now, 3)`; 6M is `subMonths(now, 6)`; 1Y is `subYears(now, 1)`; All returns null.

    Export `useExerciseChartQuery(exerciseId, metric, window)` returning `useQuery<ChartRow[]>`:
    - queryKey: `exerciseChartKeys.byExercise(exerciseId, metric, window)`
    - queryFn: calls `supabase.rpc("get_exercise_chart", { p_exercise_id: exerciseId, p_metric: metric, p_since: windowToSince(window) })`; throws on error; maps `(data ?? [])` running `ChartRowSchema.parse` on each row
    - enabled: `!!exerciseId`

    Export `useExerciseTopSetsQuery(exerciseId, window, limit = 10)` returning `useQuery<TopSetRow[]>`:
    - queryKey: `exerciseTopSetsKeys.byExercise(exerciseId, window)`
    - queryFn: calls `supabase.rpc("get_exercise_top_sets", { p_exercise_id: exerciseId, p_since: windowToSince(window), p_limit: limit })`; throws on error; maps `(data ?? [])` running `TopSetRowSchema.parse` on each row
    - enabled: `!!exerciseId`

    No staleTime override — inherits QueryClient default 30s. Document in the file-header comment that Plan 06-03 deliberately does NOT add chart-key invalidation to client.ts `['set','add']` onSettled because charts are read-side polish, not hot-path (deferred to V1.1 if soak shows the need). Verify imports compile and both hooks are named exports only (no default export per Phase 4 file convention).
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && npx tsc --noEmit` exits 0
    - `app/lib/query/keys.ts` contains literal `exerciseChartKeys` AND `exerciseTopSetsKeys` (grep both)
    - `app/lib/query/keys.ts` contains literal `byExercise` AND the literal-union strings `"weight"` AND `"All"` (grep — verifies parameter typing)
    - `app/lib/queries/exercise-chart.ts` exists and contains `export function useExerciseChartQuery` AND `export function useExerciseTopSetsQuery` (grep both)
    - File contains literal `supabase.rpc("get_exercise_chart"` AND `supabase.rpc("get_exercise_top_sets"` (grep — both RPC invocations)
    - File contains literal `ChartRowSchema.parse` AND `TopSetRowSchema.parse` (grep — Pitfall 8.13 boundary on both)
    - File contains literal `reps: z.coerce.number().int()` OR equivalent (verifies reps int constraint)
    - File contains literal `subMonths` AND `subYears` (windowToSince helper)
    - File contains literal `enabled: !!exerciseId` (disabled-when-empty guard)
    - File has NO default export (`grep -E "^export default" file` empty)
  </acceptance_criteria>
  <done>
    Both chart query hooks + cache-key factories wired. The route file in Task 3 can consume `useExerciseChartQuery` for the line chart AND `useExerciseTopSetsQuery` for the Senaste 10 passen list — both honoring the locked D-19 / D-20 contracts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author components/segmented-control.tsx NativeWind-baserat reusable</name>
  <files>app/components/segmented-control.tsx</files>
  <read_first>
    - app/components/active-session-banner.tsx (existing components folder convention; file-header + typing + NativeWind classes)
    - app/components/offline-banner.tsx (second existing component for convention check)
    - .planning/phases/06-history-read-side-polish/06-UI-SPEC.md §Visuals "Segmented Control" (lines ~620-650 — NativeWind primary path: parent View flex-row rounded-lg bg-gray-100 dark:bg-gray-800 p-1 + per-segment Pressable flex-1 py-2 px-3 rounded-md with selected tile bg-white dark:bg-gray-700 shadow-sm)
    - .planning/phases/06-history-read-side-polish/06-CONTEXT.md Claude's Discretion "Segmented-control-komponent" (NativeWind-baserat is primary path)
  </read_first>
  <action>
    Create NEW file `app/components/segmented-control.tsx`.

    Generic component `SegmentedControl<T extends string>` with props:
    - `options: ReadonlyArray<{ label: string; value: T }>`
    - `value: T`
    - `onChange: (v: T) => void`
    - `accessibilityLabel: string`

    Imports: View, Text, Pressable from react-native. Inline local `cn(...classes: (string | false | null | undefined)[]) => classes.filter(Boolean).join(" ")` helper if no project-wide cn helper exists (check imports in plans/[id].tsx — if a `cn` helper exists at @/lib/utils or similar, import it; otherwise inline).

    JSX shape per UI-SPEC §Visuals "Segmented Control" verbatim:
    - Outer container: View `className="flex-row rounded-lg bg-gray-100 dark:bg-gray-800 p-1"`, accessibilityRole="tablist", accessibilityLabel from prop
    - Each option: Pressable with className built via cn `flex-1 py-2 px-3 rounded-md items-center justify-center active:opacity-80` PLUS conditionally `bg-white dark:bg-gray-700 shadow-sm` when selected
      - onPress: `onChange(option.value)`
      - accessibilityRole="tab"
      - accessibilityState: `{ selected: option.value === value }`
      - accessibilityLabel: option.label
      - hitSlop: `{ top: 4, bottom: 4 }` (UI-SPEC accessibility floor)
      - Child Text className `text-sm font-semibold` + cn-conditional `text-gray-900 dark:text-gray-50` when selected else `text-gray-500 dark:text-gray-400` — render option.label

    Re-export the component as a named export (no default). Make the component generic so call-sites pass typed unions (Metric / Window). No new dependency. No animation lib. No icon. Composable for V1.1 polish surfaces.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit && npx expo lint app/components/segmented-control.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `app/components/segmented-control.tsx` exists
    - File contains literal `export function SegmentedControl` OR `export const SegmentedControl` (grep)
    - File contains literal `bg-gray-100 dark:bg-gray-800` AND `bg-white dark:bg-gray-700` AND `text-sm font-semibold` (grep — UI-SPEC verbatim)
    - File contains literal `accessibilityRole="tablist"` AND `accessibilityRole="tab"`
    - File contains literal `hitSlop` (UI-SPEC Accessibility Floor)
    - Component signature accepts generic `T extends string` (grep)
    - `npx tsc --noEmit` exits 0
    - No new dependency imported beyond react-native + react primitives
  </acceptance_criteria>
  <done>
    Reusable NativeWind segmented-control ready. Task 3 consumes it twice (metric + window). V1.1 polish surfaces inherit.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author /exercise/[exerciseId]/chart.tsx — toggles + memoized chart + FULL Skia tooltip + two-state empty + tappable Senaste 10 passen</name>
  <files>app/app/(app)/exercise/[exerciseId]/chart.tsx</files>
  <read_first>
    - app/app/(app)/plans/[id].tsx FULL FILE (primary analog: centralized header + useColorScheme accent binding + useExercisesQuery + Stack.Screen dynamic title)
    - app/app/(app)/workout/[sessionId].tsx FULL FILE (theme-aware backdrop convention from Phase 5 commit 6b8c604; Pressable row pattern for the Senaste 10 passen list)
    - app/lib/queries/exercise-chart.ts (Task 1 output — useExerciseChartQuery + useExerciseTopSetsQuery)
    - app/lib/queries/exercises.ts useExercisesQuery (Phase 4) — for Stack header title lookup
    - app/components/segmented-control.tsx (Task 2 output)
    - .planning/phases/06-history-read-side-polish/06-UI-SPEC.md §"Chart screen container" + §"Chart canvas" (lines 528-585 with ChartPressCallout at 555-574) + §"Chart empty-state" + §"Senaste 10 passen row" (lines 599-615) + §Copywriting (lines 258, 260-266) for empty-state two-state + tooltip text contract
    - .planning/phases/06-history-read-side-polish/06-RESEARCH.md §Pattern 2 (full Victory Native XL pattern including useChartPressState init shape per Pitfall 2 + Skia render-prop child structure + useFont + axisOptions with formatXLabel) + Pattern 2 closing paragraph on `pressState.x.value + pressState.y.y.value` for tooltip text via useDerivedValue
    - .planning/phases/06-history-read-side-polish/06-RESEARCH.md §Pitfall 1 (memoization), §Pitfall 2 (useChartPressState init shape), §Pitfall 9 (two-state empty)
    - .planning/phases/06-history-read-side-polish/06-RESEARCH.md §Assumption A2 (two-state empty-state via second `useExerciseChartQuery(id, metric, 'All')` query — cost is low; recommended approach)
    - .planning/phases/06-history-read-side-polish/06-PATTERNS.md "app/app/(app)/exercise/[exerciseId]/chart.tsx" section
    - .planning/phases/06-history-read-side-polish/06-CONTEXT.md D-14 through D-23, D-25, D-26, D-27
  </read_first>
  <action>
    Create NEW route file `app/app/(app)/exercise/[exerciseId]/chart.tsx`. Create directory `app/app/(app)/exercise/[exerciseId]/` (only chart.tsx lives inside in V1).

    Top-of-file imports per PATTERNS.md:
    - React: useMemo, useState
    - react-native: ScrollView, Text, View, Pressable, useColorScheme
    - react-native-safe-area-context: SafeAreaView
    - expo-router: Stack, useLocalSearchParams, useRouter
    - @expo/vector-icons: Ionicons
    - victory-native: CartesianChart, Line, Scatter, useChartPressState
    - @shopify/react-native-skia: Circle, RoundedRect, Text as SkiaText, useFont
    - react-native-reanimated: useDerivedValue (for reading pressState shared values inside Skia)
    - date-fns: format
    - date-fns/locale: sv
    - @/lib/queries/exercises: useExercisesQuery
    - @/lib/queries/exercise-chart: useExerciseChartQuery, useExerciseTopSetsQuery, type ChartMetric, type ChartWindow
    - @/components/segmented-control: SegmentedControl

    Theme bindings:
    - `const scheme = useColorScheme(); const isDark = scheme === "dark";`
    - `const muted = isDark ? "#9CA3AF" : "#6B7280";`
    - `const accent = isDark ? "#60A5FA" : "#2563EB";` (D-23 chart line color)
    - `const axisColor = muted;`
    - `const gridColor = isDark ? "#374151" : "#E5E7EB";`
    - `const tooltipBg = isDark ? "#1F2937" : "#FFFFFF";` (per UI-SPEC ChartPressCallout background)
    - `const tooltipStroke = isDark ? "#374151" : "#E5E7EB";`

    Route params + local state:
    - `const { exerciseId } = useLocalSearchParams<{ exerciseId: string }>();`
    - `const [metric, setMetric] = useState<ChartMetric>("weight");` (D-14 default)
    - `const [chartWindow, setChartWindow] = useState<ChartWindow>("3M");` (D-15 default — name avoids shadowing global `window`)
    - `const router = useRouter();`

    Exercise-name resolution:
    - `const exercisesQuery = useExercisesQuery();`
    - `const exerciseName = useMemo(() => { const map = new Map((exercisesQuery.data ?? []).map((e) => [e.id, e.name] as const)); return map.get(exerciseId!) ?? "Övning"; }, [exercisesQuery.data, exerciseId]);`

    Stack header: `<Stack.Screen options={{ headerShown: true, title: exerciseName }} />`

    Chart data queries (BLOCKER-3 fix — two queries for two-state empty):
    - `const chartQuery = useExerciseChartQuery(exerciseId!, metric, chartWindow);`
    - `const allTimeChartQuery = useExerciseChartQuery(exerciseId!, metric, "All");` (used ONLY to disambiguate empty states; cache-shared with future All-window navigations)
    - `const topSetsQuery = useExerciseTopSetsQuery(exerciseId!, chartWindow, 10);` (D-20 + BLOCKER-2)

    Memoized chartData (WARN-7 fix — dep array EXACTLY `[chartQuery.data]`):
    ```ts
    const chartData = useMemo(
      () => (chartQuery.data ?? []).map((row) => ({ x: new Date(row.day).getTime(), y: row.value })),
      [chartQuery.data],
    );
    ```
    Add an inline comment above this useMemo: `// D-21 memoization contract — dep array is EXACTLY [chartQuery.data]; do not add metric/window deps since they are in the queryKey.`

    Local helper: `const formatNumber = (n: number) => n.toLocaleString("sv-SE");`

    Local helper: `const formatChartValue = (n: number) => metric === "volume" ? \`\${formatNumber(n)} kg\` : \`\${n} kg\`;` (UI-SPEC lines 260-262 tooltip value-line copy contract)

    JSX structure per UI-SPEC §"Chart screen container":
    - SafeAreaView `flex-1 bg-white dark:bg-gray-900`
    - Stack.Screen options
    - ScrollView with `contentContainerStyle={{ paddingHorizontal: 16, paddingTop: 16, paddingBottom: 96 }}`
    - Inside: `<View className="gap-4">`:
      - MetricToggle: `<SegmentedControl options={[{label:"Max vikt", value:"weight"}, {label:"Total volym", value:"volume"}]} value={metric} onChange={setMetric} accessibilityLabel="Mätvärde" />`
      - Chart container View `bg-gray-100 dark:bg-gray-800 rounded-lg p-4`:
        - If `chartData.length === 0` AND `(allTimeChartQuery.data?.length ?? 0) >= 1`: render the WINDOW-EMPTY state (heading "Inga pass i detta intervall", body "Byt till All för att se hela historiken.") — UI-SPEC line 258 + BLOCKER-3
        - Else if `chartData.length === 0`: render the ALL-TIME-EMPTY state (heading "Inga pass än för den här övningen", body "Logga minst 2 set för att se trend.") — UI-SPEC line 257
        - Else: render `<View style={{ height: 240 }}><CartesianChart>...</CartesianChart></View>` + a `SparseCaption` (`"Logga ett pass till för att se trend."`) under the canvas if `chartData.length === 1`
      - WindowToggle: `<SegmentedControl options={[{label:"1M",value:"1M"},{label:"3M",value:"3M"},{label:"6M",value:"6M"},{label:"1Y",value:"1Y"},{label:"All",value:"All"}]} value={chartWindow} onChange={setChartWindow} accessibilityLabel="Tidsfönster" />`
      - Senaste 10 passen section View `mt-2 gap-2`:
        - Section header Text `text-2xl font-semibold text-gray-900 dark:text-gray-50`: "Senaste 10 passen"
        - List View: map `topSetsQuery.data ?? []` to **Pressable** rows per UI-SPEC §"Senaste 10 passen row" (lines 599-615) — `flex-row items-center justify-between rounded-lg bg-gray-100 dark:bg-gray-800 px-4 py-3 mb-2 active:opacity-80`; accessibilityRole="button"; accessibilityLabel `Öppna pass ${formattedDate}`; **onPress: `router.push(\`/history/\${row.session_id}\`)`** (BLOCKER-2 + D-20). Primary Text `text-base font-semibold text-gray-900 dark:text-gray-50`: `format(new Date(row.completed_at), "d MMM yyyy", { locale: sv })`. Secondary Text `text-base text-gray-500 dark:text-gray-400`: `${row.weight_kg} kg × ${row.reps}` (BLOCKER-2 — reps preserved; UI-SPEC line 265).
        - Omit the entire section when `chartData.length === 0` (UI-SPEC §"Chart empty-state" — Senaste 10 passen section is hidden in the empty path)

    ChartCanvas inline JSX per RESEARCH §Pattern 2 + UI-SPEC §Chart canvas verbatim:
    - `const font = useFont(null, 12);`
    - `const { state: pressState, isActive } = useChartPressState({ x: 0, y: { y: 0 } });` (Pitfall 2 — exact init shape)
    - `<CartesianChart data={chartData} xKey="x" yKeys={["y"]} chartPressState={pressState} domainPadding={{ left: 16, right: 16, top: 16, bottom: 16 }} axisOptions={{ font, tickCount: 5, labelColor: axisColor, lineColor: gridColor, formatXLabel: (ms) => format(new Date(ms), "MMM d", { locale: sv }), formatYLabel: (n) => metric === "volume" ? formatNumber(n) : \`\${n}\` }}>`
    - Render-prop children: `({ points, chartBounds }) => fragment` containing:
      - `<Line points={points.y} color={accent} strokeWidth={2} curveType="natural"/>`
      - `<Scatter points={points.y} radius={4} color={accent}/>`
      - `{isActive && <Circle cx={pressState.x.position} cy={pressState.y.y.position} r={6} color={accent}/>}`
      - **FULL Skia tooltip callout per UI-SPEC lines 555-574 (BLOCKER-1)** — when `isActive`, render an inline `ChartPressCallout` Skia node:
        ```tsx
        {isActive && (
          <>
            <RoundedRect
              x={useDerivedValue(() => Math.min(Math.max(pressState.x.position.value - 60, chartBounds.left + 4), chartBounds.right - 124))}
              y={useDerivedValue(() => Math.max(pressState.y.y.position.value - 60, chartBounds.top + 4))}
              width={120}
              height={50}
              r={8}
              color={tooltipBg}
            />
            <SkiaText
              x={useDerivedValue(() => Math.min(Math.max(pressState.x.position.value - 52, chartBounds.left + 12), chartBounds.right - 116))}
              y={useDerivedValue(() => Math.max(pressState.y.y.position.value - 40, chartBounds.top + 24))}
              text={useDerivedValue(() => formatChartValue(pressState.y.y.value.value))}
              font={font}
              color={accent}
            />
            <SkiaText
              x={useDerivedValue(() => Math.min(Math.max(pressState.x.position.value - 52, chartBounds.left + 12), chartBounds.right - 116))}
              y={useDerivedValue(() => Math.max(pressState.y.y.position.value - 22, chartBounds.top + 42))}
              text={useDerivedValue(() => format(new Date(pressState.x.value.value), "d MMM yyyy", { locale: sv }))}
              font={font}
              color={muted}
            />
          </>
        )}
        ```
        The chartBounds clamp keeps the rect inside the canvas (UI-SPEC §ChartPressCallout requirement). The two SkiaText nodes render the value-line + date-line (UI-SPEC lines 260-262). The `formatChartValue` helper switches to formatNumber for volume metric (lines 261). Position math: rect centered ~60pt above the pressed point; text inset 12pt from rect's left + offset down 24pt and 42pt for value+date lines. If the executor finds Skia's `<Text>` requires `text` as a SharedValue<string> rather than the inline-derived value, wrap each `useDerivedValue` call in a separate const so the body is more readable — both are equivalent semantically.

    No useFocusEffect needed — this route has no overlay state. No banner-error needed — the route has no mutations.

    **Deviation notes (document at top of file in a comment block):**
    - UI-SPEC line 287 prose says the chart-icon is "rightmost" but UI-SPEC §Visuals JSX (lines 671-684) shows it BETWEEN edit and remove. Plan 06-03 Task 4 ships the JSX-locked position (between edit and remove). UI-SPEC line 287 prose is flagged for a follow-up patch via Linear (npm run linear:create -- --title "UI-SPEC §Plan-detail chart-ikon: prose at line 287 contradicts JSX at lines 671-684 (between edit and remove)" --type ui --priority low --phase 6). The position is committed; the prose-fix is paperwork.
    - The two-state empty-state implementation uses TWO useExerciseChartQuery calls (one for the active window, one for "All"). RESEARCH A2 confirmed this is the recommended approach (cost is low; cache slot reuses). The second query is `enabled: !!exerciseId` so it always fires once on cold cache — acceptable given network is offline-tolerant via persister.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit && npx expo lint</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && npx tsc --noEmit` exits 0
    - `cd app && npx expo lint` exits 0 (warnings allowed)
    - File `app/app/(app)/exercise/[exerciseId]/chart.tsx` exists
    - File contains literal `useExerciseChartQuery` AND `useExerciseTopSetsQuery` AND `useChartPressState` AND `CartesianChart` (grep all four)
    - **BLOCKER-1**: File contains literal `RoundedRect` AND `SkiaText` (Skia tooltip primitives) AND `useDerivedValue` (reading pressState shared values). Acceptance: `grep -c "RoundedRect" file` ≥ 1 AND `grep -c "SkiaText\|Text as SkiaText" file` ≥ 1 (the import + usage paths)
    - **BLOCKER-2**: File contains literal `router.push(\`/history/\${` substring on the Senaste 10 row onPress (grep `router\.push\(.history/\$\{` or escape variant)
    - **BLOCKER-2**: File contains literal `${row.weight_kg} kg × ${row.reps}` OR equivalent template literal showing BOTH weight AND reps (grep `× \${.*reps}` for the multiplication-sign + reps interpolation)
    - **BLOCKER-3**: File contains BOTH literals `Inga pass än för den här övningen` AND `Inga pass i detta intervall` (grep both empty-state copy strings)
    - **BLOCKER-3**: File contains literal `Byt till All för att se hela historiken.` (grep)
    - **WARN-7**: File contains the literal substring `[chartQuery.data]` (the EXACT dep array — not `[chartQuery.data, metric]` or `[chartQuery]`) — verify via `grep -F "[chartQuery.data]" file`
    - File contains literal `Logga minst 2 set för att se trend.` (all-time-empty body — UI-SPEC verbatim)
    - File contains literal `Logga ett pass till för att se trend.` (sparse-state caption — UI-SPEC verbatim)
    - File contains literal `Senaste 10 passen` (section header — UI-SPEC verbatim)
    - File contains literal `{ x: 0, y: { y: 0 } }` (useChartPressState init shape — Pitfall 2)
    - File contains literal `xKey="x"` AND `yKeys={["y"]}` (CartesianChart prop contract)
    - File contains literal `height: 240` (fixed pixel height)
    - File contains literal `SegmentedControl` (imported + used twice for metric + window)
    - File contains literal `formatXLabel` AND `locale: sv` (D-22)
    - File contains literal `useState<ChartWindow>("3M")` (D-15 default)
    - File contains the deviation-note comment block at the top (grep for `Deviation` or `WARN-5` or `prose at line 287`)
    - File does NOT import `Modal` from react-native
    - File does NOT import any new client-bundled dependency not already in app/package.json
  </acceptance_criteria>
  <done>
    Chart route renders end-to-end: toggles → memoized chart → full Skia tooltip on tap-and-hold → two-state empty-state for 0 points → tappable Senaste 10 passen list routing back to source sessions with reps preserved. All three blockers (D-17, D-19, D-20) honored; WARN-7 (memoization dep array exactness) honored.
  </done>
</task>

<task type="auto">
  <name>Task 4: Extend plans/[id].tsx PlanExerciseRow with chart-icon (D-24) + UAT markdown</name>
  <files>app/app/(app)/plans/[id].tsx, app/scripts/manual-test-phase-06-uat.md</files>
  <read_first>
    - app/app/(app)/plans/[id].tsx FULL FILE — find the inline PlanExerciseRow component; identify the existing sibling order: drag-handle Pressable → name-block View → edit-affordance Pressable → remove-affordance Pressable. Plan 06-03 inserts ONE additional sibling Pressable BETWEEN edit-affordance and remove-affordance (per UI-SPEC §Visuals JSX lines 671-684).
    - .planning/phases/06-history-read-side-polish/06-UI-SPEC.md §"Plan-detail chart-ikon affordance" lines 671-684 (verbatim JSX position) + line 286 (hitSlop={6} contract) + line 287 (prose deviation — chart-icon ships between edit + remove per JSX, NOT rightmost per prose; documented in Task 3 deviation note)
    - .planning/phases/06-history-read-side-polish/06-RESEARCH.md §Pitfall 8 (sibling-Pressable bubble — chart-icon Pressable is a SIBLING to drag/edit/remove, NOT nested)
    - .planning/phases/06-history-read-side-polish/06-PATTERNS.md "app/app/(app)/plans/[id].tsx (MODIFIED)" section
    - app/scripts/manual-test-phase-06-uat.md current state (post-Plans 06-01a + 06-01b + 06-02; has F9 list + session-detail + delete-pass sections; Plan 03 TODO placeholder present)
    - .planning/phases/06-history-read-side-polish/06-VALIDATION.md "Manual-Only Verifications" rows for F10
  </read_first>
  <action>
    Two single-file edits.

    **(1) plans/[id].tsx — chart-icon entry-point (D-24, WARN-6 fix):**

    Locate the PlanExerciseRow inline component's flex-row container (around line 756 per PATTERNS.md). Existing siblings: drag-handle Pressable → name-block View → edit-affordance Pressable (chevron-forward) → remove-affordance Pressable (close-outline).

    Insert a NEW sibling Pressable BETWEEN the edit-affordance Pressable and the remove-affordance Pressable (UI-SPEC §Visuals JSX lines 671-684 — "between edit and remove"). The new Pressable:
    - className `p-3 active:opacity-80` (12pt padding around 22pt icon = 46pt total hit-target per UI-SPEC line 286)
    - **hitSlop={6}** per UI-SPEC line 286 + line 677 (WARN-6 fix — NOT hitSlop=4)
    - onPress: calls a new prop `onShowChart`
    - accessibilityRole="button"
    - accessibilityLabel: `Visa graf för ${exerciseName ?? "övningen"}` (use the exerciseName-by-id lookup already in scope)
    - accessibilityHint: `"Tryck för att se progressionsgraf"`
    - Child: `<Ionicons name="stats-chart" size={22} color={accent} />`

    Extend the PlanExerciseRow component props to accept `onShowChart: () => void`. Ensure `accent` color is in scope (PATTERNS.md "Differences to apply" — if `muted` is passed as a prop, add `accent` symmetrically; if it's locally derived, do the same).

    At the parent PlanDetailScreen, pass the new prop where PlanExerciseRow is instantiated: `onShowChart={() => router.push(\`/exercise/${planExercise.exercise_id}/chart\`)}`. No `as Href` cast needed — the route file ships in Task 3 of this plan.

    Do NOT modify drag-handle behavior, target-chip rendering, archive flow, or draggable-flatlist wiring. Verify the sibling-not-nested layout (Pitfall 8 guard).

    Deviation acknowledgement note in commit message: "Chart-icon placed between edit and remove per UI-SPEC JSX lines 671-684. UI-SPEC line 287 prose says 'rightmost'; the JSX is the authoritative contract (WARN-5 — Linear issue logged for prose patch)."

    **(2) manual-test-phase-06-uat.md — Plan 03 chart-route UAT sections:**

    Replace the Plan 03 TODO section with these UAT sections (mirror manual-test-phase-04 + 05 structure):

    ## F10 Chart route entry-points (D-24, D-25, D-26)
    Steps:
    1. From plans/[id] → tap chart-icon (between edit + remove icons) on any plan_exercise row → expect navigation to /exercise/<id>/chart
    2. From history/[sessionId] → tap an exercise-card header → expect same navigation (Plan 06-02 cross-link resolves)
    3. Verify Stack header title shows exercise.name (e.g. "Bänkpress"); fallback "Övning" only if exercise was deleted
    4. Manual finger-test: chart-icon hit-target ≥ 44pt (per UI-SPEC hitSlop={6} + p-3 = 46pt effective)

    ## F10 Metric + Window toggles (D-14, D-15)
    Steps:
    1. Default state on first visit: Max vikt selected + 3M window selected
    2. Tap Total volym → chart line redraws with new values; segmented-control highlights new tile
    3. Tap 1M → chart shrinks; tap All → chart expands; tap 3M → cache-hit (no spinner)
    4. Re-toggle Max vikt + 3M → instant cache-hit

    ## F10 Memoization verified (ROADMAP success #3 + D-21 + WARN-7)
    Steps:
    1. Open chart route → tap metric toggle 5× rapidly → line animation transitions smoothly (no full re-mount flicker)
    2. If line re-mounts on every tap (visible "blink" or animation restarts) → FAIL: dep array probably wrong

    ## F10 Graceful degrade — two-state empty-state (D-17, BLOCKER-3)
    Steps:
    1. Navigate to chart for an exercise with 0 logged sets → verify ALL-TIME-EMPTY state (Ionicons stats-chart-outline + "Inga pass än för den här övningen" + "Logga minst 2 set för att se trend.")
    2. Log 2 sets for that exercise across 2 days OUTSIDE the 3M window (e.g. 6 months ago using a backdated test fixture) → return to chart with default 3M window → verify WINDOW-EMPTY state ("Inga pass i detta intervall" + "Byt till All för att se hela historiken.")
    3. Tap All window → verify chart renders the 2 data points (the disambiguation worked)
    4. With ≥2 points in 3M window: verify full line chart renders
    5. With exactly 1 point in window: verify single dot + caption "Logga ett pass till för att se trend."

    ## F10 Tap-and-hold Skia tooltip (D-19, BLOCKER-1, Pitfall 2)
    Steps:
    1. On a chart with ≥3 data points → tap-and-hold on a data point → verify BOTH the highlight-circle AND the rounded-rect tooltip callout appear at/near the tapped point
    2. Verify tooltip content: top line shows `${weight} kg` for Max vikt OR formatted-number kg for Total volym; bottom line shows date in Swedish (e.g. "14 maj 2026")
    3. Drag the finger across the chart → tooltip + highlight follow the nearest data point
    4. Verify the tooltip rect stays inside the chart canvas at the left/right edges (chartBounds clamping)
    5. Lift the finger → tooltip + highlight disappear

    ## F10 Theme awareness (D-23 + Phase 1 F15)
    Steps:
    1. Light mode → chart line is blue-600 #2563EB; tooltip background white; axis labels gray-500
    2. iOS Settings → Display → Dark → return to app → chart line blue-400 #60A5FA; tooltip background gray-800; axis labels gray-400

    ## F10 Senaste 10 passen list — tappable rows with reps (D-20, BLOCKER-2)
    Steps:
    1. Below the chart on an exercise with ≥1 day of data → section "Senaste 10 passen" + list of up to 10 rows
    2. Each row shows date + `${weight} kg × ${reps}` (e.g. "14 maj 2026" / "82.5 kg × 8")
    3. **Tap any row → expect navigation to /history/<that-session-id>** — verify the source session opens with all sets visible
    4. Verify section is OMITTED when chart-empty-state path is active

    Preserve all earlier sections (F9 list + F9 session-detail + F9 delete-pass + F9 freezeOnBlur). Total file ≥ 90 lines after this task.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit && npx expo lint && grep -c "F10 Chart route entry-points" app/scripts/manual-test-phase-06-uat.md | awk -F: '{ exit ($NF >= 1) ? 0 : 1 }' && grep -c "Tap any row" app/scripts/manual-test-phase-06-uat.md | awk -F: '{ exit ($NF >= 1) ? 0 : 1 }'</automated>
  </verify>
  <acceptance_criteria>
    plans/[id].tsx:
    - `cd app && npx tsc --noEmit` exits 0
    - `cd app && npx expo lint` exits 0 (warnings allowed)
    - File contains literal `stats-chart` (chart-icon glyph)
    - File contains literal `onShowChart` (callback prop)
    - File contains literal `/exercise/${` AND `/chart` (route literal; no `as Href` cast needed)
    - File contains literal `accessibilityHint="Tryck för att se progressionsgraf"` (UI-SPEC verbatim)
    - **WARN-6**: File contains literal `hitSlop={6}` near the new chart-icon Pressable (NOT `hitSlop={4}`)
    - The new Pressable is a flex-row sibling between edit and remove (verify by reading 5 lines context around the insertion)
    - No regression: existing drag-handle + edit + remove behaviors compile

    manual-test-phase-06-uat.md:
    - File contains literal section headers: `F10 Chart route entry-points`, `F10 Metric + Window toggles`, `F10 Memoization verified`, `F10 Graceful degrade`, `F10 Tap-and-hold Skia tooltip`, `F10 Theme awareness`, `F10 Senaste 10 passen list`
    - File contains literal `Inga pass i detta intervall` (BLOCKER-3 two-state test) AND `Byt till All för att se hela historiken.`
    - File contains literal `kg × ${reps}` (BLOCKER-2 reps multiplication on Senaste 10 rows)
    - File contains literal `Tap any row → expect navigation to /history/` (BLOCKER-2 tap test)
    - File ≥ 90 non-comment lines
    - All Plan 06-01b + Plan 06-02 sections preserved
  </acceptance_criteria>
  <done>
    Chart entry-point #1 wired on plans/[id] with hitSlop={6}; manual UAT checklist complete with explicit human-verification steps for all three closed BLOCKERS. Ready for /gsd-verify-work 6 after iPhone verification.
  </done>
</task>

</tasks>

<verification>
- After all four tasks land: `cd app && npx tsc --noEmit && npm run test:rls && npm run test:exercise-chart && npx expo lint` exits 0 (no regression vs Plans 06-01a/01b/02)
- Manual smoke test on iPhone: open plan-detail → tap chart-icon between edit + remove → see chart; toggle metric + window; tap-and-hold → see Skia tooltip with value + date; scroll to Senaste 10 → tap row → land on /history/<id>
- ROADMAP success #3 (graf via CartesianChart with memoized data) verified via WARN-7 dep-array grep + manual rapid-toggle UAT
- BLOCKER-1 verified: chart.tsx contains RoundedRect + SkiaText + useDerivedValue (acceptance grep)
- BLOCKER-2 verified: Senaste 10 rows route via router.push(`/history/${row.session_id}`) AND show `${weight} kg × ${reps}`
- BLOCKER-3 verified: both empty-state copy variants exist in chart.tsx
- WARN-5 documented (chart-icon position deviation logged)
- WARN-6 closed: hitSlop={6} on plans/[id].tsx chart-icon
- WARN-9 closed: chart.tsx artifact has min_lines: 150 in frontmatter
- No new client-bundled dependency added
- Threat register: T-06-02 (covered by Plan 06-01a test-rls extension including the top-sets cross-user assertion), T-06-04 (empty-state on spoof), T-06-05 (Zod-literal + Postgres CASE WHEN), T-06-07/08/09 inherited
</verification>

<success_criteria>
- ROADMAP success #3 (graf via CartesianChart med memoized data) MET
- /exercise/[exerciseId]/chart renders end-to-end with toggles + FULL Skia tooltip + two-state empty + tappable Senaste 10 (D-17, D-19, D-20 honored)
- D-24 entry-point on plans/[id] resolved with hitSlop={6}
- D-25 entry-point from session-detail (Plan 06-02) resolves — `as Href` cast becomes redundant
- SegmentedControl reusable component shipped — V1.1 polish surfaces inherit
- All three BLOCKERS closed; both WARNs (5 + 6 + 7 + 9) closed
</success_criteria>

<output>
After completion, create `.planning/phases/06-history-read-side-polish/06-03-SUMMARY.md` per the template. Record:
- Files created/modified (count + paths)
- Chart-route layout + Skia tooltip implementation notes (BLOCKER-1)
- Senaste 10 row dual-RPC integration notes (BLOCKER-2 — both RPCs consumed)
- Two-state empty-state second-query notes (BLOCKER-3)
- WARN deviations (chart-icon position justification; Linear issue ID for the UI-SPEC prose patch)
- Recommended next step: `/gsd-secure-phase 6` + `/gsd-verify-work 6` + `/gsd-code-review` to close Phase 6
</output>
</content>
</invoke>