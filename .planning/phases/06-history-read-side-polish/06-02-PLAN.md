---
phase: 06-history-read-side-polish
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - app/lib/queries/sessions.ts
  - app/lib/query/client.ts
  - app/app/(app)/history/[sessionId].tsx
  - app/scripts/manual-test-phase-06-uat.md
autonomous: true
requirements:
  - F9
tags:
  - phase-6
  - history
  - session-detail
  - delete
  - mutation
  - inline-overlay

must_haves:
  truths:
    - "User can tap a row in (tabs)/history → routes to /history/<sessionId> → sees summary-header (set-count · total-volume · duration) + card-per-exercise with all logged sets inline + delete-pass affordance (ROADMAP success #2)"
    - "Delete-pass is gated by an inline-overlay-confirm (Phase 4 commit e07029a pattern — NOT a modal portal); confirmation body shows the exact set_count + total_volume so user sees what is being deleted (D-07 + UI-SPEC §Inline-overlay-confirm-pattern)"
    - "Successful delete navigates back to (tabs)/history via router.replace + shows 'Passet borttaget' toast (UI-SPEC §Toast pattern)"
    - "useDeleteSession.mutate(...) follows mutate-not-mutateAsync convention (Phase 4 commit 5d953b6); paused mutations under networkMode:'offlineFirst' optimistically remove from listInfinite cache and replay on reconnect via FK cascade (D-07 + RESEARCH §Pattern 6)"
    - "setMutationDefaults(['session','delete']) optimistically maps over listInfinite { pages, pageParams } envelope to filter out the deleted session from every page (RESEARCH Pitfall 6) and clears sessionsKeys.detail(id); onError rolls back; onSettled invalidates listInfinite + detail + setsKeys.list(id)"
    - "useFocusEffect resets showOverflowMenu + showDeleteConfirm on blur so freezeOnBlur:true (Phase 4 D-08) does not leave a ghost overlay on re-focus (RESEARCH Pitfall 7)"
    - "Card-header tap routes to /exercise/<exerciseId>/chart (D-11 + D-25); Plan 03 ships the chart route so the route literal is `as Href`-cast until that lands (Phase 4 cross-plan-route convention)"
    - "Empty pass (0 sets but finished_at IS NOT NULL — D-13) renders summary-header '0 set · 0 kg · X min' AND the exercise-card list is empty — the screen still lets the user delete it"
    - "Plan-name on session-detail summary uses usePlanQuery(plan_id) which does NOT filter archived_at IS NULL (D-08 — usePlanQuery already returns archived plans; Plan 02 reuses without change)"
    - "Cross-user delete is server-blocked by RLS (T-06-03 + T-06-12); test-rls.ts Phase 6 extension from Plan 01 already covers the gate — no Plan 02 test addition needed beyond manual UAT for the user-visible flow"
    - "Manual UAT markdown extends with Plan 02-specific steps for session-detail + delete flow"
  artifacts:
    - path: "app/lib/queries/sessions.ts"
      provides: "useDeleteSession(sessionId?) hook exported; scope-bound to session:<id> for FIFO replay; existing exports + Plan 01 additions untouched"
      exports: ["useDeleteSession"]
    - path: "app/lib/query/client.ts"
      provides: "['session','delete'] setMutationDefaults block added (block 14) after existing block 13 ['set','delete']; optimistic remove from listInfinite { pages, pageParams } envelope; onError rolls back; onSettled invalidates 3 cache keys"
      contains: "[\"session\", \"delete\"]"
    - path: "app/app/(app)/history/[sessionId].tsx"
      provides: "Read-only session-detail screen: Stack.Screen with dynamic title (formatted date) + headerRight ellipsis-horizontal trigger; SafeAreaView ScrollView with SummaryHeader + ExerciseCard×N; inline-overlay overflow menu + inline-overlay delete-confirm; toast on success"
      contains: "useDeleteSession"
    - path: "app/scripts/manual-test-phase-06-uat.md"
      provides: "Extended with Plan 02 manual UAT section: open session from history-list → see all sets → tap overflow → tap Ta bort pass → confirm → land on history-list with toast; offline path: queue delete, force-quit, reconnect, verify deleted on Supabase Studio"
      min_lines: 60
  key_links:
    - from: "app/app/(app)/(tabs)/history.tsx HistoryListRow onPress"
      to: "app/app/(app)/history/[sessionId].tsx"
      via: "router.push({ pathname: '/history/[sessionId]', params: { sessionId } })"
      pattern: "history/\\[sessionId\\]"
    - from: "app/app/(app)/history/[sessionId].tsx onDeleteConfirm handler"
      to: "useDeleteSession.mutate"
      via: "deleteSession.mutate({ id }, { onError })"
      pattern: "deleteSession\\.mutate"
    - from: "app/lib/query/client.ts ['session','delete'] mutationFn"
      to: "Supabase workout_sessions DELETE"
      via: "supabase.from('workout_sessions').delete().eq('id', vars.id)"
      pattern: "from\\(\"workout_sessions\"\\)\\.delete"
    - from: "app/app/(app)/history/[sessionId].tsx ExerciseCard onPress"
      to: "app/app/(app)/exercise/[exerciseId]/chart.tsx (Plan 03)"
      via: "router.push(`/exercise/${exerciseId}/chart` as Href)"
      pattern: "/exercise/\\$\\{.*\\}/chart"
---

<objective>
F9 session-detail + delete vertical slice. After this plan ships the user can: tap any row on (tabs)/history → land on /history/<sessionId> → see compact summary-header (set-count · total-volume · duration) + cards-per-exercise with all logged sets read-only inline → tap the header-right ellipsis-horizontal → tap "Ta bort pass" → confirm via inline-overlay → land back on history with "Passet borttaget" toast. The deletion cascades to exercise_sets via the existing FK on delete cascade (no migration; verified in Plan 01 test-rls extension).

Purpose: Close the F9 read-loop on the detail surface; introduce the only Phase 6 mutation (`['session','delete']`); unlock Plan 03's two entry-points (the card-header cross-link from session-detail → chart route, plus the future plan-detail row icon Plan 03 owns).

Output: One extended queries/sessions.ts (adds useDeleteSession) + one extended query/client.ts (adds block 14 ['session','delete']) + one new route file /history/[sessionId].tsx + one extended manual UAT markdown.
</objective>

<execution_context>
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-history-read-side-polish/06-CONTEXT.md
@.planning/phases/06-history-read-side-polish/06-RESEARCH.md
@.planning/phases/06-history-read-side-polish/06-UI-SPEC.md
@.planning/phases/06-history-read-side-polish/06-VALIDATION.md
@.planning/phases/06-history-read-side-polish/06-PATTERNS.md
@.planning/phases/06-history-read-side-polish/06-01-PLAN.md
@CLAUDE.md

<interfaces>
<!-- From Plan 01 (already shipped this wave): -->
- sessions.ts exports: useActiveSessionQuery, useSessionQuery(id), useStartSession, useFinishSession, useSessionsListInfiniteQuery (NEW Plan 01), SessionSummary type (NEW Plan 01)
- keys.ts: sessionsKeys.listInfinite() (NEW Plan 01)

<!-- From Phases 4 + 5 (already merged): -->
- plans.ts: useArchivePlan — analog mutation hook (mutationKey + scope.id pattern) — copy verbatim for useDeleteSession
- sets.ts: useSetsForSessionQuery(sessionId) — Phase 5 — returns sets ORDER BY exercise_id ASC, set_number ASC; Plan 02 reuses as-is for the card-per-exercise grouping
- exercises.ts: useExercisesQuery() — Phase 4 — returns user's exercises; Plan 02 uses for the Map<id,name> lookup on card-header
- plans.ts: usePlanQuery(planId) — Phase 4 — returns single plan WITHOUT archived_at filter (verified in PATTERNS.md "Differences to apply"); Plan 02 uses as-is for the summary-header plan-name display

<!-- Phase 4/5 setMutationDefaults topology in client.ts: -->
- Block 6 ['plan','archive'] at line ~338 — VERBATIM analog for ['session','delete'] (optimistic filter-out of list cache + invalidate; only structural diff is the InfiniteQuery envelope per Pitfall 6)
- Blocks 1–13 occupy line ranges ~118–~810; Plan 02 appends NEW block 14 AFTER existing block 13 ['set','delete']

<!-- Existing routes that load patterns: -->
- app/app/(app)/workout/[sessionId].tsx (Phase 5) — primary analog for card-per-exercise + Stack.Screen header pattern
- app/app/(app)/plans/[id].tsx (Phase 4) — primary analog for inline-overlay-destructive-confirm + useFocusEffect overlay reset + mutate-not-mutateAsync convention

<!-- Centralized header styling (Phase 4 commit b57d1c2): -->
- (app)/_layout.tsx already binds headerStyle.backgroundColor + headerTintColor + headerTitleStyle via useColorScheme — new screens that set `headerShown: true` inherit automatically; per-screen options need only set `title` and optional `headerRight`
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Supabase REST workout_sessions DELETE | Untrusted DELETE crosses this boundary; RLS scopes by user_id |
| AsyncStorage paused-mutation queue → reactive UI | Deleted session-id sits in the persisted mutation queue until reconnect — non-PII UUID per T-06-07 |
| screen-local React state (showDeleteConfirm) → freezeOnBlur preserved tree | Pitfall 7 — overlay flag must reset on blur or it re-appears on focus |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | workout_sessions DELETE | mitigate | RLS `using` clause on workout_sessions; Plan 01 test-rls extension already covers (no Plan 02 test addition). Client-side mutationFn calls `.delete().eq('id', vars.id)` — server enforces ownership. |
| T-06-06 | Spoofing / Information disclosure | deep-link to /history/<wrongId> | mitigate | useSessionQuery(id) returns empty data when RLS blocks; Plan 02 renders `Något gick fel. Försök igen.` (UI-SPEC §Copywriting Contract); no data disclosure |
| T-06-07 | Information disclosure | persisted delete-mutation queue in AsyncStorage | accept | UUIDs in queue are non-PII; LargeSecureStore protects auth blob; mutation queue is unencrypted but contains only ids |
| T-06-08 | Information disclosure | stale detail cache after user-switch | mitigate | Phase 3 sign-out's queryClient.clear() wipes detail cache — no Plan 02 code change |
| T-06-12 | Tampering | anon-client DELETE bypass | mitigate | RLS using clause + (app) guard; covered by Plan 01 test-rls extension |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add useDeleteSession hook + ['session','delete'] setMutationDefaults</name>
  <files>app/lib/queries/sessions.ts, app/lib/query/client.ts</files>
  <read_first>
    - app/lib/queries/sessions.ts FULL FILE (post-Plan-01 state — already extended with useSessionsListInfiniteQuery; Task 1 appends useDeleteSession at the end)
    - app/lib/queries/plans.ts `useArchivePlan` (lines 126-131 per PATTERNS.md — VERBATIM analog: mutationKey tuple + scope.id binding + useMutation generic signature)
    - app/lib/query/client.ts block 6 ['plan','archive'] (lines 335-366 — VERBATIM shape for optimistic filter-out + onError rollback + onSettled invalidate; PATTERNS.md "app/lib/query/client.ts (MODIFIED — add ['session','delete'] default)" section confirms)
    - app/lib/query/client.ts block 10 ['session','finish'] (lines 653-706 — shows how setsKeys + lastValueKeys are referenced when invalidating; same imports already in scope)
    - .planning/phases/06-history-read-side-polish/06-RESEARCH.md §Pattern 6 (full setMutationDefaults block verbatim including the { pages, pageParams } envelope mapping per Pitfall 6)
    - .planning/phases/06-history-read-side-polish/06-PATTERNS.md "useDeleteSession" + "['session','delete']" sections
  </read_first>
  <action>
    Two additive edits:

    (1) `app/lib/queries/sessions.ts` — append at end of file:
    ```
    type SessionDeleteVars = { id: string };
    export function useDeleteSession(sessionId?: string) {
      return useMutation<void, Error, SessionDeleteVars>({
        mutationKey: ["session", "delete"] as const,
        scope: sessionId ? { id: `session:${sessionId}` } : undefined,
      });
    }
    ```
    (Hook owns mutationKey + optional scope.id binding; mutationFn lives in client.ts per Phase 4 D-01 module-load-order rule. Verify `useMutation` is already imported from `@tanstack/react-query` — Plan 01 already extended that import line to include useInfiniteQuery; useMutation was already there for Phase 5 hooks.)

    (2) `app/lib/query/client.ts` — append block 14 AFTER existing block 13 ['set','delete'] (around line ~810). The block matches Pattern 6 verbatim:

    Variable types (place inline near the new block — local to client.ts; mirrors how PlanArchiveVars etc. are declared near the block):
    ```
    type SessionDeleteVars = { id: string };
    ```

    setMutationDefaults block per RESEARCH §Pattern 6 + PATTERNS.md "['session','delete']" exactly:
    - `mutationFn`: `await supabase.from("workout_sessions").delete().eq("id", vars.id)`; throw on error; return `undefined as void`
    - `onMutate`:
      - `await queryClient.cancelQueries({ queryKey: sessionsKeys.listInfinite() })`
      - `await queryClient.cancelQueries({ queryKey: sessionsKeys.detail(vars.id) })`
      - Snapshot `previousList = queryClient.getQueryData<{ pages: SessionSummary[][]; pageParams: (string|null)[] }>(sessionsKeys.listInfinite())`
      - Snapshot `previousDetail = queryClient.getQueryData(sessionsKeys.detail(vars.id))`
      - Map over pages to filter out the deleted id: `pages: previousList.pages.map((page) => page.filter((s) => s.id !== vars.id))` (Pitfall 6 — must use the envelope shape, NOT a flat filter)
      - Clear detail cache: `queryClient.setQueryData(sessionsKeys.detail(vars.id), undefined)`
      - Return `{ previousList, previousDetail }`
    - `onError`: cast ctx and roll back BOTH `listInfinite` and `detail(vars.id)` to their snapshots
    - `onSettled`: invalidate `sessionsKeys.listInfinite()` AND `sessionsKeys.detail(vars.id)` AND `setsKeys.list(vars.id)` (last one cleans the now-orphaned sets cache)
    - `retry: 1`

    Add import for the `SessionSummary` type — it's already exported from sessions.ts per Plan 01 Task 4 (`export type SessionSummary`). Add to the existing imports block at the top of client.ts:
    ```
    import { type SessionSummary } from "@/lib/queries/sessions";
    ```
    (Verify there is no circular-import issue — sessions.ts imports from client.ts? Check by grep — if sessions.ts does NOT import queryClient, this is fine. If it does, inline the type or move it to a shared types file.)

    Block 14 must NOT modify any earlier block. Use the same comment-banner pattern as blocks 1-13 (e.g., `// =========...` separator + numbered header).

    Run `cd app && npx tsc --noEmit` to confirm there is no type error from the new SessionSummary import.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit && grep -c "\\[\"session\", \"delete\"\\]" app/lib/query/client.ts | awk -F: '{ exit ($NF >= 1) ? 0 : 1 }' && grep -c "useDeleteSession" app/lib/queries/sessions.ts | awk -F: '{ exit ($NF >= 1) ? 0 : 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && npx tsc --noEmit` exits 0
    - `app/lib/queries/sessions.ts` contains literal `export function useDeleteSession`
    - `app/lib/queries/sessions.ts` contains literal `scope: sessionId ? { id: \`session:${sessionId}\` }` (template-literal scope.id — matches Phase 4 D-X scope-binding convention; tolerant of backtick variant in grep)
    - `app/lib/query/client.ts` contains literal `["session", "delete"]` (mutationKey)
    - `app/lib/query/client.ts` contains literal `from("workout_sessions").delete().eq("id"` (mutationFn shape)
    - `app/lib/query/client.ts` contains literal `previousList.pages.map` (Pitfall 6 — InfiniteQuery envelope mapping)
    - `app/lib/query/client.ts` onSettled for this block invalidates 3 keys (`listInfinite` + `detail` + `setsKeys.list`) — grep nearby
    - No circular-import error from the new `SessionSummary` import (tsc passes)
    - Existing 13 setMutationDefaults blocks untouched (verify by `git diff app/lib/query/client.ts` showing additions only after block 13)
  </acceptance_criteria>
  <done>
    Delete-session mutation hook + offline-safe optimistic update wired. Ready for the screen to consume in Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author /history/[sessionId].tsx read-only session-detail screen + inline-overlay overflow + delete-confirm + toast</name>
  <files>app/app/(app)/history/[sessionId].tsx</files>
  <read_first>
    - app/app/(app)/workout/[sessionId].tsx FULL FILE (primary analog — card-per-exercise + Stack.Screen header + useExercisesQuery+Map lookup; PATTERNS.md "history/[sessionId].tsx" cites lines 44-82 + 181-199 + 233-240 + 420-475 verbatim)
    - app/app/(app)/plans/[id].tsx FULL FILE (secondary analog — inline-overlay-destructive-confirm at lines 616-716 + useFocusEffect overlay reset at lines 168-173 + mutate-not-mutateAsync at lines 206-213 + theme-aware accent binding at lines 105-108; PATTERNS.md "history/[sessionId].tsx" cites these line ranges)
    - app/lib/queries/sessions.ts (Plan 01 + Task 1 state — useSessionQuery + useDeleteSession + SessionSummary type; useSessionQuery already implements initialData-from-listInfinite per Phase 4 commit eca0540 pattern in Plan 01 wiring? VERIFY: if initialData currently seeds from `sessionsKeys.list()` instead of `sessionsKeys.listInfinite()`, EXTEND its initialData to also try the listInfinite cache flatten — Plan 02 owns this seed because Plan 01 introduced listInfinite)
    - app/lib/queries/sets.ts useSetsForSessionQuery(sessionId) (Phase 5) — returns SetRow[] ordered by exercise_id + set_number
    - app/lib/queries/exercises.ts useExercisesQuery (Phase 4) — for Map<id, name> lookup
    - app/lib/queries/plans.ts usePlanQuery(planId) (Phase 4) — returns single plan WITHOUT archived_at filter; PATTERNS.md confirms reusable as-is
    - .planning/phases/06-history-read-side-polish/06-UI-SPEC.md §"Session-detail screen container" + §"Session-detail summary-header" + §"Session-detail exercise-card" + §"Session-detail overflow-menu trigger" + §"Session-detail overflow-menu overlay" + §"Session-detail delete-confirm overlay" + §"Post-delete toast" — verbatim JSX shapes and copy
    - .planning/phases/06-history-read-side-polish/06-CONTEXT.md D-05/D-06/D-07/D-09/D-10/D-11/D-12/D-13 (all session-detail behavioral locks)
    - .planning/phases/06-history-read-side-polish/06-PATTERNS.md "app/app/(app)/history/[sessionId].tsx" full section (imports + Stack header + loading gate + exercise-name lookup + card pattern + inline-overlay + useFocusEffect + mutation invocation + differences-to-apply)
  </read_first>
  <action>
    Create NEW file `app/app/(app)/history/[sessionId].tsx`. The screen is composed entirely from existing analogs — minimal NEW JSX is the summary-header chip-row.

    Top-of-file imports (PATTERNS.md verbatim list): React `useCallback, useMemo, useState`; RN `Pressable, ScrollView, Text, View, useColorScheme`; SafeAreaView from `react-native-safe-area-context`; expo-router `Stack, useFocusEffect, useLocalSearchParams, useRouter, type Href`; Ionicons from `@expo/vector-icons`; date-fns `format, differenceInMinutes`; date-fns/locale `sv`; queries `useSessionQuery, useDeleteSession` from `@/lib/queries/sessions`; `useSetsForSessionQuery` from `@/lib/queries/sets`; `useExercisesQuery` from `@/lib/queries/exercises`; `usePlanQuery` from `@/lib/queries/plans`.

    Theme bindings (PATTERNS.md "Theme-aware accent + axes binding" pattern): `const scheme = useColorScheme(); const isDark = scheme === "dark"; const muted = isDark ? "#9CA3AF" : "#6B7280"; const accent = isDark ? "#60A5FA" : "#2563EB";`

    Route params: `const { sessionId } = useLocalSearchParams<{ sessionId: string }>();`

    Queries:
    - `const sessionQuery = useSessionQuery(sessionId!)`
    - `const setsQuery = useSetsForSessionQuery(sessionId!)`
    - `const exercisesQuery = useExercisesQuery()`
    - `const planQuery = usePlanQuery(sessionQuery.data?.plan_id ?? undefined)` (gate on plan_id — usePlanQuery should accept undefined and stay disabled)
    - `const deleteSession = useDeleteSession(sessionId!)`

    Loading gate (PATTERNS.md "Loading-gate pattern" verbatim — gate on `!session`, not `isPending`): if `!sessionQuery.data` → render SafeAreaView with centered "Laddar…" Body Muted text. If `sessionQuery.error` → render `Något gick fel. Försök igen.` (UI-SPEC error copy).

    Exercise-name lookup (PATTERNS.md verbatim): `const exerciseNameById = useMemo(...)` building `Map<string, string>` from exercises.data.

    Set-grouping: `const setsByExercise = useMemo(() => { const m = new Map<string, SetRow[]>(); for (const s of (setsQuery.data ?? [])) { if (!m.has(s.exercise_id)) m.set(s.exercise_id, []); m.get(s.exercise_id)!.push(s); } return m; }, [setsQuery.data]);` (sets are already ordered by exercise_id ASC, set_number ASC per Phase 5 query).

    Aggregates for summary-header:
    - `const setCount = (setsQuery.data ?? []).length`
    - `const totalVolumeKg = (setsQuery.data ?? []).reduce((sum, s) => sum + s.weight_kg * s.reps, 0)` (D-13 — empty session = 0)
    - `const durationMin = sessionQuery.data?.finished_at && sessionQuery.data?.started_at ? differenceInMinutes(new Date(sessionQuery.data.finished_at), new Date(sessionQuery.data.started_at)) : null` (D-10 — '—' when finished_at is null)
    - Local `formatNumber(n)` helper using `n.toLocaleString("sv-SE")` (matches Plan 01 helper — could extract to a shared util in V1.1 but inline is fine)

    Overlay state: `const [showOverflowMenu, setShowOverflowMenu] = useState(false); const [showDeleteConfirm, setShowDeleteConfirm] = useState(false); const [bannerError, setBannerError] = useState<string | null>(null); const [showToast, setShowToast] = useState(false);`

    useFocusEffect overlay reset (PATTERNS.md verbatim — Phase 4 commit af6930c pattern; RESEARCH Pitfall 7):
    ```
    useFocusEffect(useCallback(() => {
      return () => {
        setShowOverflowMenu(false);
        setShowDeleteConfirm(false);
      };
    }, []));
    ```

    Delete handler (PATTERNS.md "Mutation invocation pattern" + mutate-not-mutateAsync):
    ```
    const onDeleteConfirm = () => {
      setShowDeleteConfirm(false);
      deleteSession.mutate(
        { id: sessionId! },
        {
          onError: () => setBannerError("Kunde inte ta bort passet. Försök igen."),
        },
      );
      setShowToast(true);
      setTimeout(() => setShowToast(false), 2200);
      router.replace("/(tabs)/history" as Href);
    };
    ```
    (Toast fires immediately after the optimistic remove from cache — UI-SPEC §Post-delete toast confirms this is the offline-safe pattern; onError surfaces banner-error if eventual replay fails after reconnect.)

    JSX structure (UI-SPEC §Visuals verbatim):
    1. `<Stack.Screen options={{ headerShown: true, title: format(new Date(session.started_at), "d MMM yyyy", { locale: sv }), headerRight: () => <OverflowMenuTrigger /> }} />`
    2. SafeAreaView `flex-1 bg-white dark:bg-gray-900`
    3. ScrollView with contentContainerStyle `{ paddingHorizontal: 16, paddingTop: 16, paddingBottom: 96 }`
    4. Inside ScrollView: `<View className="gap-6">` containing:
       - SummaryHeader: `<View className="flex-row gap-2 flex-wrap">` with three chip Views (`bg-gray-100 dark:bg-gray-800 rounded-full px-3 py-1`) holding Label-muted Texts `${setCount} set`, `${formatNumber(totalVolumeKg)} kg`, `${durationMin ?? '—'} min`
       - Exercise-cards: `<View className="gap-2">` mapping over exercise_ids in setsByExercise (insertion order = order of first appearance in sets query, which is already exercise_id-grouped). For each exercise_id: render the ExerciseCard JSX per PATTERNS.md "Card-per-exercise + set-list pattern" — card Pressable wraps the header (exercise name + chip-row with `${set_count} set` + `${max_weight} kg`) → onPress routes to `/exercise/${exerciseId}/chart` as Href (D-11 + D-25; route ships in Plan 03 so `as Href` cast is necessary).
    5. OverflowMenuOverlay (UI-SPEC §"Session-detail overflow-menu overlay" verbatim) — conditionally rendered when `showOverflowMenu` is true; Phase 4 commit 954c480 pattern; tap-outside scrim dismisses; single item "Ta bort pass" in red-600/red-400 → onPress sets showOverflowMenu=false + showDeleteConfirm=true
    6. DeleteConfirmOverlay (UI-SPEC §"Session-detail delete-confirm overlay" + PATTERNS.md "Inline-overlay delete-confirm pattern" verbatim — Phase 4 commit e07029a) — full-screen absolute View bg-black/50, dialog `bg-white dark:bg-gray-800 rounded-xl p-6 gap-4` with title "Ta bort detta pass?" + body `${setCount} set och ${formatNumber(totalVolumeKg)} kg total volym försvinner permanent. Det går inte att ångra.` + Avbryt + Ta bort buttons (Avbryt sets showDeleteConfirm=false; Ta bort calls onDeleteConfirm). Use the REACT-NATIVE-STYLE-OBJECT layout pattern (Phase 4 e07029a — NOT NativeWind classes on the overlay container) so absolute-position works on iOS.
    7. ToastPostDelete (UI-SPEC §"Post-delete toast" verbatim) — Reanimated FadeIn/FadeOut. Reuse Phase 5 toast pattern: View `absolute bottom-20 self-center bg-blue-600 dark:bg-blue-500 px-6 py-3 rounded-full` with `accessibilityRole="alert"` + Text Body semibold white "Passet borttaget". Mount conditionally on `showToast`. Reanimated `entering={FadeIn}` `exiting={FadeOut.duration(300)}`.

    OverflowMenuTrigger (defined inline near the top of the component file or as a small nested component): Pressable `p-2` with hitSlop=8 wrapping Ionicons `ellipsis-horizontal` size 24 color={muted}; onPress = setShowOverflowMenu(true); accessibilityRole="button" + accessibilityLabel="Pass-menyn".

    No analog has a banner-error inline like the form-screens — borrow the convention from `plans/[id].tsx` (`setBannerError("Kunde inte X. Försök igen.")` + a transient banner View at the top of the ScrollView). Render `{bannerError && <BannerError text={bannerError} onDismiss={() => setBannerError(null)} />}` if banner is non-null.

    Plan-name in the summary block (if you choose to surface it under the date header per D-08 + UI-SPEC mentions plan name on history-list row but session-detail's plan-name is shown via the existing Stack header title which only carries the date) — UI-SPEC limits the screen header to the date; plan-name lives on history-list row instead. So Task 2 does NOT render plan-name on the detail screen; usePlanQuery is only fetched for future use (V1.1 might surface "Avbruten plan: X" — leave the query call but don't render). Alternatively: omit the usePlanQuery call entirely since UI-SPEC doesn't render plan-name on session-detail. PLANNER CHOOSES: omit the usePlanQuery call to reduce surface area (no benefit, slight RLS check cost; the LIST already shows plan-name).

    Final empty-defensive: if `setsQuery.data` is empty (D-13 — empty pass), the screen still renders summary `0 set · 0 kg · X min` and `<View className="gap-2">` with zero cards. The user can still tap the overflow → delete.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit && npx expo lint</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && npx tsc --noEmit` exits 0
    - `cd app && npx expo lint` exits 0 (warnings allowed)
    - File `app/app/(app)/history/[sessionId].tsx` exists
    - File contains literal `useSessionQuery` AND `useSetsForSessionQuery` AND `useExercisesQuery` AND `useDeleteSession`
    - File contains literal `Stack.Screen` AND `headerRight` (overflow trigger registered)
    - File contains literal `useFocusEffect` AND a callback resetting `setShowOverflowMenu(false)` AND `setShowDeleteConfirm(false)` (Pitfall 7 closed)
    - File contains literal `Ta bort detta pass?` (delete-confirm title — UI-SPEC verbatim)
    - File contains literal `försvinner permanent` (delete-confirm body — UI-SPEC verbatim)
    - File contains literal `Passet borttaget` (toast — UI-SPEC verbatim)
    - File contains literal `mutate({ id` AND does NOT contain `mutateAsync` (mutate-not-mutateAsync convention)
    - File contains literal `router.replace` AND `/(tabs)/history` (post-delete navigation)
    - File contains literal `as Href` on the route literal `/exercise/${...}/chart` (cross-plan-route cast since Plan 03 owns the route file)
    - File contains literal `differenceInMinutes` (D-10 duration computation)
    - File contains literal `format(new Date(` AND `locale: sv` (Swedish date formatting)
    - File contains literal `bg-blue-600` AND `dark:bg-blue-500` on the toast (UI-SPEC §Color §Accent + §Post-delete toast — confirms accent blue, not success-green)
    - No import of `Modal` from react-native (inline-overlay is the locked pattern — Phase 4 commit e07029a)
    - No console.log left in final committed file
  </acceptance_criteria>
  <done>
    Session-detail route renders read-only summary + cards-per-exercise + overflow trigger + inline-overlay confirm + toast. Delete flow is wired end-to-end. The route literal `/exercise/<id>/chart` is cast `as Href` and resolves after Plan 03 ships.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend manual UAT markdown with Plan 02 session-detail + delete flow steps</name>
  <files>app/scripts/manual-test-phase-06-uat.md</files>
  <read_first>
    - app/scripts/manual-test-phase-06-uat.md current state (post-Plan-01; sections for F9 list + offline hydration already scaffolded)
    - app/scripts/manual-test-phase-04-airplane-mode.md (Phase 4 manual UAT — step-numbering + expected-result format)
    - app/scripts/manual-test-phase-05-f13-brutal.md (Phase 5 manual UAT — extensive multi-section markdown; mirror for the delete-pass offline path section)
    - .planning/phases/06-history-read-side-polish/06-VALIDATION.md "Manual-Only Verifications" table: rows for "Delete-pass inline-overlay UX + router.replace flow" + "Offline cache hydration"
    - .planning/phases/06-history-read-side-polish/06-CONTEXT.md D-07 + D-13 (the empty-pass-delete edge case is an explicit UAT target)
  </read_first>
  <action>
    Extend the existing Plan 01 UAT markdown by REPLACING the Plan 02 TODO section with actual steps. New sections to add (mirrors the markdown structure of `manual-test-phase-04-airplane-mode.md`):

    ## F9 Session-detail open + read (ROADMAP success #2)
    Pre-condition: ≥3 finished sessions exist (seed via dev account if soak data not present)
    Steps:
    1. Open Historik tab → tap any row → expect navigation to /history/<id>
    2. Verify Stack header shows the formatted date (e.g. "14 maj 2026")
    3. Verify summary-header chips: `${set_count} set`, `${total_volume_kg} kg`, `${duration} min`
    4. Verify each exercise-card shows exercise name + set-count chip + max-weight chip + N set-rows in order (Set 1, Set 2…)
    5. Verify empty-pass (0 sets) renders `0 set · 0 kg · X min` and shows no exercise-cards (D-13)
    6. Verify back-button returns to Historik

    ## F9 Delete-pass online flow (D-07)
    Steps:
    1. From session-detail → tap header-right `...` icon → overflow menu appears at top-right
    2. Verify menu item "Ta bort pass" renders in red color
    3. Tap "Ta bort pass" → overflow menu closes + delete-confirm overlay appears
    4. Verify confirmation body shows exact count + volume: `${count} set och ${volume} kg total volym försvinner permanent. Det går inte att ångra.`
    5. Tap "Avbryt" → overlay dismisses, screen still on session-detail
    6. Tap `...` again → "Ta bort pass" → "Ta bort" → expect: optimistic remove from list cache, router.replace to Historik tab, "Passet borttaget" toast bottom-center on accent-blue background, deletion persists (verify in Supabase Studio: workout_sessions row gone, all associated exercise_sets rows gone via FK cascade)
    7. Verify history-list does NOT show the deleted session even after refresh

    ## F9 Delete-pass OFFLINE flow (per networkMode:'offlineFirst' + Phase 4 queue replay)
    Steps:
    1. Online → tap delete on a test session → verify deletion lands in Supabase
    2. New test session: enter Airplane Mode → tap delete on a different test session → verify optimistic remove + toast appear immediately
    3. Force-quit app → reopen offline → verify session is still gone from history-list (cache restored from AsyncStorage)
    4. Toggle Airplane Mode off → wait 10s for resumePausedMutations → check Supabase Studio: deleted session is also gone server-side (mutation replayed)

    ## F9 freezeOnBlur overlay-reset (Pitfall 7 regression test)
    Steps:
    1. Open session-detail → tap `...` → confirm overflow menu visible
    2. Swipe back → swipe forward to return → expect overflow menu is NOT visible (useFocusEffect cleanup)
    3. Same with delete-confirm overlay: open → swipe back → swipe forward → expect overlay is NOT visible

    ## F9 Cross-link to chart (D-11 + D-25)
    Steps (works only after Plan 03 ships):
    1. From session-detail → tap an exercise-card header → expect navigation to /exercise/<id>/chart
    2. Before Plan 03 ships: tap is wired (route literal compiles via as Href cast) but navigation 404s in the dev server — this is expected and resolves when Plan 03 commits land

    Leave the Plan 03 TODO section header so the chart-route UAT can be filled in by Plan 03's manual-UAT update.

    Total new lines ≥30; total file ≥60 lines after Task 3 lands.
  </action>
  <verify>
    <automated>grep -c "F9 Session-detail open" app/scripts/manual-test-phase-06-uat.md | awk -F: '{ exit ($NF >= 1) ? 0 : 1 }' && grep -c "Passet borttaget" app/scripts/manual-test-phase-06-uat.md | awk -F: '{ exit ($NF >= 1) ? 0 : 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `app/scripts/manual-test-phase-06-uat.md` contains literal `F9 Session-detail open` section header (grep)
    - File contains literal `F9 Delete-pass online flow`
    - File contains literal `F9 Delete-pass OFFLINE flow` (offline-queue replay path)
    - File contains literal `freezeOnBlur` (Pitfall 7 regression call-out)
    - File contains literal `Passet borttaget` (toast copy)
    - File ≥ 60 non-comment lines (`grep -v '^#' file | wc -l`)
    - Plan 03 TODO section header preserved (e.g. literal `Plan 03` somewhere in the file as a TODO marker)
  </acceptance_criteria>
  <done>
    Manual UAT checklist extended with delete-pass flow (online, offline, freezeOnBlur, cross-link). Ready for human iPhone verification after the phase ships.
  </done>
</task>

</tasks>

<verification>
- After all three tasks land: `cd app && npx tsc --noEmit && npm run test:rls && npx expo lint` exits 0 (no regression vs Plan 01)
- Manual UAT smoke: open existing finished session from history → see summary + cards + sets; tap `...` → tap Ta bort pass → confirm → land on history with toast; record session is gone from Supabase Studio + history list (manual-test-phase-06-uat.md sections F9 Session-detail + Delete-pass)
- Threat register: T-06-03, T-06-06, T-06-07, T-06-08, T-06-12 mitigations all sourced from RLS (no new test surface; Plan 01 test-rls extension already gates the cross-user paths)
- No new client-bundled dependency added to app/package.json (Plan 02 adds zero deps)
</verification>

<success_criteria>
- ROADMAP success #2 (open historical session → see all sets per exercise) MET
- /history/[sessionId] route renders without error for any RLS-owned session-id; renders error copy for spoofed/missing id (T-06-06 mitigation)
- Delete-pass works online (immediate server delete + FK cascade) and offline (optimistic remove + queued mutation replays on reconnect)
- Pitfall 7 (freezeOnBlur ghost overlay) closed via useFocusEffect cleanup
- Pitfall 6 (InfiniteQuery envelope) closed via pages.map(page => page.filter(...)) in onMutate
- mutate-not-mutateAsync convention enforced; no mutateAsync grep-match in new route file
- Plan 03 entry-point (card-header tap → /exercise/[exerciseId]/chart) wired with `as Href` cast; resolves when Plan 03 ships
</success_criteria>

<output>
After completion, create `.planning/phases/06-history-read-side-polish/06-02-SUMMARY.md` per the template. Record:
- Files created/modified (count + paths)
- Mutation block 14 ['session','delete'] location in client.ts
- Cache-key envelope handling (Pitfall 6 confirmation)
- Any deviations
- Recommended next step: `/gsd-execute-phase 6` continues with Plan 03 (F10 chart slice)
</output>
