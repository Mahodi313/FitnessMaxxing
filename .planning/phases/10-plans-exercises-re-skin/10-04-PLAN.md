---
phase: 10-plans-exercises-re-skin
plan: 04
type: execute
wave: 4
depends_on: ["10-01"]
files_modified:
  - app/app/(app)/plans/[id]/exercise/[planExerciseId]/edit.tsx
  - app/locales/sv.json
  - app/locales/en.json
autonomous: true
requirements: [SKIN-03, I18N-05]
must_haves:
  decisions:
    - "D-13: plan-exercise edit screen (FExerciseEdit) — sets/reps steppers + notes + remove"
    - "D-14: targets fully optional (target_sets/reps_min/reps_max nullable; steppers clear to null)"
    - "D-15: edit-screen per-screen locale keys added"
    - "D-16: notes stored raw, never auto-translated"
  truths:
    - "The plan-exercise edit screen matches FExerciseEdit in light + dark: target preview chip, sets stepper, reps min/max steppers, notes, remove-from-plan"
    - "Targets are fully optional — a range, one bound, or none; steppers clear to null"
    - "Saving writes target_sets/target_reps_min/target_reps_max/notes via mutate (not mutateAsync)"
    - "Remove-from-plan removes the plan_exercises template row (no confirm)"
    - "Header uses Stäng (close) + Spara mål (save targets), never generic Avbryt/Spara"
  artifacts:
    - path: "app/app/(app)/plans/[id]/exercise/[planExerciseId]/edit.tsx"
      provides: "Re-skinned target-edit modal with FStepperInput steppers"
      contains: "FStepperInput"
    - path: "app/locales/sv.json"
      provides: "edit-screen chrome keys (sv)"
      contains: "saveTargets"
  key_links:
    - from: "app/app/(app)/plans/[id]/exercise/[planExerciseId]/edit.tsx"
      to: "useUpdatePlanExercise(planId)"
      via: "mutate({ id, plan_id, target_*, notes }, { onError })"
      pattern: "useUpdatePlanExercise"
---

<objective>
Re-skin the plan-exercise edit modal (`edit.tsx`) to `FExerciseEdit`: a target preview chip, an `FStepperInput` for sets (single) and reps (Min/Max pair), an optional notes box, and a danger-ghost remove-from-plan affordance. Add the edit-screen locale keys (D-15).

Purpose: SKIN-03's plan-exercise-edit deliverable (exposes target sets+reps+notes, D-13/D-14). Consumes the existing `useUpdatePlanExercise`/`useRemovePlanExercise` hooks; targets stay fully optional (nullable). This plan owns the locale file in Wave 4 (no other Wave-4 plan writes locales).
Output: Re-skinned `edit.tsx` + edit locale keys.
</objective>

<execution_context>
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-plans-exercises-re-skin/10-CONTEXT.md
@.planning/phases/10-plans-exercises-re-skin/10-RESEARCH.md
@.planning/phases/10-plans-exercises-re-skin/10-PATTERNS.md
@.planning/phases/10-plans-exercises-re-skin/10-UI-SPEC.md

<interfaces>
From app/lib/queries/plan-exercises.ts (existing):
  useUpdatePlanExercise(planId)  // .mutate({ id, plan_id, target_sets, target_reps_min, target_reps_max, notes }, { onError })
  useRemovePlanExercise(...)     // removes the plan_exercises template row

From app/components/ui (Phase 8 barrel):
  ForgeButton, ForgeField, ForgeCard, ForgeChip, Icon

target_* fields are all nullable (D-14): a range, one bound, or none.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-skin edit modal to FExerciseEdit with FStepperInput steppers</name>
  <read_first>
    - app/app/(app)/plans/[id]/exercise/[planExerciseId]/edit.tsx (full file) — current RHF/GHRV/modal shell, useUpdatePlanExercise/useRemovePlanExercise wiring, the z.coerce.number three-arg useForm generic note (STATE.md Phase 4 Plan 03)
    - app/design v2/Sources/design/forge-screens.jsx — FExerciseEdit (line 1438): target preview chip, sets/reps steppers, notes, remove; FStepperInput (line 1553)
    - app/app/(app)/plans/[id]/exercise-picker.tsx lines 161-169 — GHRV wrapper + useFocusEffect reset idiom to mirror (SP-7/SP-8)
    - app/components/ui/ForgeButton.tsx (variant/size + FIT-66 style-fn pressed/shadow); barrel app/components/ui/index.ts
    - 10-UI-SPEC.md line 161 (edit interaction), lines 66-71 (FStepperInput optical: container 120×64 radius 14, ± tiles 32×32 radius 8), lines 101-102 (stepper typography), 206-212 (edit copy)
    - 10-PATTERNS.md edit assignment (lines 246-250) + SP-2/SP-5/SP-6/SP-8
  </read_first>
  <action>
    Re-skin app/app/(app)/plans/[id]/exercise/[planExerciseId]/edit.tsx to FExerciseEdit. Compose from the Phase 8 barrel; apply optical values (stepper container 120×64 radius 14 borderStrong; ± tiles 32×32 radius 8, − on surface2, + on accent; target chip padding 6×12 radius 10 accentSoft; remove button 52 tall radius 14) as inline `style={{}}` numbers (Pitfall 3). Light+dark parity on every surface. Own GestureHandlerRootView wrapper (SP-8) + `useFocusEffect` reset of any local overlay state (SP-7).
    Header: dismiss `t('closeModal')` (Stäng, accent — modal-close, abandons edits) + confirm `t('saveTargets')` (Spara mål, accent bold — NOT generic Spara). Title `t('editTargets')` (Redigera mål).
    Build `FStepperInput` (composed from tokens — sets = single value; reps = Min/Max pair with a "–" separator) writing into `target_sets` / `target_reps_min` / `target_reps_max`. Targets FULLY OPTIONAL (D-14): each stepper is clearable to null (a range, one bound, or none). Tabular-nums on stepper values (DSGN-03). Sub-labels `t('targetSets')`/`t('reps')`/`t('repsMin')`/`t('repsMax')`; help text `t('setsHelp')`/`t('repsHelp')`. Target preview chip (`4 × 6–8 reps`) renders null-safe from the current stepper values (accent text + spark icon on accentSoft).
    Notes multiline `ForgeField` (`t('notes')` label, `t('notesPlaceholder')` placeholder, stored raw D-16).
    Submit via `useUpdatePlanExercise(planId).mutate({ id, plan_id, target_sets, target_reps_min, target_reps_max, notes }, { onError })` — `.mutate` NOT mutateAsync (SP-2). Remove-from-plan = danger ghost `ForgeButton` (trash icon, `t('removeFromPlan')`, `text-forge-danger`) → `useRemovePlanExercise` (no confirm — reversible by re-adding). All chrome via live `t()`.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - edit.tsx renders an FStepperInput-style stepper for sets + a Min/Max reps pair writing into target_sets/target_reps_min/target_reps_max (all clearable to null)
    - Submit calls `useUpdatePlanExercise(planId).mutate(...)` (not mutateAsync)
    - Header uses `t('closeModal')` + `t('saveTargets')`; remove uses `t('removeFromPlan')` (no Avbryt/bare Spara)
    - GestureHandlerRootView wrapper + useFocusEffect reset present
    - `npx tsc --noEmit` + `npm run lint` exit 0
  </acceptance_criteria>
  <done>Edit modal re-skinned to FExerciseEdit with optional-target steppers, preview chip, notes, and danger-ghost remove.</done>
</task>

<task type="auto">
  <name>Task 2: Edit-screen locale keys (sv + en)</name>
  <read_first>
    - app/locales/sv.json + en.json (Plans 02/03 added seed/taxonomy + picker keys — add edit keys here, parity-equal)
    - 10-UI-SPEC.md lines 206-212 (edit copy: editTargets, saveTargets, targetSets, reps, repsMin, repsMax, setsHelp, repsHelp, notesPlaceholder, removeFromPlan)
    - app/scripts/check-locale-parity.ts
  </read_first>
  <action>
    Add to BOTH locale files (parity-equal): `editTargets` (Redigera mål/Edit targets), `saveTargets` (Spara mål/Save targets), `targetSets` (Antal set/Sets), `reps` (Reps/Reps), `repsMin` (Min/Min), `repsMax` (Max/Max), `setsHelp` (Hur många set per pass./How many sets per session.), `repsHelp` (Sätt min och max för ett intervall, eller bara ett av dem./Set a min/max range or just one.), `notesPlaceholder` (Tempo, vilotid, formfokus…/Tempo, rest, form cues…), `removeFromPlan` (Ta bort från planen/Remove from plan). Reuse `closeModal`/`notes` from Plan 03 if already present (do NOT duplicate). No `cancel`/bare `save`. EXACT sv/en from UI-SPEC.
  </action>
  <verify>
    <automated>cd app && npm run check:locale-parity</automated>
  </verify>
  <acceptance_criteria>
    - Both locale files contain `editTargets`, `saveTargets`, `targetSets`, `repsMin`, `repsMax`, `removeFromPlan` with sv + en values
    - `npm run check:locale-parity` exits 0
    - No `cancel`/bare `save` key added; no duplicate keys
  </acceptance_criteria>
  <done>Edit chrome keys added; parity green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input (steppers/notes) → Supabase | Target numbers + free-text notes cross into plan_exercises via the update mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-13 | Tampering | Editing/removing another user's plan_exercises row | mitigate | useUpdatePlanExercise/useRemovePlanExercise run under own-row RLS (plan_exercises gated via the owning plan's user_id); no client-side authz reliance |
| T-10-14 | Tampering | Out-of-range/non-numeric target values | mitigate | z.coerce.number form schema + nullable bounds; steppers constrain input; nulls allowed (D-14) |
| T-10-15 | Denial | mutateAsync stall under offlineFirst | mitigate | `.mutate(payload, { onError })` (SP-2) |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies; pure re-skin |
</threat_model>

<verification>
- `npx tsc --noEmit` + `npm run lint` clean.
- `npm run check:locale-parity` PASS.
- Manual (device UAT): edit screen light+dark; steppers set/clear targets; remove-from-plan; flip sv↔en confirms chrome translates.
</verification>

<success_criteria>
- Edit screen matches FExerciseEdit in light + dark with optional targets.
- Save via mutate; remove no-confirm.
- Edit locale keys present + parity green.
</success_criteria>

<output>
Create `.planning/phases/10-plans-exercises-re-skin/10-04-SUMMARY.md` when done.
</output>
