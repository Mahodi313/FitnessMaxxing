---
phase: 10-plans-exercises-re-skin
plan: 05
type: execute
wave: 5
depends_on: ["10-01", "10-04"]
files_modified:
  - app/app/(app)/(tabs)/index.tsx
  - app/app/(app)/plans/new.tsx
  - app/app/(app)/(tabs)/_layout.tsx
  - app/locales/sv.json
  - app/locales/en.json
autonomous: true
requirements: [SKIN-02, SKIN-07, I18N-05]
must_haves:
  truths:
    - "The plans list (Planer tab) matches FHome's plan-list portion in light + dark (no dashboard hero/ring)"
    - "Empty state, populated plan-card list, Ny plan link, FAB, and active-session banner are preserved and re-skinned"
    - "New-plan exposes name (required) + description (optional) and creates via mutate"
    - "The tab bar (Planer/Historik/Inställningar) matches the Forge design in light + dark"
  artifacts:
    - path: "app/app/(app)/(tabs)/index.tsx"
      provides: "Re-skinned plans list (FHome plan-list portion)"
      contains: "usePlansQuery"
    - path: "app/app/(app)/plans/new.tsx"
      provides: "Re-skinned new-plan form (name + description)"
      contains: "useCreatePlan"
    - path: "app/app/(app)/(tabs)/_layout.tsx"
      provides: "Re-skinned Forge tab bar, light + dark"
      contains: "forge"
  key_links:
    - from: "app/app/(app)/(tabs)/index.tsx"
      to: "usePlansQuery (archived_at is null)"
      via: "list query"
      pattern: "usePlansQuery"
    - from: "app/app/(app)/plans/new.tsx"
      to: "useCreatePlan().mutate"
      via: "create submit"
      pattern: "\\.mutate\\("
---

<objective>
Re-skin the plans list (Planer tab `index.tsx`) to `FHome`'s plan-list portion (SKIN-02), the new-plan screen (`new.tsx`) to `FNewPlan` (SKIN-02, D-12), and the tab bar (`(tabs)/_layout.tsx`) to the Forge design in light + dark (SKIN-07). Add their per-screen locale keys (D-15).

Purpose: SKIN-02 (list + new-plan) and SKIN-07 (tab bar) deliverables. The plans list and tab bar are the user's entry chrome; new-plan surfaces the existing `description` field (no migration). This plan owns the locale file in Wave 5 (Plan 06 takes Wave 6 to avoid a locale write conflict).
Output: Re-skinned `index.tsx`, `new.tsx`, `(tabs)/_layout.tsx` + their locale keys.
</objective>

<execution_context>
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-plans-exercises-re-skin/10-CONTEXT.md
@.planning/phases/10-plans-exercises-re-skin/10-RESEARCH.md
@.planning/phases/10-plans-exercises-re-skin/10-PATTERNS.md
@.planning/phases/10-plans-exercises-re-skin/10-UI-SPEC.md

<interfaces>
From app/lib/queries/plans.ts (existing): usePlansQuery() // archived_at is null; useCreatePlan()
From app/components/ui (Phase 8 barrel): ForgeButton, ForgeField, ForgeCard, ForgeChip, ForgeStat, TabBar, Icon
From app/components/ui/TabBar.tsx (Phase 8 shell — built standalone with active/onSelect props, NOT wired to live <Tabs>, per STATE.md OQ-5)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-skin plans list (Planer tab) to FHome plan-list portion</name>
  <read_first>
    - app/app/(app)/(tabs)/index.tsx (full file) — empty state ("Inga planer än" + CTA), populated FlatList, "Skapa ny plan" FAB, usePlansQuery, active-session banner
    - app/design v2/Sources/design/forge-screens.jsx — FHome (line 88): header row, page title, section header, plan-card list, brand-mark tile, featured-card gradient (i===0)
    - app/app/(app)/(tabs)/settings.tsx lines 10-11, 54, 60 — Phase 9 live-t() list re-skin idiom (analog)
    - app/components/ui barrel + ForgeCard/ForgeButton/Icon; ForgeButton FIT-66 style-fn note
    - 10-UI-SPEC.md lines 154 (plans-list interaction), 50 (3xl bottom padding clears TabBar), 134-144 (gradient brand/featured-card-only), 169-170 (reconciliation: plan-list ONLY, NO activity-ring hero — that is Phase 12)
    - 10-PATTERNS.md SP-2/SP-5/SP-6 + new.tsx/index.tsx assignment (lines 254-258)
  </read_first>
  <action>
    Re-skin app/app/(app)/(tabs)/index.tsx to FHome's PLAN-LIST PORTION ONLY (UI-SPEC reconciliation lines 169-170 — OMIT the activity-ring/streak/volume hero; that is Phase 12 DASH-01..05). Use FHome's header row + page title `t('myPlans')` + section header `MINA PLANER` + plan-card list as the layout reference. Compose from the Phase 8 barrel; optical values inline `style` (Pitfall 3); light+dark parity.
    Preserve ALL existing behavior: `usePlansQuery` (archived_at is null), empty state (`t('noPlans')` + `t('noPlansSub')` + CTA, FAB hidden when empty), populated plan-card list (card tap → `plans/[id]`, plan name + `n övningar` meta), the "Skapa ny plan" FAB / `t('newPlan')` section link, and the active-session banner above the list (Phase 4 carry-forward). Brand-mark tile (32×32, gradFrom→gradTo, white Logo) + featured-card gradient barbell tile on `i===0` ONLY (UI-SPEC color §); barbell on plan cards is a content icon in text2/accent, never gradient. Chrome via live `t()`.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - index.tsx preserves `usePlansQuery`, the empty state, the FAB (hidden when empty), and the active-session banner
    - No activity-ring/streak/volume hero is built (Phase 12 boundary)
    - Chrome routes through `t()` (myPlans/noPlans/newPlan)
    - `npx tsc --noEmit` + `npm run lint` exit 0
  </acceptance_criteria>
  <done>Plans list re-skinned to FHome's plan-list portion, all existing behavior preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Re-skin new-plan (FNewPlan) + tab bar (SKIN-07)</name>
  <read_first>
    - app/app/(app)/plans/new.tsx (full file) — current RHF create form
    - app/app/(app)/plans/[id].tsx lines 189-222 — the RHF form + SP-2 mutate-not-mutateAsync analog for new-plan submit
    - app/design v2/Sources/design/forge-screens.jsx — FNewPlan (line 1113, focused name field 2px accent border line 1156), TabBar (in lib.jsx)
    - app/app/(app)/(tabs)/_layout.tsx (full file) — current live <Tabs> config + OfflineBanner mount (STATE.md: banner ABOVE <Tabs> inside SafeAreaView edges top)
    - app/components/ui/TabBar.tsx — Phase 8 shell (active/onSelect props); app/app/(app)/_layout.tsx lines 61-89 Stack screenOptions light/dark idiom
    - 10-UI-SPEC.md line 155 (new-plan), 162 (tab bar: active accent strokeWidth 2 label 600, inactive text3 strokeWidth 1.6, floating bg-forge-tabBg, paddingTop 10/paddingBottom 28, light+dark REQUIRED), 188-189 (new-plan copy), 214 (tab labels exist)
    - 10-PATTERNS.md tab-bar assignment (lines 262-264) — planner's call: style live Tabs OR mount TabBar shell
  </read_first>
  <action>
    Re-skin app/app/(app)/plans/new.tsx to FNewPlan: name `ForgeField` (required, focused = `border-2 border-forge-accent`, 56 tall) `t('name')` label + `t('namePlaceholder')`; description multiline box (optional, `minHeight 110`) `t('description')` label + `t('planDescHelp')` help. `workout_plans.description` already exists — NO migration (D-12). Submit `ForgeButton primary` `t('createPlan')` (Skapa plan, trailing arrowRight) via `useCreatePlan().mutate(payload, { onError })` — `.mutate` NOT mutateAsync (SP-2). Own GestureHandlerRootView wrapper (modal route, SP-8). Light+dark parity.
    Re-skin the tab bar (SKIN-07) in app/app/(app)/(tabs)/_layout.tsx — planner's-call: either style the live expo-router `<Tabs>` `tabBar`/`screenOptions` to the Forge spec OR mount the Phase 8 `TabBar` shell wired to the live tab state. Active = `text-forge-accent` + icon strokeWidth 2 + label weight 600; inactive = `text-forge-text3` + strokeWidth 1.6. Floating `bg-forge-tabBg` + blur, paddingTop 10 / paddingBottom 28. Icons: Planer=`barbell` (content icon, never gradient), Historik=`clock`, Inställningar=`settings`. Labels `t('plans')`/`t('history')`/`t('settings')` (exist). LIGHT + DARK parity REQUIRED (use `useColorScheme()` for raw-color props). PRESERVE the OfflineBanner mount placement (above <Tabs>, inside SafeAreaView edges top) and the existing routes/behavior — appearance-only change.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - new.tsx exposes name (required) + description (optional) and submits via `useCreatePlan().mutate(...)` (not mutateAsync)
    - (tabs)/_layout.tsx tab bar styled to Forge with active=accent/strokeWidth 2, inactive=text3/strokeWidth 1.6, and light+dark parity
    - OfflineBanner placement + existing tab routes preserved
    - `npx tsc --noEmit` + `npm run lint` exit 0
  </acceptance_criteria>
  <done>New-plan re-skinned to FNewPlan; tab bar re-skinned to Forge in light + dark with routes/banner preserved.</done>
</task>

<task type="auto">
  <name>Task 3: New-plan + plan-detail-shared locale keys (sv + en)</name>
  <read_first>
    - app/locales/sv.json + en.json (Plans 02/03/04 added keys — add new-plan + shared eyebrow/stat keys here, parity-equal)
    - 10-UI-SPEC.md lines 188-194 (new-plan + plan-detail field/stat copy), 214 (tab labels exist)
    - app/scripts/check-locale-parity.ts
  </read_first>
  <action>
    Add to BOTH locale files (parity-equal): `description` (Beskrivning · valfritt/Description · optional — if not already added in Plan 03/04, add it; else reuse), `namePlaceholder` (t.ex. Push, Pull, Ben/e.g. Push, Pull, Legs), `planDescHelp` (the FNewPlan line 1117 help text, sv/en), `planEyebrow` (PLAN/PLAN), `exercisesStat` (Övningar/Exercises), `lastStat` (Senast/Last), `avgTime` (Snitt-tid/Avg time). Reuse existing `myPlans`/`newPlan`/`createPlan`/`noPlans`/`noPlansSub`/`startSession`/`addExercise`/`exercises`/`previous`/`plans`/`history`/`settings`/`name`/`notes` where present (do NOT duplicate). No `cancel`/bare `save`. EXACT sv/en from UI-SPEC. (`planEyebrow`/`*Stat`/`avgTime` are consumed by Plan 06's plan-detail re-skin — added here so Plan 06 only reads keys; this keeps Wave 5 the sole locale writer of these.)
  </action>
  <verify>
    <automated>cd app && npm run check:locale-parity</automated>
  </verify>
  <acceptance_criteria>
    - Both locale files contain `namePlaceholder`, `planDescHelp`, `planEyebrow`, `exercisesStat`, `lastStat`, `avgTime` with sv + en values
    - `npm run check:locale-parity` exits 0
    - No `cancel`/bare `save` key added; no duplicate keys
  </acceptance_criteria>
  <done>New-plan + plan-detail-shared chrome keys added; parity green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input (new-plan form) → Supabase | Free-text plan name + description cross into workout_plans via the create mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-16 | Tampering | Creating/listing another user's plan | mitigate | useCreatePlan/usePlansQuery run under own-row RLS; list filters archived_at; no client-side authz reliance |
| T-10-17 | Denial | mutateAsync stall under offlineFirst | mitigate | new-plan submit uses `.mutate(payload, { onError })` (SP-2) |
| T-10-18 | Info disclosure | Tab bar / list leaks another user's plans | mitigate | usePlansQuery is RLS-scoped; tab bar is pure chrome (no data) |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies; pure re-skin |
</threat_model>

<verification>
- `npx tsc --noEmit` + `npm run lint` clean.
- `npm run check:locale-parity` PASS.
- Manual (device UAT): plans list + new-plan + tab bar light+dark; create a plan; flip sv↔en confirms chrome translates; tab bar active/inactive states.
</verification>

<success_criteria>
- Plans list matches FHome plan-list portion (no hero) light + dark; behavior preserved.
- New-plan matches FNewPlan with name + description.
- Tab bar matches Forge in light + dark (SKIN-07).
- New-plan + plan-detail-shared locale keys present + parity green.
</success_criteria>

<output>
Create `.planning/phases/10-plans-exercises-re-skin/10-05-SUMMARY.md` when done.
</output>
