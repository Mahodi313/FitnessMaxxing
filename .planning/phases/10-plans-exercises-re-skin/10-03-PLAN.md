---
phase: 10-plans-exercises-re-skin
plan: 03
type: execute
wave: 3
depends_on: ["10-01", "10-02"]
files_modified:
  - app/app/(app)/plans/[id]/exercise-picker.tsx
  - app/locales/sv.json
  - app/locales/en.json
autonomous: true
requirements: [SKIN-03, I18N-05]
must_haves:
  decisions:
    - "D-01: muscle-group dropdown emits the 5 keys; rows render keys via t()"
    - "D-02: equipment stays free-text on user-created exercises, stored as written"
    - "D-04: muscle-group filter pills (Alla + 5 single-select group pills)"
    - "D-05: filter pills AND-combine with the search box (text match within group)"
    - "D-14: add-from-picker inserts plan_exercises with null targets (set later)"
    - "D-15: picker per-screen + error/empty locale keys added"
    - "D-16: user-created exercise name/equipment/notes stored raw, never translated"
  truths:
    - "The picker matches FExercisePicker in light + dark: modal-close affordance, search field, filter-pill row, create-new CTA, exercise list"
    - "Selecting a muscle-group pill filters the list to that group; tapping the active pill or 'Alla/All' clears the filter"
    - "Filter AND-combines with the search box; search matches the displayed (translated) name"
    - "Create-new exposes name + muscle-group dropdown (5 D-01 keys) + free-text equipment + notes; submit chains create→add under shared plan scope"
    - "Tapping an exercise's + inserts a plan_exercises row with null targets and dismisses (add-now-set-later)"
    - "Seed rows render their name + equipment bilingually; user rows render raw"
  artifacts:
    - path: "app/app/(app)/plans/[id]/exercise-picker.tsx"
      provides: "Re-skinned picker with filter pills, mg dropdown, bilingual seed display"
      contains: "resolveMuscleGroupKey"
    - path: "app/locales/sv.json"
      provides: "picker chrome + error/empty keys (sv)"
      contains: "searchExercise"
  key_links:
    - from: "app/app/(app)/plans/[id]/exercise-picker.tsx"
      to: "app/lib/muscle-group.ts"
      via: "import resolveMuscleGroupKey for pill filter + display"
      pattern: "resolveMuscleGroupKey"
    - from: "app/app/(app)/plans/[id]/exercise-picker.tsx"
      to: "useCreateExercise(planId) + useAddExerciseToPlan(planId)"
      via: "shared scope.id='plan:<planId>' chained create→add"
      pattern: "plan:"
---

<objective>
Re-skin the exercise picker (`exercise-picker.tsx`) to `FExercisePicker`/`FExercisePickerNew`: modal-close affordance + search field + muscle-group filter pills (D-04/D-05) + create-new CTA + bilingual exercise list, and the inline create-new form with the muscle-group dropdown (D-01). Add the picker's per-screen + error/empty locale keys (D-15).

Purpose: This is the SKIN-03 browse+filters+create-new deliverable. It consumes Plan 02's `resolveMuscleGroupKey` + the seeded library so the picker is never empty, and Plan 01's extended `seed_key` schema for bilingual display. It must preserve the FK-safe chained-create-and-add scope contract and the GestureHandlerRootView modal wrapper.
Output: Re-skinned `exercise-picker.tsx` + picker locale keys.
</objective>

<execution_context>
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-plans-exercises-re-skin/10-CONTEXT.md
@.planning/phases/10-plans-exercises-re-skin/10-RESEARCH.md
@.planning/phases/10-plans-exercises-re-skin/10-PATTERNS.md
@.planning/phases/10-plans-exercises-re-skin/10-UI-SPEC.md

<interfaces>
From app/lib/muscle-group.ts (Plan 02):
  resolveMuscleGroupKey(raw: string | null): MuscleGroupKey | null
  type MuscleGroupKey = "chest"|"back"|"legs"|"shoulders"|"arms"|"other"

From app/components/ui (Phase 8 barrel):
  ForgeButton, ForgeField, ForgeCard, ForgeChip, Icon  // variant/size enum props, NOT className passthrough

From app/lib/queries/exercises.ts + plan-exercises.ts (existing):
  useExercisesQuery(); useCreateExercise(planId); useAddExerciseToPlan(planId)  // shared scope.id='plan:<planId>'

Existing seed/display rule (Plan 01/02): a row with seed_key !== null renders
  displayName = t('exercise.'+seed_key+'.name'); equipment = t('equip.'+equipment)
  else displayName = row.name (raw); equipment = row.equipment (raw)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-skin picker chrome + filter pills + AND-combined search</name>
  <read_first>
    - app/app/(app)/plans/[id]/exercise-picker.tsx (full file) — current search filter memo (lines 90-95), chained create→add scope (lines 87-88, 127-159), GHRV wrapper (lines 161-169), error copy (lines 129, 149)
    - app/design v2/Sources/design/forge-screens.jsx — FExercisePicker (line 1215): modal header, search field (line 1245), filter-pill row, create-new CTA, list
    - app/lib/muscle-group.ts (Plan 02) — resolveMuscleGroupKey
    - app/components/ui/ForgeButton.tsx lines 39-59, 143-146 (variant/size + style-fn pressed/shadow — FIT-66); ForgeField.tsx lines 32-72; app/components/ui/index.ts barrel
    - 10-UI-SPEC.md lines 30, 61-73 (icons, optical values, NativeWind purge note), 158-159 (picker + filter pill interaction), 120-146 (color reserved-for)
    - 10-PATTERNS.md SP-2/SP-5/SP-6/SP-8 (lines 56-114) + picker assignment (lines 220-242)
  </read_first>
  <action>
    Re-skin app/app/(app)/plans/[id]/exercise-picker.tsx to FExercisePicker. Compose ONLY from the Phase 8 barrel primitives (ForgeButton/Field/Card/Chip/Icon). Apply optical values (48 search height, 36/30 row tiles, 18/14 radii, 6px pill gap) as inline `style={{}}` numbers NOT arbitrary Tailwind classes (Pitfall 3 / UI-SPEC purge note). Light+dark parity on every surface (pair `-light` base + `dark:` DEFAULT tokens).
    Header: modal-close affordance `t('closeModal')` (Stäng, accent text button — abandons the picker, returns to plan detail), centered title `t('addExercise')` (Lägg till övning). GHRV wrapper preserved (SP-8).
    Search field: 48 tall, inline magnifier SVG, placeholder via `placeholderTextColor` `t('searchExercise')`.
    Filter-pill row (D-04): single-select horizontal scrollable row of 6 pills — `mgAll` (default active, shows everything) + the 5 group pills (`mgChest`/`mgBack`/`mgLegs`/`mgShoulders`/`mgArms`), keyed to the D-01 keys, labels via `t()`. Add `const [activeGroup, setActiveGroup] = useState<MuscleGroupKey | null>(null)` (null = Alla). Selecting a group sets it; tapping the active pill OR Alla clears (`setActiveGroup(null)`). Active pill = accent fill + accentText; inactive = surface + border + text2.
    Extend the `filtered` memo (lines 90-95) to AND-combine (D-05): `exercises.filter(e => (!activeGroup || resolveMuscleGroupKey(e.muscle_group) === activeGroup) && displayName(e).toLowerCase().includes(q))` where `displayName(e) = e.seed_key ? t('exercise.'+e.seed_key+'.name') : e.name` (search matches the TRANSLATED name).
    List rows: render `displayName(e)`; subtitle = mg label `t('mg'+Capitalized(resolveMuscleGroupKey(e.muscle_group)))` · equipment (`e.seed_key ? t('equip.'+e.equipment) : e.equipment`). Row `+` add-button (accentSoft bg + accent border + accent plus, 30×30, ≥44 hit area). Empty/no-match state `t('noExercisesMatch')`.
    Tapping `+` keeps the existing add-now-set-later flow (D-14): insert plan_exercises with null targets via the existing add path, then dismiss.
    Route the existing error copy through `t()`: "Något gick fel"→`t('errorGeneric')` (+ sub `t('errorGenericSub')`), "Du måste vara inloggad"→`t('errorNotSignedIn')` (SP-5; D-15). Use live `useTranslation()` `t` (never module-scope capture).
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - exercise-picker.tsx imports `resolveMuscleGroupKey` and renders a 6-pill filter row keyed to D-01 keys
    - The `filtered` memo references both `activeGroup` and the displayed-name search (AND-combine)
    - Error/empty copy routes through `t('errorGeneric')`/`t('errorNotSignedIn')`/`t('noExercisesMatch')` (no hardcoded Swedish)
    - GestureHandlerRootView wrapper preserved
    - `npx tsc --noEmit` + `npm run lint` exit 0
  </acceptance_criteria>
  <done>Picker browse surface re-skinned with working filter pills + AND-combined translated search + bilingual rows.</done>
</task>

<task type="auto">
  <name>Task 2: Re-skin create-new form with the muscle-group dropdown + preserve chained scope</name>
  <read_first>
    - app/app/(app)/plans/[id]/exercise-picker.tsx — the inline create-new RHF form + the chained useCreateExercise(planId)→useAddExerciseToPlan(planId) under shared scope (lines 87-88, 127-159), and SP-2 mutate-not-mutateAsync comment (lines 140-150)
    - app/design v2/Sources/design/forge-screens.jsx — FExercisePickerNew (line 1334): name field, muscle-dropdown (Välj muskelgrupp + chevronDown), equipment field, notes, Skapa & lägg till, ← Tillbaka
    - app/lib/schemas/exercises.ts (Plan 01 — form schema constrains muscle_group to the 5 keys)
    - 10-CONTEXT.md D-01/D-02/D-14 (dropdown keys; equipment free-text stored raw; add with null targets)
    - 10-UI-SPEC.md line 160 (picker-create), lines 201-205 (create labels/placeholders/CTA/back)
    - 10-PATTERNS.md SP-3 (static scope) + picker assignment lines 240-242
  </read_first>
  <action>
    Re-skin the inline create-new state (FExercisePickerNew) reached via the dashed `t('createExercise')` CTA (`t('createExerciseSub')` sub, accent dashed border + accent plus tile). `← Tillbaka` = `t('back')` returns to browse.
    Form fields: name `ForgeField` (barbell icon, `t('name')` label, stored raw D-16); muscle-group DROPDOWN (D-01) — a button showing `t('selectMuscleGroup')` placeholder + chevronDown that opens a selector over the 5 D-01 keys; the selected value is STORED as the key (e.g. "chest") in `muscle_group`, displayed via `t('mg'+Capitalized(key))`; the dropdown carries `accessibilityRole="button"` + current value label (UI-SPEC a11y). Equipment free-text `ForgeField` (scale icon, `t('equipmentPlaceholder')` placeholder, stored as written D-02). Notes multiline `t('notes')` (`minHeight 92`).
    Submit `t('createAndAdd')` (Skapa & lägg till) chains create→add: PRESERVE the existing `useCreateExercise(planId)` + `useAddExerciseToPlan(planId)` shared `scope.id='plan:<planId>'` contract (FK-safe offline replay) — fire with `.mutate(payload, { onError })` NOT mutateAsync (SP-2). The new dropdown/pills are pure UI; the mutation scope contract is unchanged. New exercises store `seed_key: null` (user-created → raw render).
    All chrome via live `t()`.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - The create-new form has a muscle-group dropdown emitting D-01 keys into `muscle_group`
    - Equipment is a free-text ForgeField stored as written
    - Submit preserves the chained create→add with shared `scope.id='plan:<planId>'` and uses `.mutate` (not mutateAsync)
    - `npx tsc --noEmit` + `npm run lint` exit 0
  </acceptance_criteria>
  <done>Create-new form re-skinned with mg dropdown, free-text equipment, and the preserved FK-safe chained scope.</done>
</task>

<task type="auto">
  <name>Task 3: Picker per-screen + error/empty locale keys (sv + en)</name>
  <read_first>
    - app/locales/sv.json + en.json (Plan 02 already added seed/taxonomy keys — add picker chrome here, parity-equal)
    - 10-UI-SPEC.md lines 195-205 (picker copy) + 218-226 (error/empty copy)
    - app/scripts/check-locale-parity.ts
  </read_first>
  <action>
    Add to BOTH locale files (parity-equal): `closeModal` (Stäng/Close), `searchExercise` (Sök övning…/Search exercise…), `createExercise` (Skapa ny övning/Create new exercise), `createExerciseSub`, `muscleGroup` (Muskelgrupp/Muscle group), `equipment` (Utrustning/Equipment), `notes` (Anteckningar · valfritt/Notes · optional), `selectMuscleGroup` (Välj muskelgrupp/Select muscle group), `equipmentPlaceholder` (t.ex. Skivstång, Hantlar/e.g. Barbell, Dumbbells), `createAndAdd` (Skapa & lägg till/Create & add), `back` (← Tillbaka/← Back), `name` (Namn/Name), `errorGeneric` (Något gick fel/Something went wrong), `errorGenericSub` (Försök igen senare./Please try again.), `errorNotSignedIn` (Du måste vara inloggad/You must be signed in), `noExercisesMatch` (Inga övningar matchar/No exercises match). Reuse `addExercise` if it already exists; do NOT add `cancel` or bare `save`. Use the EXACT sv/en from UI-SPEC.
  </action>
  <verify>
    <automated>cd app && npm run check:locale-parity</automated>
  </verify>
  <acceptance_criteria>
    - Both locale files contain `searchExercise`, `selectMuscleGroup`, `createAndAdd`, `errorGeneric`, `errorNotSignedIn`, `noExercisesMatch` with sv + en values
    - `npm run check:locale-parity` exits 0
    - No `cancel`/bare `save` key added
  </acceptance_criteria>
  <done>Picker chrome + error/empty keys added; parity green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input (create-new form) → Supabase | Free-text name/equipment/notes + dropdown key cross into exercises via the mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-10 | Tampering | Off-list muscle_group submitted from a tampered client | mitigate | Dropdown emits only the 5 D-01 keys; form schema (Plan 01) constrains muscle_group; resolveMuscleGroupKey is total (`?? 'other'`) at the read side |
| T-10-11 | Tampering / Info disclosure | Picker reads/adds another user's exercise | mitigate | useExercisesQuery + create/add run under own-row RLS; chained scope is FK-safe replay only, not an authz bypass |
| T-10-12 | Denial | mutateAsync stall under offlineFirst | mitigate | All call sites use `.mutate(payload, { onError })` (SP-2 — the Phase 4 offline-stall lesson) |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies; pure re-skin over existing hooks |
</threat_model>

<verification>
- `npx tsc --noEmit` + `npm run lint` clean.
- `npm run check:locale-parity` PASS.
- Manual (device UAT): picker light+dark; filter pills single-select + clear; search AND-combine; create-new dropdown; flip sv↔en confirms seed names/equipment + mg labels translate.
</verification>

<success_criteria>
- Picker matches FExercisePicker/FExercisePickerNew in light + dark.
- Filter pills + AND-combined translated search working.
- Create-new mg dropdown + free-text equipment + preserved FK-safe chained scope.
- Picker locale keys present + parity green.
</success_criteria>

<output>
Create `.planning/phases/10-plans-exercises-re-skin/10-03-SUMMARY.md` when done.
</output>
