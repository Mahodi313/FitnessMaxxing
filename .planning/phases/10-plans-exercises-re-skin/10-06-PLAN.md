---
phase: 10-plans-exercises-re-skin
plan: 06
type: execute
wave: 6
depends_on: ["10-01", "10-04", "10-05"]
files_modified:
  - app/app/(app)/plans/[id].tsx
  - app/locales/sv.json
  - app/locales/en.json
autonomous: true
requirements: [SKIN-02, I18N-05]
must_haves:
  truths:
    - "Plan detail matches FPlanDetail in light + dark: Starta pass CTA, quick stats, reorderable exercise rows, overflow menu"
    - "All existing behavior preserved: drag-reorder, per-row remove, soft archive (no confirm), Starta pass navigation"
    - "Targets surface as the chip 4 × 6–8 reps (null-safe) from plan_exercises.target_*; row tap opens edit-targets"
    - "Hard-delete is reachable from the overflow menu and requires a confirmation dialog (danger Ta bort + neutral Behåll plan)"
    - "Hard-delete preserves all logged history; the deleted plan's former name still reads in history via the snapshot"
    - "Starting a session writes plan_name_snapshot = plan.name"
  artifacts:
    - path: "app/app/(app)/plans/[id].tsx"
      provides: "Re-skinned plan detail + hard-delete dialog + snapshot-on-start wiring"
      contains: "useDeletePlan"
  key_links:
    - from: "app/app/(app)/plans/[id].tsx"
      to: "useDeletePlan(planId).mutate({ id })"
      via: "hard-delete confirm dialog"
      pattern: "useDeletePlan"
    - from: "app/app/(app)/plans/[id].tsx"
      to: "['session','start'] plan_name_snapshot"
      via: "startSession.mutate adds plan_name_snapshot: plan.name"
      pattern: "plan_name_snapshot"
---

<objective>
Re-skin the plan-detail screen (`plans/[id].tsx`) to `FPlanDetail` (SKIN-02): the "Starta pass" CTA, quick stats, reorderable exercise rows with target chips, and the overflow menu. ADD the hard-delete path (D-10/D-11) as an overflow action gated by a confirmation dialog, wire `plan_name_snapshot = plan.name` onto session-start, and add the plan-detail destructive-action locale keys (D-15).

Purpose: The final SKIN-02 deliverable + the new destructive capability. Consumes Plan 01's `useDeletePlan` hook + snapshot-aware `['session','start']`, and Plan 05's shared plan-detail locale keys (eyebrow/stats). The hard-delete dialog clones the same-file archive-confirm overlay byte-for-byte (inline overlay, never a portal). This plan runs last (Wave 6) so it is the sole locale writer in its wave; the row-tap target (edit.tsx, Plan 04) and the Starta-pass destination route already exist.
Output: Re-skinned `plans/[id].tsx` with hard-delete + snapshot + plan-detail locale keys.
</objective>

<execution_context>
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-plans-exercises-re-skin/10-CONTEXT.md
@.planning/phases/10-plans-exercises-re-skin/10-RESEARCH.md
@.planning/phases/10-plans-exercises-re-skin/10-PATTERNS.md
@.planning/phases/10-plans-exercises-re-skin/10-UI-SPEC.md

<interfaces>
From app/lib/queries/plans.ts (Plan 01): useDeletePlan(planId) // .mutate({ id }, { onError })
From app/lib/query/client.ts (Plan 01): ['session','start'] now accepts plan_name_snapshot in SessionInsertVars
Existing in plans/[id].tsx: usePlanQuery, useArchivePlan, useReorderPlanExercises, useStartSession,
  useState(() => randomUUID()) lazy session id (line 145), onStarta (lines 233-257),
  startSession.mutate({ id, user_id, plan_id, started_at }) (line 236),
  showOverflowMenu/showArchiveConfirm state (lines 161-162), useFocusEffect reset (lines 168-173),
  overflow popover (lines 563-619), archive-confirm overlay (lines 627-727) ← byte-for-byte hard-delete template
From app/components/ui (Phase 8 barrel): ForgeButton, ForgeCard, ForgeChip, ForgeStat, Icon
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-skin plan-detail chrome + wire snapshot-on-start</name>
  <read_first>
    - app/app/(app)/plans/[id].tsx (full file) — DraggableFlatList + useReorderPlanExercises, per-row remove, useArchivePlan (no confirm), useStartSession + onStarta (233-257), loading gate `!plan` (line 298, NOT isPending — initialData seeds cache), lazy session id (line 145)
    - app/design v2/Sources/design/forge-screens.jsx — FPlanDetail (line 223): Starta pass CTA, quick-stats row (ForgeStat), exercise rows with grip handle + target chip, overflow ellipsis
    - app/components/ui barrel: ForgeStat/ForgeChip/ForgeButton/Icon; ForgeButton FIT-66 style-fn pressed/shadow
    - 10-RESEARCH.md lines 163-171 (snapshot population at session-start call site)
    - 10-UI-SPEC.md line 156 (plan-detail interaction), 96-97 (target chip + last-value accent numeral), 138 (Starta pass accent CTA), 134-146 (color)
    - 10-PATTERNS.md plans/[id].tsx assignment (lines 203-216) + SP-2/SP-3/SP-5/SP-6/SP-7
  </read_first>
  <action>
    Re-skin app/app/(app)/plans/[id].tsx chrome to FPlanDetail. Compose from the Phase 8 barrel; optical values inline `style` (Pitfall 3); light+dark parity. Eyebrow `t('planEyebrow')` (PLAN). Quick-stats row via `ForgeStat`: `t('exercisesStat')` (Övningar, count), `t('lastStat')` (Senast), `t('avgTime')` (Snitt-tid) — plain derived counts, NO new aggregates (UI-SPEC reconciliation).
    PRESERVE ALL existing behavior (D-09): `DraggableFlatList` reorder (`useReorderPlanExercises`, `grip` handle per row), per-exercise remove, soft `useArchivePlan` (NO confirm, neutral styling — not danger), and `useStartSession` "Starta pass" CTA `t('startSession')` (accent primary, 64 tall) → `router.push('/workout/${newSessionId}')` (destination is Phase 11; navigation preserved). Keep the `useState(() => randomUUID())` lazy session id (line 145) and loading gate `!plan` (line 298, NOT isPending).
    Exercise rows: target chip `4 × 6–8 reps` (D-13) renders NULL-SAFE from `plan_exercises.target_*` (render only present bounds; mono tnum); "last value" accent numeral preserved (Core-Value highlight). Row tap → the edit-targets screen (Plan 04's edit.tsx). Section header `+ Lägg till övning` `t('addExercise')` (accent link → picker).
    Wire the snapshot (RESEARCH 163-171): in `onStarta` (lines 233-257), add `plan_name_snapshot: plan.name` to the `startSession.mutate({...})` payload (line 236) — `plan` is already in scope via usePlanQuery; `.mutate` NOT mutateAsync (SP-2). All chrome via live `t()`.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - plans/[id].tsx preserves DraggableFlatList/useReorderPlanExercises, per-row remove, useArchivePlan (no confirm), and the lazy `useState(() => randomUUID())` session id
    - `startSession.mutate(...)` payload includes `plan_name_snapshot: plan.name`
    - Target chip renders null-safe from target_*; loading gate stays `!plan`
    - `npx tsc --noEmit` + `npm run lint` exit 0
  </acceptance_criteria>
  <done>Plan detail re-skinned to FPlanDetail with all existing behavior preserved + snapshot wired onto session-start.</done>
</task>

<task type="auto">
  <name>Task 2: Add hard-delete overflow action + confirmation dialog (inline overlay)</name>
  <read_first>
    - app/app/(app)/plans/[id].tsx lines 161-173 (overlay state + useFocusEffect reset), 563-619 (overflow popover — add a hard-delete row), 627-727 (archive-confirm overlay — byte-for-byte template for the delete dialog; note literal "Avbryt" at lines 686, 700 MUST be replaced with keepPlan; danger button template at 703-723)
    - app/lib/queries/plans.ts (Plan 01) — useDeletePlan(planId)
    - 10-CONTEXT.md D-10/D-11 (hard-delete destructive + confirm; history preserved)
    - 10-UI-SPEC.md line 157 (hard-delete interaction), 146 (destructive color reserved), 164 (no generic Avbryt; keepPlan neutral), 228-229 (destructive copy + keys), 168 (inline-overlay not portal)
    - 10-PATTERNS.md SP-7 (inline overlay) + plans/[id].tsx hard-delete assignment (lines 207-212)
  </read_first>
  <action>
    Add the hard-delete path (D-10/D-11). Add `showDeleteConfirm` state alongside `showOverflowMenu`/`showArchiveConfirm` (line 161-162) and reset it in the `useFocusEffect` (lines 168-173). In the overflow popover (lines 563-619), add a second `<Pressable>` row BELOW "Arkivera plan" → `t('archivePlan')`: a hard-delete row `t('delete')` (danger-labeled) that opens `showDeleteConfirm`.
    Build the hard-delete confirmation dialog by CLONING the archive-confirm overlay (lines 627-727) — the same inline absolute-positioned `<Pressable>` scrim + inner card (SP-7; NEVER a portal Modal — UAT 2026-05-10). Change: title `t('deletePlanQ')` (Ta bort planen?), body `t('deletePlanBody')` (Planen och dess övningar tas bort. Din loggade träningshistorik påverkas inte.), primary = DANGER-styled `t('delete')` (Ta bort — reuse the danger button template at lines 703-723) → `useDeletePlan(plan.id).mutate({ id: plan.id }, { onError, onSuccess: () => router.back() })`, secondary = NEUTRAL `t('keepPlan')` (Behåll plan — copy the neutral text button at 683-702 but REPLACE the literal "Avbryt" at lines 686/700 with the keepPlan key). The neutral secondary is `text-forge-text` (NOT accent, NOT danger — the danger primary is the only colored action). `.mutate` NOT mutateAsync (SP-2). On successful delete, navigate back to the list (the optimistic onMutate already filters plansKeys.list + invalidates sessionsKeys.listInfinite). Light+dark parity on the overlay.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - plans/[id].tsx imports `useDeletePlan` and the overflow menu has a danger hard-delete row opening a confirm dialog
    - The dialog uses the inline absolute-positioned overlay pattern (no portal Modal) with danger primary `t('delete')` + neutral `t('keepPlan')` (no literal "Avbryt")
    - `showDeleteConfirm` is reset in the useFocusEffect cleanup
    - Delete fires `useDeletePlan(plan.id).mutate(...)` and navigates back
    - `npx tsc --noEmit` + `npm run lint` exit 0
  </acceptance_criteria>
  <done>Hard-delete reachable from overflow, guarded by an inline-overlay confirm with danger/neutral actions; history preserved by the Plan-01 FK + snapshot.</done>
</task>

<task type="auto">
  <name>Task 3: Plan-detail destructive-action locale keys + full-suite gate</name>
  <read_first>
    - app/locales/sv.json + en.json (Plans 02-05 added prior keys — add destructive-dialog keys here, parity-equal; eyebrow/stat keys already added in Plan 05)
    - 10-UI-SPEC.md lines 228-229 (deletePlanQ, deletePlanBody, delete, keepPlan)
    - app/scripts/check-locale-parity.ts
    - 10-VALIDATION.md lines 29-34 (per-wave full suite gate)
  </read_first>
  <action>
    Add to BOTH locale files (parity-equal): `deletePlanQ` (Ta bort planen?/Delete plan?), `deletePlanBody` (Planen och dess övningar tas bort. Din loggade träningshistorik påverkas inte./The plan and its exercises will be removed. Your logged workout history is not affected.), `delete` (Ta bort/Delete), `keepPlan` (Behåll plan/Keep plan). Reuse existing `archivePlan`/`planEyebrow`/`exercisesStat`/`lastStat`/`avgTime`/`startSession`/`addExercise` (do NOT duplicate — `planEyebrow`/stats came from Plan 05). No `cancel`/bare `save`. EXACT sv/en from UI-SPEC.
    Then run the wave/phase-gate full suite (VALIDATION lines 29-34): `npm run test:rls && npm run test:f13-brutal && npm run check:locale-parity` — all green (SKIN-08 no-regression re-affirmed at phase close; this plan touched no schema/query but the suite is the phase gate).
  </action>
  <verify>
    <automated>cd app && npm run check:locale-parity && npm run test:rls && npm run test:f13-brutal</automated>
  </verify>
  <acceptance_criteria>
    - Both locale files contain `deletePlanQ`, `deletePlanBody`, `delete`, `keepPlan` with sv + en values
    - `npm run check:locale-parity` exits 0
    - `npm run test:rls` exits 0 AND `npm run test:f13-brutal` exits 0 (phase-gate full suite)
    - No `cancel`/bare `save` key; no duplicate keys
  </acceptance_criteria>
  <done>Destructive-dialog keys added; full phase-gate suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (hard-delete) → Supabase | A delete request crosses into the RLS-gated workout_plans table |
| session-start → workout_sessions | The snapshot value crosses into the session row at insert |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-19 | Tampering / Denial | Hard-delete of another user's plan or its sessions | mitigate | useDeletePlan runs under RLS `for all using (user_id = (select auth.uid()))`; FK SET NULL only nulls the OWNER's sessions; exercise_sets untouched; test-rls (Plan 01) re-affirms "A cannot DELETE B's workout_plan" |
| T-10-20 | Repudiation / Info loss | A logged set lost on plan delete | mitigate | D-11: FK SET NULL preserves sessions/sets; plan_name_snapshot (Plan 01) keeps history readable; dialog body states history is unaffected |
| T-10-21 | Tampering | Snapshot value spoofed at session-start | accept | plan_name_snapshot is the user's OWN plan name (non-sensitive); RLS gates the session insert to the owner; a wrong snapshot only mislabels the user's own history |
| T-10-22 | Denial | mutateAsync stall under offlineFirst | mitigate | Hard-delete + Starta-pass both use `.mutate(payload, { onError })` (SP-2) |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies; pure re-skin over the Plan-01 hook |
</threat_model>

<verification>
- `npx tsc --noEmit` + `npm run lint` clean.
- `npm run check:locale-parity` + `npm run test:rls` + `npm run test:f13-brutal` PASS (phase gate).
- Manual (device UAT): plan detail light+dark; reorder/remove/archive preserved; Starta pass navigates; hard-delete dialog (danger Ta bort + neutral Behåll plan); delete a plan with logged sessions → history still lists them with the snapshot name; flip sv↔en confirms chrome translates.
</verification>

<success_criteria>
- Plan detail matches FPlanDetail in light + dark; all existing behavior preserved.
- Hard-delete reachable + confirmed (inline overlay), history preserved, snapshot wired on start.
- Plan-detail locale keys present; full phase-gate suite green.
</success_criteria>

<output>
Create `.planning/phases/10-plans-exercises-re-skin/10-06-SUMMARY.md` when done.
</output>
