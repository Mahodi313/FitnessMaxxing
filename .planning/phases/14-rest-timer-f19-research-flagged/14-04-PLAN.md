---
phase: 14-rest-timer-f19-research-flagged
plan: 04
type: execute
wave: 3
depends_on: ["14-01", "14-02"]
files_modified:
  - app/components/ui/RestTimerBanner.tsx
  - app/app/(app)/workout/[sessionId].tsx
autonomous: false
requirements: [TIMER-01, TIMER-02, TIMER-05]
user_setup: []

must_haves:
  decisions: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-19]
  truths:
    - "A working-set 'Klart' with the timer enabled auto-starts the rest fire-and-forget AFTER addSet.mutate — never awaited, never before the mutate (F13 sacred)"
    - "Only set_type='working' sets auto-start a rest (D-06); warmup/dropset/failure log with no rest"
    - "The countdown banner is a floating absolute overlay in the existing PR-banner slot that NEVER shifts the set list / input row / Klart (D-01)"
    - "The banner's M:SS numeral is re-derived from the stored endTs every render (remainingMs/formatMSS) — never a decrementing counter (TIMER-02); the 1s interval is display-only and reconciles on AppState active"
    - "[Hoppa över] cancels the rest+notification and exits; [+30s] extends endTs and reschedules (D-02/TIMER-05); logging the next working set restarts a fresh rest (D-03/D-07)"
    - "When a set is both a PR and starts a rest, the PR banner plays first then the timer banner takes the top slot; the timer endTs is set at log-time regardless (D-04)"
    - "npm run test:f13-brutal stays green (hot path + offline queue untouched)"
  artifacts:
    - path: "app/components/ui/RestTimerBanner.tsx"
      provides: "Floating countdown banner with Hoppa över / +30s controls"
      contains: "formatMSS"
      min_lines: 60
    - path: "app/app/(app)/workout/[sessionId].tsx"
      provides: "Auto-start hook after addSet.mutate + banner mount in overlay stack"
      contains: "useRestTimerStore"
  key_links:
    - from: "app/components/ui/RestTimerBanner.tsx"
      to: "app/lib/rest-timer.ts"
      via: "remainingMs/formatMSS for the display numeral"
      pattern: "remainingMs|formatMSS"
    - from: "app/components/ui/RestTimerBanner.tsx"
      to: "app/lib/rest-timer-store.ts"
      via: "useRestTimerStore for endTs + skip/extend"
      pattern: "useRestTimerStore"
    - from: "app/app/(app)/workout/[sessionId].tsx"
      to: "app/lib/rest-timer-store.ts"
      via: "getState().start after addSet.mutate"
      pattern: "useRestTimerStore.getState\\(\\).start"
---

<objective>
Ship the visible rest timer: the floating countdown banner (composed from Forge primitives, cloning PrBanner) and the fire-and-forget auto-start hook in the active-workout screen. This is the F13-sacred surface — the auto-start layers on top of the proven post-mutate side-effect pattern, never touching the ≤3s write path.

Purpose: TIMER-01 (auto-start on working-set Klart), TIMER-02 (timestamp-reconciled countdown that survives backgrounding), and the visual half of TIMER-05 (skip/+30s/next-set controls). The banner reuses the PR-banner overlay slot and geometry-protection rule (D-01) and defers behind a PR celebration when both fire (D-04).
Output: `app/components/ui/RestTimerBanner.tsx`, extended `app/app/(app)/workout/[sessionId].tsx`.
</objective>

<execution_context>
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-rest-timer-f19-research-flagged/14-UI-SPEC.md
@.planning/phases/14-rest-timer-f19-research-flagged/14-RESEARCH.md
@.planning/phases/14-rest-timer-f19-research-flagged/14-PATTERNS.md
@.planning/phases/14-rest-timer-f19-research-flagged/14-VALIDATION.md
@.planning/phases/14-rest-timer-f19-research-flagged/14-01-SUMMARY.md
@.planning/phases/14-rest-timer-f19-research-flagged/14-02-SUMMARY.md

<interfaces>
<!-- Imported from Plans 14-01 / 14-02. Use directly. -->

From app/lib/rest-timer.ts (14-01):
```typescript
export function remainingMs(endTs: number, now: number): number;   // re-derive each render (TIMER-02)
export function formatMSS(ms: number): string;                     // tabular M:SS
```

From app/lib/rest-timer-store.ts (14-02):
```typescript
export const useRestTimerStore: ... ;  // { endTs, notificationId, start, extend, skip, finish }
// start(durationMs, exerciseId, content), extend(content), skip(), finish()
// content = { title: t("restDoneTitle"), body: t("restDoneBody"), sessionId }
```

From app/lib/prefs.ts (14-01): getPref("fm:restTimerEnabled"), getPref("fm:restSeconds").
i18n keys (14-01, sv/en parity): restLabel, restSkip, restAdd30, restDoneTitle, restDoneBody.
</interfaces>

<design-tokens>
<!-- From 14-UI-SPEC.md — apply the design-taste skills (.claude/skills/) to the net-new banner. -->
- Banner: OPAQUE bg-forge-surface base (FIT-116 / Phase 13 D-04 — card title must not bleed through), forge-lg (20px) radius, forge-border hairline, shadow inline. Box-decoration (bg/radius/border-color) in className; shadow/animated transform in inline style (NativeWind 4 box-decoration-via-className, project MEMORY).
- Numeral: 32px Inter 700 mono family, tabular-nums (fontVariantNumeric "tabular-nums" + fontFeatureSettings '"tnum","ss01"') — width must not reflow each second (DSGN-03).
- Motion: spring damping 18 / stiffness 220; enter translateY 24→0 + scale 0.96→1; exit fade + translateY 0→-12; useReducedMotion() → opacity snap, numeral still ticks (D-19).
- Controls: [+30s] = accent / accent-soft fill (affirmative); [Hoppa över] = neutral ghost forge-text2 on forge-surface2 (NEVER danger red — the timer has no destructive action). Both 44px hit target via hitSlop.
- The timer NEVER reads red/success — accent is reserved for [+30s] + an optional thin accent rest-progress indicator only.
</design-tokens>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build RestTimerBanner.tsx — floating countdown banner cloning PrBanner</name>
  <files>app/components/ui/RestTimerBanner.tsx</files>
  <read_first>
    - app/components/ui/PrBanner.tsx (the EXACT clone target: :70-73 the SPRING const; :120-144 the useReducedMotion snap branch; :137-140 useEffect cleanup; :173-198 the OPAQUE-surface box-decoration-via-className + inline shadow; :271-278 the tabular-nums numeral idiom)
    - app/components/ui/SettingsRow.tsx (:70 the Toggle `hitSlop={{top:6,bottom:6,left:6,right:6}}` 44px-target idiom for the two banner controls)
    - app/lib/rest-timer.ts (remainingMs/formatMSS — 14-01; the display recompute) + app/lib/rest-timer-store.ts (useRestTimerStore — 14-02)
    - 14-RESEARCH.md §Pattern 2 (display-only 1s interval that bumps a tick; remaining recomputed each render; AppState reconcile) + §Pitfall 2 (never decrement a counter) + §Pitfall 5 (never shift Klart)
    - 14-PATTERNS.md §RestTimerBanner.tsx (motion/opaque-surface/tabular/display-tick excerpts) + §Shared Patterns
    - 14-UI-SPEC.md §Typography / §Color / §Motion / §Spacing (the full visual contract; apply the .claude/skills design-taste judgment)
  </read_first>
  <behavior>
    - The numeral renders formatMSS(remainingMs(endTs, Date.now())) and updates ~every second while endTs is set; the 1s interval only bumps a tick state to force re-render — the value is RECOMPUTED from endTs, never decremented (TIMER-02)
    - On AppState 'active' the banner forces one re-render so remaining re-derives from the stored endTs (reconcile after background)
    - When remainingMs reaches 0 the banner exits (foreground); when endTs is null the banner does not render
    - [Hoppa över] calls useRestTimerStore.getState().skip(); [+30s] calls .extend(content)
    - Under useReducedMotion() the banner snaps in with no translate/scale (opacity only) but the numeral still ticks (D-19)
  </behavior>
  <action>
    Create `app/components/ui/RestTimerBanner.tsx` cloning `PrBanner.tsx` wholesale. Subscribe to `useRestTimerStore((s) => s.endTs)`; render nothing when `endTs` is null. Display numeral = `formatMSS(remainingMs(endTs, Date.now()))` recomputed each render; drive re-render with a display-only `setInterval(() => setTick(n => n+1), 1000)` in a `useEffect([endTs])` with cleanup (RESEARCH §Pattern 2 — NEVER store/decrement a `remainingSeconds`). Add an `AppState.addEventListener("change", s => { if (s === "active") forceTick(); })` effect so a return-from-background re-derives remaining from `endTs` (TIMER-02 reconcile). Motion: copy PrBanner's `SPRING = { damping: 18, stiffness: 220 }` + `useReducedMotion()` branch, but extend the shared-value set to BOTH `translateY` (24→0) and `scale` (0.96→1) per UI-SPEC §Motion; reduced → snap (D-19). Frame: OPAQUE `bg-forge-surface-light dark:bg-forge-surface` base in `className` with `rounded-[20px]` (forge-lg, vs PrBanner's 16px), border + shadow inline (FIT-116 box-decoration-via-className rule). Numeral: 32px mono Inter-700 with `fontVariant: ["tabular-nums"]` / `fontFeatureSettings: '"tnum","ss01"'` (PrBanner :271-278 idiom). Optional eyebrow `t("restLabel")`. Two controls with 44px hitSlop: `[+30s]` (`t("restAdd30")`) accent-soft fill calling `useRestTimerStore.getState().extend(content)`; `[Hoppa över]` (`t("restSkip")`) neutral ghost (`forge-text2` on `forge-surface2`, NEVER danger) calling `.skip()`. The component accepts the notification `content` ({title, body, sessionId}) as a prop or builds it from `t()` + the route sessionId. NEVER render red/success.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit 2>&1 | grep -i "RestTimerBanner" ; npx expo lint 2>&1 | tail -3 ; grep -c "remainingMs\|formatMSS\|tabular-nums" "app/components/ui/RestTimerBanner.tsx"</automated>
    <human-check>On a real iPhone: the banner shows a fixed-width M:SS that ticks down without reflowing; backgrounding ≥30s then foregrounding shows the correctly reconciled remaining time (TIMER-02, not frozen/drifted); [+30s] jumps the numeral up; [Hoppa över] exits the banner; reduce-motion (iOS setting) snaps the entry but the numeral still ticks.</human-check>
  </verify>
  <acceptance_criteria>
    - RestTimerBanner.tsx imports remainingMs + formatMSS from rest-timer and useRestTimerStore from rest-timer-store
    - The numeral uses tabular-nums (grep shows "tabular-nums"); no `remainingSeconds` decrementing state exists (grep -c "remainingSeconds" returns 0)
    - The bg/radius are in className (box-decoration-via-className); shadow is inline
    - Controls use hitSlop for a 44px target; neither control uses forge-danger
    - tsc + expo lint clean; human-verified countdown reconcile + controls + reduce-motion
  </acceptance_criteria>
  <done>A floating, opaque, tabular-nums countdown banner re-derives remaining time from endTs every render, reconciles on foreground, and exposes the +30s / Hoppa över controls with no destructive styling.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Wire auto-start after addSet.mutate + mount the banner in the overlay stack (D-04 handoff)</name>
  <files>app/app/(app)/workout/[sessionId].tsx</files>
  <read_first>
    - app/app/(app)/workout/[sessionId].tsx (:676-678 the EXACT `void getPref("fm:haptics").then(...)` fire-and-forget post-mutate block to copy the placement+discipline from; :641-650 the `addSet.mutate` call + `set_type:"working"` log site; :469-491 the `position:"absolute"` + `pointerEvents="box-none"` PR-banner overlay stack the timer banner mounts into; the existing `banners` state for D-04 handoff coordination)
    - app/components/ui/RestTimerBanner.tsx (Task 1)
    - app/lib/rest-timer-store.ts (useRestTimerStore.getState().start / .finish — 14-02) + app/lib/prefs.ts (getPref — 14-01)
    - 14-RESEARCH.md §Pattern 3 (the exact fire-and-forget-after-mutate auto-start block; F13-safe) + §Open Questions Q3 (session-finish → finish() cancels)
    - 14-PATTERNS.md §[sessionId].tsx (auto-start + overlay + D-04 handoff excerpts) + §Shared Patterns (fire-and-forget; F13-sacred)
    - CLAUDE.md → "≤3s log hot path is sacred" + the test:f13-brutal guard
  </read_first>
  <action>
    Make three changes to `[sessionId].tsx`, all layered on existing proven idioms.
    (1) AUTO-START HOOK: place AFTER `addSet.mutate(...)` in `onKlart`, immediately beside the existing `void getPref("fm:haptics").then(...)` block (:676-678): `void getPref("fm:restTimerEnabled").then((enabled) => { if (!enabled) return; void getPref("fm:restSeconds").then((sec) => { useRestTimerStore.getState().start(sec*1000, planExercise.exercise_id, { title: t("restDoneTitle"), body: t("restDoneBody"), sessionId }); }); });`. NEVER await it; NEVER move it before/into `addSet.mutate` (D-05/F13). `onKlart` only logs `set_type:"working"` (:650) so D-06 (working-only) holds for free.
    (2) BANNER MOUNT: render `<RestTimerBanner .../>` inside the existing `position:"absolute"` + `pointerEvents="box-none"` overlay stack (:469-491), in the SAME top slot PrBanner uses — the set list / input row / Klart NEVER shift (D-01). D-04 handoff: when a set is both a PR and starts a rest, the PrBanner occupies the slot first; the RestTimerBanner takes over after the PR banner exits (coordinate via the existing `banners` state or a derived "is a PR banner showing" check; single banner in the top slot at a time). The timer `endTs` is set at log-time regardless (logical start ≠ visual mount).
    (3) FINISH CANCELS: call `useRestTimerStore.getState().finish()` in the session-finish/abandon flow (RESEARCH Q3 discretion default — no prompt) so a pending rest + its notification are cancelled on session end.
    Then device-verify all behaviors per the steps below.
  </action>
  <what-built>
    Auto-built three layered changes in `[sessionId].tsx`: (1) the fire-and-forget auto-start hook placed AFTER `addSet.mutate` beside the existing `fm:haptics` block (never awaited); (2) the `RestTimerBanner` mounted in the existing absolute `pointerEvents="box-none"` overlay slot with the D-04 PR-then-timer handoff; (3) `finish()` called on session-finish to cancel any pending rest. Now device-verify geometry, gating, reconcile, controls, handoff, and the ≤3s feel.
  </what-built>
  <how-to-verify>
    1. From `app/`, `npx expo start`, open on a real iPhone (Expo Go SDK 54). Enable the rest timer in Settings (Plan 03) and grant permission.
    2. Log a WORKING set "Klart" → assert the rest banner appears as a floating overlay and the set list / input row / Klart button do NOT shift (D-01/TIMER-01).
    3. Log a WARMUP/dropset/failure set → assert NO rest starts (D-06).
    4. While resting, log the next working set → assert a fresh full-duration rest restarts (D-03/D-07).
    5. Background ≥30s, return → assert the countdown shows the reconciled remaining time, not frozen (TIMER-02).
    6. Press [Hoppa över] → assert the banner exits and (with a backgrounded test) NO stale ping fires (TIMER-05); press [+30s] → assert the numeral jumps up.
    7. Log a set that is BOTH a PR and starts a rest → assert the PR banner plays first, then the timer banner takes the slot (D-04).
    8. Finish the session while resting → assert the pending rest + notification are cancelled.
    9. Confirm the ≤3s log feel is unchanged.
  </how-to-verify>
  <verify>
    <automated>cd app && npm run test:f13-brutal ; npx tsc --noEmit 2>&1 | grep -i "sessionId" ; grep -c "useRestTimerStore.getState().start" "app/app/(app)/workout/[sessionId].tsx"</automated>
    <human-check>Working-set Klart auto-starts a floating rest banner that never shifts Klart (D-01/TIMER-01); warmup/dropset does not (D-06); next-set restarts fresh (D-03/D-07); background reconcile correct (TIMER-02); skip/+30s behave (TIMER-05); PR-then-timer handoff (D-04); finish cancels the rest; ≤3s feel unchanged.</human-check>
  </verify>
  <acceptance_criteria>
    - The auto-start block is placed AFTER addSet.mutate (grep confirms `useRestTimerStore.getState().start` appears after the mutate call, never awaited)
    - The banner is mounted inside the existing position:"absolute" / pointerEvents="box-none" overlay (no new flex-flow element)
    - Session-finish calls useRestTimerStore.getState().finish()
    - `npm run test:f13-brutal` exits 0 (or amber ONLY on the known FIT-107 count precondition — no integrity-assertion failure, no regression)
    - tsc --noEmit clean for [sessionId].tsx
    - Human-verified: all 9 verification steps pass; ≤3s log feel unchanged
  </acceptance_criteria>
  <resume-signal>Type "approved" once auto-start, banner geometry, background reconcile, skip/+30s, the PR handoff, and the ≤3s feel all check out, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| onKlart side-effect → F13 write path | the auto-start must not enter or delay the ≤3s set-save mutation |
| stored endTs → display | the countdown re-derives from a device-clock-backed timestamp |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-10 | DoS (availability of the core promise) | F13 hot path via the onKlart auto-start | mitigate | Auto-start is fire-and-forget AFTER `addSet.mutate`, NEVER awaited and NEVER before/inside the mutate (D-05, Phase 13 D-17 pattern). No mutation default / offline queue / persister / exercise_sets write touched. Gate: `npm run test:f13-brutal` stays green (known amber FIT-107 is the environmental 25-set count precondition, not a regression) |
| T-14-11 | Tampering | banner numeral from a tampered endTs | mitigate | `remainingMs` clamps non-finite/negative to 0 and the banner exits at 0 (14-01 guard) — a tampered endTs cannot produce a negative or NaN countdown |
| T-14-05 (ref) | Reliability | stale ping after skip/next-set | mitigate | Inherited: store cancels-before-reschedule (14-02); next-set restart + skip both cancel the prior notification |
</threat_model>

<verification>
- `npm run test:f13-brutal` exits 0 (or amber ONLY on the FIT-107 count precondition — no integrity-assertion failure). This is the F13-sacred regression gate.
- `npx tsc --noEmit` clean for RestTimerBanner.tsx + [sessionId].tsx; `npx expo lint` clean.
- `grep -c remainingSeconds RestTimerBanner.tsx` returns 0 (no decrementing counter — TIMER-02).
- Device-UAT (Task 2): auto-start geometry, working-only gating, next-set restart, background reconcile, skip/+30s, PR→timer handoff, finish-cancels, ≤3s feel.
</verification>

<success_criteria>
- TIMER-01: working-set Klart auto-starts a visible floating rest countdown (timer enabled), never shifting the Klart button (D-01).
- TIMER-02: the countdown re-derives from the stored endTs and stays correct after backgrounding (no JS-timer drift).
- TIMER-05 (visual half): [Hoppa över] cancels + exits; [+30s] extends + reschedules; next-set restarts fresh.
- D-04 PR-then-timer handoff; D-06 working-only gating; session-finish cancels the rest.
- F13 sacred: `npm run test:f13-brutal` green; the ≤3s log path is untouched.
</success_criteria>

<output>
Create `.planning/phases/14-rest-timer-f19-research-flagged/14-04-SUMMARY.md` when done.
</output>
