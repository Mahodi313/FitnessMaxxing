---
phase: 14-rest-timer-f19-research-flagged
plan: 03
type: execute
wave: 3
depends_on: ["14-01", "14-02"]
files_modified:
  - app/app/(app)/(tabs)/settings.tsx
autonomous: false
requirements: [TIMER-04]
user_setup: []

must_haves:
  decisions: [D-08, D-09, D-10, D-11, D-12, D-13, D-14]
  truths:
    - "The restTimer Settings row shows enable/disable + the current duration (e.g. 'På · 2 min' / 'Av') and opens a duration picker via chevron"
    - "Enabling the timer triggers the OS notification-permission prompt in-context (D-13); the granted/denied/blocked state reflects back into the row"
    - "Denying permission still enables the in-app timer (D-12) and surfaces the restNoPermission helper in muted forge-text2 — not danger red"
    - "The duration picker offers presets (60/90/120/180/300s) plus 'Anpassad', persisting the choice to fm:restSeconds"
    - "The existing notifications bell row remains the D-14 master gate (wiring only, no visual change)"
  artifacts:
    - path: "app/app/(app)/(tabs)/settings.tsx"
      provides: "Rest-timer enable/disable + duration picker + permission prompt"
      contains: "fm:restTimerEnabled"
  key_links:
    - from: "app/app/(app)/(tabs)/settings.tsx"
      to: "app/lib/notifications.ts"
      via: "ensureNotificationPermission on enable"
      pattern: "ensureNotificationPermission"
    - from: "app/app/(app)/(tabs)/settings.tsx"
      to: "app/lib/prefs.ts"
      via: "getPref/setPref for fm:restTimerEnabled + fm:restSeconds"
      pattern: "fm:restSeconds"
---

<objective>
Wire the rest-timer control into Settings: the enable/disable row, the duration picker (presets + custom), the in-context permission prompt on enable, and the denied-state helper. This is the TIMER-04 surface and the D-13 calm-moment permission ask.

Purpose: TIMER-04 ("configure default rest duration + enable/disable in Settings, gated by permission"). Enabling fires the OS prompt at the one calm in-context moment (D-13); denial degrades gracefully (D-12); the existing bell row is confirmed as the D-14 master gate.
Output: extended `app/app/(app)/(tabs)/settings.tsx`.
</objective>

<execution_context>
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-rest-timer-f19-research-flagged/14-UI-SPEC.md
@.planning/phases/14-rest-timer-f19-research-flagged/14-PATTERNS.md
@.planning/phases/14-rest-timer-f19-research-flagged/14-01-SUMMARY.md
@.planning/phases/14-rest-timer-f19-research-flagged/14-02-SUMMARY.md

<interfaces>
<!-- Imported from Plans 14-01 / 14-02. Use directly — no exploration. -->

From app/lib/prefs.ts (14-01):
```typescript
getPref("fm:restTimerEnabled"): Promise<boolean>;   // default OFF
getPref("fm:restSeconds"): Promise<number>;         // default 120
setPref("fm:restTimerEnabled", boolean): void;
setPref("fm:restSeconds", number): void;
getPref("fm:notifications"): Promise<boolean>;       // master gate (existing bell row)
```

From app/lib/notifications.ts (14-02):
```typescript
export type PermissionState = "granted" | "denied" | "blocked";
export function ensureNotificationPermission(): Promise<PermissionState>;  // D-13 in-context request
export function getPermissionState(): Promise<PermissionState>;
```

i18n keys (14-01, already at sv/en parity): restDuration, restCustom, restNoPermission, on, off, cancel (reused), restTimer (existing).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add rest-timer enable state + duration state + mount-load to settings.tsx</name>
  <files>app/app/(app)/(tabs)/settings.tsx</files>
  <read_first>
    - app/app/(app)/(tabs)/settings.tsx (:213-214 the `useState` pattern for notifications; :220-231 the mount `useEffect` `getPref` block; :319-322 `onNotificationsToggle` set-state-then-persist shape; :480 the notifications SettingsSection; :488-495 the SettingsRow JSX)
    - app/lib/prefs.ts (the fm:restTimerEnabled / fm:restSeconds getPref/setPref — 14-01)
    - 14-PATTERNS.md §settings.tsx (state + mount-load excerpts)
    - 14-UI-SPEC.md §Surfaces / §Typography / §Color (row value formatting, muted helper color)
  </read_first>
  <action>
    Add `const [restTimerEnabled, setRestTimerEnabled] = useState(false);` and `const [restSeconds, setRestSeconds] = useState(120);` beside the existing notifications state (:213-214). In the mount `useEffect` (:220-231) add `void getPref("fm:restTimerEnabled").then(setRestTimerEnabled);` and `void getPref("fm:restSeconds").then(setRestSeconds);`. Also load the current permission state for the row helper: a `const [permState, setPermState] = useState<PermissionState>("denied");` plus `void getPermissionState().then(setPermState);` in the same mount effect. Do NOT yet render the row (Task 2). Format the duration label with a small helper that maps seconds → "2 min" / "1:30" style (reuse any existing duration formatter if present; else inline a `formatRestLabel(sec)` local).
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit 2>&1 | grep -i "settings" ; grep -c "fm:restTimerEnabled\|fm:restSeconds" "app/app/(app)/(tabs)/settings.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - settings.tsx contains restTimerEnabled + restSeconds + permState state
    - The mount effect calls getPref("fm:restTimerEnabled"), getPref("fm:restSeconds"), getPermissionState()
    - tsc --noEmit reports no errors for settings.tsx
  </acceptance_criteria>
  <done>Settings holds the rest-timer enable/duration/permission state, hydrated on mount.</done>
</task>

<task type="auto">
  <name>Task 2: Render the restTimer row + duration picker + permission-prompt-on-enable + denied helper</name>
  <files>app/app/(app)/(tabs)/settings.tsx</files>
  <read_first>
    - app/app/(app)/(tabs)/settings.tsx (:281-292 the `openUnitsSheet` ActionSheetIOS picker idiom; :319-322 `onNotificationsToggle`; :488-495 the SettingsRow JSX with icon/label/value/chevron)
    - app/lib/notifications.ts (ensureNotificationPermission / getPermissionState — 14-02)
    - 14-RESEARCH.md §Pattern 4 (permission flow + blocked-state `Linking.openSettings()`) + §Open Questions Q2 (preset list 60/90/120/180/300 + Anpassad)
    - 14-PATTERNS.md §settings.tsx (toggle-with-permission + ActionSheetIOS picker + rows excerpts)
    - 14-UI-SPEC.md §Copywriting (row value "På · {dur}" / "Av") + §Color (denied helper = forge-text2, NOT danger; D-12)
  </read_first>
  <action>
    Add `onRestTimerToggle(next: boolean)`: when `next` is true, `const state = await ensureNotificationPermission();` (D-13 in-context prompt), `setPermState(state)`, then ALWAYS `setRestTimerEnabled(true)` + `setPref("fm:restTimerEnabled", true)` regardless of grant — D-12: the in-app countdown works even when denied. When `next` is false, `setRestTimerEnabled(false)` + `setPref("fm:restTimerEnabled", false)`. Add `openRestDurationSheet()` cloning the `openUnitsSheet` ActionSheetIOS idiom (:281-292) with options `["1 min", "1:30", "2 min", "3 min", "5 min", t("restCustom"), t("cancel")]`, `cancelButtonIndex: 6`; map indices 0-4 → 60/90/120/180/300 → `setRestSeconds(sec)` + `setPref("fm:restSeconds", sec)`; index 5 ("Anpassad") opens a simple numeric custom entry (a minimal `Alert.prompt`-style numeric input or an inline stepper — planner-discretion per D-08; persist to fm:restSeconds). Render the restTimer SettingsRow (clone :488-495) in the notifications SettingsSection: `icon="clock"`, `label={t("restTimer")}`, `value={restTimerEnabled ? `${t("on")} · ${formatRestLabel(restSeconds)}` : t("off")}`, a Toggle for enable bound to `onRestTimerToggle`, and a chevron `onPress={openRestDurationSheet}` (the duration sub-control). When `permState !== "granted"` AND `restTimerEnabled`, render `t("restNoPermission")` as a muted `forge-text2` helper line beneath the row (NOT forge-danger — D-12 informational); if `permState === "blocked"` make that helper tappable to `Linking.openSettings()` (RESEARCH Pattern 4). The existing `notifications` bell row (master gate, D-14) is UNCHANGED — confirm it stays as-is.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit 2>&1 | grep -i "settings" ; npx expo lint 2>&1 | tail -3 ; grep -c "ensureNotificationPermission\|restNoPermission" "app/app/(app)/(tabs)/settings.tsx"</automated>
    <human-check>On a real iPhone: toggling the rest timer ON shows the iOS permission prompt (D-13); denying still enables the timer and shows the muted restNoPermission helper (D-12, not red); the duration chevron opens the presets+Anpassad ActionSheet and the chosen value persists across app restart (TIMER-04).</human-check>
  </verify>
  <acceptance_criteria>
    - settings.tsx restTimer row renders with icon "clock", value reflecting on/off + duration
    - onRestTimerToggle calls ensureNotificationPermission on enable and persists fm:restTimerEnabled even when denied
    - openRestDurationSheet offers 60/90/120/180/300 + restCustom and persists fm:restSeconds
    - restNoPermission renders in forge-text2 (muted), never forge-danger
    - The existing notifications bell row is unchanged; tsc + expo lint clean
    - Human-verified: permission prompt fires on enable; deny still enables + shows muted helper; duration persists
  </acceptance_criteria>
  <done>The Settings restTimer row enables/disables the timer with an in-context permission prompt, offers presets+custom durations that persist, degrades gracefully on denial, and leaves the master bell row intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → fm:restSeconds picker | a custom-duration entry is user-supplied numeric input |
| iOS permission grant → Settings row | the new OS-permission surface, surfaced informationally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-08 | Tampering/DoS | custom-duration entry → fm:restSeconds | mitigate | The custom numeric entry is persisted through `setPref("fm:restSeconds", n)`, and every READ goes through `z.coerce.number().int().positive().catch(120)` (14-01) — a zero/negative/huge/garbage custom value clamps to the 120s default on read; never a runaway timer (RESEARCH §Security Domain) |
| T-14-09 | Information disclosure | denied-permission helper text | accept | The denied state is surfaced as generic informational copy (`restNoPermission`) in muted text — no sensitive data; D-12 treats denial as informational not an error |
| T-14-07 (ref) | Elevation | permission gate | mitigate | Inherited from 14-02: scheduling gated by D-14; this plan only requests + reflects state, never bypasses the gate |
</threat_model>

<verification>
- `npx tsc --noEmit` clean for settings.tsx; `npx expo lint` clean.
- restTimer row present with enable toggle + duration chevron + on/off+duration value.
- Permission prompt fires on enable (device-UAT); denial still enables the in-app timer with the muted helper.
- Duration choice persists across restart (device-UAT).
- `npm run test:rest-timer` + `npm run test:f13-brutal` unaffected (no pure-logic or hot-path change here).
</verification>

<success_criteria>
- TIMER-04: default rest duration + enable/disable configurable in Settings, gated by the permission prompt on enable.
- D-12/D-13: prompt in-context on enable; denial degrades to in-app-only with a muted (non-red) helper; blocked → Open Settings affordance.
- The notifications bell row remains the unchanged D-14 master gate.
</success_criteria>

<output>
Create `.planning/phases/14-rest-timer-f19-research-flagged/14-03-SUMMARY.md` when done.
</output>
