---
phase: 14-rest-timer-f19-research-flagged
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/rest-timer.ts
  - app/scripts/test-rest-timer.ts
  - app/lib/prefs.ts
  - app/locales/sv.json
  - app/locales/en.json
  - app/package.json
  - app/app.json
  - app/scripts/manual-test-phase-14-uat.md
autonomous: true
requirements: [TIMER-01, TIMER-02, TIMER-04, TIMER-05]
user_setup: []

must_haves:
  decisions: [D-02, D-08, D-10, D-14, D-15]
  truths:
    - "rest-timer.ts pure module exports remainingMs, formatMSS, decideNotificationAction, shouldFireNotification, extendEndTs — all Node-importable with no React/Expo/native imports"
    - "npm run test:rest-timer runs the Case[] table and exits 0 with all cases green"
    - "fm:restSeconds (default 120) and fm:restTimerEnabled (default OFF) round-trip through getPref/setPref and fall back to defaults on corrupt input"
    - "Every new timer string exists in BOTH sv.json and en.json at parity"
    - "expo-notifications is installed at the SDK-54 line (~0.32.x) via npx expo install and registered in app.json plugins"
    - "Device-UAT checklist markdown exists covering TIMER-02/03 + D-13/D-15"
  artifacts:
    - path: "app/lib/rest-timer.ts"
      provides: "Pure rest-timer math + decision predicates"
      exports: ["remainingMs", "formatMSS", "decideNotificationAction", "shouldFireNotification", "extendEndTs"]
      min_lines: 40
    - path: "app/scripts/test-rest-timer.ts"
      provides: "Node-only Case[] unit test for rest-timer.ts"
      contains: "const cases"
    - path: "app/lib/prefs.ts"
      provides: "fm:restSeconds + fm:restTimerEnabled typed prefs"
      contains: "fm:restSeconds"
    - path: "app/locales/sv.json"
      provides: "Swedish timer strings"
      contains: "restSkip"
    - path: "app/locales/en.json"
      provides: "English timer strings at parity"
      contains: "restSkip"
    - path: "app/scripts/manual-test-phase-14-uat.md"
      provides: "Device-UAT checklist for native-only behaviors"
      contains: "TIMER-03"
  key_links:
    - from: "app/scripts/test-rest-timer.ts"
      to: "app/lib/rest-timer.ts"
      via: "import of pure functions"
      pattern: "from \"\\.\\./lib/rest-timer\""
    - from: "app/package.json"
      to: "app/scripts/test-rest-timer.ts"
      via: "test:rest-timer npm script"
      pattern: "test:rest-timer"
---

<objective>
Lay the test-first foundation for the rest timer: the pure Node-importable logic module, its unit test + npm script, the two new `fm:*` prefs, all phase i18n strings at sv/en parity, the `expo-notifications` install, and the device-UAT checklist. Nothing native or React lands here — this is the scaffold every later plan builds on.

Purpose: Wave 0 must create the test scaffold BEFORE any logic that depends on it. The pure math + decision predicates are the unit-testable heart of TIMER-01/02/05 + the D-14 gate; the prefs back TIMER-04; the i18n keys are owned here so the parallel Wave-3 plans (Settings + banner) consume them with zero invention.
Output: `app/lib/rest-timer.ts`, `app/scripts/test-rest-timer.ts`, `test:rest-timer` script, extended `prefs.ts`, sv/en parity keys, installed `expo-notifications`, `app.json` plugin entry, device-UAT checklist.
</objective>

<execution_context>
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-rest-timer-f19-research-flagged/14-RESEARCH.md
@.planning/phases/14-rest-timer-f19-research-flagged/14-PATTERNS.md
@.planning/phases/14-rest-timer-f19-research-flagged/14-VALIDATION.md

<interfaces>
<!-- The pure module's contract. Later plans import these directly — no exploration. -->

From app/lib/rest-timer.ts (this plan creates it):
```typescript
export function remainingMs(endTs: number, now: number): number;      // Math.max(0, endTs - now); 0 on non-finite
export function formatMSS(ms: number): string;                        // "M:SS"; "0:00" on non-finite; ceil(ms/1000)
export function extendEndTs(endTs: number): number;                   // endTs + 30_000 (D-02 +30s)
export function shouldFireNotification(
  timerOn: boolean, masterOn: boolean, permissionGranted: boolean,
): boolean;                                                           // D-14: timerOn && masterOn && permissionGranted
export type RestEvent = "skip" | "extend" | "nextSet";
export function decideNotificationAction(
  event: RestEvent,
): "cancel" | "reschedule";                                          // skip→cancel; extend/nextSet→reschedule (TIMER-05/D-03/D-07)
```

From app/lib/prefs.ts (this plan extends it):
```typescript
"fm:restSeconds": number;          // default 120 (D-10)
"fm:restTimerEnabled": boolean;    // default false (OFF)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install expo-notifications + register the app.json plugin</name>
  <files>app/package.json, app/app.json</files>
  <read_first>
    - app/package.json (the dependency block + the `test:units` script at :15 — the script-add target in Task 2)
    - app/app.json (the `plugins` array at :31-47 — bare-string entries `expo-secure-store`/`expo-localization` are the entry shape)
    - 14-RESEARCH.md §Standard Stack + §Pitfall 1 (the SDK-54 version trap) + §Runtime State Inventory (Metro restart note)
    - CLAUDE.md → Technology Stack "use npx expo install" rule
  </read_first>
  <action>
    From the `app/` cwd run `npx expo install expo-notifications` — NEVER `npm install expo-notifications` (npm's `latest` is `56.0.17` = SDK 56 and will break SDK 54; the SDK-54 dist-tag is `~0.32.17`, resolved only by `npx expo install`, per RESEARCH §Pitfall 1). Confirm the resolved version with `npx expo install expo-notifications --check`. Add `"expo-notifications"` as a bare-string entry to the `plugins` array in `app/app.json` (no config-plugin options object is required — Expo Go SDK 54 needs no prebuild per RESEARCH §Runtime State). Do NOT add any icon/sound asset config. After install, `expo-notifications` MUST appear in `app/package.json` dependencies pinned to the `~0.32.x` line.
  </action>
  <verify>
    <automated>cd app && node -e "const v=require('./package.json').dependencies['expo-notifications']; if(!v||!v.startsWith('~0.32')){console.error('WRONG/MISSING '+v);process.exit(1)} console.log('expo-notifications '+v+' OK')"</automated>
  </verify>
  <acceptance_criteria>
    - app/package.json dependencies['expo-notifications'] starts with "~0.32"
    - app/app.json plugins array contains the string "expo-notifications"
    - The node version guard above exits 0
  </acceptance_criteria>
  <done>expo-notifications is installed at the SDK-54 line and registered as an app.json plugin; the version guard passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the pure rest-timer.ts module + its Case[] unit test + npm script</name>
  <files>app/lib/rest-timer.ts, app/scripts/test-rest-timer.ts, app/package.json</files>
  <read_first>
    - app/lib/e1rm.ts (the header-comment + Pitfall-5 non-finite-guard idiom — copy the structure verbatim, swap the body; PATTERNS §rest-timer.ts cites e1rm.ts:1-52 + 47-52)
    - app/lib/units.ts (the Pitfall-5 `Number.isFinite` clamp precedent)
    - app/scripts/test-units.ts (the Case[] table + matcher + exit-code skeleton — copy :22-29 and :100-123 verbatim, swap the cases)
    - app/package.json (the `"test:units": "tsx scripts/test-units.ts"` line at :15 — the add target)
    - 14-RESEARCH.md §Pattern 2 (exact remainingMs/formatMSS bodies) + §Validation Architecture (the pure-logic surface list)
    - 14-PATTERNS.md §rest-timer.ts + §test-rest-timer.ts (the exact excerpts)
  </read_first>
  <behavior>
    - remainingMs(1000, 0) === 1000; remainingMs(0, 1000) === 0 (clamped, never negative); remainingMs(NaN, 0) === 0 (non-finite guard)
    - formatMSS(90000) === "1:30"; formatMSS(0) === "0:00"; formatMSS(1) === "0:01" (ceil so 1ms still shows 0:01); formatMSS(NaN) === "0:00"
    - extendEndTs(1000) === 31000 (D-02 +30s = +30_000ms)
    - shouldFireNotification(true,true,true) === true; shouldFireNotification(true,false,true) === false; shouldFireNotification(false,true,true) === false; shouldFireNotification(true,true,false) === false (D-14 all-three gate)
    - decideNotificationAction("skip") === "cancel"; decideNotificationAction("extend") === "reschedule"; decideNotificationAction("nextSet") === "reschedule" (TIMER-05 / D-03 / D-07)
  </behavior>
  <action>
    Create `app/lib/rest-timer.ts` as a PURE module — no React, no Expo, no `expo-notifications`, no AsyncStorage import (it must import-clean under Node `tsx`, exactly like e1rm.ts imports under test-units.ts). Open with the e1rm.ts-style header comment naming the Decisions (TIMER-02 re-derive-from-endTs, D-02 +30s, D-14 gate). Implement the five exports with the exact signatures in the `<interfaces>` block: `remainingMs(endTs, now)` = `Math.max(0, endTs - now)` guarded `!Number.isFinite → 0`; `formatMSS(ms)` = `Math.ceil(ms/1000)` → `${m}:${pad2(s)}` guarded `!Number.isFinite → "0:00"`; `extendEndTs(endTs)` = `endTs + 30_000`; `shouldFireNotification(timerOn, masterOn, permissionGranted)` = `timerOn && masterOn && permissionGranted`; `decideNotificationAction(event)` returns `"cancel"` for `"skip"` else `"reschedule"` over the exported `RestEvent` union. Then create `app/scripts/test-rest-timer.ts` cloning the `test-units.ts` Case[] skeleton (import the five functions via the relative `../lib/rest-timer` path; Case[] table covering every behavior bullet above; `matches()` with optional float tol; PASS/FAIL print; `process.exit(1)` on any fail else `process.exit(0)`). Add `"test:rest-timer": "tsx scripts/test-rest-timer.ts"` to `app/package.json` scripts beside `test:units`.
  </action>
  <verify>
    <automated>cd app && npm run test:rest-timer</automated>
  </verify>
  <acceptance_criteria>
    - app/lib/rest-timer.ts exports remainingMs, formatMSS, extendEndTs, shouldFireNotification, decideNotificationAction
    - `grep -v '^//' app/lib/rest-timer.ts | grep -cE "from \"(react|expo|@react-native-async-storage)" ` returns 0 (pure module — no React/Expo/native imports)
    - app/scripts/test-rest-timer.ts imports from "../lib/rest-timer"
    - app/package.json scripts contains "test:rest-timer"
    - `npm run test:rest-timer` exits 0 with all cases passing
  </acceptance_criteria>
  <done>The pure module and its test exist, the npm script runs, and every behavior case is green via tabular Case[] assertions.</done>
</task>

<task type="auto">
  <name>Task 3: Extend prefs.ts (fm:restSeconds + fm:restTimerEnabled), add i18n keys at parity, write the device-UAT checklist</name>
  <files>app/lib/prefs.ts, app/locales/sv.json, app/locales/en.json, app/scripts/manual-test-phase-14-uat.md</files>
  <read_first>
    - app/lib/prefs.ts (:40-96 — the `notificationsSchema` enum-catch idiom at :50-53, the `PrefMap` at :56-61, the `SCHEMAS` map at :64-69, and the `String(value)` numeric serializer comment at :86-92 that already anticipates fm:restSeconds)
    - app/locales/sv.json (the flat Settings block where `restTimer`/`notifications`/`haptics` already live; `cancel` key for the ActionSheet)
    - app/locales/en.json (same block — the parity target)
    - app/scripts/manual-test-phase-06-uat.md (the device-UAT checklist format to mirror)
    - 14-PATTERNS.md §prefs.ts + §locales (exact schema lines + key list) + §test (UAT mirror)
    - 14-UI-SPEC.md §Copywriting Contract (the canonical sv/en strings + proposed key names)
    - 14-VALIDATION.md §Manual-Only Verifications (the six device-UAT rows)
  </read_first>
  <action>
    In `app/lib/prefs.ts`: add `const restSecondsSchema = z.coerce.number().int().positive().catch(120);` (D-10 default 120, throw-free over `unknown` — T-09-01 lineage) and `const restTimerEnabledSchema = z.enum(["true","false"]).catch("false").transform((s) => s === "true");` (default OFF, exact `fm:notifications` enum-catch idiom). Add `"fm:restSeconds": number;` and `"fm:restTimerEnabled": boolean;` to `PrefMap`, and `"fm:restSeconds": restSecondsSchema,` + `"fm:restTimerEnabled": restTimerEnabledSchema,` to `SCHEMAS`. No change to `getPref`/`setPref` is needed — the `String(value)` serializer already handles the number (prefs.ts:86-92 comment). In `app/locales/sv.json` AND `app/locales/en.json` add these flat keys at parity: `restSkip` (sv "Hoppa över" / en "Skip rest"), `restAdd30` ("+30 s" / "+30 s"), `restLabel` ("Vila" / "Rest"), `restDuration` ("Vilotid" / "Rest duration"), `restCustom` ("Anpassad" / "Custom"), `restNoPermission` ("Notiser av — vilan räknas ändå ner i appen." / "Notifications off — rest still counts down in the app."), `restDoneTitle` ("Vilan är slut" / "Rest is over"), `restDoneBody` ("Dags för nästa set." / "Time for your next set."), `on` ("På" / "On"), `off` ("Av" / "Off"). Reuse the existing `cancel` key for the picker's cancel button — do NOT add a duplicate. Then create `app/scripts/manual-test-phase-14-uat.md` mirroring the phase-06 UAT format, with one checklist row per 14-VALIDATION §Manual-Only Verifications entry: TIMER-03 (backgrounded ping), TIMER-02 (background reconcile ≥30s), TIMER-05 (skip/next-set cancels — no stale ping), TIMER-04/D-13 (permission prompt + deny-still-counts), D-15 (tap → workout route), SET-07/D-14 (master-toggle gating matrix).
  </action>
  <verify>
    <automated>cd app && node -e "const sv=require('./locales/sv.json'),en=require('./locales/en.json');const ks=['restSkip','restAdd30','restLabel','restDuration','restCustom','restNoPermission','restDoneTitle','restDoneBody','on','off'];for(const k of ks){if(!(k in sv)){console.error('sv missing '+k);process.exit(1)}if(!(k in en)){console.error('en missing '+k);process.exit(1)}}if(Object.keys(sv).length!==Object.keys(en).length){console.error('parity count mismatch');process.exit(1)}console.log('i18n parity OK')" && npm run test:rest-timer</automated>
  </verify>
  <acceptance_criteria>
    - app/lib/prefs.ts contains "fm:restSeconds" in both PrefMap and SCHEMAS
    - app/lib/prefs.ts contains "fm:restTimerEnabled" in both PrefMap and SCHEMAS
    - All 10 listed keys exist in both sv.json and en.json; key counts match (parity gate above exits 0)
    - app/scripts/manual-test-phase-14-uat.md contains "TIMER-03"
  </acceptance_criteria>
  <done>Both new prefs round-trip with corrupt→default fallback, all timer strings are at sv/en parity, and the device-UAT checklist exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AsyncStorage → app | `fm:restSeconds` / `fm:restTimerEnabled` are device-local, user-tamperable strings read back into the app |
| npm registry → build | `expo-notifications` install pulls a package that runs a postinstall |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | `fm:restSeconds` read | mitigate | `z.coerce.number().int().positive().catch(120)` — total over `unknown`; a tampered/garbage/huge/zero value clamps to the 120s default, never throws (RESEARCH §Security Domain) |
| T-14-02 | Tampering | `fm:restTimerEnabled` read | mitigate | `z.enum(["true","false"]).catch("false").transform(...)` — throw-free; corrupt → OFF default |
| T-14-SC | Tampering | `expo-notifications` install (postinstall script) | accept | First-party Expo module (maintainers ide/brentvatne/evanbacon; repo github.com/expo/expo), installed via `npx expo install` which pins the SDK-matched version. RESEARCH §Package Legitimacy Audit clears the benign autolinking postinstall. Provenance is unusually strong — no blocking checkpoint needed. |
</threat_model>

<verification>
- `npm run test:rest-timer` exits 0 (pure-logic gate green).
- i18n parity guard exits 0 (sv/en key counts equal, all 10 keys present in both).
- `expo-notifications` pinned to `~0.32.x` in package.json and listed in app.json plugins.
- `npx tsc --noEmit` clean (project canonical — no `npm run typecheck` script) and `npx expo lint` clean.
</verification>

<success_criteria>
- The pure `rest-timer.ts` module + `test-rest-timer.ts` + `test:rest-timer` script exist and pass; the test imports ONLY the pure module.
- `fm:restSeconds` (120) and `fm:restTimerEnabled` (OFF) are typed prefs with corrupt-tolerant defaults.
- All timer i18n strings are at sv/en parity; the device-UAT checklist exists.
- `expo-notifications` is installed at the SDK-54 line via `npx expo install` and registered in `app.json`.
</success_criteria>

<output>
Create `.planning/phases/14-rest-timer-f19-research-flagged/14-01-SUMMARY.md` when done.
</output>
