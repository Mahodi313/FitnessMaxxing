---
phase: 15-bilingual-release-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/app/(app)/(tabs)/_layout.tsx
autonomous: true
requirements: [I18N-03]
must_haves:
  decisions: [D-07, D-08]
  truths:
    - "On tab switch, the active tab's icon springs from scale 0.92 to 1 on the Reanimated UI thread"
    - "Per-tab animation hooks live in their own component instance (no Rules-of-Hooks violation)"
    - "The log-a-set hot path is untouched — no change to (tabs)/index.tsx; ≤3s budget holds"
    - "Pressed feedback and accessibility attributes on each tab are preserved"
  artifacts:
    - path: "app/app/(app)/(tabs)/_layout.tsx"
      provides: "ForgeTabButton child with per-tab withSpring icon-scale worklet"
      contains: "withSpring"
  key_links:
    - from: "app/app/(app)/(tabs)/_layout.tsx ForgeTabButton"
      to: "Reanimated UI thread"
      via: "useSharedValue + withSpring(target, SPRING) + useAnimatedStyle"
      pattern: "damping: 18"
---

<objective>
Apply the single genuinely-missing Forge §07 motion-table animation: the active tab-bar
icon scales 0.92→1 on tab switch, using the established UI-thread spring worklet.

Purpose: Close the last open motion-table row (D-07) without touching the log-a-set hot
path (D-08 / MOTN-01 ≤3s budget). 7 of 8 rows are already satisfied (ScaleDecorator on
plan-row reorder is already wired at plans/[id].tsx:552 — do NOT re-implement). Tab content
crossfade is treated as satisfied by the expo-router default (RESEARCH Open Q1) and verified
visually in the Plan 04 UAT, not built here.

Output:
- `app/app/(app)/(tabs)/_layout.tsx` — `ForgeTabButton` child component owning a per-tab
  `useSharedValue`/`withSpring` icon-scale worklet.
</objective>

<execution_context>
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-bilingual-release-hardening/15-CONTEXT.md
@.planning/phases/15-bilingual-release-hardening/15-RESEARCH.md
@.planning/phases/15-bilingual-release-hardening/15-PATTERNS.md

<interfaces>
<!-- The established Forge §07 worklet idiom to clone. -->

From app/components/ui/PrBanner.tsx:71 + app/app/(app)/(tabs)/index.tsx:587 (default Forge curve):
```typescript
const SPRING = { damping: 18, stiffness: 220 } as const;
const scale = useSharedValue(isActive ? 1 : 0.92);
useEffect(() => { scale.value = withSpring(isActive ? 1 : 0.92, SPRING); }, [isActive]);
const iconStyle = useAnimatedStyle(() => ({ transform: [{ scale: scale.value }] }));
// <Animated.View style={iconStyle}><Icon .../></Animated.View>
```

Existing _layout.tsx ForgeTabBar contract to preserve (lines ~79-128):
- Pressed feedback in the style({ pressed }) callback (line ~105) — never active:opacity-*
- accessibilityRole="tab", accessibilityState, accessibilityLabel={t(labelKey)} (lines ~100-102)
- Light/dark accent/text3 raw colors via useColorScheme() (lines ~69-72)
- Navigation onPress emit/navigate logic (lines ~85-94)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract ForgeTabButton child and add the icon-scale spring worklet</name>
  <files>app/app/(app)/(tabs)/_layout.tsx</files>
  <read_first>
    - app/app/(app)/(tabs)/_layout.tsx (full file — the ForgeTabBar renderer that maps state.routes inline; the Pressable, Icon, pressed-style callback, accessibility props, useColorScheme colors, onPress navigation)
    - app/components/ui/PrBanner.tsx (lines ~55-89 — the withSpring(damping 18 / stiffness 220) worklet idiom)
    - app/app/(app)/(tabs)/index.tsx (lines ~575-599 — same worklet idiom; READ-ONLY, this file is OFF-LIMITS for edits per D-08)
    - .planning/phases/15-bilingual-release-hardening/15-RESEARCH.md §"Pattern 3" + §Pitfall 5 (Rules-of-Hooks extraction) + §Open Q1 (crossfade = do not add)
    - .planning/phases/15-bilingual-release-hardening/15-PATTERNS.md §(tabs)/_layout.tsx (per-line preserve contract)
  </read_first>
  <action>
    Refactor the inline `state.routes.map((route, index) => ...)` body in the ForgeTabBar
    renderer of app/app/(app)/(tabs)/_layout.tsx into a child component
    `ForgeTabButton` (props: route/options, isActive, onPress, colors) so each tab's
    animation hooks live in their own component instance (Rules of Hooks — Pitfall 5).
    Inside ForgeTabButton declare `const SPRING = { damping: 18, stiffness: 220 } as const`
    (the default Forge §07 curve, copied verbatim from PrBanner.tsx:71). Drive
    `const scale = useSharedValue(isActive ? 1 : 0.92)`, a `useEffect` that sets
    `scale.value = withSpring(isActive ? 1 : 0.92, SPRING)` on `[isActive]`, and an
    `iconStyle = useAnimatedStyle(() => ({ transform: [{ scale: scale.value }] }))`. Wrap
    the existing `<Icon>` in an `Animated.View style={iconStyle}`. PRESERVE: pressed
    feedback in the `style({ pressed })` callback (never active:opacity-* / never inline
    box-decoration — NativeWind className-not-style rule, FIT-66/MEMORY); box styling
    (bg/border/radius/size) stays in className; accessibilityRole/State/Label={t(labelKey)};
    light/dark accent/text3 raw colors via useColorScheme(); the onPress emit/navigate
    logic. Do NOT add a tab-content crossfade animation (Open Q1 — expo-router default fade
    is sufficient; verify visually in UAT only). Do NOT touch app/app/(app)/(tabs)/index.tsx.
    Do NOT re-implement the plan-row ScaleDecorator (already wired at plans/[id].tsx:552).
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit && npx expo lint</automated>
  </verify>
  <acceptance_criteria>
    - `app/app/(app)/(tabs)/_layout.tsx` contains `withSpring` with `damping: 18, stiffness: 220`
    - The file defines a `ForgeTabButton` child component (per-item hooks are NOT inside a `.map()` body)
    - The active tab's `<Icon>` is wrapped in an `Animated.View` driven by `useAnimatedStyle`
    - Pressed feedback remains in the `style({ pressed })` callback; no `active:opacity-*`; box-decoration stays in `className`
    - `accessibilityRole`, `accessibilityState`, and `accessibilityLabel={t(labelKey)}` are preserved on each tab
    - `app/app/(app)/(tabs)/index.tsx` is unmodified (off-limits per D-08)
    - `cd app && npx tsc --noEmit` exits 0 AND `npx expo lint` reports no new hook-order errors
  </acceptance_criteria>
  <done>
    Active tab icon springs 0.92→1 on switch via a UI-thread worklet; per-tab hooks are
    isolated in ForgeTabButton; the hot path and the already-satisfied motion rows are
    untouched. Tab-icon "feel" is confirmed on device in the Plan 04 UAT.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI-thread worklet → JS hot path | Animation must run on the Reanimated UI thread and never block the set-write JS path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | Denial of Service (hot-path budget) | app/app/(app)/(tabs)/_layout.tsx tab-icon worklet | mitigate | Animation is a UI-thread Reanimated worklet outside the log-a-set passage; (tabs)/index.tsx is not modified (D-08). The ≤3s budget is re-verified by `npm run test:f13-brutal` in Plan 04. |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | Phase 15 installs no packages (RESEARCH §Package Legitimacy Audit: N/A). No install task in this plan. |
</threat_model>

<verification>
- `cd app && npx tsc --noEmit` exits 0.
- `cd app && npx expo lint` reports no hook-order / Rules-of-Hooks errors.
- `git diff --name-only` shows `app/app/(app)/(tabs)/index.tsx` is NOT modified.
- Device check (deferred to Plan 04 UAT): tab switch shows a premium 0.92→1 spring; no log-a-set lag.
</verification>

<success_criteria>
- The single open motion-table row (tab-bar icon scale) is applied on the UI thread.
- Hot path untouched; already-satisfied rows not re-implemented.
- Covers SC4 (motion applied; hot-path budget re-verified in Plan 04).
</success_criteria>

<output>
Create `.planning/phases/15-bilingual-release-hardening/15-02-SUMMARY.md` when done.
</output>
