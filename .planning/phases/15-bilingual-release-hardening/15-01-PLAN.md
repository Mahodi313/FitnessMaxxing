---
phase: 15-bilingual-release-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/scripts/check-i18n-coverage.ts
  - app/package.json
  - app/lib/i18n.ts
  - .github/workflows/phase-branch.yml
autonomous: true
requirements: [I18N-03]
must_haves:
  decisions: [D-01, D-03, D-04]
  truths:
    - "Every statically-resolvable t('literal') key referenced in app/app/** + app/components/** exists in app/locales/sv.json"
    - "User-visible JSX text literals that bypass t() are flagged and the scan fails on them"
    - "A missing key on device produces a loud dev-time console.error, but never ships to the production bundle"
    - "The i18n-coverage gate runs as a hard CI step that fails the job on exit 1"
  artifacts:
    - path: "app/scripts/check-i18n-coverage.ts"
      provides: "t()-coverage + hardcoded-string scan (Node-only tsx exit-code gate)"
      contains: "NESTED_PREFIXES"
      min_lines: 60
    - path: "app/package.json"
      provides: "test:i18n-coverage npm script"
      contains: "test:i18n-coverage"
    - path: "app/lib/i18n.ts"
      provides: "__DEV__-gated missingKeyHandler + saveMissing"
      contains: "missingKeyHandler"
    - path: ".github/workflows/phase-branch.yml"
      provides: "i18n coverage gate CI step"
      contains: "test:i18n-coverage"
  key_links:
    - from: "app/scripts/check-i18n-coverage.ts"
      to: "app/locales/sv.json"
      via: "import as assertion source"
      pattern: "from \"../locales/sv.json\""
    - from: ".github/workflows/phase-branch.yml"
      to: "test:i18n-coverage"
      via: "hard run step in the test job (no continue-on-error)"
      pattern: "npm run test:i18n-coverage"
---

<objective>
Build the persistent i18n coverage gate and the dev-time missing-key signal, then wire
the gate into CI as a hard step.

Purpose: Prove "zero missing keys across every screen" automatically (D-01) and make any
future gap fail fast — on device during development (D-04) and in CI/release-regression
(D-03). This is the automatable half of I18N-03; the manual device sweep (Plan 04) covers
the rest.

Output:
- `app/scripts/check-i18n-coverage.ts` (NEW) — t()-coverage + hardcoded-string scan.
- `app/package.json` — `test:i18n-coverage` script.
- `app/lib/i18n.ts` — `__DEV__`-gated `missingKeyHandler` + `saveMissing`.
- `.github/workflows/phase-branch.yml` — hard `i18n coverage gate` step in the `test` job.
</objective>

<execution_context>
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-bilingual-release-hardening/15-CONTEXT.md
@.planning/phases/15-bilingual-release-hardening/15-RESEARCH.md
@.planning/phases/15-bilingual-release-hardening/15-PATTERNS.md

<interfaces>
<!-- Proven exit-code gate to clone. Executor uses this shape directly; no exploration needed. -->

From app/scripts/check-locale-parity.ts (the analog — clone header + import + exit-code shape):
```typescript
import sv from "../locales/sv.json";
const svKeys = new Set(Object.keys(sv));
// ...
if (missing.length === 0) { console.log("PASS — ..."); process.exit(0); }
console.error("FAIL — ..."); /* print details */ process.exit(1);
```

From app/lib/i18n.ts init block (preserve verbatim — only ADD __DEV__ options into this object):
```typescript
i18n.use(initReactI18next).init({
  resources: { sv: { translation: sv }, en: { translation: en } },
  lng: Localization.getLocales()[0]?.languageCode ?? "sv",
  fallbackLng: "sv",                       // UNCHANGED
  interpolation: { escapeValue: false },
  compatibilityJSON: "v4",
});
```

Nested namespaces in sv.json that are runtime-concatenated (MUST be allowlisted, never flagged missing):
  exercise.*   (t('exercise.' + seed_key + '.name'))
  equip.*      (t('equip.' + equipment))
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the i18n coverage scan (check-i18n-coverage.ts) + npm script</name>
  <files>app/scripts/check-i18n-coverage.ts, app/package.json</files>
  <read_first>
    - app/scripts/check-locale-parity.ts (analog — Node-only header, sv.json import, exit-code gate; clone this shape)
    - app/scripts/test-rls.ts (Node-only tsx-script header convention reference)
    - app/locales/sv.json (the assertion source — note the flat top-level keys AND the two nested objects `exercise` / `equip`)
    - app/package.json §scripts (sibling gate lines: test:rls, test:locale-resolve, check:locale-parity)
    - .planning/phases/15-bilingual-release-hardening/15-RESEARCH.md §"Pattern 1" + §Pitfalls 1/2/3 (exclusion + namespace rules)
    - .planning/phases/15-bilingual-release-hardening/15-PATTERNS.md §check-i18n-coverage.ts (per-line copy-from sources)
  </read_first>
  <action>
    Create app/scripts/check-i18n-coverage.ts as a Node-only tsx script cloning the
    check-locale-parity.ts header convention and exit-code gate. Import sv.json as the
    assertion source: build `flatKeys = new Set(Object.keys(sv))`. Declare
    `NESTED_PREFIXES = ["exercise.", "equip."]` and a `keyIsCovered(key)` helper that
    returns true when the key is in flatKeys OR begins with a nested prefix (per D-01,
    Pitfall 1). Enumerate `app/app/**/*.tsx` + `app/components/**/*.tsx` via Node
    `fs.globSync` (fallback `fast-glob` if globSync unavailable). Run TWO assertions:
    (1) literal-only regex `t\(\s*['"]([^'"]+)['"]` to collect keys, assert each via
    keyIsCovered — the literal-only pattern naturally skips `t(variable)` /
    `t(exerciseNameKey(...))`, which is correct (Pitfall 2, do not resolve dynamic args);
    (2) flag user-visible JSX text children NOT wrapped in `{t(...)}` / `{variable}` /
    pure punctuation/whitespace. Document the hardcoded-string exclusion list IN A HEADER
    COMMENT: testID, icon `name=` props, `key=`, route `href`, `className`/`style` string
    values, `console.*` args, `format()` date-pattern strings, single-glyph punctuation
    (× — ·). On zero misses: `console.log("PASS — ...")` and `process.exit(0)`. On any
    miss: print the missing keys + flagged literals, `process.exit(1)`. Keep the
    Node-only header note ("NEVER imported from a Metro-bundled path") so the service-role
    audit gate stays clean. Add one line to app/package.json §scripts:
    `"test:i18n-coverage": "tsx scripts/check-i18n-coverage.ts"` (no env file — pure
    source scan, like check:locale-parity).
  </action>
  <verify>
    <automated>cd app && npm run test:i18n-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && npm run test:i18n-coverage` exits 0 (or exits 1 listing genuine gaps — see Task note below)
    - `app/scripts/check-i18n-coverage.ts` contains `NESTED_PREFIXES` with both `"exercise."` and `"equip."`
    - The script's literal regex is literal-only (`t\(\s*['"]...['"]`) so it does NOT attempt to resolve `t(labelKey)` / `t(exerciseNameKey(...))`
    - A header comment documents the hardcoded-string exclusion list (testID, icon name, key, href, className/style, console.*, format() date patterns, single-glyph punctuation)
    - `app/package.json` §scripts contains `"test:i18n-coverage": "tsx scripts/check-i18n-coverage.ts"`
    - `app/scripts/check-i18n-coverage.ts` retains the Node-only header note
  </acceptance_criteria>
  <done>
    test:i18n-coverage runs as a clean exit-code gate. If it surfaces genuine missing keys
    or bypassed literals (expected few-to-none per CONTEXT/RESEARCH Open Q2), each is added
    to sv.json/en.json or wrapped in t() until the gate is green — record any closed gaps
    in the SUMMARY. Exclusion list is documented; nested namespaces and dynamic-key call
    sites do not false-fail.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add __DEV__-gated missingKeyHandler to i18n.ts</name>
  <files>app/lib/i18n.ts</files>
  <behavior>
    - In __DEV__: a t('key') that resolves to a missing key triggers console.error("[i18n] MISSING KEY: \"key\"")
    - In __DEV__: the sv-fallback STILL renders (handler does not throw, render tree survives)
    - In production (__DEV__ false): neither saveMissing nor missingKeyHandler is active; Metro dead-code-strips the branch
    - fallbackLng:"sv", interpolation.escapeValue:false, compatibilityJSON:"v4", and the lng: line are unchanged
  </behavior>
  <read_first>
    - app/lib/i18n.ts (full file — the synchronous module-load init block + the resolveLanguage export must stay untouched)
    - .planning/phases/15-bilingual-release-hardening/15-RESEARCH.md §"Pattern 2" + §Pitfall 4 (console.error not throw; __DEV__ gate both options; T-15-01)
    - .planning/phases/15-bilingual-release-hardening/15-PATTERNS.md §app/lib/i18n.ts (the exact add)
  </read_first>
  <action>
    Inside the existing `i18n.use(initReactI18next).init({ ... })` object in app/lib/i18n.ts,
    add two options per D-04: `saveMissing: __DEV__` AND a `missingKeyHandler` that is
    `__DEV__ ? (_lngs, _ns, key) => { console.error(\`[i18n] MISSING KEY: "${key}"\`); } : undefined`.
    Use `console.error`, NOT `throw` — throwing inside a render-time t() crashes the tree on
    the first miss and hides every other missing key (RESEARCH Pattern 2 / D-04 discretion).
    Both options MUST be `__DEV__`-gated so Metro dead-code-strips them from release
    (T-15-01 / Pitfall 4). Do NOT alter `fallbackLng: "sv"`, `interpolation`,
    `compatibilityJSON: "v4"`, the `lng:` line, the synchronous init structure, or the
    `resolveLanguage` export.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `app/lib/i18n.ts` contains `missingKeyHandler` AND the handler body is guarded by `__DEV__`
    - `app/lib/i18n.ts` contains `saveMissing: __DEV__`
    - The handler calls `console.error` (NOT `throw`)
    - `app/lib/i18n.ts` still contains `fallbackLng: "sv"` and `compatibilityJSON: "v4"` unchanged
    - `cd app && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    Missing keys surface loudly in dev via a red LogBox console.error while sv-fallback still
    renders; production bundle carries neither option. Init structure and resolveLanguage
    export untouched.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire test:i18n-coverage as a hard CI step in phase-branch.yml</name>
  <files>.github/workflows/phase-branch.yml</files>
  <read_first>
    - .github/workflows/phase-branch.yml (full file — the `test` job, the existing tsc/lint/RLS steps, and `defaults.run.working-directory: app`)
    - .planning/phases/15-bilingual-release-hardening/15-RESEARCH.md §Security Domain (T-15-02 — gate must not be relaxed) + §"CI/RELEASE-REGRESSION GATE" diagram
    - .planning/phases/15-bilingual-release-hardening/15-PATTERNS.md §phase-branch.yml (sibling step shape)
  </read_first>
  <action>
    Add a new step named "i18n coverage gate" to the existing `test` job in
    .github/workflows/phase-branch.yml, alongside the TypeScript check / Lint / RLS tester
    steps: `run: npm run test:i18n-coverage`. NO env block (scan reads source + sv.json
    only, no secrets). NO `continue-on-error`, NO `|| true` — it must fail the job on exit 1
    exactly like the `test:rls` step (D-03 / T-15-02). The `defaults.run.working-directory: app`
    already scopes `npm run` to the app/ cwd, so no `cd` is needed. Place it as a hard
    sibling of the RLS step so it joins tsc/lint/RLS as a final release-regression gate.
  </action>
  <verify>
    <automated>cd app && npm run test:i18n-coverage && grep -n "test:i18n-coverage" ../.github/workflows/phase-branch.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/phase-branch.yml` `test` job runs `npm run test:i18n-coverage` as a step
    - The new step has NO `continue-on-error` and NO `|| true`
    - The step has no env block (no secrets referenced)
    - `grep -v '^#' .github/workflows/phase-branch.yml | grep -c "continue-on-error"` shows no new relaxation introduced by this change
  </acceptance_criteria>
  <done>
    CI fails the `test` job whenever a missing key or bypassed literal is introduced, just
    like the RLS regression gate. The i18n-coverage gate is now part of the persistent
    CI + final-regression set (D-03).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev build → production bundle | Dev-only loudness (saveMissing/missingKeyHandler) must not cross into release |
| CI config → release gate | The `test` job is the gate; weakening it bypasses regression protection |
| Node script → secrets | The coverage scan reads .tsx source + sv.json only; never .env.local or service-role key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Information Disclosure | app/lib/i18n.ts missingKeyHandler/saveMissing | mitigate | Gate BOTH options behind `__DEV__`; Metro dead-code-strips the branch in release. Acceptance criteria assert the `__DEV__` guard; SUMMARY notes a production `expo export` shows no missing-key console output. |
| T-15-02 | Tampering (gate bypass) | .github/workflows/phase-branch.yml test job | mitigate | Add `test:i18n-coverage` as a hard step with no `continue-on-error` / `|| true`; it fails the job on exit 1 exactly like `test:rls`. Acceptance criteria assert no relaxation flag is introduced. |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | Phase 15 installs no packages (RESEARCH §Package Legitimacy Audit: N/A); slopcheck N/A. No new install task in this plan. |
</threat_model>

<verification>
- `cd app && npm run test:i18n-coverage` exits 0 (gate green).
- `cd app && npx tsc --noEmit` exits 0 (i18n.ts edit type-clean).
- `.github/workflows/phase-branch.yml` contains the hard `i18n coverage gate` step with no `continue-on-error`.
- `git grep "service_role\|SERVICE_ROLE"` still matches ONLY the allowed paths (the new scan does not import service-role anything).
</verification>

<success_criteria>
- t()-coverage + hardcoded-string scan exists, runs as `test:i18n-coverage`, and is green.
- `__DEV__` missingKeyHandler present and `__DEV__`-gated; production behavior (fallbackLng:'sv') unchanged.
- CI `test` job runs the gate as a hard step.
- Covers SC1 (automated half) and I18N-03 (automated half).
</success_criteria>

<output>
Create `.planning/phases/15-bilingual-release-hardening/15-01-SUMMARY.md` when done.
</output>
