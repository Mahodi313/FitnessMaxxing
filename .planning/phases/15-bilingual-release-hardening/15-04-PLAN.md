---
phase: 15-bilingual-release-hardening
plan: 04
type: execute
wave: 2
depends_on: [15-01, 15-02, 15-03]
files_modified:
  - .planning/phases/15-bilingual-release-hardening/15-UAT.md
autonomous: false
requirements: [I18N-03, I18N-02]
must_haves:
  decisions: [D-05, D-06, D-02]
  truths:
    - "A 15-UAT.md matrix exists with one row per router screen and 4 combo columns (sv-light / sv-dark / en-light / en-dark)"
    - "Every screen renders complete in all 4 combos with no truncated text, no empty/fallback key, no theme-contrast miss"
    - "Hard-to-reach states (error, empty, offline, PR celebration, rest timer) are deliberately driven and verified"
    - "The final regression gate (test:f13-brutal + cross-user RLS) is green; the ≤3s log-a-set budget holds; tab-icon spring feels premium"
    - "I18N-02 language-toggle (device locale + Settings override) is confirmed on device"
  artifacts:
    - path: ".planning/phases/15-bilingual-release-hardening/15-UAT.md"
      provides: "screen×combo UAT matrix (12 screens × 4 combos), checked off on device"
      contains: "sv-light"
      min_lines: 30
  key_links:
    - from: ".planning/phases/15-bilingual-release-hardening/15-UAT.md"
      to: "router screen list"
      via: "one row per screen enumerated from the router tree"
      pattern: "workout/\\[sessionId\\]"
---

<objective>
Build the `15-UAT.md` screen×combo matrix, run the four-combo release-candidate device
sweep, and run the final automated regression gate.

Purpose: This is the manual half of I18N-03 that static analysis cannot reach — wrong
translation, Swedish-longer-than-English layout breakage, and missing keys hiding in
hard-to-reach states (D-01 manual / D-05 / D-06). It also closes the device/runtime half
of I18N-02 (language-toggle) and re-verifies SC3 (final regression gate) + SC4 (hot-path
budget + tab-icon feel) on real hardware.

Output:
- `.planning/phases/15-bilingual-release-hardening/15-UAT.md` (NEW) — 12 screens × 4 combos
  matrix, checked off on device.
- Final regression gate run recorded (test:f13-brutal + test:rls green).
</objective>

<execution_context>
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-bilingual-release-hardening/15-CONTEXT.md
@.planning/phases/15-bilingual-release-hardening/15-RESEARCH.md
@.planning/phases/15-bilingual-release-hardening/15-VALIDATION.md

<interfaces>
<!-- Screen list enumerated from the router tree (RESEARCH §Code Examples). EXCLUDE _forge-gallery + all _layout. -->

Router screens for the matrix (12 rows):
  (auth)/sign-in                                  → Sign in
  (auth)/sign-up                                  → Sign up
  (app)/(tabs)/index                              → Home / Plans (active-session swap, set-log, toast)
  (app)/(tabs)/history                            → History list (+ saved toast)
  (app)/(tabs)/settings                           → Settings (theme/language/units/restTimer/sign-out)
  (app)/plans/new                                 → New plan
  (app)/plans/[id]                                → Plan detail (DraggableFlatList + ScaleDecorator)
  (app)/plans/[id]/exercise-picker                → Exercise picker (filters, create-new)
  (app)/plans/[id]/exercise/[planExerciseId]/edit → Plan-exercise edit (steppers, targets)
  (app)/workout/[sessionId]                       → Active workout (set-log hot path, PR banner, rest timer)
  (app)/history/[sessionId]                       → Session detail (breakdown, e1RM, trophy)
  (app)/exercise/[exerciseId]/chart               → Exercise chart (range, draw-on-mount)

Combo columns: sv-light · sv-dark · en-light · en-dark
Hard-to-reach states to drive (D-06): error · empty · offline · PR celebration · rest timer
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold 15-UAT.md screen×combo matrix</name>
  <files>.planning/phases/15-bilingual-release-hardening/15-UAT.md</files>
  <read_first>
    - .planning/phases/15-bilingual-release-hardening/15-RESEARCH.md §"Code Examples" (router-tree screen enumeration) + §"Common Pitfalls" (Swedish-longer layout-break vectors)
    - .planning/phases/15-bilingual-release-hardening/15-VALIDATION.md §"Manual-Only Verifications" (what each combo must catch)
    - .planning/phases/15-bilingual-release-hardening/15-CONTEXT.md §D-05/D-06 (matrix shape + pass criteria)
  </read_first>
  <action>
    Create .planning/phases/15-bilingual-release-hardening/15-UAT.md as a markdown matrix:
    one row per router screen (the 12 enumerated above — EXCLUDE _forge-gallery.tsx and all
    _layout.tsx), four checkbox columns (sv-light / sv-dark / en-light / en-dark) per D-05.
    Add a "States driven" column or per-screen sub-rows listing the hard-to-reach states
    relevant to that screen (error / empty / offline / PR celebration / rest timer per D-06).
    Add a header section stating the pass criteria (D-06): no truncated text, no empty/
    fallback key, no theme-contrast miss in ANY combo; happy-path-only is NOT sufficient.
    Add a dedicated row/checklist item for the I18N-02 language-toggle verification (set
    device sv/en + Settings override → confirm resolved language) and for the tab-icon
    spring feel + ≤3s log-a-set budget (SC4). Leave all checkboxes unchecked — they are
    filled during Task 3's device sweep.
  </action>
  <verify>
    <automated>test -f .planning/phases/15-bilingual-release-hardening/15-UAT.md && grep -q "sv-light" .planning/phases/15-bilingual-release-hardening/15-UAT.md && grep -q "workout/\[sessionId\]" .planning/phases/15-bilingual-release-hardening/15-UAT.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `15-UAT.md` contains a row per router screen (12 rows) and 4 combo columns (sv-light/sv-dark/en-light/en-dark)
    - The matrix lists the hard-to-reach states (error, empty, offline, PR celebration, rest timer) to drive
    - A header documents the D-06 pass criteria (no truncation, no fallback key, no theme-contrast miss; happy-path-only insufficient)
    - There is an explicit I18N-02 language-toggle checklist item and an SC4 (tab-icon feel + ≤3s budget) item
    - `_forge-gallery.tsx` and `_layout.tsx` files are NOT listed as screens
  </acceptance_criteria>
  <done>
    The auditable UAT matrix exists with all combos and states enumerated, ready for the
    device sweep.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run the final automated regression gate</name>
  <files>.planning/phases/15-bilingual-release-hardening/15-UAT.md</files>
  <read_first>
    - .planning/phases/15-bilingual-release-hardening/15-VALIDATION.md §"Full suite command" + §"Success Criteria → Validation Map" (SC3)
    - app/package.json §scripts (test:f13-brutal, test:rls, test:i18n-coverage, check:locale-parity, test:locale-resolve)
    - C:/Users/Mahod/.claude/projects/.../memory (reference_f13_brutal_fixture_window — count-only failure on no recent session is environmental, not a regression)
  </read_first>
  <action>
    Run the full release-regression suite from the app/ cwd: `npm run test:i18n-coverage`,
    `npm run check:locale-parity`, `npm run test:locale-resolve`, `npm run test:rls`, and
    `npm run test:f13-brutal` (D-03 — the i18n-coverage gate joins F13 + cross-user RLS as
    the final gate). Record each exit code in 15-UAT.md (a "Regression gate" section). Note
    the f13-brutal fixture-window caveat: a count-only failure when there is no recent
    session in the last 60 min is environmental (FIT-107), not a regression — flag it as
    such rather than treating it as a hard fail.
  </action>
  <verify>
    <automated>cd app && npm run test:i18n-coverage && npm run check:locale-parity && npm run test:locale-resolve && npm run test:rls</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && npm run test:i18n-coverage` exits 0
    - `cd app && npm run check:locale-parity` exits 0
    - `cd app && npm run test:locale-resolve` exits 0
    - `cd app && npm run test:rls` exits 0
    - `npm run test:f13-brutal` is run; a count-only failure with no recent session is recorded as environmental (FIT-107), not a regression
    - `15-UAT.md` has a "Regression gate" section recording each command's result
  </acceptance_criteria>
  <done>
    The automated half of the release-regression gate is green and recorded; SC3 satisfied
    pending the device sweep.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Device UAT sweep — sv/en × light/dark across every screen</name>
  <files>.planning/phases/15-bilingual-release-hardening/15-UAT.md</files>
  <what-built>
    The full i18n-coverage gate (Plan 01), the tab-icon scale animation (Plan 02), the
    I18N-02 resolver close-out (Plan 03), the 15-UAT.md matrix (Task 1), and the green
    automated regression suite (Task 2) are all in place. The remaining work is the manual
    four-combo device sweep that static analysis cannot perform.
  </what-built>
  <action>
    Walk every screen in 15-UAT.md through all four combos on a real iPhone (Expo Go),
    checking off each cell. File any genuine miss as a Linear bug and close it before
    sign-off (one fix per loop — established device-UAT-iteration pattern). Detailed steps
    in how-to-verify below.
  </action>
  <how-to-verify>
    On a real iPhone via Expo Go, walk EVERY screen in 15-UAT.md through all four combos
    (sv-light / sv-dark / en-light / en-dark). For each combo:
    1. Confirm no truncated text, no button overflow, no tab-bar/label wrap — prioritize
       Swedish-longer-than-English screens (buttons, single-line labels, tab bar) (D-06).
    2. Confirm no empty/fallback (Swedish-in-English) key appears — watch the device LogBox
       for the red `[i18n] MISSING KEY` console.error from Plan 01.
    3. Confirm theme contrast holds in both light and dark.
    4. Deliberately drive into the hard-to-reach states (error, empty, offline, PR
       celebration, rest timer) in each combo and confirm no missing key hides there (D-06).
    5. I18N-02: set the device language to Swedish (expect sv) then to another language
       (expect en), and exercise the Settings language override — confirm the app resolves
       the expected language each time.
    6. SC4: switch tabs and confirm the active-icon spring (0.92→1) feels premium; log a set
       and confirm the ≤3s budget still holds with no lag (test:f13-brutal already covered
       the budget gate).
    Check off each cell in 15-UAT.md as you go. File any genuine miss as a Linear bug
    (`npm run linear:create -- --type bug --priority high --phase 15`) and close it before
    sign-off (one fix per loop — established device-UAT-iteration pattern).
  </how-to-verify>
  <verify>
    <human-check>All 12 screens × 4 combos checked clean in 15-UAT.md; I18N-02 toggle confirmed; tab-icon feel + ≤3s budget confirmed</human-check>
  </verify>
  <done>
    15-UAT.md fully checked across all 4 combos with no truncation / fallback key /
    theme-contrast miss, including hard-to-reach states; I18N-02 device toggle confirmed;
    SC4 confirmed.
  </done>
  <resume-signal>Type "approved" once all 4 combos × all screens are checked and clean, or describe the issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev build → production bundle | UAT confirms the dev-only missing-key handler (Plan 01) does not leak user-visible noise; release behavior is sv-fallback only |
| CI config → release gate | The automated suite (Task 2) is the gate-bypass check — all gates must be hard and green |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Information Disclosure | i18n missingKeyHandler (verified here) | mitigate | UAT step 2 watches for the dev-only LogBox handler; SUMMARY notes a production export shows no missing-key output. (Implemented in Plan 01.) |
| T-15-02 | Tampering (gate bypass) | release-regression suite (Task 2) | mitigate | Task 2 runs every gate as a hard command and records exit codes; cross-user RLS (`test:rls`) is the unchanged access-control regression check (V4). |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | Phase 15 installs no packages (RESEARCH §Package Legitimacy Audit: N/A). No install task in this plan. |
</threat_model>

<verification>
- `15-UAT.md` exists with 12 screen rows × 4 combos and the states/toggle/SC4 checklist items.
- Automated regression suite green: test:i18n-coverage, check:locale-parity, test:locale-resolve, test:rls (and test:f13-brutal modulo the FIT-107 fixture-window caveat).
- Human checkpoint: all 4 combos × all screens checked clean; I18N-02 toggle confirmed; tab-icon feel + ≤3s budget confirmed.
</verification>

<success_criteria>
- 15-UAT.md matrix fully checked across all 4 combos with no truncation / fallback key / theme-contrast miss, including hard-to-reach states.
- Final regression gate green (SC3).
- Tab-icon motion premium + ≤3s budget holds (SC4).
- I18N-02 device/runtime language-toggle confirmed.
- Covers SC1 (manual half), SC2, SC3, SC4, I18N-03, and I18N-02 (runtime half).
</success_criteria>

<output>
Create `.planning/phases/15-bilingual-release-hardening/15-04-SUMMARY.md` when done.
</output>
