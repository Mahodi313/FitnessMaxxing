---
phase: 15-bilingual-release-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/scripts/test-locale-resolve.ts
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [I18N-02]
must_haves:
  decisions: [D-02]
  truths:
    - "The D-11 device-locale→engine-language mapping (Swedish device → sv, anything else → en) is fully covered by automated cases and passes"
    - "I18N-02 is reconciled to Complete in REQUIREMENTS.md only after the resolver test passes"
  artifacts:
    - path: "app/scripts/test-locale-resolve.ts"
      provides: "D-11 resolver unit test (Case[] table + exit-code gate)"
      contains: "resolveLanguage"
    - path: ".planning/REQUIREMENTS.md"
      provides: "I18N-02 marked Complete"
      contains: "I18N-02"
  key_links:
    - from: "app/scripts/test-locale-resolve.ts"
      to: "app/lib/resolve-language.ts resolveLanguageCore"
      via: "import + table-driven assertions"
      pattern: "resolveLanguage"
---

<objective>
Reconcile and close I18N-02 (D-02): confirm the device-locale-default + Settings-override
resolver is correct and fully covered, then flip the requirement row to Complete.

Purpose: The D-11 resolver (`resolveLanguageCore` in `app/lib/resolve-language.ts`), its
7-case test (`test-locale-resolve.ts`), and the Phase-9 `LocaleBootstrap` already implement
the mapping. I18N-02 is still marked Pending only because it was never explicitly verified
and flipped. This plan closes the requirement-row debt so the i18n rows are clean at
milestone close. The runtime/device half of I18N-02 (language-toggle behavior) is verified
in the Plan 04 UAT.

Output:
- `app/scripts/test-locale-resolve.ts` — verified green; a new `Case` row added ONLY if a
  D-11 case is found uncovered.
- `.planning/REQUIREMENTS.md` — I18N-02 marked Complete.
</objective>

<execution_context>
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-bilingual-release-hardening/15-CONTEXT.md
@.planning/phases/15-bilingual-release-hardening/15-RESEARCH.md
@.planning/phases/15-bilingual-release-hardening/15-PATTERNS.md

<interfaces>
<!-- The existing resolver + its test. Verify-first; extend only on a real gap. -->

From app/lib/resolve-language.ts (pure D-11 core — Node-importable):
  resolveLanguageCore / resolveLanguage : returns the union 'sv' | 'en' only.
  Mapping: stored pref 'sv'|'en' wins; pref 'system' → device Swedish ⇒ 'sv', else ⇒ 'en'.

From app/scripts/test-locale-resolve.ts (Case[] table + loop + exit-code gate, ~55 lines):
```typescript
{ name: "...", pref: "system", deviceLang: "nb", expected: "en" }  // Case shape to append if needed
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify the D-11 resolver test covers I18N-02; extend only on a gap</name>
  <files>app/scripts/test-locale-resolve.ts</files>
  <read_first>
    - app/scripts/test-locale-resolve.ts (full file — the Case[] table, the loop, the exit-code gate)
    - app/lib/resolve-language.ts (full file — resolveLanguageCore mapping; confirm it returns only 'sv'|'en')
    - .planning/phases/15-bilingual-release-hardening/15-RESEARCH.md §"I18N-02 (D-02)" + §"Wave 0 Gaps" (all 7 cases already covered; extend only if UAT reveals an uncovered case)
    - .planning/phases/15-bilingual-release-hardening/15-PATTERNS.md §test-locale-resolve.ts (conditional-modify Case shape)
  </read_first>
  <action>
    Run `npm run test:locale-resolve` and read the existing `cases` array against the D-11
    contract (stored pref 'sv'/'en' wins; pref 'system' → Swedish device ⇒ 'sv', anything
    else ⇒ 'en'). Per RESEARCH/Wave-0, all 7 cases are already covered and the test passes —
    in that case make NO code change and record "verified, no gap" in the SUMMARY. ONLY if a
    D-11 mapping branch is genuinely uncovered, append a new row to the existing `cases`
    array following the exact `Case` shape (e.g. a Nordic-neighbor `{ pref: "system",
    deviceLang: "nb", expected: "en" }`); do NOT restructure the loop or the exit-code gate.
    Confirm resolveLanguageCore returns only the `'sv'|'en'` literal union (V5 input-validation
    carry-forward — no free text reaches the i18n engine).
  </action>
  <verify>
    <automated>cd app && npm run test:locale-resolve</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && npm run test:locale-resolve` exits 0
    - Every D-11 branch (pref sv → sv; pref en → en; system+Swedish device → sv; system+other device → en) is represented by at least one Case
    - `app/lib/resolve-language.ts` resolveLanguageCore returns only `'sv'|'en'` (no free text path)
    - If no gap was found, `app/scripts/test-locale-resolve.ts` is unchanged (verify-only)
  </acceptance_criteria>
  <done>
    The resolver test is green and demonstrably covers the full D-11 mapping. Any genuine
    gap (none expected) is closed by a single appended Case row.
  </done>
</task>

<task type="auto">
  <name>Task 2: Mark I18N-02 Complete in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the §Internationalization checklist line for I18N-02 AND the requirement→phase status table row `| I18N-02 | Phase 9 | Pending |`)
    - .planning/phases/15-bilingual-release-hardening/15-CONTEXT.md §D-02 (close-out instruction)
  </read_first>
  <action>
    In .planning/REQUIREMENTS.md flip I18N-02 to Complete in BOTH places: change the
    checklist bullet `- [ ] **I18N-02**: ...` to `- [x] **I18N-02**: ...`, and change the
    status-table row `| I18N-02 | Phase 9 | Pending |` to `| I18N-02 | Phase 9 | Complete |`
    (D-02 closes it in Phase 15; keep the original implementing-phase reference as
    documented). Do this ONLY after Task 1's `test:locale-resolve` is green — the flip
    follows the passing gate, not the other way round.
  </action>
  <verify>
    <automated>cd app && npm run test:locale-resolve && grep -n "I18N-02" ../.planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` §Internationalization shows `- [x] **I18N-02**`
    - The requirement→phase status table row for I18N-02 reads `Complete` (no longer `Pending`)
    - `grep -c "I18N-02.*Pending" .planning/REQUIREMENTS.md` returns 0
  </acceptance_criteria>
  <done>
    I18N-02 reads Complete in both the checklist and the status table; the device/runtime
    half is still scheduled for the Plan 04 UAT language-toggle verification.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device locale string → i18n engine | Only the resolved `'sv'|'en'` literal union may reach i18next (no free text) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Tampering (untrusted locale input) | app/lib/resolve-language.ts resolveLanguageCore | accept | Carry-forward of T-09-04/T-08-11: resolver already constrains output to the `'sv'|'en'` literal union; no new data path. Task 1 re-asserts this; no new mitigation required. |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | Phase 15 installs no packages (RESEARCH §Package Legitimacy Audit: N/A). No install task in this plan. |
</threat_model>

<verification>
- `cd app && npm run test:locale-resolve` exits 0.
- `.planning/REQUIREMENTS.md` shows I18N-02 Complete in both the checklist and the status table.
</verification>

<success_criteria>
- D-11 mapping fully covered + green.
- I18N-02 reconciled to Complete (D-02).
- Covers I18N-02 (automated half; runtime half verified in Plan 04 UAT).
</success_criteria>

<output>
Create `.planning/phases/15-bilingual-release-hardening/15-03-SUMMARY.md` when done.
</output>
