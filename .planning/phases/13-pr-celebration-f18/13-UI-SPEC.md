---
phase: 13
slug: pr-celebration-f18
status: draft
shadcn_initialized: false
preset: none
created: 2026-06-14
---

# Phase 13 — UI Design Contract

> Visual and interaction contract for **PR Celebration (F18)**. Generated by gsd-ui-researcher, verified by gsd-ui-checker.
>
> **Scope reminder (CONTEXT `<domain>`):** This phase ADDS PR markup (trophies + celebration banner + e1RM hero swap) to screens already Forge-skinned in Phases 11/12. It does NOT re-skin those screens. Every surface below already exists; this contract specifies only the *new PR markup* dropped onto them. Design source of truth: `app/design v2/Sources/design/forge-screens.jsx` + `lib.jsx` (THEMES.forge) + `Forge Design Spec.html §07`.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (shadcn N/A — RN/Expo app, not React/Next/Vite web; full Forge token system already shipped Phase 8) |
| Preset | not applicable |
| Component library | Forge primitives (`app/components/ui/*`: `Icon`, `ForgeChip`, `ForgeStat`, gradient via `THEMES.forge`) + NativeWind 4 / Tailwind 3 |
| Icon library | In-house stroke set (`Icon` component, `lib.jsx`); **PR icons: `trophy`, `arrowUp`, `checkCircle`** |
| Font | Inter Display (`font-display` / `font-display-semibold` / `font-display-bold`) for numerals; System for body; tabular-nums (`numStyle` = `tnum`/`ss01`) on EVERY weight/e1RM/volume figure |

**Registry safety:** not applicable (no shadcn, no third-party registry). Phase introduces zero new UI dependencies — banner sweep + scale spring ride the already-installed Reanimated 4 + Skia (CONTEXT `<code_context>`).

---

## Spacing Scale

Declared values (multiples of 4; Forge optical exceptions noted). All transcribed from the mock surfaces this phase touches.

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Delta-chip icon→text gap; baseline gap on hero numeral→unit |
| sm | 8px | Hero numeral→unit baseline gap (8px); session-detail exercise-name→trophy run |
| md | 12px | Banner inner content gap (trophy tile → text block); banner vertical padding |
| — | 14px | Banner horizontal padding (Forge optical; mock `12px 14px`) |
| lg | 16px | Banner corner radius + screen horizontal gutter (`0 16px`) |
| xl | 24px | (existing card rhythm — not newly introduced here) |

**Exceptions (Forge optical, locked from mock — do NOT round to 4-grid):**
- **Banner padding `12px 14px`** (mock line 392) — 14px horizontal is the Forge card-inset optical value.
- **Banner radius 16px** (mock line 392) — between `forge-md` (14) and `forge-lg` (20); use literal `16`.
- **Trophy tile radius 10px** = `forge-sm` (banner 36px tile; mock line 398).
- **Delta-chip padding `5px 9px`, radius 8px** (chart hero; mock line 809) — carried verbatim from Phase 12 D-12 chip (do not re-derive; already shipped on chart).
- **Touch targets:** the banner is non-interactive (auto-dismiss, no tap — D-10); set-row trophy + history trophy are non-interactive status glyphs inside existing ≥44px rows. No new tap targets introduced.

---

## Typography

Inter Display + tabular-nums on numerals; System for label/body. Sizes transcribed from the exact mock lines this phase renders.

| Role | Size | Weight | Line Height | Where |
|------|------|--------|-------------|-------|
| Hero numeral (chart e1RM, PR-05) | 52px | 700 (`font-display-bold`) | 1.0 | Chart hero big number (mock 800); `letterSpacing -2`; tabular-nums |
| Banner title (PR-03) | 14px | 700 | ~1.2 | `personalBest` ("Nytt personbästa") (mock 405); `letterSpacing -0.2` |
| Banner sub / delta chip | 12–13px | 600–700 | ~1.3 | `pbSub` + "· set N" sub at 12px (mock 408); delta chip 13px/700 (mock 811); tabular-nums |
| Label (eyebrow / unit / column) | 10–11px | 600 | ~1.2 | `estimated1RM` eyebrow 11px (mock 796); hero "kg" unit 18px/500; uppercase `letterSpacing 1` |

**Weight rule:** exactly 2 effective body weights — **600 (semibold)** for labels/values + **700 (bold)** for banner title + hero numeral. (Hero/banner numerals use the `font-display-*` families; RN does not synthesize weight — pick the explicit family per tailwind config OQ-4.)

**Locked numeral rule:** every PR figure (banner kg/reps, hero e1RM, session-detail per-exercise e1RM, history-row stats) renders through `numStyle` tabular-nums (DSGN-03) so figures align and a kg↔lbs toggle doesn't reflow (D-20).

---

## Color

60/30/10 split is inherited from the Forge system (already shipped). PR markup spends **only** the accent/gradient 10% slice and the success semantic — it adds no new dominant/secondary surface.

| Role | Value (dark / light) | Usage |
|------|----------------------|-------|
| Dominant (60%) | `#000000` / `#FAFAF7` (`forge-bg`) | Screen background (unchanged) |
| Secondary (30%) | `#0E0E10` / `#FFFFFF` (`forge-surface`), `#18181B` / `#F2F1EC` (`forge-surface2`) | Cards, set-list, rows (unchanged) |
| Accent (10%) | `#FF5A1F` / `#E14E10` (`forge-accent`) + gradient `gradFrom #FF7A2E → gradTo #FF2D55 / #FF3D5E` | **PR-only** (see reserved list) |
| Semantic — success | `#30D158` / `#1E9E45` (`forge-success`) | Chart hero delta chip (e1RM gain), + `checkCircle` on non-PR set rows |

**Accent / gradient reserved for (explicit — never "all interactive elements"):**
1. **PR celebration banner** — surface is a low-alpha gradient wash `linear-gradient(135deg, gradFrom15, gradTo15)` + `1px accent30` border (mock 393–394); trophy tile is the FULL `gradFrom→gradTo` gradient (mock 399).
2. **Set-row PR trophy** — 24px circle, full `gradFrom→gradTo` gradient, white trophy glyph; **replaces the green `checkCircle`** on a PR-at-log-time row (D-13, mock 454–461).
3. **History session-row trophy** — 24px gradient circle, white trophy (D-14, mock 627–635).
4. **Session-detail per-exercise trophy** — 18px gradient circle, white trophy (D-15, mock 739–746).
5. **Chart hero delta chip** — `forge-success` foreground on `success@~13%` background (gain only; D-16, mock 810–814).

**Semantic discipline (locked):**
- **PR celebration is NEVER red.** The danger token (`#FF453A` / `#D70015`) is reserved exclusively for data-loss-adjacent destructive actions (carry-forward Phase 11 D-16 / Phase 12) — no destructive action exists in this phase.
- **Delta chip is success-or-absent, never red** (Phase 12 D-12 carry-forward): a *negative* range delta shows NO chip; e1RM hero never renders a red down-chip. (The PR feature only celebrates; it never scolds.)
- **Trophy replaces check, never stacks** (D-13): a PR row shows the gradient trophy *instead of* the green check; a normal row shows the check. The narrow 36px column holds exactly one glyph.

---

## Copywriting Contract

All strings route through `t()` (D-21, Phase 8 flat 1:1 sv/en convention). Existing keys **reuse**, new keys **add at sv/en parity**.

| Element | Copy (sv / en) | Key |
|---------|----------------|-----|
| Banner title | "Nytt personbästa" / "New personal best" | `personalBest` ✅ exists (`locales/{sv,en}.json:106`) |
| Banner sub (numbers) | "{{kg}} kg × {{reps}} reps" | `pbSub` ✅ exists (`:107`) — interpolated `{{kg}}`/`{{reps}}` |
| Banner sub — set suffix | "· set {{n}}" (mock: sv "· sätt 3", en "· set 3") | **NEW** — add a set-number suffix key at parity (planner names it; flat-key convention, D-21). Note the mock uses sv "sätt" but the in-app convention is "set" everywhere; planner confirms against existing `set`/`sets` keys to avoid drift. |
| Chart hero eyebrow | "Estimerat 1RM" / "Estimated 1RM" | `estimated1RM` ✅ exists (`:138`) |
| Session-detail per-exercise stat label | "Max · kg" (existing maxWeight label pattern) → planner decides if a new e1RM label key is needed for per-exercise e1RM | `maxWeight` ✅ exists / **NEW** if e1RM gets its own label |
| Delta chip | "+{{kg}} kg" / "+{{pct}} %" (sign-prefixed, success-only) | Reuses Phase 12 D-12 chip formatter; numeral via `formatWeight` |

**Empty / first-set state (no party — D-02):**
| Element | Behaviour |
|---------|-----------|
| First-ever working set on an exercise | **No banner, no trophy.** Silently establishes the baseline (D-02). The set row shows the normal green `checkCircle`. No empty-state copy — absence IS the state. |
| Exercise with no prior PR in history list / detail | No trophy rendered (the row/exercise simply has no glyph). No placeholder text. |
| Chart hero, no working sets in range | Existing Phase 12 empty-state path (`useExerciseSummaryQuery → null`) — hero shows the established empty treatment; e1RM numeral is not invented from nothing. |

**Error state:** none authored. PR detection is **read-side + fire-and-forget after `addSet.mutate`** (D-17); a failed/absent best-reference query (e.g. offline, never synced) **silently yields "no PR"** — it must NEVER surface an error, block the set save, or breach the ≤3s budget. Offline = last-synced reference (D-06); a missed celebration is acceptable, a blocked save is not.

**Destructive actions:** **none in this phase.** PR is derived, never stored (no `is_pr` column — out-of-scope per REQUIREMENTS); nothing is deleted or mutated. No confirmation dialogs.

---

## Motion Contract (PR-03 banner — §07 Motion)

Locked from `Forge Design Spec.html §07` (line 551) + CONTEXT D-09/D-10/D-11/D-19.

| Property | Spec |
|----------|------|
| Entry | Scale **0.96 → 1.0** + **gradient sweep across the surface** + `notificationSuccess` haptic |
| Curve | spring **damping 18 / stiffness 220** (Forge default, §07) |
| Duration budget | ~600ms entry (§07 table) |
| Dwell | ~3–4s, then fade out (D-10) — auto-dismiss, **no tap required** |
| Trigger | **One fresh banner per PR set** (D-11) — incl. a later set in the same session beating an earlier one (D-07); banners do not merge into one updating surface |
| Placement | **Absolutely-positioned floating overlay** (D-09) — set list, input row, and "Klart" button NEVER shift. Inline-rendered, **no Modal portal** (Phase 11/12 D-22). This deliberately overrides the mock's inline-above-the-card placement to protect the ≤3s log budget. |
| Haptic gate | `notificationSuccess` routes through the **same `fm:haptics` gate** as the set-logged haptic (D-18) — silent when haptics off |
| Reduce-motion | OS reduce-motion → **snap to final state** (no scale, no sweep), but banner + trophy still render (D-19); haptic still fires if enabled |
| Budget guard | Detection + banner are fire-and-forget after the mutate (D-17) — `npm run test:f13-brutal` stays green; no mutation/queue/persister/`exercise_sets` path touched |

---

## Surface-by-Surface Component Inventory

The exact PR markup per screen (each screen is already Forge — only the listed delta is new).

| # | Surface | File | New PR markup | Mock ref | Decisions |
|---|---------|------|---------------|----------|-----------|
| 1 | Active workout — banner | `app/app/(app)/workout/[sessionId].tsx` | Floating gradient banner: 16px-radius gradient-wash surface + 36px gradient trophy tile (10px radius, 18px white trophy) + `personalBest` 14/700 + `pbSub`+"· set N" 12px sub | 389–413 | D-09..D-11, D-17..D-19 |
| 2 | Active workout — set-row trophy | same | 24px gradient circle + 12px white trophy **replacing** the 20px green `checkCircle`, only on PR-at-log-time rows | 454–464 | D-12, D-13 |
| 3 | History list — session trophy | `app/app/(app)/(tabs)/history.tsx` | 24px gradient circle + 12px white trophy on rows where any set was a PR-at-log-time | 627–635 | D-14 |
| 4 | Session detail — per-exercise | `app/app/(app)/history/[sessionId].tsx` | Per-exercise e1RM stat + 18px gradient circle + 10px white trophy on PR-at-the-time exercises, inside the Phase 12 D-15 hybrid card | 739–746 | D-15 |
| 5 | Exercise chart — hero | `app/app/(app)/exercise/[exerciseId]/chart.tsx` | Hero swaps current-best → 52px e1RM numeral + `estimated1RM` eyebrow + success delta chip (best-in-range − earliest-in-range) | 793–816 | D-16 |

**Trophy size ladder (locked from mock — do not normalize):** banner tile **36px** (18px glyph) · set-row + history **24px** (12px glyph) · session-detail per-exercise **18px** (10px glyph). All full `gradFrom→gradTo` gradient, white trophy, `strokeWidth 2.2`.

**Unit reactivity (D-20):** every numeral on surfaces 1–5 reads through the reactive `useUnitStore` (FIT-111) — e1RM computed in kg via `lib/e1rm.ts`, then display-converted; a kg↔lbs toggle re-renders banner/hero/detail live, no restart.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| none (no shadcn) | n/a | not applicable — no registry, no third-party UI blocks; zero new UI deps |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
