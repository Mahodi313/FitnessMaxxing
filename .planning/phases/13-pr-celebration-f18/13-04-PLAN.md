---
phase: 13-pr-celebration-f18
plan: 04
type: execute
wave: 3
depends_on: ["13-01", "13-03"]
files_modified:
  - app/components/ui/PrBanner.tsx
  - app/components/ui/PrTrophy.tsx
  - app/app/(app)/workout/[sessionId].tsx
  - app/locales/sv.json
  - app/locales/en.json
autonomous: false
requirements: [PR-01, PR-02, PR-03]
user_setup: []
must_haves:
  decisions: [D-01, D-02, D-03, D-04, D-05, D-07, D-08, D-09, D-10, D-11, D-12, D-13, D-17, D-18, D-19, D-20, D-21]
  truths:
    - "A logged working set whose e1RM beats the prior best (all-time + earlier-this-session) is detected as a PR, computed client-side via lib/e1rm.ts over cached data"
    - "The very first working set on an exercise is NOT a PR (D-02 baseline)"
    - "A PR set shows a gradient trophy replacing the green check on its set row (D-13)"
    - "A PR mounts a floating gradient banner that NEVER shifts the set list / input row / Klart button (D-09)"
    - "Detection + banner + haptic are fire-and-forget AFTER addSet.mutate, never awaited (D-17); npm run test:f13-brutal stays green"
    - "The PR haptic (notificationSuccess) respects the fm:haptics gate (D-18); reduce-motion snaps the banner to final (D-19)"
  artifacts:
    - path: "app/components/ui/PrBanner.tsx"
      provides: "Floating Skia gradient-sweep celebration banner (D-09/D-10/D-11/D-19)"
      min_lines: 40
      contains: "useReducedMotion"
    - path: "app/components/ui/PrTrophy.tsx"
      provides: "Gradient trophy circle (24px set-row / reused sizes) replacing the check (D-13)"
      contains: "trophy"
    - path: "app/app/(app)/workout/[sessionId].tsx"
      provides: "Fire-and-forth PR detection + banner mount + set-row trophy swap"
      contains: "epley1RM"
    - path: "app/locales/sv.json"
      provides: "pbSetSuffix key at parity"
      contains: "pbSetSuffix"
  key_links:
    - from: "app/app/(app)/workout/[sessionId].tsx"
      to: "lib/e1rm.ts + useBestE1rmQuery"
      via: "epley1RM(candidate) > max(allTimeBest, earlier-in-session) AFTER addSet.mutate"
      pattern: "epley1RM"
    - from: "app/app/(app)/workout/[sessionId].tsx"
      to: "PrBanner / PrTrophy"
      via: "floating overlay mount + set-row glyph swap"
      pattern: "PrBanner|PrTrophy"
---

<objective>
Wire the live, offline-safe PR detection into the hot-path workout screen and surface it as a set-row trophy (PR-02) + a floating celebration banner (PR-03), all without touching the ≤3s log budget (D-17).

Detection (PR-01): after `addSet.mutate`, fire-and-forget, compute `epley1RM(candidate)` via `lib/e1rm.ts` and compare against `max(allTimeBest[exerciseId] from useBestE1rmQuery, max e1RM of earlier working sets THIS session from useSetsForSessionQuery)` (D-06 + D-07). A strict increase `> 0` and `weight_kg > 0` and not-the-first-set is a PR (D-01/D-04/D-05/D-02).

Surfacing: the PR set's row swaps its green check for a gradient trophy (D-12/D-13); a fresh floating banner mounts per PR set (D-11) with `personalBest` + `pbSub` + `pbSetSuffix`, scale 0.96→1 + gradient sweep + `notificationSuccess` haptic, ~3-4s dwell, auto-dismiss (D-10).

Purpose: the celebration is delight layered ON TOP of the sacred log path — floating, fire-and-forget, never shifting "Klart" (Pitfall 4).
Output: PrBanner + PrTrophy components, workout-screen wiring, the one new locale key.
</objective>

<execution_context>
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-pr-celebration-f18/13-CONTEXT.md
@.planning/phases/13-pr-celebration-f18/13-PATTERNS.md
@.planning/phases/13-pr-celebration-f18/13-RESEARCH.md
@.planning/phases/13-pr-celebration-f18/13-UI-SPEC.md

<interfaces>
<!-- From 13-01 / 13-03 (Wave 1 / earlier-in-wave). Consume directly — no exploration. -->

From app/lib/e1rm.ts (13-01):
```typescript
export function epley1RM(weightKg: number, reps: number): number; // 0 for weight≤0 / reps≤0 / non-finite
```

From app/lib/queries/best-e1rm.ts (13-03):
```typescript
export function useBestE1rmQuery(): UseQueryResult<Record<string, { weight_kg: number; reps: number }>>;
// keyed by exerciseId; offline-first persister-hydrated; {} when not yet synced
```

From app/lib/queries/sets.ts (existing — in-session working sets for D-07):
useSetsForSessionQuery(sessionId) → the working sets already logged this session (compare source for D-07).

From app/components/ui/Sparkline.tsx:64-92, 121-128 (THE Skia+Reanimated precedent):
```typescript
const SPRING = { damping: 18, stiffness: 220 } as const;     // §07 (Sparkline.tsx:65)
const reduced = useReducedMotion();                          // synchronous boolean (:80)
const v = useSharedValue(0);
useEffect(() => { v.value = reduced ? 1 : withSpring(1, SPRING); }, [reduced, v]); // D-19 snap
const clipRect = useDerivedValue(() => Skia.XYWHRect(0, 0, w * v.value, h));        // animated geometry
```

From app/app/(app)/workout/[sessionId].tsx:521-563 (the onKlart fire-and-forget site — PR hooks in HERE):
```typescript
addSet.mutate({ ... }, { onSuccess: () => reset({ ... }) });
void getPref("fm:haptics").then((on) => { if (on) void Haptics.impactAsync(Medium); });
// → PR detection + banner mount + notificationAsync(Success) go right after this, same gate
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: PrTrophy + PrBanner components (Skia gradient sweep, reduce-motion aware)</name>
  <files>app/components/ui/PrTrophy.tsx, app/components/ui/PrBanner.tsx, app/locales/sv.json, app/locales/en.json</files>
  <read_first>
    - app/components/ui/Sparkline.tsx (full — THE canonical Skia `<Canvas>` + `<LinearGradient>` + Reanimated precedent: SPRING const :65, useReducedMotion :80, useDerivedValue animated geometry :121-128, Skia/Reanimated imports :34-50)
    - app/components/ui/Icon.tsx (the `trophy` / `checkCircle` glyph names + stroke API)
    - app/lib/units-store.ts + app/lib/units.ts (useUnitStore selector + toDisplayWeight/formatWeight — D-20)
    - app/locales/sv.json + en.json (existing `personalBest`:106, `pbSub`:107; the flat-key convention; `"set":"Set"` / `"sets":"set"` at :91-92)
    - .planning/phases/13-pr-celebration-f18/13-UI-SPEC.md (Motion Contract; Surface-by-Surface rows 1-2; trophy size ladder 36/24/18; Copywriting `pbSetSuffix` contract)
    - .planning/phases/13-pr-celebration-f18/13-PATTERNS.md (§ PR banner component lines 212-245 — the NativeWind box-decoration rule)
    - .planning/phases/13-pr-celebration-f18/13-RESEARCH.md (Pattern 4 lines 225-269; Pitfall 6 box-decoration; A7 scale-spring fallback)
  </read_first>
  <action>
    (a) Add the ONE new locale key `"pbSetSuffix": "· set {{n}}"` to BOTH `app/locales/sv.json` and `app/locales/en.json` at parity (identical string in both — sv uses "set" not "sätt" per the in-app convention, overriding the mock). `{{n}}` is the 1-based session set ordinal. Do NOT add any other keys (read side reuses existing keys).

    (b) Create `app/components/ui/PrTrophy.tsx` — a reusable gradient trophy circle accepting a `size` prop (24px set-row default; 18px session-detail; consumers pass size). Full `gradFrom→gradTo` gradient circle (via Skia surface or a NativeWind-classed View + gradient) with a centered white `trophy` glyph (`Icon` name `trophy`, `strokeWidth 2.2`). CRITICAL (Pitfall 6 / MEMORY): bg/border/radius/size go in `className`, NEVER an inline `style()` callback (renders NAKED under NativeWind 4); keep `style()` for shadow/opacity/transform only.

    (c) Create `app/components/ui/PrBanner.tsx` — the floating celebration banner mirroring `Sparkline.tsx`. Props: the set's `weightKg`, `reps`, `setNumber`, an `onDismiss` callback. Layout (UI-SPEC row 1): 16px-radius low-alpha gradient-wash surface (`gradFrom15 → gradTo15`) + `1px accent30` border + 36px gradient trophy tile (10px radius, 18px white trophy) + `personalBest` title (14/700) + sub = `pbSub({kg, reps})` concatenated with `pbSetSuffix({n: setNumber})` (12/600, tabular-nums). Numerals through `useUnitStore` + `formatWeight` (D-20 — e1RM is kg-stored, display-converted; the banner shows the SET's actual weight/reps, also unit-converted). Motion (§07): `scale = useSharedValue(0.96) → withSpring(1, SPRING)`; `sweep = useSharedValue(0) → withTiming(1, {duration: 600})` animating a `<LinearGradient>` start/end via `useDerivedValue` (Sparkline mechanism). `useReducedMotion()` → snap both to final (D-19), banner still renders. Auto-dismiss: a `setTimeout(onDismiss, ~3500)` on mount, cleared on unmount; fade out on dismiss. Box-decoration in `className` (Pitfall 6). Flag the 0.96→1 scale spring for the §856-864 `withTiming` fallback if it "pops" on device (A7) — leave a comment breadcrumb.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `pbSetSuffix` present in BOTH sv.json and en.json with identical value `"· set {{n}}"` (grep both)
    - `PrBanner.tsx` uses `useReducedMotion` and `useDerivedValue` (grep — Skia/Reanimated sweep, not a JS setInterval)
    - `PrTrophy.tsx` + `PrBanner.tsx` put bg/border/radius in `className` (no bg/border/radius literal inside a `style={...}` object — Pitfall 6)
    - banner numerals route through `useUnitStore` / `formatWeight` (grep — no raw `kg` math)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>PrTrophy + PrBanner exist (Skia sweep, reduce-motion aware, className box-decoration); pbSetSuffix at parity; tsc clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire fire-and-forget detection + banner + set-row trophy into the workout screen</name>
  <files>app/app/(app)/workout/[sessionId].tsx</files>
  <read_first>
    - app/app/(app)/workout/[sessionId].tsx (the onKlart fire-and-forget site :521-563; the set-row check glyph column :964-970; the check-scale entrance :856-864; the in-tree overlay shape :1291-1305)
    - app/lib/e1rm.ts (13-01 — epley1RM signature)
    - app/lib/queries/best-e1rm.ts (13-03 — useBestE1rmQuery → Record<exerciseId,...>)
    - app/lib/queries/sets.ts (useSetsForSessionQuery — in-session working sets for D-07)
    - app/components/ui/PrBanner.tsx + PrTrophy.tsx (Task 1)
    - .planning/phases/13-pr-celebration-f18/13-PATTERNS.md (§ workout/[sessionId].tsx lines 249-289 — the three self-precedent patterns)
    - .planning/phases/13-pr-celebration-f18/13-RESEARCH.md (Pitfall 4 banner geometry; Pitfall 5 detection-blocking)
    - .planning/phases/13-pr-celebration-f18/13-CONTEXT.md (D-07, D-09, D-11, D-12, D-13, D-17, D-18)
  </read_first>
  <action>
    Mount `useBestE1rmQuery()` at the top of the screen (cached/persisted; no fresh fetch in the hot path). In `onKlart`, AFTER `addSet.mutate(...)` (right beside the existing `fm:haptics` `Haptics.impactAsync` at :560-562) — fire-and-forget, NEVER awaited, NEVER preceding the mutate (Pitfall 5 / T-11-06): compute `cand = epley1RM(input.weight_kg, input.reps)`; read `best = bestE1rm[planExercise.exercise_id]` (its e1RM via `epley1RM(best.weight_kg, best.reps)`, or `0`/undefined if none → first-set baseline, D-02); compute `sessionMax = max(epley1RM over earlier working sets THIS session from useSetsForSessionQuery)` (D-07). `isPR = input.weight_kg > 0 && cand > 0 && hasPriorReference && cand > max(bestE1rm, sessionMax)` — strict `>` (D-05), first-set with no prior reference is NOT a PR (D-02). On PR: (1) record THIS set's id/set_number into a `prSetIds` Set in state so its row renders the trophy (D-12 — historically honest, a later higher set does NOT remove an earlier trophy); (2) push a fresh banner descriptor (weight/reps/setNumber) into a banner state so a NEW PrBanner mounts per PR set (D-11); (3) fire `void getPref("fm:haptics").then((on) => on && Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success))` through the SAME gate as the set-logged haptic (D-18).

    Render the banner(s) as a `position: absolute` floating overlay OVER the set list — NO Modal portal (D-22), NO backdrop, never in the flex flow (Pitfall 4 / D-09): the set list, input row, and "Klart" button MUST NOT move. Each PrBanner auto-dismisses (~3-4s) and removes itself from the banner state.

    In the set-row glyph column (:964-970), when the row's set id is in `prSetIds`, render `<PrTrophy size={24} />` INSTEAD of the green `checkCircle` (D-13 — replaces, never stacks; the 36px column holds exactly one glyph). A normal row keeps the check.

    DO NOT touch the mutation, queue, persister, or `exercise_sets` logging path (D-17). `npm run test:f13-brutal` must stay green.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit && npm run test:f13-brutal</automated>
  </verify>
  <acceptance_criteria>
    - detection runs AFTER `addSet.mutate` in onKlart (the `epley1RM`/isPR block sits below the mutate call, beside the existing haptic — not before it)
    - PR haptic uses `Haptics.notificationAsync` through `getPref("fm:haptics")` (grep — same gate as set-logged)
    - banner rendered with `position: absolute` (or NativeWind `absolute`), no `Modal` import added (grep — D-09/D-22)
    - set-row renders `PrTrophy` only for ids in the PR set (D-13 replaces check)
    - `npx tsc --noEmit` exits 0; `npm run test:f13-brutal` stays green (no regression beyond known-amber FIT-107)
    - no edit to `addSet` mutationFn / onMutate / queue / persister (D-17)
  </acceptance_criteria>
  <done>Live offline-safe detection + per-PR floating banner + set-row trophy swap wired; f13-brutal green; hot path untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Device UAT — banner geometry, trophy swap, haptic gate, reduce-motion, offline</name>
  <what-built>Live PR detection (offline-safe via cached best-reference) + a floating gradient-sweep celebration banner + a set-row gradient trophy replacing the green check, all fire-and-forth after the set save.</what-built>
  <how-to-verify>
    On a real iPhone via Expo Go (the manual-only set per 13-VALIDATION.md):
    1. PR-02/D-13 — Log a working set that beats the prior best e1RM for an exercise → its set row shows the gradient trophy (NOT the green check); a normal set row still shows the check.
    2. PR-03/D-09 (CRITICAL geometry) — When the banner appears, the set list, the input row, and the "Klart" button must NOT move/jump. The banner floats over them.
    3. PR-03/D-10 — Banner scales 0.96→1 with a gradient sweep, dwells ~3-4s, fades out with no tap.
    4. D-11 — Log a SECOND, higher PR set in the same session → a fresh banner appears with the new set's numbers (D-07 in-session comparison); the first set's trophy remains (D-12).
    5. D-02 — The very first working set on a brand-new exercise shows NO banner and NO trophy (silent baseline).
    6. D-18 — Settings → toggle haptics OFF → next PR fires NO haptic; ON → `notificationSuccess` fires.
    7. D-19 — OS reduce-motion ON → banner + trophy render with NO animation (snap), still readable; haptic still fires if enabled.
    8. PR-01 offline — Airplane mode → log a PR → trophy + banner still fire from the last-synced best-reference cache.
    If the 0.96→1 scale spring "pops" (A7), report it — the executor falls back to the §856-864 `withTiming` curve.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (one fix per loop — read the UI-SPEC before changing motion/geometry).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cached best-reference → live detection | The persisted best-e1rm Record drives a UI-only celebration; a stale/absent value must degrade to "no PR", never an error or a blocked save. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-09 | Denial of service | Detection blocking/preceding the set save | mitigate | Detection + banner + haptic are fire-and-forget AFTER `addSet.mutate`, never awaited (D-17 / Pitfall 5); best-reference is cached, no hot-path fetch. `npm run test:f13-brutal` gate. |
| T-13-10 | Tampering | PR display e1RM drift | mitigate | All e1RM via `lib/e1rm.ts` (D-08); no inline `w*(1+r/30)` in the screen. |
| T-13-11 | Information disclosure | reduce-motion / haptic respecting OS + pref | mitigate | `useReducedMotion()` snap (D-19); `fm:haptics` gate (D-18) — no behavior leaks past a user's accessibility/haptic choice. |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH: zero new deps; banner rides installed Reanimated 4 + Skia). |

This plan has a `checkpoint:human-verify` → `autonomous: false`. The checkpoint is never auto-advanced.
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- `npm run test:f13-brutal` green (D-17 hot-path unregressed)
- Device UAT (Task 3) all-pass: banner geometry, trophy swap, haptic gate, reduce-motion, offline detection
</verification>

<success_criteria>
- PR-01 offline detection, PR-02 set-row trophy, PR-03 floating celebration banner — all live
- The ≤3s log budget is provably intact (f13-brutal green; banner never shifts "Klart")
</success_criteria>

<output>
Create `.planning/phases/13-pr-celebration-f18/13-04-SUMMARY.md` when done.
</output>
