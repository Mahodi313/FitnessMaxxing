---
phase: 13-pr-celebration-f18
plan: 03
type: execute
wave: 2
depends_on: ["13-02"]
files_modified:
  - app/lib/query/keys.ts
  - app/lib/queries/best-e1rm.ts
  - app/lib/queries/pr-history.ts
  - app/lib/queries/exercise-sets-in-range.ts
  - app/lib/queries/session-pr-flags.ts
  - app/lib/query/client.ts
autonomous: true
requirements: [PR-01, PR-04, PR-05]
must_haves:
  decisions: [D-06, D-08, D-14, D-17]
  truths:
    - "useBestE1rmQuery returns Record<exerciseId, {weight_kg, reps}> hydrated through the persister (offline-first)"
    - "best-e1rm query uses Record (not Map) so it survives the AsyncStorage JSON round-trip"
    - "usePrHistoryQuery + useExerciseSetsInRangeQuery + useSessionPrFlags Zod-parse every RPC row (never as-cast)"
    - "useSessionPrFlags(sessionIds) makes ONE rpc call over a session-id array and returns Record<session_id, has_pr> (hooks-legal history-list aggregator, D-14)"
    - "Finishing a session invalidates the best-e1rm cache via ONE additive line in the existing ['session','finish'].onSettled"
    - "No setMutationDefaults body / mutationKey / onMutate / persister scope-binding is touched (D-17)"
  artifacts:
    - path: "app/lib/queries/best-e1rm.ts"
      provides: "Offline-first all-time-best reference for live PR detection (D-06)"
      contains: "Record<"
    - path: "app/lib/queries/pr-history.ts"
      provides: "Chronological was_pr per-set hook for read-side trophies (D-14/D-15)"
      contains: "get_exercise_pr_history"
    - path: "app/lib/queries/exercise-sets-in-range.ts"
      provides: "Raw range working sets for chart e1RM hero + delta (D-16)"
      contains: "get_exercise_sets_in_range"
    - path: "app/lib/queries/session-pr-flags.ts"
      provides: "Session-level has_pr aggregator hook for the history LIST (one call over the visible session-id list, D-14)"
      contains: "get_session_pr_flags"
    - path: "app/lib/query/keys.ts"
      provides: "bestE1rmKeys / prHistoryKeys / exerciseSetsInRangeKeys / sessionPrFlagsKeys factories"
      contains: "sessionPrFlagsKeys"
  key_links:
    - from: "app/lib/queries/best-e1rm.ts"
      to: "get_best_working_sets RPC"
      via: "supabase.rpc with Zod-parsed rows into a Record"
      pattern: "get_best_working_sets"
    - from: "app/lib/queries/session-pr-flags.ts"
      to: "get_session_pr_flags RPC"
      via: "supabase.rpc({ p_session_ids }) with Zod-parsed rows folded into Record<session_id, has_pr>"
      pattern: "get_session_pr_flags"
    - from: "app/lib/query/client.ts"
      to: "bestE1rmKeys.all"
      via: "invalidateQueries in ['session','finish'].onSettled"
      pattern: "bestE1rmKeys"
---

<objective>
Build the read-side query layer that feeds both live in-workout detection and the read-side surfaces, and wire the ONE allowed `client.ts` edit (D-17).

Four hooks over the four RPCs from 13-02:
- `useBestE1rmQuery()` — offline-first, persister-hydrated `Record<exerciseId, {weight_kg, reps}>` (mirror `last-value.ts`); the live-detection baseline (D-06/PR-01).
- `usePrHistoryQuery(exerciseId)` — `was_pr`-per-set rows for the read-side session-detail trophies (D-14/D-15/PR-04).
- `useExerciseSetsInRangeQuery(exerciseId, since)` — raw range sets for the chart e1RM hero + delta (D-16/PR-05).
- `useSessionPrFlags(sessionIds)` — ONE call over the visible session-id array → `Record<session_id, has_pr>` for the history LIST trophies (D-14). This is the hooks-legal aggregator that replaces the unworkable "fold usePrHistoryQuery across every session's exercises" loop; it consumes `get_session_pr_flags` shipped in 13-02.

Plus the single additive invalidation line so a finished session refreshes the best-reference.

Purpose: All downstream consumers (13-04 detection, 13-05 read-side) import from here — interface-first. Every RPC row is Zod-parsed at the boundary (D-08 returns raw sets; JS owns the formula).
Output: key factories, four query hooks, one additive `onSettled` line.
</objective>

<execution_context>
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-pr-celebration-f18/13-CONTEXT.md
@.planning/phases/13-pr-celebration-f18/13-PATTERNS.md
@.planning/phases/13-pr-celebration-f18/13-RESEARCH.md

<interfaces>
<!-- The offline-first Record pattern + the RPC-Zod pattern + the keys factory shape. -->

From app/lib/queries/last-value.ts:18-22, 60-63 (Record-not-Map contract — load-bearing for the persister):
```typescript
// Record, NOT Map: TanStack persists via JSON.stringify; a Map rehydrates as {} and .get throws.
return useQuery<Record<number, LastValueEntry>>({
  queryKey: lastValueKeys.byExercise(exerciseId),
  queryFn: async () => { if (!userId) return {}; /* ... build Record ... */ },
  enabled: !!exerciseId && !!userId,
  staleTime: 1000 * 60 * 15,
});
```

From app/lib/queries/exercise-chart.ts (RPC + Zod-parse boundary; nullable-since cast):
```typescript
const { data, error } = await supabase.rpc("get_exercise_summary", { p_exercise_id, p_metric, p_since: since as unknown as string });
if (error) throw error;
return (data ?? []).map((row: unknown) => RowSchema.parse(row));   // parse every row, never `as`-cast
```

From app/lib/query/keys.ts (existing factories to mirror — lastValueKeys:65, exerciseSummaryKeys:141):
```typescript
export const lastValueKeys = { all: [...], byExercise: (id) => [...] } as const;
```

From app/lib/query/client.ts:830-842 (the ['session','finish'].onSettled — the ONE allowed edit site, next to lastValueKeys.all):
```typescript
onSettled: (_d, _e, vars) => {
  void queryClient.invalidateQueries({ queryKey: sessionsKeys.active() });
  void queryClient.invalidateQueries({ queryKey: lastValueKeys.all });
  // → ADD: void queryClient.invalidateQueries({ queryKey: bestE1rmKeys.all });
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add key factories + best-e1rm.ts (offline-first) + the client.ts invalidation line</name>
  <files>app/lib/query/keys.ts, app/lib/queries/best-e1rm.ts, app/lib/query/client.ts</files>
  <read_first>
    - app/lib/queries/last-value.ts (full — the offline-first persister-hydrated Record-not-Map RLS hook; the `userId` belt-and-braces filter; `enabled`/`staleTime`)
    - app/lib/query/keys.ts (lines 65-141 — lastValueKeys / exerciseSummaryKeys factory shape)
    - app/lib/query/client.ts (lines 820-845 — the ['session','finish'].onSettled block; the lastValueKeys.all invalidate at :836)
    - app/lib/queries/exercise-chart.ts (the supabase.rpc + Zod-parse boundary)
    - .planning/phases/13-pr-celebration-f18/13-PATTERNS.md (§ best-e1rm.ts lines 145-177; § client.ts lines 325-336)
    - .planning/phases/13-pr-celebration-f18/13-RESEARCH.md (Pattern 3 lines 206-223; the D-17 HARD LINE)
  </read_first>
  <action>
    (a) In `app/lib/query/keys.ts`, add four factories mirroring `lastValueKeys` / `exerciseSummaryKeys`: `bestE1rmKeys` (with `all` + a base key — no per-exercise arg since the RPC returns all exercises), `prHistoryKeys` (`all` + `byExercise(id)`), `exerciseSetsInRangeKeys` (`all` + `byExerciseSince(id, since)`), `sessionPrFlagsKeys` (`all` + `byIds(sessionIds: string[])` — key off a stable join of the sorted id list so the same visible set hits one cache entry). Use the existing `as const` tuple convention.

    (b) Create `app/lib/queries/best-e1rm.ts` mirroring `last-value.ts` EXACTLY: `export function useBestE1rmQuery()` returning `useQuery<Record<string, { weight_kg: number; reps: number }>>` keyed `bestE1rmKeys.all` (or its base). queryFn: read `userId` from `useAuthStore`, `if (!userId) return {}`, call `supabase.rpc("get_best_working_sets", {})`, `if (error) throw error`, Zod-parse each row (`z.object({ exercise_id: z.string().uuid(), weight_kg: z.coerce.number(), reps: z.coerce.number().int() })`), fold into a `Record<exerciseId, {weight_kg, reps}>` (NOT a Map — survives JSON persist, last-value.ts:18-22). `enabled: !!userId`, `staleTime: 1000 * 60 * 15`. Header doc-comment cites D-06 (offline-first all-time-best) + the Record-not-Map rationale + D-08 (returns raw sets, JS computes e1RM).

    (c) In `app/lib/query/client.ts`, add EXACTLY ONE line inside the existing `['session','finish'].onSettled` block, next to the `lastValueKeys.all` invalidate (:836): `void queryClient.invalidateQueries({ queryKey: bestE1rmKeys.all });` with a comment citing D-06 (refresh best-reference after a finished session). Import `bestE1rmKeys` at the top with the other key imports. D-17 HARD LINE: touch NOTHING else in this file — no `setMutationDefaults` body, no `mutationKey`, no `onMutate`/`onError`, no persister scope-binding. This is one additive read-side `invalidateQueries` in an EXISTING onSettled.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit && npm run test:f13-brutal</automated>
  </verify>
  <acceptance_criteria>
    - `best-e1rm.ts` returns `Record<` (not `Map`) and calls `get_best_working_sets` (grep both)
    - `keys.ts` exports `bestE1rmKeys`, `prHistoryKeys`, `exerciseSetsInRangeKeys`, `sessionPrFlagsKeys` (grep)
    - `client.ts` non-comment `bestE1rmKeys` reference count is exactly 1 invalidate (the invalidate appears once in onSettled, plus the import)
    - `client.ts` has no new `setMutationDefaults` / `mutationKey` / `onMutate` edits (diff scoped to the one invalidate line + the import)
    - `npx tsc --noEmit` exits 0; `npm run test:f13-brutal` stays green (no regression beyond known-amber FIT-107 count precondition)
  </acceptance_criteria>
  <done>best-e1rm offline-first hook + key factories + the single onSettled invalidation line shipped; f13-brutal green.</done>
</task>

<task type="auto">
  <name>Task 2: pr-history.ts + exercise-sets-in-range.ts + session-pr-flags.ts query hooks</name>
  <files>app/lib/queries/pr-history.ts, app/lib/queries/exercise-sets-in-range.ts, app/lib/queries/session-pr-flags.ts</files>
  <read_first>
    - app/lib/queries/exercise-chart.ts (full — the useExerciseSummaryQuery shape: supabase.rpc + Zod schema + per-tuple queryKey + the nullable-since cast `since as unknown as string`)
    - app/lib/query/keys.ts (the prHistoryKeys / exerciseSetsInRangeKeys / sessionPrFlagsKeys factories added in Task 1)
    - .planning/phases/13-pr-celebration-f18/13-PATTERNS.md (§ pr-history.ts lines 181-208 — the schema + hook)
    - .planning/phases/13-pr-celebration-f18/13-RESEARCH.md (Pattern 5 lines 271-278 — chart hero is pure client math over raw RPC sets; A3 / Open Question 1 — the session-level aggregator)
  </read_first>
  <action>
    (a) Create `app/lib/queries/pr-history.ts`: `const PrHistoryRowSchema = z.object({ set_id: z.string().uuid(), session_id: z.string().uuid(), completed_at: z.string(), weight_kg: z.coerce.number(), reps: z.coerce.number().int(), was_pr: z.boolean() })`. `export function usePrHistoryQuery(exerciseId: string)` → `useQuery({ queryKey: prHistoryKeys.byExercise(exerciseId), queryFn: async () => { const { data, error } = await supabase.rpc("get_exercise_pr_history", { p_exercise_id: exerciseId }); if (error) throw error; return (data ?? []).map((row: unknown) => PrHistoryRowSchema.parse(row)); }, enabled: !!exerciseId })`. Export the row type via `z.infer`.

    (b) Create `app/lib/queries/exercise-sets-in-range.ts`: `const SetInRangeRowSchema = z.object({ completed_at: z.string(), weight_kg: z.coerce.number(), reps: z.coerce.number().int() })`. `export function useExerciseSetsInRangeQuery(exerciseId: string, since: Date | null)` → `useQuery({ queryKey: exerciseSetsInRangeKeys.byExerciseSince(exerciseId, since?.toISOString() ?? null), queryFn: async () => { const { data, error } = await supabase.rpc("get_exercise_sets_in_range", { p_exercise_id: exerciseId, p_since: since as unknown as string }); if (error) throw error; return (data ?? []).map((row: unknown) => SetInRangeRowSchema.parse(row)); }, enabled: !!exerciseId })`. The nullable-since cast `since as unknown as string` matches exercise-chart.ts (PostgREST treats nullable timestamptz as required string; SQL guards NULL).

    (c) Create `app/lib/queries/session-pr-flags.ts` — the hooks-legal history-LIST aggregator (D-14): `const SessionPrFlagRowSchema = z.object({ session_id: z.string().uuid(), has_pr: z.boolean() })`. `export function useSessionPrFlags(sessionIds: string[])` → `useQuery<Record<string, boolean>>({ queryKey: sessionPrFlagsKeys.byIds(sessionIds), queryFn: async () => { const { data, error } = await supabase.rpc("get_session_pr_flags", { p_session_ids: sessionIds }); if (error) throw error; const rows = (data ?? []).map((row: unknown) => SessionPrFlagRowSchema.parse(row)); return Object.fromEntries(rows.map((r) => [r.session_id, r.has_pr])); }, enabled: sessionIds.length > 0, staleTime: 1000 * 60 * 5 })`. Returns a `Record<session_id, has_pr>` (NOT a Map — JSON-persist safe, last-value.ts:18-22 rationale; absent key = no PR). This is the ONE call the history list makes over its visible session-id list — it replaces the rules-of-hooks-violating "usePrHistoryQuery in a loop" approach. Header doc-comment cites D-14 (PR-at-log-time, chronological, never migrating) + D-08 (boolean flag only, no e1RM) + the hooks-legal single-call rationale.

    All three hooks: NEVER `as`-cast the rows — parse every row (PITFALLS §8.13, generated types are compile-time only).
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `pr-history.ts` calls `get_exercise_pr_history` and parses rows with `PrHistoryRowSchema.parse` (grep — no `as ` cast of `.rpc` data)
    - `exercise-sets-in-range.ts` calls `get_exercise_sets_in_range` and parses rows (grep)
    - `session-pr-flags.ts` calls `get_session_pr_flags` with `p_session_ids`, parses rows, and returns a `Record<` of session_id→has_pr (grep `get_session_pr_flags` + `Record<` + `Object.fromEntries`)
    - all hooks use `z.coerce.number()` for numerics and `.int()` for reps; session-pr-flags uses `z.boolean()` for has_pr
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Three read-side RPC hooks ship Zod-parsed (incl. the hooks-legal useSessionPrFlags aggregator); tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase RPC response → client | RPC rows (untyped at runtime — generated types are compile-time only) cross into the app and feed PR display + detection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06 | Tampering | Untyped RPC row trusted blindly | mitigate | Zod parse at every `supabase.rpc()` boundary (`PrHistoryRowSchema.parse` / `SetInRangeRowSchema.parse` / `SessionPrFlagRowSchema.parse` / best-e1rm row schema), never `as`-cast (PITFALLS §8.13). |
| T-13-07 | Information disclosure | best-e1rm cache hydration | mitigate | `userId` belt-and-braces filter + RLS-scoped RPC (inherits 0001 policies); non-PII workout data through the existing encrypted persister (no new sensitive storage). |
| T-13-08 | Denial of service | hot-path regression via client.ts edit | mitigate | ONLY an additive `invalidateQueries` in an existing onSettled (D-17); no mutation-default change; `npm run test:f13-brutal` gate in Task 1. |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH: zero new deps). |
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- `npm run test:f13-brutal` green (D-17 — the client.ts edit is additive read-side only)
- best-e1rm returns a `Record` (persister-safe); all four hooks Zod-parse rows; useSessionPrFlags makes a single aggregator call over the session-id list
</verification>

<success_criteria>
- Interface-first: 13-04 + 13-05 consume these hooks with zero codebase exploration; history list derives has_pr from ONE useSessionPrFlags call (no per-exercise loop)
- D-17 preserved: only one additive invalidation line in client.ts
</success_criteria>

<output>
Create `.planning/phases/13-pr-celebration-f18/13-03-SUMMARY.md` when done.
</output>
