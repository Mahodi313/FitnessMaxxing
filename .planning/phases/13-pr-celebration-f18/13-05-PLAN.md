---
phase: 13-pr-celebration-f18
plan: 05
type: execute
wave: 3
depends_on: ["13-01", "13-03"]
files_modified:
  - app/app/(app)/(tabs)/history.tsx
  - app/app/(app)/history/[sessionId].tsx
  - app/app/(app)/exercise/[exerciseId]/chart.tsx
autonomous: false
requirements: [PR-04, PR-05]
user_setup: []
must_haves:
  decisions: [D-08, D-13, D-14, D-15, D-16, D-20, D-21]
  truths:
    - "A history session row shows a gradient trophy if any set in it was a PR-at-log-time (D-14, chronological, never migrating)"
    - "Session detail shows per-exercise e1RM and a trophy on exercises that hit a PR-at-the-time in that session (D-15)"
    - "The exercise chart hero swaps from current-best to estimated 1RM (max e1RM in range) + range delta (best-in-range − earliest-in-range) (D-16/PR-05)"
    - "Every read-side e1RM numeral is computed via lib/e1rm.ts and display-converted via useUnitStore (D-08/D-20)"
  artifacts:
    - path: "app/app/(app)/(tabs)/history.tsx"
      provides: "Session-row PR trophy from chronological derivation (D-14)"
      contains: "PrTrophy"
    - path: "app/app/(app)/history/[sessionId].tsx"
      provides: "Per-exercise e1RM stat + per-exercise PR trophy (D-15)"
      contains: "epley1RM"
    - path: "app/app/(app)/exercise/[exerciseId]/chart.tsx"
      provides: "e1RM hero numeral + range delta (D-16)"
      contains: "epley1RM"
  key_links:
    - from: "app/app/(app)/(tabs)/history.tsx"
      to: "usePrHistoryQuery-derived has_pr"
      via: "trophy on rows where any set was_pr"
      pattern: "PrTrophy|was_pr"
    - from: "app/app/(app)/exercise/[exerciseId]/chart.tsx"
      to: "useExerciseSetsInRangeQuery + epley1RM"
      via: "hero = max(epley1RM over range sets); delta = best − earliest"
      pattern: "epley1RM"
---

<objective>
Surface PR/e1RM on the three read-side screens (PR-04 + PR-05), reusing the trophy component and the formula from Wave 1/earlier-wave plans. These screens are already Forge-skinned (Phases 11/12) — this plan ADDS the PR markup the prior phases deliberately omitted (Phase 12 D-13), it does NOT re-skin them.

- History list (D-14/PR-04): a gradient trophy on session rows where any set was a PR-at-log-time (chronological `was_pr` from `usePrHistoryQuery`; trophies never migrate).
- Session detail (D-15/PR-04): per-exercise e1RM stat + a trophy on exercises that hit a PR-at-the-time in that session, inside the existing Phase 12 D-15 hybrid card.
- Exercise chart hero (D-16/PR-05): swap the Phase 12 current-best placeholder to `estimated1RM` (max e1RM among working sets in range) + the success range-delta chip (best-in-range − earliest-in-range) — swap the DATA, keep the GEOMETRY.

Purpose: "the trophy tells the truth about when" — PR-at-the-time, derived chronologically, never stored. Every e1RM via `lib/e1rm.ts` (D-08), display-converted via `useUnitStore` (D-20).
Output: three modified read-side screens.
</objective>

<execution_context>
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-pr-celebration-f18/13-CONTEXT.md
@.planning/phases/13-pr-celebration-f18/13-PATTERNS.md
@.planning/phases/13-pr-celebration-f18/13-UI-SPEC.md

<interfaces>
<!-- From 13-01 / 13-03 / 13-04. Consume directly. -->

From app/lib/e1rm.ts (13-01):
```typescript
export function epley1RM(weightKg: number, reps: number): number;
```

From app/lib/queries/pr-history.ts (13-03):
```typescript
export function usePrHistoryQuery(exerciseId: string): UseQueryResult<
  { set_id: string; session_id: string; completed_at: string; weight_kg: number; reps: number; was_pr: boolean }[]
>;
```

From app/lib/queries/exercise-sets-in-range.ts (13-03):
```typescript
export function useExerciseSetsInRangeQuery(exerciseId: string, since: Date | null): UseQueryResult<
  { completed_at: string; weight_kg: number; reps: number }[]
>;
```

From app/components/ui/PrTrophy.tsx (13-04): `<PrTrophy size={24 | 18} />` gradient circle + white trophy.

From app/app/(app)/exercise/[exerciseId]/chart.tsx:342-451 (the hero to swap — 52px numeral, 18/500 unit, 11/600 eyebrow `estimated1RM`, 13/700 success delta chip; useUnitStore already in chartData memo dep at :163-169,227,316):
```typescript
const heroNumeral = summary != null ? String(toDisplayWeight(summary.current_best, units)) : "–"; // ← swap source to max e1RM in range
```

Existing locale keys (reuse, do NOT add): `estimated1RM` (sv/en :138), `personalBest`:106, `maxWeight`. The delta chip reuses the Phase 12 D-12 success-only formatter.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: History session-row trophy (D-14) + session-detail per-exercise e1RM + trophy (D-15)</name>
  <files>app/app/(app)/(tabs)/history.tsx, app/app/(app)/history/[sessionId].tsx</files>
  <read_first>
    - app/app/(app)/(tabs)/history.tsx (the existing Forge session-row markup; the FlatList row; where a status glyph fits in the ≥44px row)
    - app/app/(app)/history/[sessionId].tsx (the Phase 12 D-15 hybrid exercise card — name + right max-weight stat + per-set list; where the e1RM stat + trophy sit)
    - app/lib/queries/pr-history.ts (13-03 — usePrHistoryQuery → was_pr per set)
    - app/lib/e1rm.ts (13-01) + app/lib/units-store.ts + app/lib/units.ts (useUnitStore + formatWeight, D-20)
    - app/components/ui/PrTrophy.tsx (13-04 — size prop)
    - .planning/phases/13-pr-celebration-f18/13-PATTERNS.md (§ history.tsx lines 291-293; § history/[sessionId].tsx lines 297-299)
    - .planning/phases/13-pr-celebration-f18/13-UI-SPEC.md (Surface rows 3-4; trophy size ladder 24/18; mock refs 627-635, 739-746)
    - .planning/phases/13-pr-celebration-f18/13-CONTEXT.md (D-14, D-15, D-20)
  </read_first>
  <action>
    (a) History list (`(tabs)/history.tsx`, D-14): derive per-session `has_pr` from chronological `was_pr`. Per the RPC split, fold `usePrHistoryQuery` results across the session's exercises (or, if a session-level aggregator RPC was shipped in 13-02, consume that instead — whichever 13-02 delivered). A session has a trophy if ANY of its sets was a PR-at-log-time. Render `<PrTrophy size={24} />` (24px gradient circle + 12px white trophy) on those rows as a NON-interactive status glyph inside the existing ≥44px row — no new tap target (mock 627-635). Rows with no PR get no glyph (no placeholder — UI-SPEC empty state). Trophies are historically accurate and never migrate (D-14).

    (b) Session detail (`history/[sessionId].tsx`, D-15): inside the existing Phase 12 D-15 hybrid exercise card, add a per-exercise e1RM stat = `epley1RM` of that exercise's top working set in the session, display-converted via `useUnitStore` + `formatWeight` (D-20 — computed in kg, then converted; tabular-nums). Add an 18px gradient trophy (`<PrTrophy size={18} />`) on exercises that hit a PR-at-the-time in THIS session (a `was_pr` set with `session_id === thisSessionId` from `usePrHistoryQuery` for that exercise). Exact placement inside the card is the executor's call (UI-SPEC leaves it to the planner/executor). If a per-exercise e1RM label key is needed, reuse `maxWeight` pattern or add ONE parity key — prefer reuse (do not invent surplus keys; Phase 15 owns the global audit).
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `history.tsx` renders `PrTrophy` gated on a derived `has_pr` (grep `PrTrophy`)
    - `history/[sessionId].tsx` computes per-exercise e1RM via `epley1RM` and display-converts via `useUnitStore`/`formatWeight` (grep — no inline `w*(1+r/30)`, no raw kg literal)
    - per-exercise trophy gated on `was_pr` for THIS session (D-15)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>History session trophies (D-14) + session-detail per-exercise e1RM + trophy (D-15) wired via the chronological RPC + lib/e1rm.ts.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Chart hero swap → estimated 1RM + range delta (D-16/PR-05)</name>
  <files>app/app/(app)/exercise/[exerciseId]/chart.tsx</files>
  <read_first>
    - app/app/(app)/exercise/[exerciseId]/chart.tsx (the hero numeral computation :342-349; the hero markup geometry :410-451; the useUnitStore read :163-169 + chartData memo dep :227,316; the range state / rangeToSince)
    - app/lib/queries/exercise-sets-in-range.ts (13-03 — useExerciseSetsInRangeQuery(exerciseId, since))
    - app/lib/e1rm.ts (13-01)
    - app/lib/units.ts (toDisplayWeight/formatWeight — D-20)
    - .planning/phases/13-pr-celebration-f18/13-PATTERNS.md (§ chart.tsx lines 303-321 — swap DATA not GEOMETRY)
    - .planning/phases/13-pr-celebration-f18/13-UI-SPEC.md (Surface row 5; Typography hero 52/18/11/13; Color delta chip success-only)
    - .planning/phases/13-pr-celebration-f18/13-RESEARCH.md (Pattern 5 lines 271-278)
  </read_first>
  <action>
    Swap the chart hero DATA from `summary.current_best` to e1RM, keeping the EXACT geometry (52px `font-display-bold` numeral, 18/500 unit, 11/600 `estimated1RM` eyebrow, 13/700 success delta chip with `arrowUp`, the ratified Forge tokens at :410-451 — do NOT touch the markup). Feed the hero from `useExerciseSetsInRangeQuery(exerciseId, rangeToSince(range))`: hero numeral = `max(epley1RM(weight_kg, reps))` over the returned working sets (D-16), in kg then `toDisplayWeight` (D-20). Delta chip = `bestE1rmInRange − earliestE1rmInRange` where "earliest" is the e1RM of the chronologically-first set in range — reuse the Phase 12 D-12 current-best-delta success-only formatter (positive → `+{{kg}} kg` chip; negative or zero → NO chip, never red — Color discipline). Change the eyebrow key `currentBest` → `estimated1RM` (already exists, :138). e1RM computed via `lib/e1rm.ts` ONLY (D-08 — no SQL display e1RM, no inline formula). `useUnitStore` stays in the chartData memo dep so a kg↔lbs toggle re-renders the hero live (D-20). Empty range (`useExerciseSetsInRangeQuery` returns []) → the existing Phase 12 empty-state treatment (hero shows the established empty figure, do not invent a numeral). The chart LINE/scatter (Phase 12 MOTN-03) is UNTOUCHED — only the hero data swaps.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit && npm run test:f13-brutal</automated>
  </verify>
  <acceptance_criteria>
    - hero numeral sourced from `max(epley1RM(...))` over `useExerciseSetsInRangeQuery` rows (grep `epley1RM` + `useExerciseSetsInRangeQuery`)
    - eyebrow key is `estimated1RM` (grep — swapped from `currentBest`)
    - delta chip is success-only (negative range delta renders NO chip — grep for the existing success-only guard, never a red down-chip)
    - hero numeral display-converted via `toDisplayWeight`/`formatWeight`; `useUnitStore` in the chartData memo dep (D-20)
    - the chart line/scatter draw-on-mount (MOTN-03) is unchanged
    - `npx tsc --noEmit` exits 0; `npm run test:f13-brutal` green (read-only, D-24/D-17 intact)
  </acceptance_criteria>
  <done>Chart hero swapped to estimated 1RM + success range delta, geometry preserved, units reactive, line untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Device UAT — read-side trophies, per-exercise e1RM, chart e1RM hero, unit reactivity</name>
  <what-built>PR markup on the three read-side screens: history session trophies (PR-at-log-time), session-detail per-exercise e1RM + trophy, and the chart hero swapped to estimated 1RM + range delta.</what-built>
  <how-to-verify>
    On a real iPhone via Expo Go, light + dark:
    1. PR-04/D-14 — A history session that contained a PR-at-the-time set shows a gradient trophy; a session with no PR shows none. Set a NEW higher record later → the OLD session's trophy stays (trophies don't migrate).
    2. PR-04/D-15 — Open a session detail → exercises that hit a PR-at-the-time in that session show the 18px trophy + a per-exercise e1RM stat; the e1RM reads sensibly (kg or lbs per your unit setting).
    3. PR-05/D-16 — Open an exercise chart → the hero shows "Estimerat 1RM" with a 52px e1RM numeral + a success delta chip (e.g. "+15.5 kg") for a range where you improved. A range where you did NOT improve shows NO delta chip (never a red down-chip). The chart line still draws on mount.
    4. D-20 unit reactivity — Settings → toggle kg↔lbs → the chart hero, the session-detail e1RM, and all PR numerals re-render immediately (no restart), staying tabular-aligned.
    5. Empty state — An exercise with no working sets in the selected range shows the established Phase-12 empty hero, not an invented numeral.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (one fix per loop — read the UI-SPEC before changing geometry/tokens).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RPC rows → read-side display | Chronological `was_pr` + raw range sets feed trophies and the e1RM hero; rows must be the caller's own (RLS) and parsed (Zod, done in 13-03 hooks). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Information disclosure | read-side RPC consumption (history/detail/chart) | mitigate | RPCs are `security invoker` (13-02), RLS-scoped to the caller; cross-user `test:rls` gate (13-02). Screens render only the caller's own rows. |
| T-13-12 | Tampering | display e1RM drift across read-side screens | mitigate | Every read-side e1RM via `lib/e1rm.ts` (D-08); no inline formula, no SQL display e1RM consumed. |
| T-13-13 | Information disclosure | delta chip never scolds (no red down-chip) | accept | Success-only delta (Phase 12 D-12 carry-forward) — a negative delta shows no chip; cosmetic, no data exposure. |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH: zero new deps). |

This plan has a `checkpoint:human-verify` → `autonomous: false`.
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- `npm run test:f13-brutal` green (read-side only; D-17/D-24 intact)
- Device UAT (Task 3) all-pass: read-side trophies, per-exercise e1RM, chart e1RM hero, unit reactivity, empty state
</verification>

<success_criteria>
- PR-04 (history trophy + session-detail e1RM/trophy) and PR-05 (chart estimated-1RM hero + range delta) live
- All read-side e1RM derived chronologically + via the single formula source; units reactive
</success_criteria>

<output>
Create `.planning/phases/13-pr-celebration-f18/13-05-SUMMARY.md` when done.
</output>
