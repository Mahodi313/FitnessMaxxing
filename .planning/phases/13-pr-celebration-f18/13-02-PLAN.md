---
phase: 13-pr-celebration-f18
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/supabase/migrations/0012_phase13_pr_rpcs.sql
  - app/types/database.ts
  - app/scripts/verify-deploy.ts
  - app/scripts/test-rls.ts
autonomous: true
requirements: [PR-01, PR-04, PR-05]
must_haves:
  decisions: [D-02, D-03, D-04, D-05, D-06, D-08, D-14, D-15, D-16]
  truths:
    - "Three read-only RPCs deploy live: get_exercise_pr_history, get_best_working_sets, get_exercise_sets_in_range"
    - "Each new RPC is SECURITY INVOKER with set search_path='' (verified via pg_proc)"
    - "get_exercise_pr_history flags was_pr chronologically (running max over strictly-prior working sets); first set = false (D-02)"
    - "Cross-user: caller A calling an RPC on B's exercise_id returns empty, not an error (RLS-scoped)"
    - "app/types/database.ts regenerated and contains the three new function names"
  artifacts:
    - path: "app/supabase/migrations/0012_phase13_pr_rpcs.sql"
      provides: "Chronological PR-history window RPC + all-time-best reference + raw-sets-in-range, all security invoker"
      contains: "get_exercise_pr_history"
    - path: "app/scripts/verify-deploy.ts"
      provides: "phase13Functions deploy gate (INVOKER + search_path)"
      contains: "phase13Functions"
    - path: "app/scripts/test-rls.ts"
      provides: "Cross-user isolation assertions per new RPC + was_pr correctness on a seeded fixture"
      contains: "get_exercise_pr_history"
    - path: "app/types/database.ts"
      provides: "Regenerated types incl. the three new RPCs"
      contains: "get_exercise_pr_history"
  key_links:
    - from: "app/scripts/verify-deploy.ts"
      to: "pg_proc"
      via: "phase13Functions pg_proc INVOKER + search_path loop"
      pattern: "phase13Functions"
    - from: "app/scripts/test-rls.ts"
      to: "get_exercise_pr_history"
      via: "clientA.rpc with B's exercise_id asserts empty"
      pattern: "get_exercise_pr_history"
---

<objective>
Ship migration `0012_phase13_pr_rpcs.sql` with the three read-only RPCs the read side and the live-detection baseline consume, push it live, regenerate types, and extend the deploy gate + cross-user RLS suite.

Three RPCs (Claude's-discretion RPC-split per CONTEXT, RESEARCH A3/A4/A5 recommendation):
1. `get_exercise_pr_history(p_exercise_id uuid)` — the chronological running-max window engine flagging each working set `was_pr` (PR-at-log-time, D-14/D-15). Its running-max endpoint also IS the all-time best.
2. `get_best_working_sets()` — all-time-best working set per exercise (raw weight_kg/reps), the offline-first live-detection baseline (D-06). Returns RAW sets; JS computes displayed e1RM (D-08).
3. `get_exercise_sets_in_range(p_exercise_id uuid, p_since timestamptz)` — raw working sets in range for the chart hero e1RM + range delta (D-16/PR-05). NOT an extension of get_exercise_summary (that would re-open a 2nd formula home — violates D-08).

Purpose: One migration, the live read-side data path. SQL e1RM (`weight_kg * (1 + reps/30.0)`) is INTERNAL ordering/flagging ONLY — never returned as a display column (D-08). Every displayed numeral comes from `lib/e1rm.ts`.
Output: the migration, regenerated `database.ts`, extended `verify-deploy.ts` + `test-rls.ts`.
</objective>

<execution_context>
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-pr-celebration-f18/13-CONTEXT.md
@.planning/phases/13-pr-celebration-f18/13-PATTERNS.md
@.planning/phases/13-pr-celebration-f18/13-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Exact RPC convention. Copy header verbatim, change the body. -->

From app/supabase/migrations/0011_phase12_dashboard_rpcs.sql:207-243 (get_exercise_summary — convention + inner-join RLS shape):
```sql
create or replace function public.get_exercise_summary(p_exercise_id uuid, p_metric text, p_since timestamptz)
returns table ( ... )
language sql
security invoker          -- caller JWT → RLS on exercise_sets+workout_sessions (0001)
stable
set search_path = ''      -- defense-in-depth, applies to INVOKER too
as $$
  ... from public.exercise_sets es
  inner join public.workout_sessions s
    on s.id = es.session_id and s.finished_at is not null
  where es.exercise_id = p_exercise_id and es.set_type = 'working'
    and (p_since is null or es.completed_at >= p_since) ...
$$;
revoke all on function public.get_exercise_summary(uuid, text, timestamptz) from public;
grant execute on function public.get_exercise_summary(uuid, text, timestamptz) to authenticated;
```

From app/scripts/verify-deploy.ts:128-150 (phase12Functions pg_proc loop — mirror exactly):
```typescript
const phase12Functions = ["get_dashboard_summary", "get_exercise_summary"];
// loop: select proname, prosecdef, proconfig from pg_proc where pronamespace='public'::regnamespace and proname=fname
// PASS iff prosecdef === false (INVOKER) && proconfig joined includes 'search_path='
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 0012_phase13_pr_rpcs.sql (three read-only RPCs)</name>
  <files>app/supabase/migrations/0012_phase13_pr_rpcs.sql</files>
  <read_first>
    - app/supabase/migrations/0011_phase12_dashboard_rpcs.sql (full — the locked RPC convention: header, inner-join RLS shape, revoke/grant footer; get_exercise_summary at :207-243)
    - app/supabase/migrations/0006_phase6_chart_rpcs.sql (the `distinct on` top-set precedent for get_best_working_sets)
    - .planning/phases/13-pr-celebration-f18/13-PATTERNS.md (§ `0012_*.sql` lines 91-141 — window-function body + the three-RPC split)
    - .planning/phases/13-pr-celebration-f18/13-RESEARCH.md (Pattern 2 lines 132-204 — the full window RPC; Pitfalls 1/2/3 lines 303-319)
    - .planning/phases/13-pr-celebration-f18/13-CONTEXT.md (D-02, D-03, D-04, D-05, D-06, D-08, D-14, D-15, D-16)
  </read_first>
  <action>
    Create `app/supabase/migrations/0012_phase13_pr_rpcs.sql` with THREE `create or replace function public.*` definitions, each copying the 0011 header convention verbatim: `language sql` + `security invoker` + `stable` + `set search_path = ''` + fully-qualified `public.exercise_sets` / `public.workout_sessions` + `inner join ... and s.finished_at is not null` + `es.set_type = 'working'` (D-03) + `es.weight_kg > 0` (D-04) + per-function `revoke all on function public.<name>(<args>) from public; grant execute on function public.<name>(<args>) to authenticated;`. NO explicit `auth.uid()` — RLS inherits the 0001 parent-table policies via INVOKER.

    (1) `get_exercise_pr_history(p_exercise_id uuid)` returns table `(set_id uuid, session_id uuid, completed_at timestamptz, weight_kg numeric, reps int, was_pr boolean)`. In a `ranked` CTE compute `es.weight_kg * (1 + es.reps / 30.0) as sql_e1rm` — use `30.0` float division NOT integer `30` (Pitfall 3); this column is INTERNAL ordering/flagging only, NEVER returned. Then select `was_pr` as: `coalesce(sql_e1rm > max(sql_e1rm) over (order by completed_at, set_id rows between unbounded preceding and 1 preceding), false)`. The `rows between unbounded preceding and 1 preceding` frame covers STRICTLY-prior sets (Pitfall 1 — default frame includes current row → no PR ever flags). `order by completed_at, set_id` gives a deterministic tiebreak on identical timestamps (Pitfall 2). First set → empty frame → max NULL → coalesce false (D-02 baseline, not a PR). Strict `>` = D-05 (any increase counts, a tie is not a PR). `order by completed_at, set_id` on the final select too.

    (2) `get_best_working_sets()` returns table `(exercise_id uuid, weight_kg numeric, reps int)` — the single best-e1RM working set per exercise across ALL the caller's finished sessions. Order candidates by the SQL e1RM expression `weight_kg * (1 + reps/30.0)` desc and take the top per `exercise_id` (`distinct on (exercise_id) ... order by exercise_id, sql_e1rm desc` — the 0006 `distinct on` precedent). Return RAW `weight_kg/reps` only — no e1RM column (D-08); JS recomputes via `lib/e1rm.ts`.

    (3) `get_exercise_sets_in_range(p_exercise_id uuid, p_since timestamptz)` returns table `(completed_at timestamptz, weight_kg numeric, reps int)` — raw working sets for one exercise in range, `(p_since is null or es.completed_at >= p_since)` nullable-since "All" idiom. RAW sets only; JS computes the e1RM hero max + earliest-vs-best delta (D-16). Order by `completed_at`.
  </action>
  <verify>
    <automated>cd app && grep -c "security invoker" supabase/migrations/0012_phase13_pr_rpcs.sql</automated>
  </verify>
  <acceptance_criteria>
    - File defines all three functions (grep finds `get_exercise_pr_history`, `get_best_working_sets`, `get_exercise_sets_in_range`)
    - `grep -c "security invoker"` returns 3; `grep -c "set search_path = ''"` returns 3
    - Window frame uses `rows between unbounded preceding and 1 preceding` (grep)
    - SQL uses `30.0` (float div), not bare `/ 30` integer division (grep `reps / 30.0`)
    - No `security definer` anywhere (grep returns 0)
    - Each function has paired `revoke all ... from public` + `grant execute ... to authenticated`
  </acceptance_criteria>
  <done>0012 migration written with three INVOKER read-only RPCs following the 0011 convention.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 2: [BLOCKING] Push migration live, regenerate types, verify deploy</name>
  <files>app/types/database.ts, app/scripts/verify-deploy.ts</files>
  <read_first>
    - app/scripts/verify-deploy.ts (lines 121-160 — the phase12Functions pg_proc loop to mirror)
    - app/package.json (line 12 `gen:types`; line 30 `test:f13-brutal`)
    - CLAUDE.md (Database conventions — migration-as-truth, gen:types co-commit, verify-deploy after push; "Drift verification on Windows-without-Docker" — use verify-deploy.ts not `supabase db diff`)
  </read_first>
  <action>
    BLOCKING ordering: this runs AFTER Task 1 writes the SQL and BEFORE any RPC-consuming work (Wave 2) depends on the live functions. Build/type checks pass WITHOUT this push (types come from config, not the live DB) — skipping it creates a false-positive verification state.

    Step 1 — push: from `app/`, run `supabase db push`. Non-TTY: ensure `SUPABASE_ACCESS_TOKEN` is set, or use the project's established push path (Phase 12 12-01 resolved an identical non-interactive push by piping empty stdin → Y). If push fails on auth, surface the exact error and STOP (do not fake success).
    Step 2 — regen types: run `npm run gen:types` (writes `types/database.ts` via `npx supabase gen types`). NEVER hand-edit `database.ts` — it is generated. Co-commit it with the migration.
    Step 3 — extend the deploy gate: in `app/scripts/verify-deploy.ts`, add a `=== Phase 13 RPC verification (Migration 0012) ===` block with `const phase13Functions = ["get_exercise_pr_history", "get_best_working_sets", "get_exercise_sets_in_range"];` and the IDENTICAL pg_proc loop as `phase12Functions` (assert `prosecdef === false` INVOKER + proconfig joined includes `search_path=`), incrementing a `phase13Failures` counter and exiting non-zero on any failure (mirror the phase12 failure-exit shape).
    Step 4 — verify: run `npx tsx --env-file=.env.local scripts/verify-deploy.ts` from `app/`; all three Phase 13 functions must report INVOKER + search_path set.
  </action>
  <verify>
    <automated>cd app && npx tsx --env-file=.env.local scripts/verify-deploy.ts</automated>
  </verify>
  <acceptance_criteria>
    - `verify-deploy.ts` prints PASS for all three phase13Functions (INVOKER + search_path set) and exits 0
    - `app/types/database.ts` contains `get_exercise_pr_history`, `get_best_working_sets`, `get_exercise_sets_in_range` (grep — proves regen ran against the live DB)
    - `app/scripts/verify-deploy.ts` contains `phase13Functions` (grep)
    - `database.ts` was regenerated, not hand-edited (the three function names appear in the generated Functions block)
  </acceptance_criteria>
  <done>Migration pushed live; types regenerated; verify-deploy green for all three RPCs.</done>
</task>

<task type="auto">
  <name>Task 3: Extend test-rls.ts — cross-user isolation + was_pr correctness</name>
  <files>app/scripts/test-rls.ts</files>
  <read_first>
    - app/scripts/test-rls.ts (lines 943-1032 — the per-RPC cross-user block for get_exercise_chart / get_exercise_top_sets; the Phase 12 extension at :1011+; the pass()/fail() helpers + try/finally cleanup)
    - .planning/phases/13-pr-celebration-f18/13-PATTERNS.md (§ `test-rls.ts` lines 340-356 — the cross-user assertion block + the was_pr fixture requirement)
    - .planning/phases/13-pr-celebration-f18/13-CONTEXT.md (D-02, D-05 — baseline/tie are NOT PRs)
  </read_first>
  <action>
    Extend `app/scripts/test-rls.ts` with a Phase 13 section mirroring the existing per-RPC cross-user blocks. For EACH of the three new RPCs (`get_exercise_pr_history`, `get_best_working_sets`, `get_exercise_sets_in_range`): have client A call the RPC scoped to B's data — A calls `get_exercise_pr_history`/`get_exercise_sets_in_range` with B's `exercise_id`; for `get_best_working_sets` (no args) assert A's result contains none of B's exercise ids. Assert the call returns empty (RLS-filtered), NOT an error — `pass()` on empty, `fail()` on error or on any leaked B rows (CLAUDE.md "cross-user verification is a gate"). Additionally seed a small progression fixture for ONE exercise owned by A (a baseline working set, then a strictly-higher working set, then a tie) and assert `get_exercise_pr_history` returns `was_pr=false` on the first row (D-02 baseline), `was_pr=true` on the higher row, and `was_pr=false` on the tie (D-05 strict-`>`). Clean up the fixture in the try/finally (existing convention). Use the existing `createClient<Database>` typed-client pattern (WR-03 — untyped clients are a false-pass risk).
  </action>
  <verify>
    <automated>cd app && npm run test:rls</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:rls` exits 0 with the Phase 13 assertions passing
    - test-rls.ts contains a cross-user assertion for each of the three RPCs (grep: `get_exercise_pr_history`, `get_best_working_sets`, `get_exercise_sets_in_range`)
    - test-rls.ts asserts `was_pr` first-set=false (D-02) and higher-set=true on a seeded fixture
    - new clients use `createClient<Database>` (typed)
  </acceptance_criteria>
  <done>test:rls green; cross-user isolation + was_pr correctness asserted for all three RPCs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (caller JWT) → Postgres RPC | Authenticated client calls the new read-only RPCs; rows must be scoped to the caller. Untrusted `p_exercise_id` / `p_since` params cross here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Information disclosure | `get_exercise_pr_history` / `get_best_working_sets` / `get_exercise_sets_in_range` | mitigate | `security invoker` → inherit `exercise_sets`/`workout_sessions` RLS from migration 0001; caller JWT scopes rows. Cross-user `test:rls` assertion per RPC (Task 3 gate). |
| T-13-02 | Tampering / Elevation | search_path hijack on the new functions | mitigate | `set search_path = ''` on every function + fully-qualified `public.*`; `verify-deploy.ts` phase13Functions asserts proconfig includes `search_path=` (Task 2 gate). |
| T-13-04 | Tampering | SQL injection via RPC params | mitigate | Typed `uuid` / `timestamptz` params; no dynamic SQL / string concatenation in any function body (pure window/aggregate SQL). |
| T-13-05 | Tampering | Display e1RM drift between SQL ordering and JS display | mitigate | SQL e1RM (`* (1 + reps/30.0)`) is INTERNAL ordering/flagging only — never returned as a column (D-08). All displayed numerals come from `lib/e1rm.ts`. |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH Package Legitimacy Audit: zero new deps). |
</threat_model>

<verification>
- `grep -c "security invoker"` on 0012 = 3; no `security definer`
- `npx tsx --env-file=.env.local scripts/verify-deploy.ts` green for all three phase13Functions
- `npm run test:rls` exits 0 (cross-user isolation + was_pr correctness)
- `database.ts` regenerated (contains the three function names)
- `npm run test:f13-brutal` unaffected (read-only RPCs; expect no regression beyond known-amber FIT-107)
</verification>

<success_criteria>
- PR-01 baseline source (D-06) + PR-04 chronological derivation (D-14/D-15) + PR-05 range-sets (D-16) all live and RLS-scoped
- Deploy gate + cross-user gate extended and green
</success_criteria>

<output>
Create `.planning/phases/13-pr-celebration-f18/13-02-SUMMARY.md` when done.
</output>
