---
phase: 07
plan: 05
plan_id: "05"
type: execute
wave: 5
depends_on: ["01", "02", "03", "04"]
files_modified:
  - .planning/phases/07-v1-polish-cut/07-HUMAN-UAT.md
autonomous: false
requirements: [F11, F12, F15]
tags: [uat, core-flow, manual-verify, phase-closeout, soak-readiness]

must_haves:
  truths:
    - "07-HUMAN-UAT.md exists with a manual UAT checklist covering: F11 RPE inline-input + history-suffix, F12 capture + edit-in-history + offline-edit, F15 theme-toggle (3-mode + persistence + corruption-resilience), Core-flow ≤2 min × 3 timed runs"
    - "Användare har kört 3 chronometered core-flow-runs och alla 3 är ≤2 min utan UI-bugg"
    - "Manuell airplane-mode UAT på F12 edit + F12 capture passerar"
    - "Manuell airplane-mode UAT på F12 offline edit+delete på samma session passerar (T-07-03 FIFO-verifiering, NON-OPTIONAL per W-1)"
    - "Manuell theme-toggle UAT på alla 9 (user-mode × iOS-mode)-kombinationer visar korrekt StatusBar-kontrast och inga FOUC-blippar vid cold-start"
    - "Code gates: tsc + lint + test:rls + test:f13-brutal + service-role-audit alla gröna"
    - "Schema-migration-mappen oförändrad (inga nya 00XX_*.sql-filer i Phase 7)"
  artifacts:
    - path: ".planning/phases/07-v1-polish-cut/07-HUMAN-UAT.md"
      provides: "Manual UAT-script + signed-off result table for all 19 acceptance rows from 07-SPEC.md + 3 timed core-flow runs + non-optional T-07-03 offline-edit-delete-race assertion"
      contains: "Core-flow"
  key_links:
    - from: "07-HUMAN-UAT.md UAT script"
      to: "07-SPEC.md acceptance criteria (19 rows)"
      via: "1:1 traceability"
      pattern: "F11|F12|F15|Core-flow"
    - from: "Manuel verifiering"
      to: "Phase 7 ROADMAP success criteria #1-5 closed"
      via: "User sign-off in 07-HUMAN-UAT.md result table"
      pattern: "approved|signed off"
---

## Phase Goal

**As a** projektets enda användare (V1-validerare), **I want to** köra ett manuellt UAT-script som verifierar att alla 6 Phase-7-requirements (F11 RPE + F11 history-suffix + F12 capture + F12 edit + F15 toggle + Core-flow ≤2 min) fungerar end-to-end på riktig iPhone-hårdvara, **so that** V1 är redo för 4-veckors personlig soak-validering mot PRD §8.

<objective>
Stäng Phase 7 genom att (a) skriva ett `07-HUMAN-UAT.md`-script som täcker alla 19 SPEC-acceptance-rader plus 3 chronometered core-flow-runs ≤2 min vardera plus en non-optional T-07-03 offline-edit+delete-race-rad (W-1 fix), (b) köra UAT manuellt på riktig iPhone via Expo Go, (c) dokumentera resultatet med sign-off i samma fil. Inga code-changes — UAT är en human-verify-gate per Phase 5 + Phase 4 precedent.

Purpose: Phase 5 + Phase 4 etablerade konventionen att UI-tunga features stängs med manual UAT på iPhone (`05-HUMAN-UAT.md`, `04-04-PLAN.md` step 4). Phase 7 ärver. Detta är V1-cut-gaten: när den passerar är V1 körbar för 4-veckors soak.

Output:
- `.planning/phases/07-v1-polish-cut/07-HUMAN-UAT.md` — UAT-script + result-table med sign-off från användaren
- Phase 7 stängd med threats_open=0 på alla 4 feature-plans + UAT signed off (inkl. icke-valfri T-07-03-rad)
- ROADMAP Phase 7 → ✓ Complete (sker via `/gsd phase.complete 7` efter detta plan)
</objective>

<execution_context>
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Mahod/Desktop/Projects/FitnessMaxxing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-v1-polish-cut/07-SPEC.md
@.planning/phases/07-v1-polish-cut/07-CONTEXT.md
@.planning/phases/07-v1-polish-cut/07-01-SUMMARY.md
@.planning/phases/07-v1-polish-cut/07-02-SUMMARY.md
@.planning/phases/07-v1-polish-cut/07-03-SUMMARY.md
@.planning/phases/07-v1-polish-cut/07-04-SUMMARY.md
@.planning/phases/05-active-workout-hot-path-f13-lives-or-dies/05-HUMAN-UAT.md
@.planning/phases/04-plans-exercises-offline-queue-plumbing/04-04-PLAN.md

<interfaces>
<!-- The 19 SPEC.md acceptance rows (verbatim transcript for the UAT script). -->

From 07-SPEC.md "Acceptance Criteria" section (19 rows the script must verify):
1. Inline-raden i workout/[sessionId].tsx renderar `[Vikt] [Reps] [RPE (valfri)] [Klart]` — RPE-fältet är synligt
2. Klart utan att fylla RPE → set landar med `rpe IS NULL` i `exercise_sets`
3. Klart med RPE=8.5 → set landar med `rpe=8.5` (decimaltal accepteras)
4. RPE > 10 eller < 0 rejects med inline-felmeddelande via Zod-schema
5. `history/[sessionId].tsx` visar `· RPE {rpe}`-suffix på set-rader där `rpe IS NOT NULL`; suffix saknas där `rpe IS NULL`
6. AvslutaOverlay i `workout/[sessionId].tsx` innehåller TextInput för anteckning; submit fångar texten i `finishSession.mutate`-payloaden
7. Avsluta utan att fylla anteckning → `workout_sessions.notes IS NULL`
8. Avsluta med text → `workout_sessions.notes` lagrar texten exakt
9. `history/[sessionId].tsx` renderar `session.notes` i textblock när det finns
10. Redigera-affordance i `history/[sessionId].tsx` öppnar inline-overlay med förfylld text; spara uppdaterar raden; töm fältet sparar `NULL`
11. Settings-fliken (`(tabs)/settings.tsx`) visar segmenterad kontroll `[System | Ljust | Mörkt]`
12. Välj "Mörkt" på iPhone med "Ljust" iOS-tema → appen blir mörk inom 1 sek utan restart
13. Kill app → reopen → tema persisterar i AsyncStorage under `fm:theme`
14. Default vid första app-start (eller saknad AsyncStorage-värde) = "System"
15. Core-flow UAT: plan → starta pass → 3 set → avsluta → historik → öppna pass på ≤2 min × 3 körningar utan fel
16. `app/supabase/migrations/`-mappen oförändrad i Phase 7 (inga nya 00XX_*.sql-filer)
17. Service-role-audit: `git grep "service_role\|SERVICE_ROLE"` matchar bara `app/scripts/test-rls.ts`, `app/.env.example`, `.planning/`, `CLAUDE.md`
18. `npm run test:rls` förblir grön (29+ assertions; ingen ny tabell)
19. Phase 5 F13 brutal-test (`npm run test:f13-brutal`) förblir grön efter inline-RPE-tillägg

Plus one inserted non-optional row (W-1 fix):
20. **T-07-03 — offline edit+delete on same session must replay correctly via scope.id session:${id} FIFO** (NON-OPTIONAL — moved from prior 3.10 `(Optional)` status). Exercises the FIFO contract that protects T-07-03 (Plan 07-04 threat-register).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author 07-HUMAN-UAT.md script + run automated code-gates</name>
  <files>.planning/phases/07-v1-polish-cut/07-HUMAN-UAT.md</files>

  <read_first>
    - .planning/phases/07-v1-polish-cut/07-SPEC.md (Acceptance Criteria section — 19 rows the script transcribes)
    - .planning/phases/05-active-workout-hot-path-f13-lives-or-dies/05-HUMAN-UAT.md (precedent format: numbered steps, expected results, pass/fail check-boxes, sign-off table)
    - .planning/phases/04-plans-exercises-offline-queue-plumbing/04-04-PLAN.md (Task 4 — checkpoint:human-verify pattern for airplane-mode UAT)
    - 07-CONTEXT.md `<specifics>` "Core-flow ≤2 min UAT-script (för human-UAT.md)" — 8-step script
  </read_first>

  <behavior>
    - `07-HUMAN-UAT.md` is created with five sections: (1) Pre-flight code gates, (2) F11 RPE manual UAT, (3) F12 notes capture + edit manual UAT (Section 3 includes the NON-OPTIONAL T-07-03 offline edit+delete-race step), (4) F15 theme-toggle manual UAT, (5) Core-flow ≤2 min × 3 timed runs.
    - Each section enumerates numbered steps with expected outcomes and a pass/fail checkbox.
    - The Core-flow section has a stopwatch-style table for 3 runs with start-time + end-time + total + pass/fail.
    - A sign-off section at the bottom captures user's `approved` / `partial` / `blocked` decision + date + commit hash of the head of `gsd/phase-07-v1-polish-cut`.
    - Pre-flight gates section runs automated code-checks BEFORE the user starts manual UAT (catch regressions cheaply).
  </behavior>

  <action>
    1. **Run pre-flight automated gates** from the project root and capture results to inline in 07-HUMAN-UAT.md:
       ```bash
       cd app
       npx tsc --noEmit
       npm run lint
       npm run test:rls
       npm run test:f13-brutal
       npm run test:set-schemas  # if extended in Plan 07-02
       cd ..
       ls app/supabase/migrations/   # verify NO new 00XX_*.sql files
       git grep "service_role\\|SERVICE_ROLE" -- '*.ts' '*.tsx' '*.js' '*.json' '*.md' | grep -v -E '^\\.planning/|^CLAUDE\\.md|app/scripts/test-rls\\.ts|app/\\.env\\.example'
       # expect: zero matches outside the allowlist
       ```

    2. **Author `07-HUMAN-UAT.md`** with the following structure (filled in per the SPEC.md acceptance criteria transcript above):

       ```markdown
       # Phase 7 — Human UAT Script
       
       **Branch:** gsd/phase-07-v1-polish-cut
       **Tested on:** [iPhone model + iOS version + Expo Go version]
       **Tested by:** [@user]
       **Started:** YYYY-MM-DD
       **Status:** in-progress | approved | partial | blocked
       
       ## Section 0 — Pre-flight Code Gates (automated, run before manual UAT)
       
       | # | Gate | Command | Result | Notes |
       |---|------|---------|--------|-------|
       | 0.1 | TypeScript | `cd app && npx tsc --noEmit` | [ ] PASS / [ ] FAIL | |
       | 0.2 | ESLint | `cd app && npm run lint` | [ ] PASS / [ ] FAIL | |
       | 0.3 | RLS | `cd app && npm run test:rls` | [ ] PASS / [ ] FAIL | (≥29 assertions) |
       | 0.4 | F13 brutal | `cd app && npm run test:f13-brutal` | [ ] PASS / [ ] FAIL | |
       | 0.5 | Set schemas | `cd app && npm run test:set-schemas` | [ ] PASS / [ ] FAIL | (if extended in 07-02) |
       | 0.6 | No new migrations | `ls app/supabase/migrations/` | [ ] No 00XX_*.sql added since Phase 6 | |
       | 0.7 | Service-role audit | `git grep "service_role\\|SERVICE_ROLE"` outside allowlist | [ ] Zero matches | |
       
       ## Section 1 — F11 RPE inline-input + history-suffix
       
       1.1 Open active workout from a plan.
       1.2 Verify inline-raden shows 4 columns: `[Vikt] [Reps] [RPE] [Klart]`. RPE column is visually narrower (w-16) than Vikt/Reps. Klart-button is square (w-16 min-h-[56px]).
           - Expected: 4 visible columns; RPE placeholder reads "RPE".
           - [ ] PASS / [ ] FAIL
       1.3 Type Vikt=80, Reps=10, leave RPE blank → tap Klart.
           - Expected: set saved (logged-list updates).
           - Verify via Supabase Studio SQL: `SELECT rpe FROM exercise_sets ORDER BY completed_at DESC LIMIT 1;` → returns NULL.
           - [ ] PASS / [ ] FAIL — SQL: ____________________
       1.4 Type Vikt=80, Reps=10, RPE="8,5" (Swedish comma) → tap Klart.
           - Expected: set saved.
           - Verify SQL: same query → returns `8.5`.
           - [ ] PASS / [ ] FAIL — SQL: ____________________
       1.5 Type Vikt=80, Reps=10, RPE="11" → tap Klart.
           - Expected: inline-error "RPE 10 eller lägre" appears under RPE field in red; set NOT saved.
           - [ ] PASS / [ ] FAIL
       1.6 Type Vikt=80, Reps=10, RPE="-1" → tap Klart.
           - Expected: inline-error "RPE 0 eller högre".
           - [ ] PASS / [ ] FAIL
       1.7 Finish the workout. Navigate to /history → tap the session.
       1.8 Verify set-rows: sets with rpe=null show no suffix; sets with rpe show `· RPE {value}` suffix in muted text.
           - [ ] PASS / [ ] FAIL — Observation: ____________________
       
       ## Section 2 — F12 Notes capture in AvslutaOverlay
       
       2.1 Start a new pass → log 3 sets → tap "Avsluta" header.
       2.2 AvslutaOverlay opens. Verify it contains: title + body + multi-line TextInput (placeholder "Anteckningar (valfri)") + counter "0/500" + button-row.
           - [ ] PASS / [ ] FAIL
       2.3 Tap inside TextInput → iOS keyboard pops up.
           - Expected: Fortsätt + Avsluta buttons remain visible above keyboard (KeyboardAvoidingView wraps card).
           - [ ] PASS / [ ] FAIL
       2.4 Type a note "Test from UAT 2.4". Verify counter increments.
           - [ ] PASS / [ ] FAIL
       2.5 Type 481+ characters (paste from clipboard). Verify counter color flips to red at >480.
           - [ ] PASS / [ ] FAIL
       2.6 Try to type past 500. Expected: input hard-caps at 500.
           - [ ] PASS / [ ] FAIL
       2.7 Tap backdrop (outside card). Overlay closes. Re-open Avsluta → TextInput is empty.
           - [ ] PASS / [ ] FAIL
       2.8 Open Avsluta again → type "Final note" → tap Avsluta (the confirm button).
           - Navigate to /history → tap latest session.
           - Verify notes-block displays "Final note" above SummaryHeader chips.
           - Verify SQL: `SELECT notes FROM workout_sessions ORDER BY finished_at DESC LIMIT 1;` → "Final note".
           - [ ] PASS / [ ] FAIL — SQL: ____________________
       2.9 Repeat with empty notes (leave TextInput blank, tap Avsluta).
           - Expected: SQL returns NULL.
           - [ ] PASS / [ ] FAIL — SQL: ____________________
       2.10 Repeat with whitespace-only ("   ").
           - Expected: SQL returns NULL (trim normalization).
           - [ ] PASS / [ ] FAIL — SQL: ____________________
       
       ## Section 3 — F12 Notes view + edit in history-detail
       
       3.1 Navigate to a history session WITH notes (from Section 2.8).
       3.2 Verify notes-block above SummaryHeader chiparna. Shows the notes text + pencil-icon on the right.
           - [ ] PASS / [ ] FAIL
       3.3 Tap pencil → edit-overlay opens, pre-filled with current notes, keyboard auto-focused.
           - [ ] PASS / [ ] FAIL
       3.4 Modify text to "Edited note" → tap Spara.
           - Expected: overlay closes, notes-block immediately shows "Edited note" (optimistic).
           - SQL confirms update.
           - [ ] PASS / [ ] FAIL
       3.5 Tap pencil → delete all text → tap Spara.
           - Expected: notes-block flips to "+ Lägg till anteckning" with accent + circle icon.
           - SQL returns NULL.
           - [ ] PASS / [ ] FAIL
       3.6 Tap "+ Lägg till anteckning" (notes-null state) → overlay opens with empty TextInput.
           - Type "Added back" → Spara.
           - Block re-renders with text + pencil.
           - [ ] PASS / [ ] FAIL
       3.7 Tap pencil → modify text → tap Avbryt.
           - Overlay closes; notes-block still shows ORIGINAL text (no save).
           - Re-open pencil → TextInput shows ORIGINAL text (no draft restoration).
           - [ ] PASS / [ ] FAIL
       3.8 **Offline edit**: airplane mode → tap pencil → modify → Spara.
           - Overlay closes; notes-block shows new text optimistically.
           - Reconnect → wait 5 sec → SQL confirms update landed.
           - [ ] PASS / [ ] FAIL — SQL: ____________________
       3.9 **freezeOnBlur cleanup**: open overlay → switch to (tabs)/index → switch back to history-detail.
           - Expected: overlay closed; pencil-tap opens fresh state (no stale draft).
           - [ ] PASS / [ ] FAIL
       3.10 **(NON-OPTIONAL — T-07-03 FIFO verification, W-1 fix)** Offline edit + offline delete on same session.

           **Purpose:** Exercises the `scope.id "session:${id}"` FIFO contract shared across useFinishSession / useDeleteSession / useUpdateSessionNotes (Plan 07-04 D-E3 + threat-register T-07-03). Without this row, T-07-03 is asserted but never tested on hardware.

           **Steps:**
           a. Pick a session that has notes (use one created in Section 2.8 or 3.4).
           b. Enable airplane mode.
           c. Open the history-detail for that session.
           d. **Order A — edit-then-delete:**
              - Tap pencil → modify text to "FIFO test A" → tap Spara (queued — overlay closes, optimistic update shows new text).
              - Open overflow menu → Ta bort → confirm deletion (queued — session disappears from the list optimistically).
              - Disable airplane mode → wait 10 sec for both mutations to replay.
              - **Expected:** session is gone from the list. SQL: `SELECT count(*) FROM workout_sessions WHERE id='<sessionId>'` returns 0. No "stuck" mutation in the offline queue (DevTools mutation cache empty for this session).
              - [ ] PASS / [ ] FAIL — SQL: ____________________ — DevTools mutation queue empty: [ ] yes / [ ] no
           e. **Order B — delete-then-edit** (create a fresh session with notes first):
              - Log a new session via Section 5's run-script with a notes-bearing finish.
              - Enable airplane mode.
              - Open history-detail for the new session.
              - Open overflow menu → Ta bort → confirm (queued).
              - Tap "+ Lägg till anteckning" — **expected:** affordance still works on the optimistically-deleted session because the session is still in the detail-cache (TanStack v5 paused mutation does not yet evict). Type "FIFO test B" → Spara (queued).
              - Disable airplane mode → wait 10 sec.
              - **Expected:** delete replays first (FIFO), then update-notes replays and 404s (session no longer exists). The update-notes onError fires; `setBannerError("Kunde inte spara anteckningen. Försök igen.")` lights the banner-error block briefly OR the rollback restores the (already deleted) cache to its previous state. Net result: session is gone; no orphan rows. SQL: count=0.
              - [ ] PASS / [ ] FAIL — SQL: ____________________ — Banner-error observed: [ ] yes / [ ] no — Net behavior: ____________________

           **If 3.10 fails:** This is a T-07-03 regression. Capture a Linear issue with `--type bug --priority urgent --phase 7` AND include the DevTools mutation-cache snapshot. Do NOT mark Phase 7 approved.

           **Deterministic fallback (if airplane-mode toggling is flaky):** Skip 3.10.d/e on hardware and substitute with a Node-script-driven assertion using `queryClient.getMutationCache().getAll()` from a paused state (see 07-04 Plan Task 1 action note). Mark 3.10 as `[ ] PASS via deterministic fallback` and attach the script output. The hardware UAT remains the preferred path.

    
       ## Section 4 — F15 Theme-toggle in Inställningar
       
       4.1 Open (tabs)/settings. Verify a `Tema` section with SegmentedControl `[System | Ljust | Mörkt]` is visible between email-row and the spacer above sign-out.
           - [ ] PASS / [ ] FAIL
       4.2 With iPhone iOS in Ljust mode, app initially in System mode → tap "Mörkt".
           - Expected: app turns dark within 1 sec without restart. StatusBar icons turn light.
           - [ ] PASS / [ ] FAIL
       4.3 Kill the app (swipe up). Reopen.
           - Expected: app starts in Mörkt (no flicker to Ljust during splash).
           - [ ] PASS / [ ] FAIL
       4.4 In settings, tap "Ljust" → app turns light immediately. StatusBar icons turn dark.
           - [ ] PASS / [ ] FAIL
       4.5 Tap "System" → app follows iOS. With iPhone iOS=Ljust → app=Ljust. Toggle iPhone iOS to Mörkt → app flips to Mörkt automatically.
           - [ ] PASS / [ ] FAIL
       4.6 (T-07-01 mitigation) From a dev tool/REPL: `await AsyncStorage.setItem('fm:theme', 'hax')`. Kill + reopen app.
           - Expected: app defaults to System mode, no crash. console.warn `[theme] AsyncStorage read failed — defaulting to system` may appear (acceptable).
           - [ ] PASS / [ ] FAIL
           - **If dev REPL not available, mark this row N/A and document the Zod-enum-catch coverage in the SUMMARY instead.**
       4.7 StatusBar contrast across 9 combinations: walk through each (App-mode × iOS-mode) pair and verify status-bar icons are visible.
           | App-mode \\ iOS-mode | Ljust | Mörkt | Auto |
           |---|---|---|---|
           | System | [ ] PASS | [ ] PASS | [ ] PASS |
           | Ljust  | [ ] PASS | [ ] PASS | [ ] PASS |
           | Mörkt  | [ ] PASS | [ ] PASS | [ ] PASS |
       
       ## Section 5 — Core-flow ≤2 min × 3 timed runs (SPEC §6)
       
       For each run, use a stopwatch. Start when tapping the Hem-tab; stop when the history-detail screen has rendered the just-finished session.
       
       **Run script** (CONTEXT.md `<specifics>` Core-flow):
       1. Tap Hem-tab.
       2. Tap "Starta pass" on an existing plan.
       3. Log set 1: vikt + reps + RPE → Klart.
       4. Log set 2: same exercise, next set → Klart.
       5. Log set 3: next exercise → Klart.
       6. Tap "Avsluta" header → AvslutaOverlay → optionally type a note → tap Avsluta.
       7. Tap Historik-tab → tap the just-finished session.
       8. Verify RPE-suffix on at least one set-row + notes-block displayed (if notes typed).
       
       | Run | Start (HH:MM:SS) | End (HH:MM:SS) | Duration | Pass (≤2 min)? | Notes / Bugs |
       |-----|------------------|----------------|----------|----------------|--------------|
       | 1   |                  |                |          | [ ] YES / [ ] NO |          |
       | 2   |                  |                |          | [ ] YES / [ ] NO |          |
       | 3   |                  |                |          | [ ] YES / [ ] NO |          |
       
       **Result:** 3 of 3 runs ≤2 min: [ ] YES / [ ] NO (SPEC §6 acceptance)
       
       ## Sign-off
       
       - Tested-by: ____________________
       - Date: YYYY-MM-DD
       - Branch head commit: ____________________
       - All 19 SPEC acceptance criteria covered above: [ ] yes
       - Non-optional T-07-03 row (Section 3.10) covered: [ ] yes  (W-1 — must be checked before approval)
       - Decision: [ ] approved / [ ] partial (list gaps) / [ ] blocked
       - Carry-over Linear issues created: ____________________ (FIT-XX format)
       ```

    3. **Linear sync** — if this is the first plan-execution after `linear:sync-phase 7` ran, any new bugs surfaced during UAT should be captured with `npm run linear:create -- --title "Bug: ..." --type bug --priority high --phase 7` per CLAUDE.md "Skapa issue automatiskt när du hittar".

    4. **Save the file** to `.planning/phases/07-v1-polish-cut/07-HUMAN-UAT.md` and commit.
  </action>

  <verify>
    <automated>
      cd app && npx tsc --noEmit && npm run lint && npm run test:rls && npm run test:f13-brutal
    </automated>
    <file-exists>
      test -f .planning/phases/07-v1-polish-cut/07-HUMAN-UAT.md
    </file-exists>
    <coverage>
      grep -c "F11" .planning/phases/07-v1-polish-cut/07-HUMAN-UAT.md >= 3 (multiple references — RPE section)
      grep -c "F12" .planning/phases/07-v1-polish-cut/07-HUMAN-UAT.md >= 3 (multiple references — notes capture + edit)
      grep -c "F15" .planning/phases/07-v1-polish-cut/07-HUMAN-UAT.md >= 2 (theme section)
      grep -c "Core-flow" .planning/phases/07-v1-polish-cut/07-HUMAN-UAT.md >= 1
      grep -c "PASS" .planning/phases/07-v1-polish-cut/07-HUMAN-UAT.md >= 19 (at least one PASS row per acceptance criterion)

      <!-- W-1 enforcement: T-07-03 row must be present and explicitly marked NON-OPTIONAL. -->
      grep -c "T-07-03" .planning/phases/07-v1-polish-cut/07-HUMAN-UAT.md >= 1
      grep -c "NON-OPTIONAL" .planning/phases/07-v1-polish-cut/07-HUMAN-UAT.md >= 1
      grep -ic "3\\.10" .planning/phases/07-v1-polish-cut/07-HUMAN-UAT.md >= 1   <!-- the step number -->
      grep -c "(Optional)" .planning/phases/07-v1-polish-cut/07-HUMAN-UAT.md == 0   <!-- no row may carry an `(Optional)` tag that side-steps T-07-03 -->
    </coverage>
  </verify>

  <acceptance_criteria>
    - `07-HUMAN-UAT.md` exists in `.planning/phases/07-v1-polish-cut/`.
    - File has 5 sections covering all 19 SPEC acceptance rows.
    - Each section has numbered steps with `[ ] PASS / [ ] FAIL` boxes.
    - Section 3.10 is the NON-OPTIONAL T-07-03 row covering both order A (edit-then-delete) AND order B (delete-then-edit) — W-1 fix.
    - Section 5 has a 3-row stopwatch table for the ≤2 min runs.
    - Sign-off section includes tested-by + date + commit hash + decision (approved/partial/blocked) AND explicit confirmation that 3.10 was covered.
    - Pre-flight automated gates section is filled with the actual results from running `tsc + lint + test:rls + test:f13-brutal` (committed alongside the file).
    - All code gates green before submitting the file to the user for manual UAT.
  </acceptance_criteria>

  <done>07-HUMAN-UAT.md script is authored (including the non-optional T-07-03 row), code-gates are run and recorded, file is ready for user manual UAT execution.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User runs 07-HUMAN-UAT.md manually on iPhone + signs off (includes NON-OPTIONAL T-07-03 §3.10)</name>
  <files>.planning/phases/07-v1-polish-cut/07-HUMAN-UAT.md</files>
  <action>Human-only verification — user runs the UAT script authored in Task 1 against the real iPhone build and fills in PASS/FAIL boxes. See <how-to-verify> block below for the 13-step execution checklist. Section 3 step 3.10 (T-07-03 offline edit+delete race) is NON-OPTIONAL per W-1 fix; must be covered before sign-off. Claude cannot automate manual iPhone UAT; this task is a blocking checkpoint awaiting user sign-off in chat or via committed 07-HUMAN-UAT.md update.</action>
  <what-built>
    Plans 07-01 through 07-04 have shipped: theme toggle, RPE inline input + history suffix, notes capture in AvslutaOverlay, notes edit in history-detail (with FIFO scope.id session:${id} shared across finish/delete/update-notes). All code gates green. Branch `gsd/phase-07-v1-polish-cut` is at head commit with all 4 features wired end-to-end. 07-HUMAN-UAT.md script is ready for execution, including the non-optional T-07-03 hardware verification step.
  </what-built>
  <how-to-verify>
    1. **Open the UAT script**: `.planning/phases/07-v1-polish-cut/07-HUMAN-UAT.md`.
    2. **Confirm pre-flight gates section is GREEN** (Section 0 — TypeScript, ESLint, RLS, F13 brutal, set-schemas all PASS; no new migrations; service-role audit clean).
    3. **Run the build**: `cd app && npx expo start --tunnel` (or LAN) and open in Expo Go on iPhone.
    4. **Execute Section 1 — F11 RPE** (steps 1.1-1.8): verify inline-raden 4-kolumns + empty/RPE-8.5/RPE-11/RPE-(-1) cases + history-suffix.
    5. **Execute Section 2 — F12 capture** (steps 2.1-2.10): verify AvslutaOverlay TextInput + KeyboardAvoidingView + counter color flip + empty/whitespace normalization.
    6. **Execute Section 3 — F12 edit** (steps 3.1-3.10): verify notes-block display + pencil-edit + add-affordance + clear-to-null + Avbryt + offline-edit + freezeOnBlur. **Step 3.10 is NON-OPTIONAL** — must execute BOTH order-A (edit-then-delete) and order-B (delete-then-edit) airplane-mode scenarios; capture SQL count + DevTools mutation-queue state in each. If hardware airplane-mode toggling is flaky, fall back to the deterministic queryClient.getMutationCache() inspection per the row's fallback note.
    7. **Execute Section 4 — F15 theme** (steps 4.1-4.7): verify 3-mode toggle + persistence across kill+reopen + System-mode auto-follow + corruption-resilience + StatusBar contrast across 9 combinations.
    8. **Execute Section 5 — Core-flow ≤2 min × 3 timed runs** with stopwatch.
    9. **Fill in PASS/FAIL boxes** as you go. Take screenshots of any FAIL for the SUMMARY.
    10. **Sign off the bottom**: tested-by + date + branch head commit (`git rev-parse HEAD`) + decision (approved / partial / blocked) AND tick the explicit "Non-optional T-07-03 row covered" checkbox.
    11. **If FAIL on any blocker**: do NOT mark approved. Either (a) fix inline via a new git commit and re-run that section, or (b) capture as Linear issue `npm run linear:create -- --type bug --priority high --phase 7` and mark `partial`. Phase 7 closeout (`/gsd phase.complete 7`) will not proceed until decision = approved AND 3.10 covered.
    12. **Commit 07-HUMAN-UAT.md** with the filled-in results.
    13. **Resume signal**: write `approved` (or `partial` / `blocked`) in chat, or commit + push the signed-off file.
  </how-to-verify>
  <resume-signal>Type "approved", "partial: [gaps]", or "blocked: [reason]". If approved, the orchestrator proceeds to phase.complete 7. Sign-off is invalid if Section 3.10 (T-07-03) is not covered.</resume-signal>
  <verify>Manual — user fills in PASS/FAIL boxes in 07-HUMAN-UAT.md (incl. 3.10) and writes signed-off decision in the Sign-off section. Automated structural check after commit: grep returns > 0 lines matching either approved/partial/blocked AND > 0 lines containing `T-07-03` + `NON-OPTIONAL` AND zero `(Optional)` tokens.</verify>
  <done>User has executed all 5 sections of 07-HUMAN-UAT.md on real iPhone hardware INCLUDING the NON-OPTIONAL T-07-03 step 3.10, and written a sign-off decision (approved/partial/blocked) in the file. The file is committed with the result. Any FAILs are either fixed inline or captured as Linear issues per Section 5 closeout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manual UAT execution → SUMMARY.md sign-off | Human-driven verification; trust boundary is the user's diligence. Mitigated by SPEC-acceptance-row coverage requirement (19/19) AND the new W-1-enforced NON-OPTIONAL T-07-03 row. |
| Linear issue creation during UAT | If bugs are surfaced, they enter the Linear backlog with priority labels; trust boundary is Linear API auth (LINEAR_API_KEY) which is already established. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-19 | Repudiation | UAT signed-off without all 19 rows verified | mitigate | The UAT-script enumerates all 19 SPEC acceptance rows + the non-optional T-07-03 row + 3 timed runs; sign-off section explicitly asks "All 19 SPEC acceptance criteria covered above" AND "Non-optional T-07-03 row (Section 3.10) covered". Skipping a row leaves a visible unchecked box. Coverage check via grep gate (`grep -c "PASS" 07-HUMAN-UAT.md >= 19` + `grep -c "NON-OPTIONAL" >= 1` + `grep -c "(Optional)" == 0`) confirms structural completeness before manual execution begins. |
| T-07-20 | Tampering | Code change between UAT execution and phase.complete invalidates the sign-off | mitigate | Sign-off section captures the branch-head commit hash. Subsequent commits before phase.complete bust the audit trail — explicit policy in CLAUDE.md: "Phase-arbete sker på gsd/phase-07-v1-polish-cut-branch via PR mot dev; aldrig direktcommit". Any post-UAT fix triggers re-run of the affected section. |

threats_open: 0
</threat_model>

<verification>
After Task 2 completes with user sign-off = `approved`:

1. Verify `07-HUMAN-UAT.md` is committed to the branch with all 19 PASS rows + non-optional 3.10 PASS + 3 timed runs ≤2 min.
2. Trigger Phase 7 closeout via the orchestrator: `/gsd phase.complete 7` (or `/gsd-secure-phase 7` → `/gsd-verify-work 7` → `/gsd-code-review` → phase.complete).
3. ROADMAP.md Phase 7 row flips from `○ Not started` (or `○ Planning`) → `✓ Complete`.
4. The phase branch is merged via PR to `dev` (CI workflow `.github/workflows/phase-branch.yml` auto-creates the PR).
5. Linear: any `partial`-decision gaps are captured as Linear issues `npm run linear:create -- --type debt --priority medium --phase 7` and listed in the SUMMARY.
6. 4-week personal soak validation begins per PRD §8 (1 bug/vecka tolerans, alla pass loggade utan papper).
</verification>

<success_criteria>
- [ ] 07-HUMAN-UAT.md authored with 5 sections + sign-off
- [ ] Section 3.10 (T-07-03 offline edit+delete race) is present, NON-OPTIONAL, and covers BOTH order-A and order-B (W-1)
- [ ] Pre-flight automated gates all green (TypeScript, ESLint, RLS, F13 brutal, set-schemas if extended, no new migrations, service-role audit clean)
- [ ] User has executed manual UAT on real iPhone hardware (including 3.10)
- [ ] All 19 SPEC acceptance criteria have PASS or documented partial/deferred status
- [ ] 3 of 3 core-flow timed runs ≤2 min
- [ ] StatusBar contrast verified across all 9 (app-mode × iOS-mode) combinations (Section 4.7 grid)
- [ ] User signed off in 07-HUMAN-UAT.md (decision = approved | partial | blocked) AND ticked the 3.10-covered confirmation
- [ ] Any FAILs captured as Linear issues with phase=7
- [ ] threat_model T-07-19 / T-07-20 dispositioned
</success_criteria>

<output>
After completion, create `.planning/phases/07-v1-polish-cut/07-05-SUMMARY.md` documenting:
- The user's sign-off decision (approved / partial / blocked)
- Number of FAIL boxes (target: 0)
- Section 3.10 outcome (Order A + Order B individually) — explicit T-07-03 verification status
- Linear issues created during UAT (FIT-XX list)
- Total elapsed time for Section 5 runs (3-run average)
- Any deviations from the 19 SPEC acceptance criteria (target: 0 deviations)
- 4-week soak start date (if approved)
</output>
</content>
</invoke>