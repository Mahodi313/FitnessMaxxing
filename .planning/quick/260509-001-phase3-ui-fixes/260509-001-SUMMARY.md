---
mode: quick
ticket: 260509-001-phase3-ui-fixes
phase_ref: 03-auth-persistent-session
type: execute-summary
wave: 1
status: complete
requirements_closed:
  - UR-01  # Pillar 6 a11y instrumentation (BLOCKER)
  - UR-02  # Pillar 1 Lösen↔Lösenord drift reconciliation (WARNING)
  - UR-03  # Pillar 6 RHF mode contract drift (WARNING)
  - UR-04  # Pillar 6 network-error copy specificity (WARNING)
  - UR-05  # Pillar 6 banner dismiss affordance (WARNING)
files_modified:
  created: []
  modified:
    - app/app/(auth)/sign-in.tsx
    - app/app/(auth)/sign-up.tsx
    - app/app/(app)/index.tsx
    - .planning/phases/03-auth-persistent-session/03-UI-SPEC.md
key-decisions:
  - "UR-02 (Lösen→Lösenord): amend spec, not code — Lösenord matches conventional Swedish form (Apple iCloud / Google / banking apps); fewer touchpoints to amend than to revert across 7 strings"
  - "UR-03 (onBlur→onSubmit): amend spec, not code — UAT Test 3 explicitly recorded user preference (mode: 'onSubmit' is now the ratified contract per UR-03)"
  - "UR-04: place AuthRetryableFetchError check BEFORE the WR-04 default branch and OMIT console.error on the offline arm — network failure is expected, not unmapped (preserves WR-04 diagnostic intent for truly-unknown error codes)"
  - "UR-05: nested Pressable pattern (outer banner-tap dismiss + inner ✕ icon) — preserves the original full-banner tap target for VoiceOver users while adding a clear visible affordance for sighted users"
metrics:
  duration_minutes: 9
  task_count: 3
  file_count_modified: 4
  commits: 3
completed_date: 2026-05-09
---

# Quick Task 260509-001 — Phase 3 UI-REVIEW priority-5 fixes

Closed all 5 priority findings from `.planning/phases/03-auth-persistent-session/03-UI-REVIEW.md` against the shipped Phase 3 auth screens. Pillar 6 (Experience Design) raised from 2/4 toward 4/4; Pillar 1 (Copywriting) from 2/4 toward 4/4 by reconciling spec ↔ shipped reality on two intentional drifts the user explicitly preferred.

## Tasks Completed

| Task | Closes | Commit | Files |
|------|--------|--------|-------|
| 1 | UR-01 | `a454312` | `sign-in.tsx`, `sign-up.tsx`, `(app)/index.tsx` |
| 2 | UR-02 + UR-03 | `00863d7` | `03-UI-SPEC.md` |
| 3 | UR-04 + UR-05 | `15d98d5` | `sign-in.tsx`, `sign-up.tsx` |

### Task 1 (UR-01) — VoiceOver a11y instrumentation

Added `accessibilityLabel`, `accessibilityRole`, and `accessibilityLiveRegion` props across all three Phase 3 surfaces. Swedish labels matching visible copy per CLAUDE.md and UI-SPEC §Copywriting Contract. Final count gates (post-edit grep, code lines only):

| File | accessibilityLabel | accessibilityRole | accessibilityLiveRegion |
|------|--------------------|-------------------|-------------------------|
| `sign-in.tsx` | 5 (≥4 required) | 3 (≥2) | 3 (≥3) |
| `sign-up.tsx` | 7 (≥6) | 4 (≥3) | 5 (≥5) |
| `(app)/index.tsx` | 1 (≥1) | 1 (≥1) | n/a |

English VoiceOver leakage check: 0 hits (clean).

### Task 2 (UR-02 + UR-03) — UI-SPEC amendment, no code change

Eight edits to `03-UI-SPEC.md`:
1. Frontmatter: added `amended_at` + `amendment` fields
2. §Typography per-screen role mapping: `Lösen` → `Lösenord`, `Bekräfta lösen` → `Bekräfta lösenord`
3. §Copywriting Contract form-level table: same vocabulary update
4. §Copywriting Contract error states table: 3 row updates + new `weak_password` row (server-side D-12 rejection on sign-up)
5. §Error states additional rules: replaced `mode: 'onBlur'` paragraph with `mode: 'onSubmit'` + UAT Test 3 quote
6. §Interaction Contracts table: replaced "Field blur" row with "Form submit" row
7. §Accessibility Floor table: setFocus row gains honesty note about RHF Controllers shipping without ref forwarding (priority finding #4 deferred to V1.1)
8. Checker Sign-Off: Approval line updated from `pending` to amended status

Code, schemas, and database files: zero changes. Spec is the only file touched in this commit.

### Task 3 (UR-04 + UR-05) — Offline-error arm + banner close affordance

**UR-04** — `AuthRetryableFetchError` detection added to `onSubmit` switch default branch in both `sign-in.tsx` and `sign-up.tsx`, BEFORE the WR-04 unmapped-error fall-through. Network-failure path surfaces `"Du verkar vara offline. Kontrollera din anslutning."` and `break`s without invoking `console.error` — preserving WR-04's diagnostic intent for genuinely unmapped codes while removing Metro-log noise on expected offline events. Source-order verified: `AuthRetryableFetchError` block appears at offset earlier than `[sign-in]/[sign-up] unexpected error` log in both files.

**UR-05** — All three banners (`sign-in` bannerError, `sign-up` bannerError, `sign-up` infoBanner) now render a flex-row View with the message `Text` on the left and an inline `✕` `Pressable` on the right. Outer Pressable still dismisses on whole-banner tap (preserves UR-01 a11y baseline). Inner `✕` is a separate VoiceOver focusable with `accessibilityLabel="Stäng"`, `accessibilityHint="Tryck för att stänga"` on the outer banner, `hitSlop={8}` on the icon, and palette tokens within UI-SPEC §Color (red-600/dark:red-400 for error; existing blue-700/dark:blue-300 for info, untouched).

Final ✕ count (Grep): sign-in 1 (≥1), sign-up 2 (≥2 — bannerError + infoBanner). Stäng a11y label count: sign-in 1, sign-up 2.

## Verification Suite

All blocking gates passed:
- `npx tsc --noEmit` (worktree app/) — exit 0, no type errors
- `npm run lint` (expo lint) — exit 0, clean
- `npm run test:auth-schemas` — 8/8 schema cases passed (no schema files touched; sanity regression net)
- All grep gates from PLAN.md `<verify>` blocks — passed (counts above)

Skipped:
- `npm run test:rls` — bailed on missing `app/.env.local` in worktree (expected — `.env.local` is gitignored, only present in main checkout). This fix-set did NOT touch DB schemas, RLS policies, or `app/scripts/test-rls.ts`, so RLS regression risk is zero. Re-run from the main repo after merge if a defense-in-depth check is wanted.

## Deviations from Plan

None — plan executed exactly as written. The verification gate skip on `test:rls` is documented above as expected behaviour (worktree env limitation, not a regression).

### Notes for the user

- The original `<verify>` block in Task 1 used PowerShell `Where-Object { $_.Line -notmatch '^\s*//' }` to exclude commented matches. Counts above were obtained with that exact filter.
- Task 1 verification scripts also used `accessibilityLabel="(Email address|Password|Submit|Sign in|Sign up|Log out)"` to detect English VoiceOver leakage — 0 matches.
- Worktree-mode caveat: this run encountered the upstream cwd-drift issue (#3097/#3099) on the first attempt. Initial Edit calls used absolute paths to the main repo and were reverted via `git checkout --` before being re-applied with absolute paths to the worktree root. Pre-commit HEAD assertion + post-commit deletion check both held. No commits landed on the wrong branch; no files lost.

## Acceptance Criteria

- [x] UR-01: All 3 screens have a11y instrumentation per Task 1 grep gates
- [x] UR-02: UI-SPEC vocabulary now reads Lösenord across all relevant tables; code unchanged
- [x] UR-03: UI-SPEC §Interaction Contracts row 7 documents `mode: 'onSubmit'` with UAT Test 3 rationale; code unchanged
- [x] UR-04: AuthRetryableFetchError arm exists in both auth screens, surfacing offline-specific Swedish copy without console.error noise
- [x] UR-05: All three banners render a ✕ close affordance with `accessibilityLabel="Stäng"` and dark-mode pair coverage
- [x] `tsc --noEmit` clean, `expo lint` clean, `test:auth-schemas` green
- [x] 3 atomic commits, each referencing the UR-NN convention in the message subject

## Self-Check: PASSED

All 4 modified files exist at the worktree paths committed:
- `app/app/(auth)/sign-in.tsx` — FOUND (commits a454312, 15d98d5)
- `app/app/(auth)/sign-up.tsx` — FOUND (commits a454312, 15d98d5)
- `app/app/(app)/index.tsx` — FOUND (commit a454312)
- `.planning/phases/03-auth-persistent-session/03-UI-SPEC.md` — FOUND (commit 00863d7)

All 3 commits exist in `git log --oneline`:
- `a454312` fix(03): wire VoiceOver a11y props across auth + app home — UR-01
- `00863d7` docs(03): amend UI-SPEC for shipped Lösenord vocab + onSubmit mode — UR-02, UR-03
- `15d98d5` feat(03): offline-error arm + banner close affordance — UR-04, UR-05
